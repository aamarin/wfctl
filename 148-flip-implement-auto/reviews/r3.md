# Review: 9433e81..a5366fa (reviewer r3)

## Passes run

1. **Correctness (adversarial).** Read `wfctl/_pipeline.py` in full and traced
   every caller of `next_step_content` and `_infer_steps` (`wfctl/cli.py`
   `next_cmd`, `resume_cmd`, `status_cmd`, `build_report`). Confirmed the halt
   invariant: `next_step_content` checks `step == "implement"` with both
   `repo_root` and `spec_dir` present, reads `tasks.md`, and — only when no
   checkbox is open — calls `_verification_block(repo_root)` *before* falling
   through to the `_STEPS` table lookup (`wfctl/_pipeline.py:376-382`). A
   truthy block returns `("wfctl verify", False)` unconditionally, so the new
   `True` on `_STEPS["implement"]` (line 29) is never reached while the
   definition of done hasn't passed. Verified this by reading `_verify.py`
   end to end (`load_config`, `load_record`, `_verification_block`'s five
   ordered reasons) and by running the suite (below) rather than trusting the
   docstring.
   - Checked the one call path that reaches `_STEPS.get("implement")` with
     `auto=True`: tasks still have an open `[ ]` — real work remains, so
     auto-running `/speckit.implement` again is exactly the intended
     behavior, not a premature "done".
   - Checked the pre-existing degrade path (no `verify` key in `wfctl.json`
     → `_verification_block` returns `None` immediately → step reads "done"
     off checkbox/sentinel state alone). This looks like it would blunt the
     stated argument for flipping `implement` ("wfctl owns the evidence"),
     but it's explicitly accepted architecture from before this diff
     (`docs/architecture/wfctl-runs-the-verification.md`: "Absence of a
     declared command degrades to today's behavior") — not something this
     commit introduces or changes. Not a finding against this diff.
   - Checked the `implement-complete.md` sentinel + still-open-checkbox
     combination in `_infer_steps` (lines 253-266): if the sentinel exists
     while checkboxes are still open, the code skips straight to
     `_verification_block` and can report "done" with open tasks. Traced this
     to pre-#148 code, untouched by the diff, and structurally prevented in
     practice by `speckit-implement/SKILL.md` step 9b, which instructs the
     agent to write the sentinel only after marking every task `[x]`. Not
     introduced or worsened by this change.
   - No other table (`README.md`, other `_STEPS`-shaped structures) still
     encodes the old `implement: False` assumption — grepped for it.

2. **Security.** N/A — a boolean literal flip in a private module-level dict.
   No new input handling, no subprocess/argv change (`_verify.run_verification`
   is untouched by this diff).

3. **Architecture.** The change makes `implement` match the shape `specify`,
   `plan`, and `tasks` already have in the same table — no new pattern, no new
   coupling. Consistent with the accepted decision record cited above and with
   `speckit-orchestrate/SKILL.md`'s documented `auto` semantics (`auto: true` →
   `EXECUTE_COMMAND`, `auto: false` → stop), which is the only consumer of the
   flag I could find (`grep -rn "auto" wfctl/cli.py` and the skill file).

4. **Readability.** One-line table change; the new test's docstring names the
   failure it guards against ("a `true` here would emit `EXECUTE_COMMAND` for
   a story whose definition of done has not passed") per this repo's
   convention of explaining *why*, not narrating the diff. No nesting,
   naming, or comment issues.

5. **Performance.** N/A — no loop, no I/O change.

6. **Over-engineering.** Nothing to cut. Two-line production diff, one
   regression test that earns its place by proving the one thing the flip
   could have broken (`test_a_blocked_implement_does_not_advance_unattended`).

## Additional verification

- `uv run pytest -q` → 863 passed.
- `uv run ruff check wfctl/ tests/` → all checks passed.
- `uv run mypy wfctl/` → no issues, 15 source files.
- Did not run `uv run wfctl verify` (excluded per instructions — writes a
  record outside the worktree).
- Did not look at the GitHub PR/issue beyond the problem statement given to
  me.

## Findings

None. I looked specifically for ways the invariant ("pipeline must still stop
when the definition of done has not passed") could be bypassed by the flip —
partial-arg call sites, the sentinel/checkbox edge case, the no-DoD degrade
path — and traced each to either being structurally prevented by
`next_step_content`'s ordering, or being pre-existing/accepted behavior this
commit does not touch.

One observation, not raised as a finding because it's genuinely marginal: the
new `True` outcome for `next_step_content("implement")` (tasks still open) is
exercised at the unit level (`test_step_table_resolves_in_order` calls
`next_step_content` directly) and indirectly at the CLI level
(`test_open_tasks_still_route_to_the_step_command` checks the command text but
not the `auto: true` string in `next-step.md`), whereas the `False`/halt
outcome got a dedicated CLI-level assertion
(`test_a_blocked_implement_does_not_advance_unattended`). The composition of
existing tests covers the positive case correctly; a single CLI-level
assertion of `auto: true` would make that symmetry explicit but isn't needed
for correctness.

net: 0 lines possible — lean already.

**Verdict: Approve.**

# Review: 9433e81..a5366fa (feat(pipeline): let implement advance without a prompt)

## Findings

WARNING  tests/test_pipeline_commands.py:L616 — `test_open_tasks_still_route_to_the_step_command`
(pre-existing, not touched by this commit) only asserts `"/speckit.implement" in
result.output` for the mid-implementation case (open checkboxes). Before this
commit that was fine — every branch of `implement` said `auto: false`, so the
command string was the only thing worth checking. After the flip, this is the
one case that actually returns `auto: true` through the full `next`/`resume`
path, and it is the headline behavior #148 ships (`implement` running
unattended). The commit adds a regression test for the *negative* case
(`test_a_blocked_implement_does_not_advance_unattended`, proving `auto: false`
still holds when blocked) but nothing pins the *positive* case end-to-end —
`test_step_table_resolves_in_order` only exercises the raw `_STEPS` entry via
`next_step_content(n)` with `repo_root=None, spec_dir=None`, which never enters
the `implement`-specific branch at all. A regression that hard-coded
`next_step_content`'s implement branch to always return `False` regardless of
`_STEPS`, or a future edit that fat-fingers the `_STEPS` fallthrough, would
still pass the full suite. → add `assert "auto: true" in result.output` (and/or
in `next-step.md`) to `test_open_tasks_still_route_to_the_step_command`, or add
a sibling assertion alongside the new blocked-case test.

NIT  wfctl/agents/skills/speckit-orchestrate/SKILL.md:L46-55 — not part of this
diff, and not a defect in it, but worth recording since it bears directly on
the invariant this change leans on ("the halt is carried by wfctl's `auto`
flag"). Step 0's "epic-inherited spec check" fires when a sub-issue worktree
has no spec dir of its own; when it finds no open/merged PR it tells the
orchestrator "next command is `/speckit.implement` … next command is
X, not /speckit.brainstorm" directly, bypassing `wfctl status --json` and its
`auto` field entirely for that branch (wfctl can't infer anything without a
spec dir — `_infer_steps` returns `pending` for every step in that case, so its
own `auto` for `brainstorm` would read `false`). That path already existed
before this commit and `implement`'s flag was `false` then too, so this flip
does not add new risk here — but it does mean the argument "wfctl owns the
evidence gate" has one documented escape hatch that never consults the gate.
Not asking for a fix in this diff; flagging so it doesn't get inherited as
settled by future readers of #148.

## Passes run

1. **Correctness** — traced `_STEPS["implement"]` → `next_step_content` →
   `_infer_steps`'s `implement` branch → `_verification_block` →
   `wfctl/_verify.py` (`load_config`, `load_record`, `code_identity`), and the
   two CLI call sites that write `next-step.md` (`next_cmd`, `resume_cmd` in
   `wfctl/cli.py`). Confirmed the ordering the task called out: in
   `next_step_content`, the `step == "implement"` guard runs *before* the
   `_STEPS.get(step, …)` fallthrough, so a blocked verification always returns
   `("wfctl verify", False)` regardless of `_STEPS["implement"]`'s value — the
   flip only changes behavior for the branch where tasks still have open
   checkboxes (mid-implementation), never the "tasks done, DoD unmet" branch.
   Also checked the case where `_verification_block` returns `None` (passed):
   `_infer_steps` then reports `implement` as `done`, so `_current_step_name`
   never selects it as current again and `next_step_content("implement", …)` is
   not reached in production for a passing verification — no route exists where
   a passed-but-current `implement` could emit the wrong flag. Ran
   `uv run pytest -q`: 863 passed. `uv run ruff check wfctl/ tests/`: clean.
   `uv run mypy wfctl/`: clean. Did not run `uv run wfctl verify` (excluded by
   the task). One test-coverage gap found (see WARNING above); no logic defect.
2. **Security** — N/A. No user input, no secrets, no I/O boundary touched by
   this diff (a two-tuple boolean flip and its tests).
3. **Architecture** — Fits the existing single-table pattern (`_STEPS`) exactly;
   no new pattern, no new module, no new dependency. Consistent with
   `docs/architecture/wfctl-runs-the-verification.md` (#96), which already
   establishes wfctl, not the agent, as the owner of `implement`'s completion
   evidence — this commit is the policy consequence of that decision, not a new
   boundary. No architecture record accompanies this commit; concluded that's
   correct rather than missing; `design-levels` excludes changes drawing no new
   boundary, and the boundary here (wfctl owns the verdict) was already decided
   and recorded under #96.
4. **Readability & simplification** — Diff is two lines of production code plus
   one added test. Test docstring follows the project's convention (names the
   failure it closes: "no test could tell the halting path from the routing
   one"). No nesting, no naming issues, nothing to simplify.
5. **Performance** — N/A, boolean literal change.
6. **Over-engineering** — Nothing to cut. Diff is minimal by construction.

net: 0 lines possible. Lean already.

Verdict: **Approve** (no blockers; one WARNING — add the missing positive-case
assertion before or shortly after merge).

# Review: 9433e81..a5366fa (r1)

## Target

```
tests/test_pipeline_commands.py | 17 ++++++++++++++++-
wfctl/_pipeline.py              |  2 +-
```

One `_STEPS["implement"]` flag flip (`False` → `True`) plus one new regression
test (`test_a_blocked_implement_does_not_advance_unattended`).

## Passes run and what each checked

1. **Correctness** — read `wfctl/_pipeline.py` in full: `_STEPS`,
   `_verification_block`, `_infer_steps`'s `implement` branch,
   `next_step_content`, `build_report`. Traced both call sites
   (`wfctl/cli.py` `next_cmd`/`resume_cmd`/`status_cmd`) and the consumer
   (`wfctl/agents/skills/speckit-orchestrate/SKILL.md` steps 2–4). Verified the
   stated invariant by hand-tracing every reachable state of `implement`:
   - open checkboxes → `next_step_content` skips its implement-specific guard
     (`tasks_text and not _has_open_checkboxes(...)` is false) and falls
     through to `_STEPS.get("implement")` = `("/speckit.implement", True)`.
     Correct: work remains, nothing to gate.
   - tasks closed, `_verification_block` returns a reason → explicit
     `return "wfctl verify", False` fires *before* the table lookup. The flip
     never reaches this branch.
   - tasks closed, `_verification_block` returns `None` → `_infer_steps` marks
     `implement` `"done"`, so `_current_step_name` has already moved past it;
     `next_step_content("implement", ...)` is not invoked for the current step
     in this state at all.
   No path exists where the flag change lets an unverified `implement` slip
   through as `auto: true`. The halt still rests on the single `False` the
   commit message names.
2. **Security** — n/a; no external input, no new subprocess/IO. Not applicable
   to a boolean table-value change.
3. **Architecture** — fits the existing one-table pattern the file's own
   header comment argues for (`_STEPS` is deliberately one table rather than
   three, precisely so a step can't carry a command with no flag or vice
   versa). No new pattern introduced.
4. **Readability** — checked comment/test conventions against
   `AGENTS.md` ("comments explain why, never what"; "test names are sentences,
   docstrings say why the test exists"). The new test's docstring names the
   specific defect it would catch ("a `true` here would emit `EXECUTE_COMMAND`
   for a story whose definition of done has not passed") — matches the
   project's bar exactly.
5. **Performance** — n/a, no new loop/IO/allocation.
6. **Over-engineering** — diff is already minimal: a one-character semantic
   flip plus one test that earns its place (commit message: "before the flip
   both branches said `auto: false`, so no test could tell the halting path
   from the routing one" — true; `test_a_blocked_implement_routes_to_verification`
   only checked the command name, never the flag). Nothing to cut.

Also checked, beyond the six passes:
- **Doc/table drift**: grepped `README.md`, `docs/architecture/`, and
  `wfctl/agents/` for anything that hardcodes `implement`'s old auto value or
  a stable count of "how many steps are manual" — found none. The orchestrate
  skill reads `auto` generically off `wfctl status --json`; it doesn't special
  case `implement`, so nothing there goes stale.
- **`speckit-orchestrate/SKILL.md` step 0** (the sub-issue epic-inherited
  path) already bypasses wfctl's own status/auto machinery entirely when a
  sub-issue has no spec dir under its own branch — it hardcodes "next command
  is `/speckit.implement`" without consulting `auto` or `_verification_block`.
  This predates the diff (untouched by it) and isn't a regression this commit
  introduces, but it means the flag flip is not the only path by which
  `implement` already auto-advances for sub-issues. Noting for the record, not
  as a finding against this diff.
- **Full verification**: `uv run pytest -q` → 863 passed. `uv run ruff check
  wfctl/ tests/` → clean. `uv run mypy wfctl/` → clean. (`wfctl verify` and
  `wfctl doctor` intentionally not run per instructions / because no
  `wfctl/agents/` files changed.)

## Findings

None.

net: 0 lines possible — already minimal (a 1-line semantic change plus one
justified regression test; nothing to shrink, delete, or simplify).

Verdict: **Approve**

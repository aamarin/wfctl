# Review: `git show e9e83e2` (branch `361-brainstorm-has-no-skill`, issue #361) — reviewer r2

Target resolved as the single commit on the branch (`git diff origin/main...HEAD`):
5 files, +195/−152.

## Verification run

| Command | Result |
|---|---|
| `uv run pytest -q` | 1576 passed in 129.57s |
| `uv run ruff check wfctl/ tests/` | All checks passed |
| `uv run mypy wfctl/` | Success: no issues found in 22 source files |

Not run, per the read-only brief: `wfctl install-skills`, `wfctl doctor`, `wfctl verify`.
The mirror is nonetheless demonstrably live in this worktree — `speckit-brainstorm`
appears in this session's own skill index with the new description, which is direct
evidence the `_MIRRORED_SKILLS` entry reaches `.claude/skills/` and that #361's stated
failure (an agent handed `/speckit.brainstorm` finding no skill) is closed on the
entry side.

## Findings

WARNING  tests/test_skill_cross_references.py:L189–L203 — the new two-surface loop asserts five entries on each copy but never asserts the two `allowed-tools` lines are *equal*, so the four unasserted entries are free to drift — including `Bash(wfctl status*)`, whose loss `approval-mode-is-stored-intent.md:L221` names as a *silent* failure ("the skill simply never sees a switch") and which is the skill body's very first instruction (SKILL.md:L18). The docstring claims the loop exists because "a copy is what drifts"; the remedy does not cover the drift it names. → after the loop, `assert len({_arch._frontmatter(p.read_text())["allowed-tools"] for p in surfaces.values()}) == 1, "the two copies diverged"`.

WARNING  wfctl/agents/skills/speckit-brainstorm/SKILL.md:L145 — the skill has no terminus that advances the pipeline. Six speckit skills end with "**Pipeline**: Invoke `speckit-orchestrate`" (`speckit-orchestrate/SKILL.md:L32` counts them); the typed route gets the same effect from the wrapper's `handoffs:` block, which by design does not travel to a SKILL.md. So a model-initiated `Skill(speckit-brainstorm)` — the exact route this change was built to create, and the only speckit step deliberately put on Claude's native discovery path — can now *enter* brainstorm and has no stated way out. `speckit-specify` and `speckit-delivery-plan` share the omission, but neither is mirrored, so neither is reachable that way; this change is what makes the gap newly reachable. → add a closing "**Pipeline**: Invoke `speckit-orchestrate`." step (`Bash(wfctl status*)` is already granted, so orchestrate's session gate works).

WARNING  wfctl/agents/skills/speckit-brainstorm/SKILL.md:L3 — the fix's load-bearing mechanism is unchecked prose. Discovery from `next_command` works only because the description literally contains `/speckit.brainstorm`, and the canonical string is `_pipeline.py:L63`. Reword the description and the mirror stays green, ships, and stops being findable from the payload — which is #361 verbatim. A violation is visible in an artifact the work produces (the shipped SKILL.md), which puts it on the *yes* side of `a-rule-is-expressed-as-a-check`. → `assert _pipeline.STEPS["brainstorm"].command in _frontmatter(skill)["description"]`, reading the constant rather than a literal.

WARNING  docs/architecture/design/307-the-coverage-map-is-the-evidence.md:L50–L53 — an *Observed* bullet now states something false: "`wfctl/agents/commands/speckit.brainstorm.md` layers auto-approve behaviour over `brainstorming` and `idea-refine` … That is the wrapper-as-layer pattern `vendor-upstream-skills` asks for, already working in this tree." The layer moved to the skill in this commit, and the commit message settles the ownership question in the skill's own prose. Two declarations now disagree about which file is the layer, which is the shape this repo treats as a defect. `approval-mode-is-stored-intent.md:L192` carries a weaker version of the same reference. Both records are `proposed`, so correctable in place. → repoint the path to `wfctl/agents/skills/speckit-brainstorm/SKILL.md`, or add one line noting #361 relocated it.

WARNING  tests/test_unattended_pause_rules.py:L3, tests/test_scan_files.py:L245, L260, L344, tests/test_design_records_downstream.py:L98 — five docstrings name `speckit.brainstorm.md` as the precedent for a pause table, a section shape, a conditional layer, and the `feature-paths` idiom. All four things moved; a reader following any of those pointers now finds a one-line pointer, and nothing fails. The commit repointed the two docstrings whose tests *read* the file and left the five that only *name* it. Under this repo's convention (docstrings carry the why; cross-file prose pointers are load-bearing), that is drift, not style. → repoint all five to `wfctl/agents/skills/speckit-brainstorm/SKILL.md`.

NIT      tests/test_skill_cross_references.py:L194 — `path.read_text().split("---")[1]` is the brittle hand-split; the diff propagated it into a loop. `_arch._frontmatter` already exists, is used for exactly this in `test_no_suppressed_wrapper_carries_more_than_a_pointer`, and handles column-zero keys and the closing delimiter correctly. → `allowed = _arch._frontmatter(path.read_text())["allowed-tools"]` (also makes the equality check above a one-liner).

NIT      wfctl/agents/skills/speckit-brainstorm/SKILL.md:L133 — "If `design.md` already exists when **this command** starts". L26 was correctly rewritten from "command" to "skill" in the same move; this one was not, and the file is no longer a command. → "when this skill starts".

NIT      wfctl/agents/skills/speckit-brainstorm/SKILL.md:L4 — copying `allowed-tools` onto a mirrored skill widens `Write`, `mkdir`, `git add` and `git commit` from a typed turn to a model-initiated one. Deliberate, designed in #361, and the correct call. Worth noting only that AGENTS.md's Safety section sanctions `start-session`'s widened grant *by name* and nothing sanctions this one; a grant justified only in a commit message is a grant the next reader re-litigates. → one sentence in the Safety section, or none — author's call.

## What each pass checked

**1. Correctness.** Verified the split is a faithful move: diffed the pre-commit wrapper body (`e9e83e2~1`, from line 12) against the new SKILL.md body — byte-identical except the two intended edits (the `command`→`skill` rewording and the four added sentences answering the layer-ownership question). No prose lost. Traced `_mirror_supersedes_wrapper`: it keys on `item.stem`, which for `speckit.brainstorm.md` is `speckit.brainstorm`, absent from `_MIRRORED_SKILLS` — suppression correctly never fires and the typed route survives, as the commit claims. Confirmed `_claude_native_skill_mirror` gates on `item.name`, so `speckit-brainstorm` does mirror. Checked the doctor orphan scan (`cli.py:L5250–L5320`) and `_scan_owns` — the new mirror has a base-layer twin, so it is correctly owned and will not be reported abandoned. Checked the exit path (finding 2) and the discovery path (finding 3).

**2. Security.** No secrets, no input handling, no network. The only surface is the `allowed-tools` widening (NIT 3) — enumerated it against `start-session`'s precedent, which grants strictly more (`wfctl install-skills`). No unbounded grant, no `Edit`, no `Bash(*)`. `disable-model-invocation` correctly *not* carried onto the skill; carrying it would have reproduced #204's defect one file over.

**3. Architecture.** Read the four named records in full. `vendor-upstream-skills`: neither derived skill is edited, and `speckit-brainstorm` is wfctl's own (score-0 against spec-kit templates, same class as `speckit-delivery-plan`/`speckit-orchestrate`), so no attribution line is owed and `test_skill_attribution`'s two-declaration pair is unaffected. `layer-model`: source under `wfctl/agents/`, nothing dotted edited. `knowledge-placement`: worker guidance in the skill tree is right, but the constraint half is now stated in two places that disagree (finding 4). `a-rule-is-expressed-as-a-check`: findings 1 and 3 are both on the *yes* side of its test; SKILL.md:L142's "prose rather than a check, deliberately" is correctly on the *no* side and carries its own argument. Verified against `test_every_declared_mirror_names_a_shipped_skill`, `test_no_suppressed_wrapper_carries_more_than_a_pointer` (brainstorm is correctly out of scope — no `speckit-brainstorm.md` wrapper), and `test_skill_frontmatter`'s `ALLOWED_KEYS` (`allowed-tools` and `compatibility` both legal; no `handoffs:` leaked onto the SKILL.md).

**4. Readability & simplification.** Frontmatter matches the `start-session` precedent for a mirrored skill carrying `allowed-tools`. Wrapper pointer matches the exact sentence form the other eleven wrappers use, and `test_skill_cross_references`' `_REFERENCE` scan confirms the pointer resolves to a shipped skill. Findings 5–7 came out of this pass.

**5. Performance.** No runtime cost. `_MIRRORED_SKILLS` is a frozenset; one more member, one more `shutil` copytree at install time. Nothing in a hot path.

**6. Over-engineering.** Nothing to delete. The 10-line comment on the `_MIRRORED_SKILLS` entry matches the file's established density and carries the #170 tie argument a future reader would otherwise re-derive. The dict-of-surfaces loop buys a named failure for +10 lines, which is the trade the repo's test conventions ask for. `allowed-tools` duplication is required, not redundant. The only shrink available is NIT 6, and it is net-negative by one line at best.

net: Lean already.
Verdict: **Approve** — the change definitely improves code health and closes #361's entry-side failure. No blockers. Finding 2 is the one that decides whether the fix is complete for the unattended scenario the issue was filed about; findings 1 and 3 are the two cheap checks that would keep it from silently un-fixing itself.

# Review (r4): `git diff main...HEAD` — #361, 3 commits (e9e83e2, 83cd3e1, a9d0901)

Rubric: `.agents/skills/code-review/SKILL.md`, all six passes run.
Target verified green before reviewing: `uv run pytest -q` 1579 passed,
`uv run ruff check wfctl/ tests/` clean, `uv run mypy wfctl/` clean.
(`wfctl verify` and `install-skills` deliberately not run — shared worktree.)

## Findings

```
WARNING  tests/test_skill_cross_references.py:L171 — the #361 check does not detect #361
WARNING  docs/architecture/vendor-upstream-skills.md:L75 — accepted record's body edited, no Log line
NIT      wfctl/agents/skills/speckit-brainstorm/SKILL.md:L149 — 11 lines of reviewer-facing narration in a runtime file
NIT      docs/architecture/mirror-supersedes-the-wrapper.md:L15 — "Eleven skills are mirrored" is now 13, and brainstorm is a category the record does not describe
NIT      docs/architecture/vendor-upstream-skills.md:L78 — "The prefix" left orphaned, wrap broken
NIT      wfctl/cli.py:L2379 — 23-line entry comment, ~3x its neighbours; middle paragraph is already in two other places
```

---

### WARNING — `tests/test_skill_cross_references.py:L171`

`test_every_pipeline_step_reaches_a_skill_an_agent_can_invoke` claims to be
"#361 as a check rather than as eight files that happen to comply", and commit
83cd3e1 says "the next step added with an inline wrapper fails instead of
shipping … verified by mutation". **It passes on `main` — the exact tree that
had the defect.**

`_REFERENCE` matches any `.agents/skills/<name>` anywhere in the wrapper's text,
and `main:wfctl/agents/commands/speckit.brainstorm.md:102` contains
`` (`.agents/skills/reading-design-records/SKILL.md`) `` inside the inline
workflow. Running the test's own body against `main`:

```
brainstorm: ['reading-design-records']      ← the broken step, green
specify:    ['speckit-specify']
clarify:    ['speckit-clarify', 'writing-a-scan-file']
plan:       ['reading-design-records', 'speckit-plan']
tasks:      ['reading-design-records', 'speckit-tasks']
analyze:    ['reading-design-records', 'speckit-analyze', 'writing-a-scan-file']
decompose:  ['speckit-delivery-plan']
implement:  ['reading-design-records', 'speckit-implement']
```

So the mutation that was run must have been "delete the pointer line", not
"restore the inline body" — and the inline body is the defect. Six of the eight
wrappers cite `reading-design-records` in passing, so a future step that carries
its workflow inline and cites any skill anywhere in its prose ships green. Under
`a-rule-is-expressed-as-a-check` (accepted) that leaves the rule a comment with a
test-shaped wrapper around it, which is worse than prose because it reads as
covered.

**Fix** — assert the wrapper names *its own* skill, not *a* skill. Verified:
passes on HEAD for all eight steps, fails on `main` with
`{'brainstorm': 'speckit-brainstorm'}`.

```python
# decompose is the one step whose skill name does not fall out of its command;
# pinned so the exception is visible rather than absorbed by a looser match.
_SKILL_FOR_STEP = {"decompose": "speckit-delivery-plan"}

for step, spec in _STEPS.items():
    name = spec.command.lstrip("/")
    want = _SKILL_FOR_STEP.get(step, name.replace(".", "-"))
    wrapper = _AGENTS / "commands" / f"{name}.md"
    if not wrapper.exists() or want not in set(_REFERENCE.findall(wrapper.read_text())):
        inline[step] = want
```

### WARNING — `docs/architecture/vendor-upstream-skills.md:L75`

The record is `status: accepted`, and the diff edits its **body**.
`architecture-decisions/SKILL.md:L106-128` is unambiguous: *"Once a record is
`accepted`, exactly two things ever change: `status: accepted → superseded |
retired`, `Log: one line appended`. The body is never edited. Not to fix the
reasoning, not to add a consideration that came up later"* — and names the
failure as *"a body edited after the fact makes the record agree with the
present"*, which is exactly what this edit does.

The record's own `## Log` shows the project has in practice amended this
particular record twice (`2026-09-05 amended`, `2026-09-06 amended`), so there
is a live tension between the skill's rule and the practice. This change
satisfies neither reading: it edits the body *and* appends no Log line. All 31
records under `docs/architecture/` carry a `## Log`, and nothing checks
amendments, so this fails silently.

**Fix** — either append
`- 2026-09-12  amended  — `speckit-brainstorm` named as wfctl's own (#361)`
to the Log, or drop the body edit and let the fact live in the `_MIRRORED_SKILLS`
comment where it already is.

### NIT — `wfctl/agents/skills/speckit-brainstorm/SKILL.md:L149-159`

The `**Pipeline:** invoke `speckit-orchestrate`.` line is right. The 11 lines
after it are a defence of that line against three sibling skills, addressed to a
reviewer ("the route #361 opened", "Six sibling speckit skills end on this same
line", "The three that do not … are not a precedent"). `CLAUDE.md`: *"Comments
inform the next reader of the file; they do not narrate the change to a
reviewer. If a comment only makes sense beside the diff, it belongs in the commit
message"* — and it already is there, near-verbatim, in 83cd3e1. The file's other
rationale passages (the `a-rule-is-expressed-as-a-check` paragraph, "Prose rather
than a check, deliberately") explain why a rule binds *the agent executing it*;
this one does not. Same reading applies to the two sentences added at L29-32.

**Fix** — keep the Pipeline line, delete L149-159. −11 lines.

### NIT — `docs/architecture/mirror-supersedes-the-wrapper.md:L15`

"Eleven skills are mirrored, and eight of them also shipped a wrapper under the
same name." `_MIRRORED_SKILLS` is now 13 (12 before this branch, so the count was
already stale — not this change's defect, but this change is the moment it is in
front of someone). The record is `status: proposed`, so its body is not frozen
(`architecture-decisions/SKILL.md`: *"A `proposed` body is not frozen"*) — it is
outside the carve-out 83cd3e1 states for the four `design/` records.

The more interesting half: `speckit-brainstorm` is the first mirrored skill whose
wrapper is *not* suppressed, because `speckit.brainstorm` and
`speckit-brainstorm` differ by a dot. "eight of them also shipped a wrapper under
the same name" stays true only by accident of that dot, and the record describes
no third case. **Fix** — update the count and add the clause, or leave a line
saying the count is not maintained.

### NIT — `docs/architecture/vendor-upstream-skills.md:L78`

The inserted sentence ends mid-line, leaving `one. The prefix` / `is a naming
convention` split across a break the rest of the file does not have. **Fix** —
rewrap the paragraph.

### NIT — `wfctl/cli.py:L2379-2401`

23 lines for one frozenset entry, against ~6 for `opening-a-change` and
`software-design-decisions`. The middle paragraph ("The wrapper survives it:
… differ by a dot, so `_mirror_supersedes_wrapper` never fires") restates
`_mirror_supersedes_wrapper`'s own docstring and
`mirror-supersedes-the-wrapper.md`. **Fix** — drop that paragraph; the first and
third carry the argument that is not written anywhere else. −6 lines.

---

## What each pass checked, and what it cleared

**1. Correctness.** Diffed the moved body byte-for-byte against
`main:wfctl/agents/commands/speckit.brainstorm.md` — the transfer is faithful;
the only changes are "this command"→"this skill" (×2) and the layer sentence at
L29-32. No rule dropped. Traced `_mirror_supersedes_wrapper` (`cli.py:L2505`):
`item.stem` for `speckit.brainstorm.md` is `speckit.brainstorm`, which is not in
`_MIRRORED_SKILLS`, so suppression genuinely never fires and the typed route
survives — claim verified, not taken on trust. Traced
`_claude_native_skill_mirror` (`cli.py:L2477`): the new name is a directory under
`agents/skills/`, so it mirrors. `handoffs:` staying on the wrapper is forced —
`test_skill_frontmatter.ALLOWED_KEYS` does not contain it. `compatibility:` on
the new SKILL.md is in that set, and matches `start-session`'s spelling exactly.
Pointer text matches the five sibling wrappers verbatim in form; the
`../skills/…` fallback resolves on both the `.claude/` and `.bob/` layers.
`MANIFEST.in` grafts `wfctl/agents`, so the new directory ships.

**2. Security.** The real change is that `Write`, `mkdir`, `git add` and
`git commit` now bound a *model-initiated* turn — previously the wrapper's
`disable-model-invocation: true` meant only a typed command could spend them.
Checked the grant against `a-human-grants-outward-facing-authority`: nothing
outward-facing is in it (`wfctl issue comment/create/label`, `wfctl notify` are
all absent), so the gated row is untouched. `start-session` (#204) and
`software-design-decisions` are both mirrored with comparable write grants, so
the shape is precedented, and omitting `disable-model-invocation` from the skill
is correct — carrying it over would have shipped the defect one file across. No
finding.

**3. Architecture.** Checked against `layer-model` (mirroring declared in the
installer, not in the file — satisfied), `knowledge-placement` (both routes now
land on one file), `vendor-upstream-skills` (neither derived skill edited; the
layer sits above them), `a-rule-is-expressed-as-a-check` (this is where the
WARNING above comes from), `mirror-supersedes-the-wrapper` (the NIT above).
Confirmed `test_no_suppressed_wrapper_carries_more_than_a_pointer` does not
false-fire: it loops over `_MIRRORED_SKILLS` names that have a `<name>.md`
wrapper, and `speckit-brainstorm.md` does not exist.

**4. Readability.** Test names are sentences; docstrings name the failure they
caught, per the repo convention. `from wfctl._pipeline import _STEPS` inside the
function matches the file's existing idiom and `tests/test_predicates.py`.
`inline` and `grants` describe their contents. `spec.command.lstrip('/')` is
imprecise (`removeprefix` is the exact operation) but harmless. The prose repoints
in `test_scan_files.py`, `test_design_records_downstream.py` and
`test_unattended_pause_rules.py` are all accurate against the new file. No
finding beyond the NITs above.

**5. Performance.** Not applicable — the new tests are three file reads and one
regex over eight small files; the full suite moved 146s→146s.

**6. Over-engineering.** Three new tests for one change, all three distinct:
`_STEPS` coverage, the description string, the frozenset pin. None duplicates
`test_every_declared_mirror_names_a_shipped_skill` (opposite direction) or
`test_every_referenced_skill_ships`. The `allowed-tools` equality assertion
freezes duplication rather than removing it, but the duplication is forced (a
wrapper's grant and a skill's grant are separate surfaces) and the assertion is
the cheapest guard available. The description check reads only the first
`description:` line — it would miss a wrapped description, acceptable given
`test_skill_frontmatter` pins column-zero keys.

net: −17 lines possible (11 in SKILL.md, 6 in cli.py)

**Verdict: Request changes** (2 warnings, no blockers). The split itself is
correct and the body moved intact; what needs fixing is the check that is
supposed to hold it — it does not fail on the tree that had the bug — and the
unlogged edit to an accepted record.

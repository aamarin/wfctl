## Review: `git diff main...HEAD` (#361, e9e83e2 · 83cd3e1 · a9d0901) — reviewer r6

Rubric: `.agents/skills/code-review/SKILL.md`, all six passes run against the
whole branch diff. Context read first: the three commit messages, `CLAUDE.md`,
`docs/architecture/{vendor-upstream-skills,a-rule-is-expressed-as-a-check,
layer-model,knowledge-placement,architecture-decisions(SKILL)}`, `_pipeline._STEPS`,
`cli._MIRRORED_SKILLS` / `_mirror_supersedes_wrapper` / `_claude_native_skill_mirror`,
the eleven sibling `speckit.*` wrappers and nine sibling `speckit-*` skills.
Green here: `uv run pytest -q` 1579 passed, `uv run ruff check wfctl/ tests/`,
`uv run mypy wfctl/`. (`wfctl verify` / `install-skills` deliberately not run —
shared worktree.)

WARNING  tests/test_skill_cross_references.py:L175 — `test_every_pipeline_step_reaches_a_skill_an_agent_can_invoke` passes on the pre-fix tree, so it holds nothing. The predicate is "does the wrapper mention *any* `.agents/skills/<name>`", and `main`'s inline `speckit.brainstorm.md` mentions one at its line 102 (``(`.agents/skills/reading-design-records/SKILL.md`)``) inside the bullets-only paragraph. Re-running the test body over `git show main:` for all eight steps yields `brainstorm → ['reading-design-records']` — not flagged. So the docstring's two claims are both false: it is not "#361 as a check", and "the next `_STEPS` row added with an inline wrapper" fails only if that inline wrapper never names a skill in prose. `a-rule-is-expressed-as-a-check` (accepted) puts this case on the wrong side of its own test: the violation *is* visible in an artifact the work produces, so prose-shaped-as-a-check is the rule's absence documented. → assert the wrapper points at *its own* step's skill, not at any skill: derive the expected name from the row (`spec.command.lstrip('/').replace('.', '-')`, with the one-line exception map for `decompose → speckit-delivery-plan`) and assert it is in `_REFERENCE.findall(...)`; or assert the wrapper carries no rule of its own (body is the pointer sentence plus at most the `$ARGUMENTS` block). Verify the replacement the way the other two were verified — by running it against `git show main:wfctl/agents/commands/speckit.brainstorm.md`, which is the mutation that matters and the one the "verified by mutation" claim in a9d0901 does not cover for this test.

WARNING  tests/test_skill_cross_references.py:L175 — the same check's *name* over-claims across the tree it walks. `speckit.clarify.md` (324 lines) and `speckit.analyze.md` (461 lines) still carry substantive wfctl-own layers inline — the scan-file rule, the rejected-options rule, the unattended-pause rules — and an agent that resolves `/speckit.clarify` out of `next_command` and invokes `Skill(speckit-clarify)` reads the spec-kit-derived skill without any of it. That is #361's shape at two more steps, and this check reports both green forever. Not this diff's to fix (both layers sit in wrappers deliberately, because the skills they point at are vendored and may not be edited), but the check now *certifies* the gap. → either rename the check to what it asserts (`test_every_pipeline_wrapper_names_a_skill`) or file the clarify/analyze case and reference the issue in the docstring, so the next reader does not take a green run as coverage.

WARNING  docs/architecture/vendor-upstream-skills.md:L75 — accepted record amended with no `Log` line. The file is `status: accepted`, and its `## Log` carries two dated `amended` entries (2026-09-05 #213, 2026-09-06 #216/#229), each naming what changed. This diff adds a third amendment — a new name in the wfctl's-own list plus three lines of fresh reasoning about layering — and appends nothing. The convention is the only thing distinguishing an amendment from a body edit that `architecture-decisions`' "An accepted record is immutable" forbids outright. → append `- 2026-09-12  amended    — `speckit-brainstorm` named as wfctl's own, split out of its wrapper rather than pulled from upstream (#361)`. (Worth noting separately, not this diff's to resolve: the skill says an accepted body is *never* edited while this record's own Log shows the repo amending accepted bodies twice. One of the two is wrong and nothing checks either.)

WARNING  wfctl/agents/skills/speckit-brainstorm/SKILL.md:L147 — the terminus was added to the file *both* routes read, so it changes the typed route, and nothing in the diff, the tests or the commit message says so. `_STEPS["brainstorm"]` is `_AUTOMATIC`; once the design gate is satisfied, `speckit-orchestrate` step 5 reads `auto: true` for `/speckit.specify` and emits `EXECUTE_COMMAND: speckit.specify`. Before this change the typed run ended at the wrapper's `handoffs:` "Start Specify" offer — a human click. After it, the agent may auto-chain past that button on the one step whose whole purpose is human design gates. The commit message frames the line purely as "the mirrored route had no terminus". Six siblings do chain this way, so the alignment is defensible — but it is a behaviour change to an attended route, asserted nowhere. → say it in the skill (one line: the handoff and the terminus are the same exit, and a run that reaches the terminus advances without waiting for the button), and pin whichever of the two behaviours is intended.

WARNING  wfctl/cli.py:L2396 — the authority widening is argued only in a source comment. The skill is model-invocable and mirrored, and its `allowed-tools` grants `Write`, `mkdir`, `git add`, `git commit` to a model-initiated turn. The comment's own precedent, three entries down, sanctions `start-session`'s comparable grant by citing the Safety section of `AGENTS.md` — which says nothing about a model-initiated turn that commits. → add the sentence to `CLAUDE.md`'s Safety section (where a human auditing who may write and commit unattended actually looks), or cite the record that covers it. The cli.py comment is where the installer's authors read; it is not where that question is asked.

NIT      wfctl/agents/skills/speckit-brainstorm/SKILL.md:L149-159 — the nine lines after the terminus narrate the change rather than instruct the agent: `#361`, why `handoffs:` could not travel, which six siblings comply, why the three that do not are no precedent. `CLAUDE.md`: comments inform the next reader; a justification that only makes sense beside the diff belongs in the commit message — and it already is there, near-verbatim, in 83cd3e1's first section. It is also the last thing an agent reads, *after* being told to hand off. → cut to `**Pipeline**: Invoke \`speckit-orchestrate\`.`; keep the argument in the commit.

NIT      wfctl/agents/skills/speckit-brainstorm/SKILL.md:L147 — terminus wording deviates from the five siblings that carry it (`**Pipeline**: Invoke \`speckit-orchestrate\`.` in clarify, checklist, implement, tasks; `Invoke …` in analyze). This file writes `**Pipeline:** invoke` — colon inside the bold, lowercase verb. → match the siblings.

NIT      wfctl/cli.py:L2393-2401 — the third paragraph of the new comment (the `allowed-tools` widening) restates what `test_brainstorm_allows_the_commands_its_records_need`'s docstring now argues at greater length. At 22 lines this is the longest entry in the frozenset. → keep the first two paragraphs and the last sentence; drop the rest to the test that owns the invariant.

**Checked and found sound** (recorded so a later reader does not re-derive it):
- `_mirror_supersedes_wrapper` does not fire: it keys on `item.stem in _MIRRORED_SKILLS`, `speckit.brainstorm` ≠ `speckit-brainstorm`, so the typed route survives and `.claude/commands/speckit.brainstorm.md` is present in this worktree alongside the `.claude/skills/speckit-brainstorm/` mirror (byte-identical to source).
- `disable-model-invocation` cannot be added to the new SKILL.md silently: `test_skill_frontmatter.ALLOWED_KEYS` omits it, and only `i-have-adhd` is exempt. The route's second silent-loss mode is already held.
- Frontmatter conforms (`name`, `description`, `allowed-tools`, `compatibility` all in `ALLOWED_KEYS`); `_arch._frontmatter` parses the unquoted `allowed-tools` value; `compatibility` matches the sibling idiom.
- Body moved verbatim: `diff` of `main`'s wrapper body against the skill body shows exactly three changes — two self-reference retargets (`this command is where that override is stated` → `this skill …`, `when this command starts` → `when this skill runs`) and one added four-line paragraph stating that the skill, not the wrapper, is the layer. No rule was dropped, softened or reworded in the move.
- The two repointed reads (`test_the_producer_warns_that_only_bullets_are_read`, `test_brainstorm_orders_the_records_before_the_one_pager`) were repointed rather than widened; both would have passed over the empty pointer, which is what the commit claims and what the assertions show.
- Eleven remaining `/speckit.brainstorm` mentions under `wfctl/agents/` all name the *command*, which still exists; none claims the wrapper carries the layer.
- The four stale-prose records left untouched (`approval-mode-is-stored-intent`, `design-md-indexes-the-records`, `a-human-accepts-a-decision`, `brainstorm-is-one-step-with-addressable-levels`, plus `design/307-…`) are all `status: proposed` and held pending end-to-end validation; declining to triage them is correct and consistent with 83cd3e1's stated scope.
- Security pass: no new input handling, no secrets, no execution path added. The only authority change is the `allowed-tools` copy, covered above.
- Performance pass: nothing in the diff runs at runtime except three file reads in tests. Nothing to say.

net: −15 lines possible (≈9 terminus narration, ≈5 cli.py comment, ≈1 record wording)
Verdict: **Request changes** (0 blockers; 5 warnings, of which the first — a regression check that is green on the tree it was written to fail — is the one that must not ship as-is, because it will be read as coverage for the defect this branch exists to close)

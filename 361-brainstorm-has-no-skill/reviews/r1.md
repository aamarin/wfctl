# Review: `git show e9e83e2` (branch `361-brainstorm-has-no-skill`, issue #361) — reviewer r1

Rubric: `.agents/skills/code-review/SKILL.md`, all six passes. Context read first:
`gh issue view 361`, root `CLAUDE.md`/AGENTS.md in full, `uv run wfctl arch context`
(10 accepted records), `docs/architecture/vendor-upstream-skills.md`,
`approval-mode-is-stored-intent.md`, `design-md-indexes-the-records.md`, and the
tests before the source.

Verification actually run (read-only): `uv run pytest -q` → **1576 passed** in 130s;
`uv run ruff check wfctl/ tests/` → clean; `uv run mypy wfctl/` → clean. No file in
the worktree was modified.

---

## Findings

WARNING  tests/test_skill_cross_references.py:L178-L203 — the copy is asserted, the *agreement* is not. The loop pins the same five `Bash(...)` entries on the wrapper and on the SKILL.md, but both `allowed-tools` lines carry twelve entries. `Bash(wfctl status*)` is not among the five, and it is the one grant the skill's own first instruction needs (`wfctl status --json`, SKILL.md:L18) and the one `approval-mode-is-stored-intent` L221 added for exactly that reason: *"the omission fails silently — the skill simply never sees a switch."* Drop it from the skill copy and the model-initiated route silently reads no mode, with the suite green. The issue asked for this by name: *"a check that they agree is the thing that keeps them from drifting apart."* → assert the two `allowed-tools:` lines are equal (or that the skill's set ⊇ the wrapper's), keeping the five-entry loop as the floor.

WARNING  wfctl/cli.py:L2379-L2388 — `"speckit-brainstorm"` in `_MIRRORED_SKILLS` is the entire fix for #361, and nothing asserts it. Delete the entry and the skill still ships, still installs under `.agents/skills/`, never reaches `.claude/skills/`, and 1576 tests still pass — #361 reopened in silence. `a-rule-is-expressed-as-a-check` (accepted) applies: the violation is visible in an artifact the work already produces. Three siblings already have this pin — `test_review_fanout_skill.py:L174`, `test_skill_cross_references.py:L241` (`software-design-decisions`), `:L271` (`start-session`) — and `test_every_declared_mirror_names_a_shipped_skill` only checks the other direction, its own docstring saying *"a declaration nothing checks is a comment."* → add `assert "speckit-brainstorm" in _MIRRORED_SKILLS` beside the existing two in `test_skill_cross_references.py`, and assert the skill's `description` still names `/speckit.brainstorm` — that sentence is the bridge from `next_command` to the skill index, and it is the thing a reword would quietly cut.

WARNING  wfctl/cli.py:L2379-L2388 — the mirror widens a grant from a typed turn to a model-initiated one, and the comment does not say so. `Bash(git add*)`, `Bash(git commit*)`, `Bash(mkdir*)` and `Write` were previously pre-approved only when a human typed `/speckit.brainstorm`; on a mirrored, model-invocable skill they are pre-approved when the model itself decides the description matches. The repo has a precedent for naming that crossing rather than inheriting it — the `start-session` entry twelve lines below spends three lines sanctioning `Bash(wfctl install-skills*)` against the Safety section of AGENTS.md. This entry inherits a wider grant than that one and argues none of it. The grant is probably right (records land `proposed`, nothing pushes, committing records is the step's job) — the missing thing is the sentence. → one sentence in the comment, or in the commit message's `allowed-tools` paragraph, naming `git add`/`git commit` as the entries that now reach a model-initiated turn and why that is acceptable.

WARNING  tests/test_unattended_pause_rules.py:L3, :L146; tests/test_scan_files.py:L245, :L260, :L344 — five docstrings still route the next reader to `speckit.brainstorm.md` for content that is no longer there. *"`speckit.brainstorm.md` has carried a table of its skill's pauses since #283"* (the table is now SKILL.md:L35-L40), *"`speckit.brainstorm.md` is the precedent and it names every upstream pause it overrides"*, *"`test_skill_cross_references` already holds this for `speckit.brainstorm.md`, which is where the shape comes from"* (it now holds it for both surfaces), *"`speckit.brainstorm.md`'s layer is conditional on `auto_approve`"* (the layer is the skill — the diff itself changed that sentence inside the moved file). The two tests that *read* the file were repointed; the five that *cite* it were not, and this repo's testing convention is that the docstring is how the reason survives. A reader following any of them lands on a twelve-line pointer. → repoint the five citations to `speckit-brainstorm/SKILL.md`.

WARNING  docs/architecture/vendor-upstream-skills.md:L75 — an accepted record now under-enumerates. *"`speckit-delivery-plan` and `speckit-orchestrate` are absent because they are wfctl's own. The prefix is a naming convention, not a provenance claim"* — there are three such skills as of this commit. `speckit-brainstorm` is correctly absent from the derived table (it is new, wfctl-authored, carries no `Derived from` line, and `test_skill_attribution` therefore passes), but the sentence that explains *why* a `speckit-` prefix can be missing from that table names only two of the three. Nothing checks that sentence, so it drifts silently and the next provenance audit reads the absence as an omission. This is distinct from the #229 attribution question the issue defers — that one is about the wrapper's derived body, this one is about a new file the accepted record's own exception list should name. → add `speckit-brainstorm` to that sentence.

NIT  wfctl/agents/skills/speckit-brainstorm/SKILL.md:L133 — *"If `design.md` already exists when this command starts"*. L26 was updated from "this command" to "this skill" in the same move; this one was not. The file is now reachable without a command being typed, which is the point of the change. → "when this skill starts" (L47's "this file" and L70's "this file" read fine).

NIT  wfctl/agents/commands/speckit.brainstorm.md:L2 — the wrapper's `description` still claims the wrapper's old job: *"Wraps the design-levels + brainstorming + idea-refine skills."* The body one line below says the opposite — that the skill is the layer and *"this file states no rule of its own."* → "Start a brainstorming session. Points at the `speckit-brainstorm` skill, whose output lands in specs/<branch>/design.md for speckit pickup."

NIT  docs/architecture/approval-mode-is-stored-intent.md:L192, :L221; design-md-indexes-the-records.md:L107 — three proposed records name `speckit.brainstorm.md` as the file that holds the auto-approve layer and the `allowed-tools` requirement. Both records are *proposed*, and proposed records here are held deliberately pending end-to-end validation, so this is reported rather than prescribed: the branch is now in two files (the skill states the layer, the wrapper grants the typed turn), and whoever moves either record to accepted should reconcile those lines then.

---

## Passes — what each one actually checked

**1. Correctness.** Traced the mechanism end to end rather than trusting the commit message. `Path("speckit.brainstorm.md").stem == "speckit.brainstorm"`, which is not in `_MIRRORED_SKILLS`, so `_mirror_supersedes_wrapper` (cli.py:L2492) returns False and the typed route survives — the commit's central claim holds. `_claude_native_skill_mirror` keys on `item.name` for a directory under `agents/skills`, so `speckit-brainstorm/` mirrors to `.claude/skills/speckit-brainstorm/`. Diffed the moved body byte-for-byte against `e9e83e2~1:wfctl/agents/commands/speckit.brainstorm.md`: nothing was lost or silently reworded — the only edits are the frontmatter swap, the `# Brainstorm` heading, and the "this skill is the layer above them, not the wrapper" paragraph. `handoffs:` and `disable-model-invocation:` correctly stayed on the wrapper; `allowed-tools` correctly copied rather than moved (the wrapper is not suppressed, so a typed turn reads the wrapper's frontmatter and needs the full list). Frontmatter keys (`name`, `description`, `allowed-tools`, `compatibility`) are all inside `ALLOWED_KEYS` in `test_skill_frontmatter.py`; the space-separated `allowed-tools` form and the quoted `name`/`compatibility` style match `start-session` and the other `speckit-*` skills. Packaging needs nothing: `MANIFEST.in` grafts `wfctl/agents` whole. The `split("---")[1]` in the repointed test is safe — the skill's frontmatter is the first block and the body uses em dashes, not `---`. No edge case found where the change breaks a route that worked before.

**2. Security.** The new grant surface is the only security-relevant fact in the diff and it is finding W3 above: `Write`, `mkdir`, `git add`, `git commit` move from human-typed-only to model-initiated. Bounded in practice — the description is pipeline-scoped, commits are local, records land `proposed` and the skill re-states that an agent never writes `accepted` — so not a blocker, but the missing sanction sentence breaks the pattern #204 set. No secrets, no input handling, no injection surface in the diff.

**3. Architecture.** Checked against the four records the panel was pointed at. `layer-model`: source stays under `wfctl/agents/`, the generated dotted trees are untouched — clean. `knowledge-placement`: guidance for the worker moved to the file the worker reads, and the two routes now land on one file instead of two free to drift — this is the change's strongest argument and it holds. `vendor-upstream-skills`: neither `brainstorming` nor `idea-refine` was edited, and the layer-above statement was made explicit rather than left to the diff; the only gap is the accepted record's exception list (W5). `a-rule-is-expressed-as-a-check`: three invariants the change states in prose (mirroring membership, grant agreement, "the wrapper states no rule of its own") are all checkable from artifacts the work produces, and two are unchecked (W1, W2) — the third, that the wrapper stays a pointer, has no check either and is the one that would rebuild the two-layers-that-disagree shape the skill's own prose forbids; folding it into the W1 fix costs one assertion. Noted but not filed as a defect: this makes `speckit-brainstorm` the only speckit step on Claude's native discovery path, an asymmetry the cli.py comment states deliberately and #361 asks for.

**4. Readability.** The wrapper's pointer sentence matches the house form in `speckit.specify.md` and `speckit.plan.md` verbatim, plus one sentence of its own. The repointed tests keep the repo's convention that a docstring says why the test exists and names what moved. Comments in `cli.py` explain *why this shape* rather than narrating the diff, per the code-style section. Two stale-wording nits above.

**5. Performance.** Not applicable — markdown movement plus one `frozenset` member. The `install-skills` copy loop gains one directory.

**6. Over-engineering.** Nothing to delete. The duplicated twelve-entry `allowed-tools` line looks like the obvious `shrink:` target and is not one: the wrapper is not suppressed on the Claude layer, so a typed turn reads the wrapper's own frontmatter and dropping it would revoke the grant. The two-surface test loop is three lines longer than the original and buys the surface name in the failure message. `net: lean already.`

---

**Verdict: Approve** — no blockers; the change definitely improves code health and the mechanism works as the commit message describes. Five WARNINGs, of which W1 and W2 are the ones I would want fixed before merge: both are one-assertion changes to a test this diff already opened, and both guard the specific failure mode #361 was filed about.

## Review: `origin/main...HEAD`

WARNING tests/test_restart_hook_cli.py:L88 — the hook integration tests pass `os.environ` directly but do not remove `WFCTL_RESTART_THRESHOLD`; a developer with the supported variable exported gets a different in-force threshold and this test fails (reproduced with `WFCTL_RESTART_THRESHOLD=90000`: expected 200000, recorded 90000). Clear the variable in the fixture or pass an explicit environment mapping to every test that asserts the default.

No BLOCKER findings.

Passes run:
- Correctness: traced hook, event-log, detached sender, handoff, and installer flows; found the test isolation issue above.
- Security: reviewed payload/config parsing, subprocess arguments, event handling, and output; no finding.
- Architecture: inspected all 11 accepted records via `wfctl arch context` (the required `uv run` invocation panicked in this sandbox; the worktree venv command succeeded); no violation found.
- Readability & simplification: no finding.
- Performance: no finding.
- Over-engineering: Lean already.

Validation:
- Relevant tests with inherited environment: 159 passed, 1 failed as above.
- Same 160 tests with `WFCTL_RESTART_THRESHOLD` unset: passed.
- Ruff, mypy, and `git diff --check`: passed.

net: Lean already.
Verdict: Request changes

---
Note: codex's sandbox refused to write this file directly ("sandbox policy
permits writes only inside this worktree") — this content was recovered from
its stdout log
(`/private/tmp/claude-501/-Users-andremarin-Development-wfctl-wt-371-write-state-before-clear/1528e5e9-3e96-44bc-94d4-a5834c8bb00d/scratchpad/codex_r4.log`)
and copied here verbatim by the session coordinator, not edited.

# Review: 104e333 (closes #198)

Reviewer: r3. Target: `git show 104e333` (3 files: `tests/test_skill_cross_references.py`,
`wfctl/agents/skills/design-levels/SKILL.md`, new
`wfctl/agents/skills/software-design-decisions/SKILL.md`).

## Passes run

1. **Correctness** — read the new `SKILL.md` against the pre-existing
   `design-record-template.md` line by line: every section it names (`Direct
   baseline`, `Diagram`, `Considered`, `status`, `Log`) exists in the template
   with matching meaning; no field invented, none of the template's guidance
   restated. Confirmed the template itself is untouched in the diff (byte-for-
   byte same content, only new files/edits elsewhere).
2. **Packaging trace** — followed the file from source to every consumer:
   - `MANIFEST.in` grafts `wfctl/agents` wholesale (`graft`, not glob) — new
     `SKILL.md` ships in the wheel/sdist same as the template already did.
   - `pyproject.toml` `[tool.setuptools.packages.find]` includes `wfctl*`, no
     narrower package-data filter in play.
   - `wfctl/cli.py` `_BASE_LAYER` copies all of `agents/skills` to `.agents/skills/`
     for every agent unconditionally (confirmed via `_BASE_TARGETS`/`_BASE_LAYER`
     wiring at cli.py:2427). `bob` additionally mirrors the whole
     `agents/skills` tree to `.bob/skills/`, `copilot` to `.github/skills/`
     (`_AGENT_TARGETS`, cli.py:1267-1279) — neither is filtered by
     `_MIRRORED_SKILLS`, so both get `SKILL.md` and the template together.
   - Only `.claude/skills/` (the native-discovery extra,
     `_claude_native_skill_mirror`) requires membership in `_MIRRORED_SKILLS`,
     and the new skill is deliberately not added there. Ran `uv run pytest -q
     tests/test_skill_cross_references.py tests/test_install_skills.py` (172
     passed) to confirm nothing in the install-skills suite assumes otherwise.
   - Checked the precedent: `architecture-decisions` (the level-2 sibling) is
     *also* only reached via the base layer plus bob/copilot mirrors — it has no
     `.claude/commands/*.md` wrapper and no `digest.md` either. So the omission
     here is symmetric with the skill this change is explicitly modeled on, not
     a new gap. The commit message states the choice and reason
     ("Not mirrored into `.claude/skills/`... its own reviewable diff") —
     verified that reasoning against the actual code rather than taking it on
     trust, and it holds.
3. **Staleness / consistency grep** — searched for other places that describe
   the level-2/level-3 split or enumerate shipped skills:
   `docs/architecture/` (only `architecture-decisions/record-template.md` is
   named, in `docs/architecture/design/122-a-record-persists-no-routing-state.md`,
   unrelated to level 3), `README.md` (names `design-levels` only, not
   individual sub-skills — no update needed), `wfctl/agents/skills/
   architecture-design/` (no reference to either record skill), `speckit-*`
   skills (no matches), `NOTICES.md` (does not exist in this repo — not
   applicable). Nothing found that should have been touched and wasn't.
4. **Definition-of-done check against #198** — both bullets:
   - "A level-3 agent reaches `design-record-template.md` through a reference
     that ships" — satisfied: `design-levels/SKILL.md`'s level-3 gate
     ("Software design — which of these claims...") and its "Where the levels
     land" section both now name `.agents/skills/software-design-decisions`,
     matched by `_REFERENCE` regex and asserted by
     `test_the_level_3_gate_names_the_design_record_skill`.
   - "A test pins whatever lands, in the shape of
     `test_the_record_template_ships_beside_the_adr_skill`" — satisfied:
     `test_the_design_record_template_ships_beside_its_skill` mirrors that
     test's shape (asserts `SKILL.md` and `design-record-template.md` both
     exist under the skill dir), and a second test
     (`test_the_level_3_gate_names_the_design_record_skill`) mirrors
     `test_the_level_2_gate_names_the_record_skill`. Ran both:
     `uv run pytest -q tests/test_skill_cross_references.py` → 14 passed.
5. **Style/lint** — `uv run ruff check` on the new skill path reports no
   Python files (expected, it's Markdown); full-repo ruff/mypy not run since
   the diff touches no `.py` logic beyond the two new test functions, which
   are plain asserts matching the file's existing idiom.

## Findings

None. Checked correctness, packaging/consumer reach for every layer
(`.agents/`, `.claude/`, `.bob/`, `.github/` via copilot), cross-reference
staleness elsewhere in the tree, and both #198 definition-of-done bullets —
all satisfied and verified against the files, not the commit message's claims
about them.

net: 0 lines possible (no over-engineering found; the asymmetric
`_MIRRORED_SKILLS` omission is a deliberate, correctly-scoped exclusion, not
padding).

**Verdict: Approve**

## Review: 104e333 (origin/main..HEAD)

Passes run: correctness (full test suite + ruff + mypy on the touched Python
file), architecture/duplication (diffed against the level-2 model and its own
"do not restate" instruction), security (n/a — no code path, docs + one test),
performance (n/a), over-engineering, and a check of the three scope decisions
the author called out.

Verified, not assumed: `uv run pytest -q` (1108 passed), `uv run ruff check
tests/test_skill_cross_references.py` (clean), `uv run mypy
tests/test_skill_cross_references.py` (clean), `git show 104e333`, `gh issue
view 198`, `gh issue view 121`, `wfctl/_arch.py:145` (`load_records` globs
`*.md` non-recursive, confirming `<arch-root>/design/` stays out of the
in-force projection as claimed), `wfctl/cli.py:1512` (`_MIRRORED_SKILLS`
contents), `ls wfctl/agents/commands/` (no wrapper for any of the three skills
in play here).

---

WARNING  `wfctl/agents/skills/software-design-decisions/SKILL.md:59-96` —
restates the template it points at, immediately after telling the reader not
to → cut the four restated passages down to bare pointers.

The "Writing one" section opens with an explicit rule (L60-61): *"the template
carries the field guidance and the placement rule; do not restate them here or
in the record."* The next four things in the same file do exactly that:

| SKILL.md | design-record-template.md |
|---|---|
| L66-70 "smallest implementation that introduces no new abstraction... A baseline written as a sentence of hand-waving justifies a decision rather than making one" | L44-46 "smallest implementation that introduces no new abstraction... A baseline that is a sentence of hand-waving justifies a decision rather than making one" |
| L72-76 "same components in each... Two identical graphs say nothing" | L55-58 "same components in each... two identical graphs say nothing" |
| L80-82 "draws only dividers already in force... appearing for the first time means the decision was level 2 — stop, route it up" | L68-71 "A Level-3 record draws only dividers already in force. One appearing here for the first time means the decision was Level 2 — stop, route it up" |
| L90-91 "`proposed` → `approved` → `superseded` \| `rejected`. Only a human moves a record past `proposed`" | L17 "`status` runs proposed -> approved -> superseded \| rejected. Only a human moves it past proposed" |

Compare `architecture-decisions/SKILL.md`'s own "Writing one" (L58-63): it
copies zero field content from `record-template.md` — it names the six
required sections and stops. `record-template.md:16-17` carries the identical
"hand-waving" sentence this diff duplicates, and the level-2 skill still
doesn't restate it. So this isn't the established shape drifting slightly; the
new file breaks the pattern its own commit message claims to follow ("does
not restate its field guidance").

This is exactly the failure class `test_the_change_description_skill_does_not_restate_the_template`
(tests/test_skill_cross_references.py:215) and
`test_the_panel_skill_does_not_restate_the_rubric`
(tests/test_review_fanout_skill.py) exist to catch elsewhere in this repo
(#50: a second home for a fact drifts silently) — but neither test's method
covers this file: the first diffs against a PR template by literal `#`
headings, the second by rubric pass headings: prose restated as bold-lead
paragraphs slips through both. No test failed here; this was read by eye
against the template.

Fix: mirror the level-2 shape. Something like *"Two of its sections are the
ones an agent drops first: `Direct baseline` and `Diagram`. Fill them in from
the template — do not summarize them here."* for L66-76, and drop the status
paragraph (L90-91) entirely since it's already the record's own frontmatter
comment. That's roughly 15-18 fewer lines and removes the internal
contradiction.

---

WARNING  `wfctl/cli.py:1512` / `wfctl/agents/skills/design-levels/SKILL.md:145-151` —
`software-design-decisions` has no command wrapper and isn't in
`_MIRRORED_SKILLS`, so it isn't on Claude's native Skill-tool discovery path —
confirmed live: this session's own available-skills listing carries
`architecture-decisions` and `design-levels` by name, not
`software-design-decisions`.

The commit message's stated reason for the omission is "the reference chain
resolves without it." That's true for an agent that treats
`.agents/skills/software-design-decisions` in `design-levels`' prose as a raw
path to `Read`, and false for one that relies on the Skill tool's own
listing — which is the primary route through every other skill in this file,
`software-design-decisions` included going by its own frontmatter
`description`. A skill absent from the listing cannot be invoked by name
through that tool at all; the only way in is a human handing over the path,
or an agent that happens to already be reading `design-levels/SKILL.md` as
text rather than through the Skill tool. That is the shape of #198 itself —
"an agent reached it only if a human handed over the path" — recurring one
hop down the pointer chain the fix just built.

There's a direct precedent test for this exact shape:
`test_the_panel_skill_is_model_invocable`
(tests/test_review_fanout_skill.py:166-175) asserts
`"fanning-out-code-review" in _MIRRORED_SKILLS` specifically because that
skill has no command wrapper and its trigger ("a change about to be merged")
is a moment nobody types a command — the docstring: *"Membership in
`_MIRRORED_SKILLS` is what puts the skill on Claude's native discovery path;
without it the command wrapper is the only way in, which is the failure being
designed out."* `software-design-decisions` has the identical shape (no
wrapper under `wfctl/agents/commands/`, description-triggered, meant to be
reached without a human naming it) and carries no equivalent guard.

I don't think this sinks the change — it is still strictly better than the
pre-#198 state (zero references anywhere), and the commit is explicit that
mirroring is a separate, reviewable decision rather than a drive-by. But the
stated justification doesn't hold for the interaction mode this repo's own
tooling actually uses, and the issue's own definition of done ("a level-3
agent reaches `design-record-template.md` through a reference that ships") is
only half-delivered if "reaches" means "the Skill tool can invoke it." Worth
a deliberate yes/no from the author rather than resolving on the strength of
the commit message alone — if the answer is still no, a short comment at
`_MIRRORED_SKILLS` (or a test asserting the omission, mirroring
`test_the_panel_skill_is_model_invocable`'s shape but asserting *not in*)
would make the decision auditable the next time someone reads that frozenset
looking for the rule.

---

Scope decisions checked against the files, not taken on trust:

- **Template unchanged, #202 open** — confirmed;
  `design-record-template.md` has no changes in this diff. Holds.
- **`diagram-grammar.md` not written** — confirmed against #121 scope item 2
  (three files named: `SKILL.md`, a record template, `diagram-grammar.md`).
  `design-record-template.md`'s `## Diagram` section (read in full) already
  carries the grammar: box/divider/stability-axis rules, arrow labelling, the
  escalation tell. That file predates this diff (#123), so this commit isn't
  making that call now — it's correctly out of scope for #198, and #121's
  scope item 2 is either already satisfied by inlining or still open
  elsewhere; either way not this diff's problem. Holds.
- **Not added to `_MIRRORED_SKILLS`** — see the WARNING above. Doesn't hold
  cleanly; flagging for a deliberate second look rather than blocking on it.

No BLOCKER-level findings — nothing here is incorrect behavior, a security
hole, or data loss; both new tests pass, mirror the level-2 pair's naming and
docstring conventions ("why this test exists," per AGENTS.md's testing
conventions section), and `uv run pytest -q` is green at 1108 passed with no
new ruff/mypy findings on the one Python file touched.

net: −15 lines possible (the restated template content)
Verdict: Request changes (2 warnings — the restatement is a small, mechanical
fix; the discoverability gap is a judgment call the author should confirm
rather than one I'd block alone on, but it should be an explicit yes on
review, not silence)

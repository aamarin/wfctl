# Review: 104e333 (feat(skills): give the level-3 record template a skill that points at it)

Reviewer: r2

## Passes run

1. **Context** — read issue #198 in full (`gh issue view 198`), the target commit
   (`git show 104e333`), confirmed `git diff origin/main..HEAD` is exactly this
   one commit / three files (no drift between the two diff forms named in the
   brief). Read root `CLAUDE.md` in full (code style, testing conventions,
   definition of done, comments-inform-not-narrate rule).
2. **Model comparison** — read `architecture-decisions/SKILL.md` and
   `record-template.md` (the level-2 pair this change mirrors) side by side with
   the new `software-design-decisions/SKILL.md` and the pre-existing
   `design-record-template.md`.
3. **Followability walkthrough** — read the new `SKILL.md` top to bottom as an
   agent producing a level-3 record would, cross-checking every instruction
   against the template it hands off to.
4. **Falsifiable claims** — checked every assertion the new prose makes about
   wfctl's actual behavior against the source and by running commands:
   - `uv run wfctl arch-root` → prints `.../docs/architecture`, matches the
     skill's description of the command exactly.
   - `uv run wfctl arch context --help` and `wfctl/cli.py:848-910` — confirmed
     `arch_context_cmd` only reports `status == "accepted"` records, and
     `_arch.load_records` (`wfctl/_arch.py:135-144`) globs `root.glob("*.md")`
     non-recursively, so `<arch-root>/design/` is structurally invisible to it.
     This backs the skill's "Level-3 records are durable and never binding"
     claim.
   - `wfctl/cli.py:299-307` and `:4192-4197` — explicit comments confirm
     `design/` is deliberately excluded from the boundary-touched check and from
     `arch validate`, for the same level-3-records-aren't-ownership-claims
     reason the new skill states. Also surfaced a pre-existing, already-tracked
     gap (#166, cited in the code) that the level-3 template's status vocabulary
     (`proposed → approved → superseded | rejected`) differs from the level-2
     one (`accepted` not `approved`) and is unchecked — pre-existing in the
     unmodified template, correctly out of scope here per the commit message
     ("The template itself is unchanged; if it is wrong, that is #202").
   - `.wf-skills-manifest.json`, `.claude/skills/`, `.agents/skills/` on disk —
     confirmed `architecture-decisions` mirrors into `.claude/skills/` and
     `software-design-decisions` does not; `wfctl/cli.py:1601-1620` shows this
     is gated by an explicit `_MIRRORED_SKILLS` allowlist, and the reference
     `design-levels` makes is by `.agents/skills/…` path (present for every
     agent), not by `.claude/skills/` discovery — so the commit message's claim
     that "the reference chain resolves without it" is accurate, and leaving
     the mirror out is a legitimate, explained scope cut, not a silent gap.
   - `MANIFEST.in`'s `graft wfctl/agents` — confirmed the new `SKILL.md` needs
     no separate packaging entry; the whole tree is grafted, matching how
     `design-record-template.md` already ships.
5. **Test scrutiny** — read both new tests against `test_every_referenced_skill_ships`,
   `test_the_record_template_ships_beside_the_adr_skill`,
   `test_the_level_2_gate_names_the_record_skill`, and `test_skill_frontmatter.py`,
   checking for tautology or coverage overlap.
6. **Full verification** — ran the repo's definition of done:
   `uv run pytest -q` (1108 passed), `uv run ruff check wfctl/ tests/` (clean),
   `uv run mypy wfctl/` (clean, 17 files). Also ran the frontmatter parse on the
   new file directly (`_arch._frontmatter`) to confirm only `name`/`description`
   keys, matching `ALLOWED_KEYS` in `test_skill_frontmatter.py`.

## Findings

None. This is a clean, well-grounded change.

- **Followability**: walked `software-design-decisions/SKILL.md` end to end as
  the agent that would use it. Order is Overview → When to Use → Where records
  live (destination path, established before the copy step) → Writing one
  (copy `design-record-template.md`, don't restate its fields) → Escalation →
  Status → Common Rationalizations → Verification checklist. Nothing
  contradicts the template: "Direct baseline" and "Diagram" (called out as the
  two sections agents drop first) match the template's own field guidance
  word for word in intent; the "checked/assumed split stays in `design.md`;
  the record carries only the claims its own decision rests on" line matches
  the template's `Verified`/`Assumed` sections, which literally say "list only
  the facts the decision rests on." I could not find a step where I would not
  know what to do next.
- **Test weight**: `test_the_design_record_template_ships_beside_its_skill` and
  `test_the_level_3_gate_names_the_design_record_skill` are the level-3 mirror
  of the level-2 pair and are not redundant with
  `test_every_referenced_skill_ships` — that test only proves "if X is
  referenced, X ships," never the converse ("design-levels still references
  X"), which is exactly the regression these two catch (deleting the new
  reference line would leave `test_every_referenced_skill_ships` green while
  silently reintroducing #198). Neither is tautological; both docstrings name
  the failure mode per the repo's convention.
- **design-levels/SKILL.md edit**: read both insertions in full surrounding
  context (the level-3 gate section and "Where the levels land"), not just the
  diff hunks. Both are accurate claims about current wfctl behavior (verified
  above), consistent with each other, and read as information for the next
  reader rather than narration of the diff (no issue-number or "this change"
  language in the skill body itself — #198 only appears in the test docstrings,
  which matches the existing convention elsewhere in this same test file, e.g.
  `#204`, `#187`, `#226`).
- Diffstat matches the stated scope: one commit, three files, 156
  insertions / 2 deletions.

**Verdict: Approve.**

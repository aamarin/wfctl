## Review: 1d7284b (fix(verify): reach the changes that skip the pipeline)

Reviewer: r3

### Scope checked
- `git show 1d7284b` and `git diff origin/main...HEAD` are identical (single
  commit, matches branch).
- Read `CLAUDE.md`, `docs/architecture/wfctl-runs-the-verification.md`,
  `docs/architecture/a-rule-is-expressed-as-a-check.md`, and (because the diff's
  own docstring leans on it) `docs/architecture/the-underscore-is-the-module-contract.md`.
- Traced every call site of `verification_block` / `_verification_block`
  (grep across the whole tree — none left on the old private name).
- Traced `resolve_agent_dir(repo_root, resolve_branch(repo_root))` inside
  `verification_block` against `_resolve_context()`'s identical computation in
  `verify_cmd`, to confirm `check-body` reads the record from the same place
  `wfctl verify` writes it.
- Ran, read-only: `uv run --frozen --extra dev pytest -q` (1110 passed),
  `uv run --frozen --extra dev ruff check wfctl/ tests/` (clean),
  `uv run --frozen --extra dev mypy wfctl/` (clean). Did not run `wfctl verify`,
  did not edit or commit anything.

### Pass 1 — Correctness
No BLOCKER. Traced the full call chain:
- `verification_block(repo_root)` (renamed from `_verification_block`, logic
  unchanged) still checks the config-read-before-git ordering the docstring
  claims (`load_config` first, `errs`/`not commands` return before any
  subprocess call) — confirmed by reading the body, not just the docstring.
- `_verification_finding()` in `cli.py` correctly degrades to `[]` on
  `SystemExit` from `get_repo_root()` (not-a-repo case), and passes through
  `verification_block`'s `None` (no config / passed) as `[]` too. Three
  silences collapse to the same empty list, as documented.
- The three example lines in `opening-a-change/SKILL.md` (`unverified`,
  `failed — 1 of 3 at a1b2c3d: pytest -q`, `stale — verified at a1b2c3d, HEAD
  is e4f5a6b`) match the actual f-strings in `_pipeline.py` byte-for-byte
  (format, punctuation, em dash).
- Requirement "must not be a hard gate": confirmed nothing in the repo
  consumes `check-body`'s exit code programmatically — grepped
  `finishing-a-development-branch/SKILL.md` and `opening-a-change/SKILL.md`;
  the only caller is the agent, told to "run it, then read what it says."
  Same as the pre-existing shape/panel findings' gate-nothing contract.
- Requirement "must be silent when the repo declares no definition of done":
  confirmed both by reading `load_config` (missing/empty `wfctl.json` → `[],
  []` → `None`) and by the new
  `test_check_body_is_silent_when_the_repository_declares_no_definition_of_done`.
- `verification_block` going public is exactly the move
  `the-underscore-is-the-module-contract` (proposed, not accepted) recommends
  for a name `cli` needs — the diff follows that record's reasoning even
  though it's non-binding, and does so via `cli.py`'s established pattern of
  function-scoped `from wfctl._pipeline import ...` (matches 6 other call
  sites in the file, not a new pattern).

### Pass 2 — Security
Nothing applicable — no new user input, no new subprocess construction (reuses
existing argv-list `_verify`/`_paths` calls), no secrets.

### Pass 3 — Architecture
Clean. `_shape` and `_body` stay pure string functions; the one function that
touches git state (`_verification_finding`) lives in `cli.py`, matching its own
docstring's stated boundary. Reuses one function (`verification_block`) for
both the `implement`-step check and the file-level check rather than forking a
second, looser implementation — the thing `test_check_body_reports_a_record_the_branch_has_moved_off`'s
docstring explicitly calls out as the point of the test.

### Pass 4 — Readability
Docstrings are dense but match this repo's stated convention ("comments
inform... explain *why this shape*", `CLAUDE.md`). No nesting, no boolean-flag
params, no clever tricks introduced.

### Pass 5 — Performance
Negligible. `verification_block` returns before touching git when no
`wfctl.json` exists (the common case for most repos), and when commands exist
the added cost is 1–2 already-cheap git subprocess calls, run once per
`check-body` invocation (an interactive, not hot-path, command).

### Pass 6 — Over-engineering
Lean. `_verification_finding()` as a separate function is justified by its own
docstring (keeps `_shape`/`_body` state-free) and isn't speculative — one
caller, one reason to exist separately.
net: no reduction found; nothing to cut.

### Findings

WARNING `wfctl/cli.py:3306` (`_verification_finding`, the `except SystemExit:
return []` branch) — untested. This is new logic (the old code path never had
to decide what to do when `get_repo_root()` fails), and no test exercises
`check-body` when `WFCTL_REPO_ROOT` is unset and the CLI runner's cwd is
outside a git repo. Everything else the diff added has a direct test
(`test_check_body_reports_a_definition_of_done_that_never_ran`,
`..._is_silent_when...`, `..._goes_quiet_once...`,
`..._reports_a_record_the_branch_has_moved_off`); this branch does not.
Low risk (it's a two-line try/except) but it's exactly the kind of thing that
silently regresses to a stack trace if `get_repo_root`'s exception type ever
changes → add one test that monkeypatches `WFCTL_REPO_ROOT` to a non-repo path
(or unsets overrides entirely with cwd outside git) and asserts `check-body`
still exits cleanly on shape/panel-clean input.

NIT `README.md:165` — the `check-body` command-table row ("Check a PR
description's drawings against `conversation-response-shape`") still only
describes the drawing-rules finding, not the panel finding (pre-existing gap)
or now the verification finding (new gap this diff could have closed while it
was touching the command's contract). Not a regression this diff caused — the
panel-check omission predates it — but it was the natural moment to fix it.

net: 0 lines to cut (nothing found to remove or shrink).

Verdict: **Approve** (0 blockers, 1 warning worth fixing before/soon after
merge, 1 optional nit).

# Review: r2 — `git diff main...HEAD` (390-code-review-dependency-check)

Target: one commit, `0ad07db`, +7 lines in
`wfctl/agents/skills/code-review/SKILL.md`. No spec or plan exists in
`FEATURE_DIR` — correct for a change `design-levels` excludes, and the commit
message carries the reasoning a plan would have.

## Coverage — what each pass covered

**1. Correctness.** Checked the addition against issue #390's five items: all
five are present (existing stack, size, maintenance, vulnerabilities, license),
it lands inside an existing pass rather than as a seventh pass or a new report
section, and it adds no report-format obligation. Checked the file's own
invariants that the addition could have falsified: the frontmatter
`description` ("six lenses"), the body's "the five quality axes" (line 10), the
`## Step 4 — The six passes` heading, and
`tests/test_review_fanout_skill.py::test_the_panel_skill_does_not_restate_the_rubric`,
which re-derives the pass list from `^\*\*\d\. ` headings and asserts exactly
six. The addition adds no numbered heading, so all four still hold. Checked
`wfctl/agents/commands/code-review.md`: it is a pointer to the skill and
enumerates the six lenses only, so it needed no edit. `.agents/` is in sync with
the source, so the author did re-install. Finding: none.

**2. Security.** The addition instructs a reviewing agent to run `pip-audit` and
`npm audit`. `pip-audit` against a project (rather than a lockfile) resolves
dependencies through the build backend, which executes repository code — but the
reviewer is already in a repo whose test suite Step 6 tells it to run, so the
incremental exposure is nil. No secrets, no input handling, no new execution path
in wfctl itself. Finding: none.

**3. Architecture.** Checked placement against three accepted records.
`knowledge-placement` supports the single home: a cross-reference from the
Security pass to the same checklist would be the second home it forbids, and the
author's choice of Architecture over Security is argued in the commit rather than
assumed. `a-rule-is-expressed-as-a-check` — I ran its test rather than skipping
it, since it is the axis panels in this repo have missed before. It resolves to
the prose side, on an argument already written for this exact class:
`opening-a-change` Step 1 says of the review panel's own reports that "those
files are written by the same agent whose work they attest to ... A report the
agent wrote about its own work, in a store outside the tree, answers that no. So
the rule is prose." A dependency-review bullet inside a reviewer's rubric is the
same self-report one level in. No check is owed and none should be filed.
`vendor-upstream-skills` — see WARNING 2. Checked for duplication across the
skills tree: no other skill carries dependency-review guidance;
`architecture-design`'s "a dependency the project already carries" is a
design-time fit check at a different moment, not a second copy of this.

**4. Readability & one voice.** Read the whole file, not the hunk. The lead
sentence carries its rationale in the repo's register and reads as one voice
with the file. Two seams: the sub-list is the only two-level list in the file
(`^  - ` matches these five lines and nothing else), and within the sub-list the
first item is a question while the other four are noun fragments. Line widths
(max 77) match the file's wrap. Spelling: "License" against `docs/architecture`'s
5:2 preference for "licence" — the repo is genuinely mixed here, so not raised.

**5. Performance.** Not applicable to a prose diff, with one substantive reading
of it — the cost this change imposes on every future review run. Three of the
five sub-bullets require data outside the diff (registry metadata, advisory
database, license), which is the review-time cost and the source of WARNING 1.

**6. Over-engineering.** The addition is the shortest thing that carries five
distinct questions, but it spends a hierarchy level and a rationale clause the
surrounding passes do not. See the NIT and the metric.

## Findings

WARNING  wfctl/agents/skills/code-review/SKILL.md:L86-89 — three of the five
sub-bullets (size, maintenance, advisories) need data the diff does not contain,
and the rubric says nothing about the reviewer that cannot reach it. Since pass 3
resolves to the prose side, the prose is the only thing observing this pass —
and a reviewer who skipped the lookups produces a report identical to one that
ran them clean. → close the bullet with the disposition, e.g. "say which of these
you could not check rather than passing it". One clause, and it is what makes the
addition change what a reviewer does rather than what it reads.

WARNING  wfctl/agents/skills/code-review/SKILL.md:L83-89 — provenance is
undeclared. The five items match `addyosmani/agent-skills`'
`code-review-and-quality` "Dependency Discipline" list one-for-one in content and
order (existing stack / size / maintained / vulnerabilities / license), reworded;
#390 names that skill as the source, and the commit message says only "the
comparison". That upstream is already attributed in this repo for `idea-refine`,
so a reader who follows #390 will look for a row in `vendor-upstream-skills` and
find none — the situation #370 had to amend that record to resolve for
`python-pattern-selection`. I do **not** think a row or a last-line attribution
is owed: the mechanism attributes a whole file, and asserting that the entire
`code-review` skill is derived would be false, while five commonplace
dependency-review questions are the "public and unowned" case that record already
names. What is missing is that disposition written down anywhere. → one sentence
in the PR body's Additional Context naming the upstream and why nothing is owed.
Amending the record is the heavier option and disproportionate here.

NIT  wfctl/agents/skills/code-review/SKILL.md:L83-89 — `shrink:` the only
two-level list in the file, and #390 asked for "the same one-line-per-finding
format the other passes use". The lead clause also justifies itself, which no
other bullet in Step 4 does. → fold to a flat bullet, which also has room for
WARNING 1's clause:

    - A new third-party dependency is coupling pointed outward, and earns its
      place or goes: what the existing stack already does, install size and the
      tree behind it, recent releases, advisories (`pip-audit`, `npm audit`),
      license fit — say which of those you could not check rather than passing it.

Worth saying against my own NIT: the sub-list makes each question separately
scannable for an agent working down a checklist, which is a real argument for
keeping it. Author's call.

net: −3 lines possible
Verdict: Approve (0 blockers; 2 warnings worth fixing before merge — both are
one line each, and the NIT's shorter form carries the first of them)

## Review: r1 — `git diff main...HEAD` on 390-code-review-dependency-check

Target: one commit, `0ad07db`, +7 lines in
`wfctl/agents/skills/code-review/SKILL.md` (Step 4, pass 3 Architecture).
Context read: issue #390 as quoted in the brief, the commit body, `CLAUDE.md`,
`uv run wfctl arch context`, `docs/architecture/a-rule-is-expressed-as-a-check.md`,
`wfctl/agents/skills/NOTICES.md`, the tests that read this file, and the
sibling skills that reference the rubric. No tests were run — the brief forbids
it — so everything below about tests is read from their source.

### Passes run

**1. Correctness** — checked the five questions #390 asked for against the five
delivered: existing stack, install/bundle cost, maintenance, advisories,
licence. All five are present and none was added beyond them. The addition is a
bullet inside an existing pass, not a seventh pass and not a new report section,
which is what the issue asked. Checked the tests that constrain this file:
`tests/test_review_fanout_skill.py::test_the_panel_skill_does_not_restate_the_rubric`
counts `^\*\*\d\. …\*\*` headings and asserts exactly 6 — unchanged, since the
addition adds no heading; `_REFERENCE` in the three test files matches only
`.agents/skills/<name>`, so the new backticked `pip-audit` / `npm audit` are not
read as skill references. No test asserts line length or list shape on skill
bodies. Every other place that enumerates the rubric says "six" — the command
wrapper `wfctl/agents/commands/code-review.md`, `fanning-out-code-review`,
`python-pattern-selection`, two arch records — and all of them stay true. One
finding, below, about a bullet a reviewer cannot actually answer.

**2. Security** — the diff adds prose and no code, so there is no input,
boundary, secret or query to assess. Reviewed the added text *as* security
guidance instead: naming `pip-audit` / `npm audit` and the advisory database
points at local, first-party tooling rather than at an arbitrary scanner, and a
known-CVE dependency already maps to BLOCKER under Step 5's table ("security
hole"), so a finding raised through the new bullet has a severity path without
the table being touched. Nothing found.

**3. Architecture** — the placement question, which is the substance of this
change. Four checks:
- *Which pass.* Architecture already carries "coupling that shouldn't exist";
  an adopted dependency is that question pointed outward, and the commit body
  argues it against the file's own "one review instead of four overlapping
  ones". A seventh pass would also have broken the `== 6` assertion above and
  the "six lenses" wording in five other files. Correct call. Splitting
  advisories off into Security was the alternative #390 left open; keeping all
  five together is defensible and I would not move them — a reviewer answering
  "is this dependency worth it" answers all five at once or none.
- *Layer.* Edited under `wfctl/agents/`, the committed source, not a generated
  dotted directory — `layer-model` satisfied. `.agents/skills/code-review/SKILL.md`
  in this worktree is byte-identical, and the `.claude` layer reaches this skill
  through the command wrapper rather than the `_MIRRORED_SKILLS` mirror, so
  there is no third copy to drift.
- *Not a derived skill.* `NOTICES.md` and `vendor-upstream-skills` list the
  superpowers- and spec-kit-derived files; `code-review` is on neither list, so
  the in-place edit is safe from the upstream-pull reversion `CLAUDE.md` warns
  about for `speckit-clarify` / `speckit-analyze`. Worth stating because the
  same edit made one directory over would have been silently reverted.
- *Prose vs check* (`a-rule-is-expressed-as-a-check`). The new rule constrains
  the reviewer, and the record's test is whether a violation is visible in an
  artifact the work already produces. Nothing in wfctl reads `REVIEW.md` — the
  only two mentions of it are in skill bodies — so observing "did the reviewer
  ask about the dependency" would need the diff paired with a report nothing
  parses today, which is the artifact-invented-for-the-check case the record's
  *already produces* clause excludes, and inferring it from report prose is the
  judge the record declined. This sorts onto the prose side, where the record
  says it is not a defect. A skill body is prose delivered at the moment it
  binds, so the change is on the right side of that line.
No findings in this pass.

**4. Readability** — voice and shape against the rest of the file. The lead-in
carries its rationale ("is coupling too, pointed outward"), which matches the
file's other rationale-carrying bullets ("Apply Chesterton's Fence…",
"'Compiles' and 'tests pass' are **not** proof of correctness") rather than
reading as a graft. Wrap is 73–77 columns against a file that wraps at ~80.
Two-space nested bullets are used by `end-session`, `start-session` and
`conversation-response-shape`, so the sub-list is house idiom and not imported
shape — I checked this before flagging it, and it is not a finding. Spelling is
American throughout the added lines, matching the file ("behavior",
"sanitized"). One NIT below, on the issue's stated format rather than on voice.

**5. Performance** — no runtime code. The applicable cost is context: +7 lines
(~70 tokens) in a file every reviewer reads in full, and a `fanning-out-code-review`
panel reads it three times per run. Roughly 200 tokens per panel. Negligible
against the file's ~2.5k. Nothing found.

**6. Over-engineering** — no dead code, no speculative flexibility, no
abstraction with one caller; the addition is content, not structure. The only
shrink available is the one in the NIT.

### Findings

WARNING  wfctl/agents/skills/code-review/SKILL.md:L87 — "Maintained — releases
and answered issues recently, not abandoned." names no source the reviewer can
read. Every other bullet in the six passes is answerable from the diff, the
called functions, or `git blame`; this one is answerable only from the package's
release history, which a reviewer without network reaches only through training
data that is stale by construction. The likely output is not a missing finding
but a fabricated clean one — a package archived after the cutoff reports as
maintained, and the rubric has just laundered a guess into a check. L88 escapes
this only because it names the commands. → name the action the same way L88
does, and say what to write when it cannot be run: "Maintained — release history
and open issues, checked not assumed; report *unverified* rather than guessing."

NIT      wfctl/agents/skills/code-review/SKILL.md:L83-L89 — `shrink:` #390 asked
to "keep it to the same one-line-per-finding format the other passes use", and
the three bullets above it each pack several checks onto one line ("Duplication
that should be shared; coupling that shouldn't exist."). The sub-list is a
different shape from the pass it joins. → optional, and I would not block on it:
discrete bullets are easier for an agent to answer one at a time, which is a
real argument the issue did not weigh. The compressed form, if the author wants
the issue's letter: "- A new third-party dependency is outward coupling and has
to earn its place: does the stack already do this; what the install and its
transitive tree cost; release history; advisories (`pip-audit`, `npm audit`);
licence fit." — three lines, same five questions.

net: −4 lines possible
Verdict: Approve (0 blockers; 1 warning worth a one-line fix before merge)

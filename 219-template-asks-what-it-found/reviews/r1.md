## Review: git diff (working tree) — issue #219

Target: two-file diff, `.github/pull_request_template.md` and
`wfctl/agents/configs/github/.github/pull_request_template.md` — extends the
`## Additional Context` comment block to prompt for findings that are not the
change itself, and adds one `## Checklist` line.

### Passes run

1. **Correctness** — read both changed files in full, diffed them against each
   other (`diff .github/pull_request_template.md
   wfctl/agents/configs/github/.github/pull_request_template.md`) → identical,
   confirming the AC "both copies... remain byte-identical" holds today. Read
   `gh issue view 219` in full and checked the new prose against every stated
   acceptance criterion. No new section was added; `Additional Context` stays
   the single home. The checklist line and the extended comment block read
   correctly and match the issue's own proposed wording (and go a little
   further, folding in the "measurement vs. conclusion" distinction from the
   issue's PR #214 evidence — that's elaboration on the issue's own material,
   not scope creep).
2. **Security** — n/a, markdown/comment-only change, no code path touched.
3. **Architecture** — checked `.agents/skills/opening-a-change/SKILL.md`: it
   still carries no section list (confirmed by reading it and by the diff not
   touching it), matching the issue's explicit constraint. Checked
   `wfctl/cli.py`'s `_CONFIG_SOURCES`/install-config machinery
   (`wfctl/cli.py:1140`): `install-config github` is a straight file copy from
   `wfctl/agents/configs/github/.github/pull_request_template.md`, so the
   seeded template automatically carries the new prompt with no separate
   wiring needed — AC "`wfctl install-config github` seeds the updated
   template" is satisfied structurally.
4. **Readability** — multi-line `<!-- … -->` block matches the style already
   used by `Before / After`, `Implementation Details`, and `Test Details` in
   the same file; line lengths (max 82 in the new block) are within the file's
   existing range (up to 108 elsewhere). No nits.
5. **Performance** — n/a.
6. **Over-engineering** — nothing to cut; this is two prose paragraphs and one
   checklist line, proportionate to the issue.

### Tests run

- `uv run pytest -q tests/test_install_config.py tests/test_skill_cross_references.py tests/test_skill_commands.py` → 35 passed
- `uv run pytest -q` (full suite) → 939 passed
- `uv run ruff check wfctl/ tests/` → all checks passed
- Grepped the whole repo for any place that depends on the old exact wording of
  `Additional Context` (`grep -rn "Anything else a reviewer\|Additional Context" wfctl/*.py .agents/skills/**/*.md`) → nothing found, so the rewording breaks nothing.

### Finding

WARNING `tests/` (no file) — the issue's own Verification section says:
"Automated: whatever currently pins the two templates as identical still
passes; extend it if nothing does." I checked for an existing pin —
`test_install_config.py`'s `_seed_github` tests operate on a fake bundle
fixture, not the real repo files; `test_skill_cross_references.py`'s
`test_the_change_description_skill_does_not_restate_the_template` reads the
shipped copy but only to check its headings don't leak into the skill, not to
compare it against the root copy; grepped `.github/workflows/` and
`.github/scripts/*.py` for any diff of the two paths — none exists. So nothing
in this repo pins `.github/pull_request_template.md` and
`wfctl/agents/configs/github/.github/pull_request_template.md` as identical,
and this diff, despite touching both files by hand in lockstep, adds no such
check → this is the third time this repo has hit this exact drift class (#50,
#174 per the issue body) with no regression test added. Fix: add a small test
(e.g. in `tests/test_install_config.py` or `test_skill_cross_references.py`)
asserting `Path(".github/pull_request_template.md").read_text() ==
Path("wfctl/agents/configs/github/.github/pull_request_template.md").read_text()`.
Not a blocker — the two files are in fact identical right now and the change
itself is correct — but it's an explicit, named ask in the issue's Verification
section that was silently skipped, in a repo whose own AGENTS.md/issue history
treats this specific drift as a recurring failure mode.

net: 0 lines removable — nothing to cut, only a test to add (~3-5 lines).

Verdict: Approve, with the one WARNING above worth picking up before merge
(cheap fix, matches the issue's own stated verification step).

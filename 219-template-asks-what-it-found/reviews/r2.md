# Review: 219-template-asks-what-it-found (r2)

Target: `git diff` in the worktree — two files, both copies of the GitHub PR
template (`.github/pull_request_template.md` and
`wfctl/agents/configs/github/.github/pull_request_template.md`).

## Context gathered before reading the diff

- `gh issue view 219` — full text read. Acceptance criteria:
  1. `Additional Context`'s comment block prompts for findings that are not
     this change, and asks whether each was filed
  2. A checklist line covers it
  3. Both copies of the template are updated and remain byte-identical
  4. `wfctl install-config github` seeds the updated template
  5. No new section, and no section list added to `opening-a-change`
- Project rules: `CLAUDE.md` at worktree root (code style, testing
  conventions, releasing).
- Read `.agents/skills/code-review/SKILL.md` directly (not the forked
  background invocation — stopped attempting to rely on it since a fork
  cannot be steered mid-flight and this is a shared, do-not-edit worktree).

## Passes run

**1. Correctness** — Diff checked line-by-line against every acceptance
criterion:
- `Additional Context` comment block now prompts for findings unrelated to
  the change and whether each was filed: yes, added text does this.
- Checklist line added: yes, `- [ ] Anything this change turned up in
  passing is filed as its own issue, or named under Additional Context`.
- Both copies byte-identical: verified with `diff .github/pull_request_template.md
  wfctl/agents/configs/github/.github/pull_request_template.md` → no output,
  exit 0.
- `install-config github` seeds the updated template: mechanical — the
  command copies the bundle file verbatim (`tests/test_install_config.py`
  covers the copy path), and the bundle copy is one of the two edited files,
  so this follows without a code change. No `install-config` code was
  touched or needed touching.
- No new section: confirmed — the diff only extends the existing
  `Additional Context` HTML comment and adds one line under the existing
  `## Checklist`. No new `##`/`###` heading appears in either file.
- No section list added to `opening-a-change`: confirmed — the diff touches
  only the two template files; `opening-a-change/SKILL.md` is untouched.

**2. Security** — N/A, markdown/comment text only, no code path parses this
content (`wfctl/_shape.py`'s section checks operate on headings, not this
prose).

**3. Architecture** — Fits the file's existing convention: other sections
(`Before / After`, `Issue Links`, `Test Details`) already carry long
rationale-heavy HTML comments in the same voice. The addition doesn't
introduce a new pattern.

**4. Readability** — The expanded comment reads clearly and keeps the
original two-sentence prompt verbatim inside the new block (nothing lost).
Tone matches the rest of the file.

**5. Performance** — N/A.

**6. Over-engineering** — The added "write the measurement and the
conclusion as two things" paragraph goes beyond the issue's minimal
"Roughly" proposal text, but it's directly grounded in the issue's own
evidence table (PR #214's "nothing else scores above 1%" mis-stated as "nothing
else is derived") and stays inside the existing comment block rather than
adding new structure. Not flagging — it earns its place given the issue
explicitly calls this out as the failure mode motivating the change.

## Verification run (read-only)

- `uv run pytest -q tests/test_install_config.py tests/test_skill_cross_references.py tests/test_skill_commands.py` → 35 passed
- `uv run pytest -q` (full suite) → 939 passed in 74.63s
- Specifically re-ran `tests/test_install_config.py::test_shipped_github_config_matches_this_repos_own_copy`
  (`root / "wfctl/agents/configs/github"` vs `root`, `rglob` + `read_text() ==`
  per file) → passes. This is the automated check the issue's Verification
  section asks for ("whatever currently pins the two templates as identical
  still passes; extend it if nothing does") — it already exists and already
  covers `pull_request_template.md`, so nothing needed extending.
- Checked `test_the_change_description_skill_does_not_restate_the_template`
  (asserts no `opening-a-change/SKILL.md` line duplicates a heading line from
  the template) still passes — the diff adds no heading lines, only comment
  prose and one checklist bullet, so it can't trip this guard.
- Did not run `wfctl doctor`, `install-skills`, or `install-config` against
  this repo (out of scope per instructions — read-only review).

## Findings

None. No BLOCKER, WARNING, or NIT.

I want to flag one thing I *checked and ruled out*, since another reviewer's
background pass raised it: there is no missing automated parity check between
the two template copies. `tests/test_install_config.py::test_shipped_github_config_matches_this_repos_own_copy`
already asserts byte-for-byte equality between every file under
`wfctl/agents/configs/github` and its counterpart at the repo root, `.github/pull_request_template.md`
included, and it currently passes. Acceptance criterion 3 and the issue's
Verification note are both satisfied by an existing test, not a gap.

net: 0 lines removable — lean already, and no new structure to trim.
Verdict: **Approve**

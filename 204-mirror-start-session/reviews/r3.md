# Review: 5fdf83f — `feat(skills): put start-session on Claude's discovery path…` (reviewer r3)

Target: `git diff HEAD~1` (+59 / −5, 5 files). Context read: `AGENTS.md`/`CLAUDE.md`,
`gh issue view 204 --comments`, `docs/architecture/mirror-supersedes-the-wrapper.md`,
`wfctl/cli.py` (`_MIRRORED_SKILLS`, `_mirror_supersedes_wrapper`, `_AGENT_TARGETS`,
`_CLAUDE_ONLY_FRONTMATTER_KEYS`, `_strip_claude_frontmatter`, `_scanned_dirs`,
`_scan_owns`, the install loop at 2151/2236), the three tests that already guard the
frozenset, `speckit-orchestrate/SKILL.md`, `start-session/SKILL.md`,
`commands/{start,end}-session.md`. No spec.md exists for this branch — the issue thread
is the requirement.

Verification run (read-only): `uv run pytest -q` → **940 passed**;
`uv run ruff check wfctl/ tests/` → clean; `uv run mypy wfctl/` → clean.
(Note in passing: `AGENTS.md` still says "653 tests" — pre-existing, not this diff.)

---

WARNING  commit trailer / wfctl/cli.py:L1336 — `Closes #204` over-closes a P1 whose
residue this change explicitly does not cover. The issue owner's own comment sets the
ordering: *"mirror `start-session` as its own small change … then decide the two shapes
on the residue"*, and *"It does not replace either. It resolves this gate … It does
nothing for a gate whose remedy is a command with no skill behind it."* The issue body
is about all 27 wrappers; this fixes one. `/end-session` is the next instance and it is
in the same file — `speckit-orchestrate/SKILL.md:L119` displays "Story complete — open
PR or run `/end-session`", `commands/end-session.md:L2` carries
`disable-model-invocation`, and `end-session` is not in `_MIRRORED_SKILLS`. Merging as
written closes the tracking for work nobody has filed elsewhere.
→ `Refs #204` (or `Part of #204`), or open the residue issue before merge.

WARNING  wfctl/agents/skills/start-session/SKILL.md:L4 — `allowed-tools` moved from a
*command* file to a *skill* file, and the diff treats the two as reading the key
identically. Nothing in the diff, the tests, or the repo establishes that. Both readings
have a cost and neither is addressed:
  (a) **pre-approval** → the grant is strictly wider than either file had before. On the
      wrapper the list could only apply when a human typed `/start-session`, because
      `disable-model-invocation` guaranteed that; on the mirrored skill it applies to
      model-initiated invocations too. It pre-approves `Bash(wfctl install-skills*)`,
      which `AGENTS.md` § Safety describes as able to overwrite hand-authored files
      (backed up, but still). The commit's cost argument — "`.claude/commands/` was the
      only place it ever ran unstripped" — accounts for the layer the key *left* and not
      for the route it just *gained*.
  (b) **restriction** → the list becomes the ceiling on the skill while it is active,
      and it is incomplete against the skill's own body: step 7 tells the agent to run
      `wfctl issue view <id>` to confirm a likely-done issue, and no pattern in the list
      covers it (`commands/end-session.md:L4` does carry `Bash(wfctl issue view*)`). The
      gap is inherited rather than introduced, but this change moves it onto the exact
      route the issue exists to unblock: an unattended run that stalls for a prompt
      inside step 7 has failed the same way it failed at the gate.
→ Establish the semantics once and write the answer down (one line in the frozenset
comment or in the record), and add `Bash(wfctl issue view*)` so the list is correct
under either reading.

WARNING  wfctl/agents/skills/start-session/SKILL.md:L4 — the key just added is a member
of `_CLAUDE_ONLY_FRONTMATTER_KEYS` (`wfctl/cli.py:L1356-1359`), and the skills path has
no strip. `_AGENT_TARGETS` copies `agents/skills` verbatim to `.bob/skills` and
`.github/skills`; the install loop `copytree`s directories (`wfctl/cli.py:L2236`) and
only `.bob/commands/` reaches `_copy_command_for_bob`. This is the same leak the record
already documents for the sibling key —
`docs/architecture/mirror-supersedes-the-wrapper.md:L62-66`: `i-have-adhd`'s
`disable-model-invocation` survives into `.bob/skills/` because "the skills copy is a
`copytree` that never reaches the strip", found by three reviewers and by no test. The
key is spec-legal in a SKILL.md (`tests/test_skill_frontmatter.py` ALLOWED_KEYS), so it
is plausibly inert for bob and copilot — but `start-session` is now the only shipped
SKILL.md carrying it, nothing asserts that, and the commit message's inventory of who
pays stops at the command layers.
→ Check what bob/copilot do with it and record the answer beside the key, or strip it on
the non-Claude skill copies.

WARNING  (whole change) — no behavioural evidence recorded. #204's own acceptance check
is stated in the thread: `grep -c '"event": "start"' "$(wfctl state-dir --branch <b>)/events.jsonl"`
after an unattended run. All three assertions added here are structural — set
membership, frontmatter keys, gate text — and none of them shows that
`Skill(start-session)` resolves after `wfctl install-skills --agent claude`. Given #182
is the repo's standing complaint about harness claims repeated without checking, and
that the refusal message in the issue is the only measurement anyone has, this deserves
one run and one line of output in the PR body. Worth stating there too: because both
file bodies changed, doctor's bundle hash *does* move, so an existing install self-heals
through the `/start-session` freshness repair — the ADR consequence about
suppression-only changes being invisible to doctor does not bite here.

NIT  tests/test_skill_cross_references.py:L132 — `any("/start-session" in d for d in displayed)`
scans every `Display:` line in the file, not the gate's. Three exist (`SKILL.md:L11`,
`L119`, `L127`). Reword step 0's remedy to `wfctl start` *and* let any other Display line
mention `/start-session`, and the test stays green — which is precisely the failure the
docstring at L115-118 claims it converts into a red run.
→ Anchor it: match `No wfctl session` and `/start-session` on the same line, or assert
on `displayed[0]`.

NIT  tests/test_skill_cross_references.py:L141 — the test pins that `allowed-tools`
exists, not what it covers. Trimming `Bash(wfctl install-skills*)` — the freshness repair
`AGENTS.md` § Safety says `/start-session` runs unattended — leaves the suite green.
→ Assert the two patterns the workflow cannot lose (`wfctl install-skills`, `wfctl start`).

NIT  wfctl/agents/skills/speckit-orchestrate/SKILL.md:L36-39 vs. the new test — the gate
says "do not run either one yourself to clear your own gate", and
`disable-model-invocation` was the mechanical half of that sentence. On the Claude layer
this change removes the mechanism and leaves the prose as the only guard. The commit is
right that the guard was already porous (the read-the-file route did exactly this), so
this is not a regression in kind — but the new test now pins the *remedy* line while the
line that actually constrains the model is unpinned.
→ Pin "do not run either one yourself" in the same test; it is one more `in` check and it
is now load-bearing.

NIT  docs/architecture/mirror-supersedes-the-wrapper.md:L14-15, L84 — stale as of this
commit. "Ten skills are mirrored, and seven of them also shipped a wrapper" is now eleven
and eight, and the consequence bullet's example — "a key like the `allowed-tools` on
`start-session.md`" — is the very key this diff moved; `end-session.md` is now the sole
instance. `tests/test_install_skills.py:L331-336` got exactly that correction and the
record did not. The record is `status: proposed`, so nothing in force is contradicted.
→ Same one-line edit, plus the two counts.

NIT  wfctl/cli.py:L1332-1335 — the second comment paragraph narrates the change rather
than informing the next reader ("carried … would have dropped … lives on the SKILL.md
now"), which `AGENTS.md` § Code style puts in the commit message — where it already is,
nearly verbatim. The forward-useful half is the inverse: un-mirroring this name has to
move `allowed-tools` back to the wrapper.
→ Keep that sentence, drop the history. (Twelve comment lines for one set entry is the
largest in the frozenset, but the file's convention is density, so no more than a nit.)

net: `Lean already.` (−4 comment lines at most; nothing in the diff is deletable
speculation, the added test earns its length, and the frozenset entry is the whole fix.)

**Verdict: Approve** — no blockers; the change definitely improves code health, uses
machinery that already exists, and pays the price
`test_no_suppressed_wrapper_carries_more_than_a_pointer` asks for instead of exempting
itself from it. Fix the `Closes #204` trailer before merge (that one is cheap and
irreversible once merged); the other three warnings are evidence-and-record work that can
land with it or immediately after.

---

## Passes run and what each checked

**1. Correctness.** Traced the install path end to end: `_MIRRORED_SKILLS` →
`_claude_native_skill_mirror` → the plan loop (`cli.py:L2151`, `L2158-2170`) →
`_mirror_supersedes_wrapper` (layer-scoped, keyed on `_AGENT_SKILL_EXTRAS`, so bob and
copilot keep the wrapper). Checked the suppression's blast radius on the other layers:
bob still gets `.bob/commands/start-session.md` with Claude keys stripped, copilot gets
no commands at all — the commit's claim verified against `_AGENT_TARGETS`. Checked the
orphan story for an existing install: `_scanned_dirs`/`_scan_owns` deliberately keep
`.claude/commands/` out and the left-behind wrapper is caught as a recorded path no
longer shipped, with `--prune` (which `/start-session` passes) removing it. Verified the
edited docstring's claim that `end-session.md` still carries a key its SKILL.md lacks —
it does. Verified the new test's regex actually matches (`grep -n "Display:"` → 3 hits,
one of them step 0), that `_arch._frontmatter` returns a key→value dict so both `in`
checks are key checks, and that function-level imports are this file's existing
convention. Findings: the `wfctl issue view` gap above; everything else holds.

**2. Security.** The only privilege-shaped change in the diff is the `allowed-tools`
move — raised above as the widened-grant branch. Otherwise: no secrets, no new input
parsing, no new execution path; the frozenset is a literal and the mirror copies files
wfctl already ships. Re-read `AGENTS.md` § Safety against it — the `--yes`/backup story
is unchanged by this diff.

**3. Architecture.** The change is the mechanism working as designed (a name in a set,
not new machinery), which is exactly what the issue thread's "third shape" argued for.
It fits `layer-model` and `mirror-supersedes-the-wrapper`. Two frictions recorded above:
the record's stale counts/example, and the loss of the mechanical half of "a human types
this" for a skill whose gate forbids self-clearing in prose.

**4. Readability & simplification.** Test name is a sentence, docstrings say why the
tests exist and name the failures they catch — matches `AGENTS.md`. One comment-style
nit. No naming, nesting, or length problems; the frozenset is alphabetical and the entry
is in place.

**5. Performance.** One additional small `copytree` per Claude install and one more
member in a frozenset. Nothing measurable; no loops, queries, or hot paths touched.

**6. Over-engineering.** Nothing to delete. No new abstraction, no config, no flag; the
`_AGENT_SKILL_EXTRAS` hook it rides was already there with a caller. The added test is
the minimum that binds the three ways the route can be lost silently, and the diff's
best available outcome is roughly its current length.

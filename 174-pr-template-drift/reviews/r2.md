# Review: 5ba013e..fe7f2fe (fix(configs): reconcile the two PR templates and give Summary a subject line, closes #174)

Reviewed per `.agents/skills/code-review/SKILL.md`. Context gathered first: read
`CLAUDE.md` (repo root), `gh issue view 174` (full body incl. Definition of
Done and Related), the commit message/diff (`git show fe7f2fe`), and the new
test plus its neighbors (`tests/test_install_config.py`,
`tests/test_skill_cross_references.py`).

Files touched: `.github/pull_request_template.md`,
`wfctl/agents/configs/github/.github/pull_request_template.md`,
`tests/test_install_config.py`. No other files in the diff.

## Passes run

**1. Correctness** — Verified each issue requirement against the actual diff, not
the commit message's claims:
- Byte-identity: `diff .github/pull_request_template.md
  wfctl/agents/configs/github/.github/pull_request_template.md` → identical.
  Confirmed the fix lands in the bundle (issue's explicit requirement — "a fix
  that edits only `.github/` recreates the bug it is fixing").
- Regression test: `test_shipped_github_config_matches_this_repos_own_copy`
  (`tests/test_install_config.py`) walks every file the `github` bundle ships
  and asserts byte-equality against the repo-root copy. Verified `_CONFIG_SOURCES`
  (`wfctl/cli.py:1139`) lists only `workmux` and `github`, and the `github`
  bundle ships exactly the one file compared — the test's own claim ("the only
  config that seeds verbatim") checks out against the source it's scoped from.
- `Context:` slot added to Summary, worded to name the subject rather than the
  change, matching issue's rule-4 framing.
- Drawing (`Before / After`) now precedes `What/Why/Impact` in reading order —
  confirmed by reading the rendered file top to bottom.
- Verified the commit message's historical claims against git history: 77ea0c5
  (2026-09-02) touched only `.github/pull_request_template.md` (6 lines,
  confirmed via `git show 77ea0c5 --stat`); 271bb2c (2026-08-17) is the last
  commit to touch the bundle copy before this one (`git log --follow`). Both
  check out.
- Explicit out-of-scope items from the issue (doctor drift-scan, new sections,
  more checklist rows) — confirmed untouched: `git diff 5ba013e..fe7f2fe --
  wfctl/cli.py` is empty, and no new `##`/`- [ ]` lines appear outside the
  Summary section.
- Full suite: `uv run pytest -q` → 823 passed. `uv run ruff check wfctl/
  tests/` → clean. `uv run mypy wfctl/` → clean (per CLAUDE.md's definition of
  done; `wfctl doctor` was not run, per this review's own constraint not to run
  `wfctl verify`/write outside the one report file — doctor is read-only so this
  is stricter than required, but skipped out of caution since it wasn't asked for).

**2. Security** — N/A, no user input, no runtime code touched (docs/test only).

**3. Architecture** — New test fits the existing `test_install_config.py`
pattern (module-level `Path` import reused, no new imports). No new
abstractions.

**4. Readability** — Test docstring names the failure it caught (two weeks of
consumers seeded stale guidance) per CLAUDE.md's testing convention ("docstrings
say *why* the test exists"). Comment density in both template files matches the
existing register (dense rationale, not narration).

**5. Performance** — N/A, trivial file walk over one file.

**6. Over-engineering** — The test generalizes over `bundle.rglob("*")` rather
than hardcoding the one filename, which is right-sized (github's bundle may
grow files later) rather than speculative. No `delete`/`stdlib`/`native`/
`yagni`/`shrink` findings.

## Findings

**NIT** `tests/test_skill_cross_references.py:118` (pre-existing test, not
touched by this diff) — `test_the_change_description_skill_does_not_restate_the_template`
collects template headings via `line.startswith("## ")`. Demoting `## Before /
After` to `### Before / After` (this diff) silently drops it from that
guard's coverage — it's no longer checked against being restated verbatim in
`opening-a-change/SKILL.md`. Not a functional bug (the skill never restated
that heading as a heading line, only as inline prose, so the test still passes
and always would have), but it's a quiet narrowing of an existing regression
guard as a side effect of a heading-level change in a different file. Worth a
one-line mention in a follow-up if `Before / After` gets restated as a heading
anywhere later — currently nothing would catch it. Not blocking; author's call.

No BLOCKER or WARNING findings. The heading-level change (`## Before / After` →
`### Before / After`, nesting it under Summary rather than reordering two
top-level sections) deviates from the issue's ASCII sketch, which showed
`## Before / After` as a peer of `## Summary` — but the commit message gives an
explicit, sound rationale (a single `## Summary` can't be split around an
intervening top-level section without repeating the heading; the alternative
considered — a 4th top-level section for What/Why/Impact — was deliberately
rejected). This is a judgment call within scope, not a defect.

net: 0 lines possible — nothing to cut.

**Verdict: Approve.**

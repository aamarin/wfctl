# Review: 5ba013e..fe7f2fe (fix(configs): reconcile the two PR templates and give Summary a subject line)

## Passes run

1. **Correctness** — read both templates in full, diffed the two shipped copies,
   replayed the pre-fix state from `5ba013e` to confirm the templates really did
   diverge and that the new test would have failed against it, traced the
   `install-config` code path (`wfctl/cli.py` `install_config_cmd`, ~L2508-2625)
   to confirm `github` seeds via `shutil.copy2` with no substitution (only
   `workmux` patches placeholders), and independently ran the CLI against an
   isolated copy of the repo to verify the issue's stated definition-of-done
   ("`install-config github` in this repo is a no-op afterwards").
2. **Security** — N/A. No user input, no secrets, no new code paths that touch
   auth or external data; the change is markdown content plus a filesystem-diff
   test.
3. **Architecture** — compared the new test's `Path(__file__).resolve().parent.parent`
   pattern against existing precedent (`tests/test_install_skills.py:354`,
   `tests/test_dev_environment.py:18`) — consistent with how this suite already
   asserts real-repo-file properties.
4. **Readability & simplification** — read every changed line of prose against
   `conversation-response-shape/SKILL.md` to check the commit message's claims
   (the "selection table" language at SKILL.md:301-311, "the drawing leads" at
   SKILL.md:358, "establish the subject" as rule 4 at SKILL.md's rule list) —
   all check out, the template's paraphrases are accurate.
5. **Performance** — N/A, no runtime-hot code.
6. **Over-engineering** — the new test is a tight 22-line loop-and-assert, no
   speculative abstraction. Nothing to cut.

Also read `gh issue view 174` in full and checked every definition-of-done bullet
against the diff (see findings below), and ran the project's full definition of
done: `uv run pytest -q` (823 passed), `uv run ruff check wfctl/ tests/` (clean),
`uv run mypy wfctl/` (clean).

## Findings

WARNING  wfctl/agents/skills/opening-a-change/SKILL.md:84 — the "no template
found" fallback shape ("Summary (what / why / impact) · Before / After · What
changed · How it was tested · Issue links") still names the pre-fix field set
and order. It doesn't mention `Context`, and it lists `Before / After` after
`Summary (what/why/impact)` — the exact order this diff just inverted for the
real template. `tests/test_skill_cross_references.py::test_the_change_description_skill_does_not_restate_the_template`
exists specifically to catch a second home for the template's section list
drifting from the first, but only matches literal `## `-prefixed headings
copied verbatim; this prose summary of the field names slips past it. It's a
smaller-blast-radius instance of the exact bug this PR fixes (one copy edited,
a second one holding the same facts left behind) — low practical impact since
it only surfaces in a target repo with no PR template at all, but it's the same
species of drift the issue and this PR are about. → update the fallback line to
`Context · Before / After · What/Why/Impact · What changed · How it was tested
· Issue links` (or fold Context into the parenthetical), and optionally widen
the cross-reference test to also flag `what / why / impact`-shaped restatements,
not just literal headings.

## Verified against the issue's own acceptance criteria

- Bundle and repo-root copies are byte-identical (`diff` exit 0), and the fix
  landed in the bundle first (the diff touches
  `wfctl/agents/configs/github/.github/pull_request_template.md`, not just
  `.github/`), so it doesn't recreate the bug the issue describes.
- `install-config github --force`, run against an isolated copy of this repo
  (not the working tree under review), is a true content no-op afterwards —
  confirms the issue's explicit DoD bullet.
- Something now detects a future divergence:
  `tests/test_install_config.py::test_shipped_github_config_matches_this_repos_own_copy`
  fails on the pre-fix tree (replayed against `5ba013e`, confirmed `diff` exit 1
  there) and passes now. Correctly scoped to `github` only, since `workmux` is a
  substituted template rather than a verbatim seed (confirmed in
  `wfctl/cli.py`).
- `Context:` slot added ahead of `What/Why/Impact`, matching issue point 2 and
  `conversation-response-shape` rule 4.
- `Before / After` demoted to `###` and moved above `What/Why/Impact`
  (still under `## Summary`), matching issue point 3 and the skill's "the
  drawing leads" rule (SKILL.md:358).
- Nothing in scope was added beyond the issue's proposed shape: no new
  sections, no new checklist rows, no `doctor` scan changes — matches the
  issue's explicit "not proposed" list.
- No version bump in `pyproject.toml` (this isn't a release).

## Over-engineering metric

Lean already — nothing to cut.

## Verdict

**Approve.** One WARNING, low blast radius, doesn't block merge — author's call
whether to fix in this diff or file a follow-up.

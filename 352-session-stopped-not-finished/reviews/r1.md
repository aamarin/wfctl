# Review: `git diff origin/main..HEAD` (#352, 2706f14..f4c6c2f) — reviewer r1

Ran all six passes of `.agents/skills/code-review/SKILL.md` against the eight
changed files, with `spec.md`, `data-model.md`, `contracts/wfctl-end.md`,
`contracts/start-session-step-9.md`, `tasks.md` and `quickstart.md` as the
requirements, and `CLAUDE.md` as the rule set. Definition-of-done commands run
read-only: `uv run --frozen --extra dev pytest -q` → **1554 passed** (2m23s),
`ruff check wfctl/ tests/` → clean, `mypy wfctl/` → clean. `wfctl verify` and
`wfctl doctor` deliberately not run.

## Findings

WARNING  wfctl/agents/skills/start-session/SKILL.md:291 — **no step-9 row matches
"trunk branch, no stop recorded, quotable handoff".** Step 4's table names that
outcome ("nothing | no session has stopped on this branch"), but row one needs an
issue branch *or* a continued stop, row two needs "a trunk branch whose **last
stop** was not continued", and row three needs an unquotable handoff. A trunk
state dir holding a filled `session-summary.md` and no `end` line satisfies none
of the three. This state was covered explicitly before the change ("a summary
naming a first action, and **no** `end` event" → *Do not ask*), and
`data-model.md` § Branch history says the intended answer has flipped: "Empty …
On trunk it routes with *finished* rather than with continued — trunk's default
is the question." So the behaviour changed and the artifact that carries the rule
does not say so, leaving the resolution to an agent's judgment on `main` — the
one branch row two exists to protect. → Row two's condition becomes "a trunk
branch whose last stop was not continued, **or that has no stop recorded**, with
a summary naming a first action". `contracts/start-session-step-9.md` carries the
same table and needs the same six words.

WARNING  wfctl/agents/skills/start-session/SKILL.md:4 — **the command this change
adds is the only one in the skill body with no `allowed-tools` entry.** Step 4
now prescribes `grep '"event": "end"' "$(wfctl state-dir)/events.jsonl" | tail -1`
and says to run *that* rather than improvising. `allowed-tools` covers `wfctl
start|status|arch context|state-dir|doctor|install-skills|issue list|issue
view|change list` and `git status|log|symbolic-ref` — every other fenced command
in the file — but neither `grep` nor `tail`. On the unattended trunk path this is
a permission prompt, i.e. the stall the feature exists to remove, and the step's
own wording closes the escape hatch (an agent that falls back to `Read` is doing
the improvisation the paragraph forbids). Stated honestly: `commands/end-session.md`
already pipes `git log -p` into `grep` with no `Bash(grep…)` rule, so the looseness
predates this diff — but that path is attended by definition and this one is not.
→ Add `Bash(grep*) Bash(tail*)` to line 4, in the file's existing spelling.

WARNING  tests/test_start_session_asks_conditionally.py:213 — **`test_a_trunk_branch_is_still_asked`
is byte-identical in body to `test_a_branch_that_has_ended_a_session_before_is_still_asked`
(L89).** Both select `_row("a trunk branch whose last stop was not continued")` and
assert `"Ask:" in row` / `"Do not ask" not in row`; only the docstrings differ.
Two tests, one check — and the second one's docstring claims it covers #244's
criterion (b), which the first already claims. → Delete one and fold the surviving
docstring's `main`-accumulates-a-handoff paragraph into the other.

WARNING  tests/test_start_session_asks_conditionally.py:154,209 — **two assertions
that cannot fail.** `_row(c)` selects rows by `stripped[2:].startswith(c)` (L61-76),
so with `c = "an issue branch, or a stop marked continued"`, both
`assert "a stop marked continued" in row` and `assert "an issue branch" in row`
are restatements of the selector: any row `_row` returns contains them, and if no
row does, `_row` has already called `pytest.fail`. Both tests reduce to their
third line. This module's own docstring is about a check the violating artifact
passes — "the rule's absence, documented" — so a tautology here is the exact
failure it was written to catch. → Assert what the selector does not already
guarantee: that exactly one row in the table says "Do not ask", or that row two's
condition does *not* contain "continued — with a summary".

WARNING  docs/architecture/scans/352-clarify.md:5 — **the scans do not cover the
pivot.** The clarify scan records `Asked: 3 · Answered: 3` and points at
`spec.md` § Clarifications for detail; that section now carries **four**, and the
fourth ("Is the stop's kind the right thing for the next session to branch on, or
is it the branch itself?" → the branch, kind as trunk tie-break) is the decision
that widened FR-004, narrowed FR-005 and added FR-016. It landed in f4c6c2f,
after both scan commits (2706f14, 2d73c76); `352-analyze.md` likewise reports
`Requirement-to-task coverage 85%` over a requirement set that did not yet
contain FR-016, and over a `tasks.md` that did not yet contain T024-T029. Per
`CLAUDE.md`, the scans exist *because* `FEATURE_DIR` resolves outside the tree —
"a scan that found six problems and one that never ran are the same pull
request." As committed, the branch's largest routing decision is invisible from
the PR. → Append a second `## Session` block to `352-clarify.md` for the fourth
question (and re-run/append analyze), or state in the scan that the fourth
clarification was taken outside a scan round and why.

NIT      wfctl/agents/skills/end-session/SKILL.md:44, wfctl/agents/skills/using-wfctl/SKILL.md:23 —
**nothing in the shipped skills names `--continued` at the moment of stopping.**
`end-session` step 3 prints a bare `wfctl end`; the `using-wfctl` reference table's
`wfctl end` row describes the summary scaffold and no flag. The only skill that
mentions the flag is `start-session`, which is read at the wrong end of the
session. FR-009 is satisfied by the CLI (and `--help`), and no task in `tasks.md`
scoped this, so it is the parent's call — but a flag an agent never sees is one a
person has to know about already. → One clause in `using-wfctl`'s table row, and
one sentence under `end-session` step 3 for the "the work carries on" case.

NIT      tests/test_start_session_asks_conditionally.py:61 — `yagni:` `_row`'s
`section: str | None = None` parameter is never passed by any caller; all three
step-4 tests call `_step_four()` and assert on the string directly. → Drop the
parameter and the `section if section is not None else` branch.

NIT      tests/test_start_session_asks_conditionally.py:235 — `assert "issue" in
step_four` passes on the pre-existing comment `wfctl status --json   # issue,
branch, per-step state, the next command`, so the test would stay green with the
whole new "Step 9 needs `issue` out of that payload" paragraph deleted. The
`"unknown"` assertion beside it does pin new text. → Assert on something only the
new paragraph carries, e.g. `key_pattern`.

NIT      wfctl/_session.py:490 — `names_no_first_action` stops its scan on `## `
only, so a later `# ` H1 (or a `---`-underlined heading) does not end the section
and content beneath it answers for the TODO. Cannot fire on the shipped template,
whose single H1 is the first line, and the test at
`test_a_section_filled_below_an_empty_one_does_not_answer_for_it` covers the `##`
case. → `if line.startswith("## ") or line.startswith("# "):`, or leave it and
say so in the comment on L488-489.

net: −15 lines possible (duplicate test ~10, `section` parameter ~3, two
tautological assertions ~2)

**Verdict: Request changes** — no blockers; the two warnings against
`start-session/SKILL.md` are both one-line fixes in the single artifact this
feature ships to every consuming repo, and the routing gap in the first is a
silent behaviour change relative to `data-model.md`.

## What each pass checked and found clean

**1. Correctness.** Traced `end_cmd` → `_session.end` → `append_event`, and
`names_no_first_action` against all four handoff shapes plus the template
renderer. Checked each requirement the diff claims: FR-001/FR-011 (`continued`
written on every line, `False` by default — CLI default, with the
keyword-only-no-default signature keeping in-process callers honest, and
`_session.end` has exactly one caller so no site was missed), FR-002 (write-once
untouched by the flag; `summary_written` still drives the kept-file line),
FR-008 (append only — `append_event` unchanged), FR-013 (warning after the stop
is recorded, never a refusal, and the ordering comment says why), FR-015
(`status` and `status --json` assert byte-identical), SC-005 (`_stall`'s two
`event in ("start", "end")` matches and `_STYLES["end"]` all unchanged, and the
new test drives all three payload spellings including the absent one through
them). The warning's claim "the next session will ask" holds under the *new*
step 9 too, on issue branches and trunk alike, because the quote is the gate in
row three — checked, since a branch-first rule could have falsified it. Two
imprecisions are bounded and acknowledged in the code's own docstrings: the
warning can fire on a handoff naming a first action under a differently-titled
heading, and cannot fire on a filled-but-useless one. `escape()` is still applied
to `observed.step` and the two new console strings are literals with no
interpolation, so no markup path opened. `grep '"event": "end"'` matches
`json.dumps`' default `": "` separator, and `events.jsonl` always exists by step 4
because step 2 runs `wfctl start`.

**2. Security.** No new external input, no secrets, no subprocess, no
deserialization of untrusted data. `summary_path.read_text()` reads a file in the
user's own XDG state dir and its content is never echoed — only a fixed sentence
is printed. No authz surface: the flag records a fact, it does not lift the
`authority:notify` gate, and `close` is untouched. Nothing to report.

**3. Architecture.** The payload-key-vs-second-event-kind choice is recorded in
`docs/architecture/stop-kind-is-a-field-not-an-event.md` with its direct baseline
and three rejected alternatives, and the record's central claim (all four readers
match on the kind, none reads the payload) is checked by
`test_the_payload_is_inert_to_every_existing_reader` — `a-rule-is-expressed-as-a-check`
honoured. `status: proposed` is the normal state for a new record here (20 of 29
are). The two template constants live beside the renderer per T011's reasoning,
which is the right side of that boundary. `_session` keeps domain logic and `cli`
keeps rendering; `names_no_first_action` is a pure string predicate with no I/O,
which is why the test suite can reach it. Scan files are in `docs/architecture/scans/`
per `CLAUDE.md` rather than mixed among the records.

**4. Readability.** Names are accurate (`continued`, `opening`,
`names_no_first_action` reads as the question it answers). No nesting past two
levels, no boolean-flag parameter smuggled into a positional slot (it is
keyword-only). Comments explain *why this shape* — the ordering of the FR-013
block, the `## ` break, the shared constants — and do not narrate the diff, which
is `CLAUDE.md`'s standard; the closest to narration is the "the first clause is
the only thing the flag changes" paragraph in `cli.py`, and it earns its place by
saying what the sentence claims and does not claim. Test docstrings name the
failure each test exists to catch, per the repo's convention. `NO_COLOR` is
inherited from `conftest.py` and the new module says so instead of re-pinning it.

**5. Performance.** One extra boolean in a JSON line, one extra `read_text()` of a
~300-byte file per `end` invocation, one linear scan of it. No loops, no I/O in a
hot path, nothing that scales with branch history. The `grep | tail -1` the skill
prescribes is O(log size) in process terms and replaces an unbounded "read the
whole file" instruction.

**6. Over-engineering.** The production diff is lean: `_session.py` +54 / `cli.py`
+32, with no speculative flexibility — there is no `--finished`, no enum, no
migration writer, and `contracts/wfctl-end.md` argues each absence. `delete:`/
`stdlib:`/`native:` found nothing. The three `yagni:`/`shrink:` candidates are all
in `tests/test_start_session_asks_conditionally.py` and are listed above; the new
`tests/test_session_stopped_not_finished.py` has 17 tests with no duplicate pair
among them.

## Review: `git diff origin/main..HEAD` (2706f14..f4c6c2f) — #352, reviewer r2

Target: 8 files, +1010/−36. Read first: `spec.md`, `contracts/wfctl-end.md`,
`contracts/start-session-step-9.md`, `data-model.md`, `AGENTS.md`,
`docs/architecture/stop-kind-is-a-field-not-an-event.md`, and the callers/readers
the diff touches (`_io.append_event`, `_stall._passes_this_sitting`,
`_stall.opens_a_new_sitting`, `cli.log_cmd`, `cli.status_cmd`'s JSON payload,
`worktree-handoff/SKILL.md`, `using-wfctl/SKILL.md`).

Baseline run read-only: `ruff` clean, `mypy` clean (22 files), `pytest -q` 1554
passed in 142s. `wfctl verify` deliberately not run.

---

BLOCKER  wfctl/agents/skills/start-session/SKILL.md:L4 — step 4 now mandates a shell command the skill is not allowed to run. `allowed-tools` lists `Read Bash(wfctl …) Bash(git status*) Bash(git log*) Bash(git symbolic-ref*)` and nothing else; the new step 4 block is `grep '"event": "end"' "$(wfctl state-dir)/events.jsonl" | tail -1`. I extracted every bash line in the file: before this change all 12 mapped onto an allowlist entry, and this is the first that does not. The two outcomes are both the failure the feature exists to remove — an unattended run stalls on a permission prompt, or the agent falls back to `Read` and improvises, which is the exact "improvising a grep gets this wrong in the direction that starts work on a branch a person deliberately left" the step warns about four lines below. → add `Bash(grep*) Bash(tail*)` to the frontmatter list. (Caveat stated rather than hidden: if this harness treats a skill's `allowed-tools` as advisory the runtime symptom softens, but the file's own convention is still broken and the fix is one token.)

WARNING  wfctl/cli.py:L829-L833 — the FR-013 warning is a guaranteed false positive on a worktree handoff, which is #352's own scenario. `names_no_first_action` returns `True` whenever `## Next Session TODO` is absent. `worktree-handoff/SKILL.md:L162` tells the handoff author in as many words: *"Do not add a TODO section to get one"* — the quotable first action lives in its section 2 instead. So an interrupted run on a fresh worktree (summary kept, no TODO heading) prints "the handoff names no first action — the next session will ask", and step 9 row one then carries on without asking. Both clauses are false, and the operator's natural remedy is to add the TODO section another shipped skill forbids. The observable checked is "the template's section was never filled in"; the sentence claims something about the whole handoff and then predicts the next session. → say what was observed: `⚠ the handoff's "Next Session TODO" is unfilled — the next session may ask.` (`contracts/wfctl-end.md` specifies the current string, so the contract needs the same edit; the defect is in the contract, not only the code.)

WARNING  wfctl/agents/skills/start-session/SKILL.md:L291 — step 9's table has no row for *trunk branch, no stop at all, summary naming a first action*. Row one needs an issue branch or a continued stop; row two says "whose last stop **was** not continued", which does not describe a branch with no last stop; row three needs an unquotable summary. `data-model.md` settles it explicitly ("Empty … On trunk it routes with *finished* rather than with continued"), and step 4's own outcome table has a `nothing` row — but step 9, the normative table the tests assert against, drops it. Pre-#352 this case routed to *do not ask*; an agent resolving the gap by precedent lands on the behaviour FR-005 forbids. → row two's condition: "a trunk branch whose last stop was not continued, **or that has no stop at all**". Mirror it in `contracts/start-session-step-9.md`, and pin it with a `_row` assertion.

WARNING  wfctl/cli.py:L829 — `summary_path.read_text()` is unguarded, and it runs *after* the stop has been appended and after the success lines have printed. A handoff carrying an invalid UTF-8 byte (hand-edited, or pasted terminal output) raises `UnicodeDecodeError` and `wfctl end --continued` exits non-zero with a traceback on a stop that was in fact recorded — the resetter reading the exit code sees a failure that did not happen. The module next door guards exactly this shape for exactly this reason (`_session.auto_approve`: "`ValueError` rather than `JSONDecodeError` because an invalid UTF-8 byte raises `UnicodeDecodeError`"). → `try: text = summary_path.read_text() except (OSError, ValueError): text = ""` — empty reads as unfilled, which warns, which is the safe direction.

WARNING  tests/test_start_session_asks_conditionally.py:L213-L224 — `test_a_trunk_branch_is_still_asked` is byte-identical in its body to `test_a_branch_that_has_ended_a_session_before_is_still_asked` (L87-L101): same `_row("a trunk branch whose last stop was not continued")`, same two asserts. Two tests, one check; neither can fail without the other. AGENTS.md: "A test that only restates its assertion in prose is not carrying its weight." → delete the new one, or fold its docstring into the existing test.

WARNING  wfctl/agents/skills/using-wfctl/SKILL.md:L23 — `--continued` is not in the command reference, and that table documents flags by convention (`wfctl start` carries `--force` and `--auto-approve`, `state-dir` carries `--branch`). The skill's own framing is "reach for this skill when you need a command those don't cover", which is precisely the resetter/operator case FR-009 ships the flag for. `start-session/SKILL.md` mentions the flag three times in prose, but only where it explains routing. → add to the `wfctl end` row: "`--continued` records that the work carries on rather than that it was wrapped up."

NIT  wfctl/_session.py:L490 — the section scan breaks only on `## `. An empty `## Next Session TODO` followed by `### Notes`, `# Open Questions` or a `---` rule reads as *filled* and the warning goes silent — verified against the function directly (all three return `False`). The neighbouring test `test_a_section_filled_below_an_empty_one_does_not_answer_for_it` pins only the `## ` case. → break on `line.startswith("#")` or on `#`/`---`, and extend that test with one of the three.

NIT  tests/test_start_session_asks_conditionally.py:L61 — `_row` gained `section: str | None = None` and no caller ever passes it (all 8 call sites verified). `_step_four` is used directly with `in` assertions instead. Unused flexibility, and the docstring still says "The one table row **in step 9**". → drop the parameter.

NIT  tests/test_start_session_asks_conditionally.py:L199-L210, L145-L156 — `test_an_issue_branch_is_never_asked_what_to_work_on` and `test_a_stop_marked_continued_shares_the_do_not_ask_row` each assert a substring that `_row`'s prefix match already guaranteed (`"an issue branch" in row`, `"a stop marked continued" in row` — the selector string contains both). What remains in each is `"Do not ask" in row`, already asserted at L84 and L165. Given the module's own history — a panel proved its assertions passed on a swapped-cell copy — a tautological assert is the shape it should be most careful about. → keep one, and make its distinct assertion something the selector cannot supply.

NIT  tests/test_session_stopped_not_finished.py:L310-L325 — the docstring says "Four readers … so it is worth a test that the three spellings of that payload are indistinguishable to all of them", but the test exercises `session_started` (which matches `"start"`, and is not one of the four the architecture record names), `opens_a_new_sitting` and `_passes_this_sitting`. `log_cmd._STYLES` is covered by the test above it. → name the three it actually calls, and say the fourth is pinned next door.

NIT  docs/architecture/stop-kind-is-a-field-not-an-event.md:L102 — "FR-011 falls out of the shape …: a call site that passes nothing writes `continued: false`". At the Python boundary the opposite is now true and deliberately so: `end()` takes `continued` keyword-only with **no** default, so a call site that passes nothing does not run at all (`_session.py:L498`, and its docstring argues for it). The sentence is true of the CLI (`typer.Option(False, …)`) and false of the function the record is about. → "a `wfctl end` invocation that passes no flag".

NIT  docs/architecture/scans/352-clarify.md:L6 — "Asked: 3 · Answered: 3", and `spec.md § Clarifications` under the same session heading now carries four Q/A entries (the fourth, "the branch, with the stop's kind as the tie-break on trunk", arrived with `f4c6c2f`). The scan exists so a reviewer can audit what the step covered; a count that trails the artifact it points at is the drift it was written to prevent. → bump the count, or file the fourth under its own session block.

---

Passes run, and what each checked:

1. **Correctness** — traced `end_cmd` → `_session.end` → `_io.append_event` (confirmed `json.dumps` default separators emit `"event": "end"` with the space step 4's grep matches, and `true`/`false` lowercase for the `"continued": true` row); exercised `names_no_first_action` against 8 handmade handoffs (missing/empty/placeholder/filled/nested-heading/hr/longer-heading/html-comment) and found the `## `-only break; walked step 9's three rows against the spec's routing table for all six input combinations and found the missing trunk/no-stop cell; confirmed `wfctl end --help` emits `--continued` with no `--no-continued`, matching the contract's "there is no `--finished`"; confirmed `events.jsonl` always exists by step 4 because step 2 runs `wfctl start`, so the grep cannot error.
2. **Security** — no external input reaches a sink. `--continued` is a boolean; the FR-013 string is a constant and the handoff's *content* is never printed, so no rich-markup injection is introduced (`log_cmd`'s unescaped detail string is pre-existing and untouched). No secrets, no auth surface, nothing parses untrusted data into a privileged decision.
3. **Architecture** — the payload-key-over-second-event-kind argument holds: all four readers named in the record do match on `event ==` and none reads the payload, verified by grep (`_stall.py:165`, `_stall.py:203`, `cli.py:848`, step 9). `NEXT_SESSION_TODO`/`NEXT_ACTION_PLACEHOLDER` beside the renderer is the right placement and is argued for in the comment. The skill change reaches consumers through `install-skills` (FR-012) and the installed copy in this worktree is byte-identical to the source, so the change has been exercised.
4. **Readability** — comments explain *why this shape* and do not narrate the diff, matching AGENTS.md. `opening = … if continued else …` is the minimal form. No function crosses 50 lines, no nesting past two levels, names describe content.
5. **Performance** — one extra `read_text()` of a file just written, on a command that already stats it; one boolean in a JSONL line. Nothing measurable.
6. **Over-engineering** — findings above: `_row(section=…)` dead parameter, one duplicate test, two tests whose distinct assertions are tautologies of the selector.

net: −27 lines possible (duplicate test −13, one of the two tautological tests −12, `_row`'s parameter −2)
Verdict: **Request changes** (1 blocker, 5 warnings)

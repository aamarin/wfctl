## Review: `git diff origin/main..HEAD` (#352, 2706f14..f4c6c2f) — reviewer r3

Target read cold: 8 files, 6 commits. Requirements read from
`<spec-root>/352-session-stopped-not-finished/{spec.md,data-model.md,contracts/*}`.
Project rules from `AGENTS.md` (worktree root) and `.agents/skills/code-review/SKILL.md`.

Definition-of-done commands run read-only, all green:
`uv run ruff check wfctl/ tests/` · `uv run mypy wfctl/` (22 files) ·
`uv run pytest -q` (**1554 passed**, 139s). `wfctl verify` deliberately not run.
`.agents/skills/start-session/SKILL.md` is byte-identical to the source under
`wfctl/agents/`, so the installed layer is current.

---

### Findings

**WARNING  wfctl/agents/skills/start-session/SKILL.md:204** — step 4 now instructs a
command the skill's own `allowed-tools` forbids. Line 4 enumerates
`Read Bash(wfctl start*) … Bash(git symbolic-ref*)` and nothing else; the new
instruction is `grep '"event": "end"' "$(wfctl state-dir)/events.jsonl" | tail -1`,
whose first token is `grep` and whose pipe segment is `tail`. Neither binary is
listed, and `$(wfctl state-dir)` inside a substitution does not make the command
match `Bash(wfctl state-dir*)`. This frontmatter is maintained in lockstep with the
instructions — it is the only skill in `wfctl/agents/skills/` that carries an
`allowed-tools` line at all, and `Bash(wfctl doctor*)`/`Bash(wfctl install-skills*)`
are there because step 2 asks for them. So this is the first instruction in the file
with no entry behind it. The old text needed none: it asked for *a fact* about
`events.jsonl`, reachable with `Read`, which is allowed. The failure lands on the
unattended run this feature exists to serve — a denied tool call, and an agent that
either falls back to `Read` (recovering, but then improvising the `tail -1` the step
spent a paragraph saying not to improvise) or stalls on a permission prompt with
nobody there.
→ add `Bash(grep*) Bash(tail*)` to the `allowed-tools` line, or keep the
instruction as a `Read` of `events.jsonl` and state the most-recent-line rule in
prose.

**WARNING  wfctl/agents/skills/start-session/SKILL.md:291** — step 9's table has no
row for *trunk + a quotable handoff + no stop record at all*, and that is a
reachable state with an unsafe default. Step 4's outcome table explicitly names
the case ("nothing | no session has stopped on this branch"), then step 9 never
consumes it: row 1 reads "an issue branch, **or a stop marked continued**" (no stop
is neither), row 2 reads "a trunk branch whose **last stop was not continued**"
(there is no last stop). `data-model.md` resolves it — "Empty … On trunk it routes
with *finished* rather than with continued" — but `data-model.md` does not ship;
`SKILL.md` is the operative text and it is silent. Reachable on `main` whenever a
handoff exists in the state dir but no `end` has run: `worktree-handoff` writes
`$(wfctl state-dir --branch X)/session-summary.md` before any session runs there, and
a rotated/lost `events.jsonl` gets there too. Note the *old* table resolved this case
toward row 1 ("a summary naming a first action, and **no** `end` event" → do not ask),
so the change intends to flip it and does not say so. An agent resolving the
ambiguity the old way begins work against `main`'s accumulated TODO — precisely
SC-002 and #244's criterion (b). Untested, too: `test_step_four_names_all_three_outcomes_of_that_read`
pins the three step-4 outcomes, and nothing pins that step 9 covers all three.
→ row 2's condition becomes "a trunk branch whose last stop was not continued, **or
which has no stop at all**, with a summary naming a first action", mirrored in
`contracts/start-session-step-9.md`, plus a test asserting the no-stop outcome names a row.

**WARNING  tests/test_start_session_asks_conditionally.py:199,145** — two of the new
tests assert only what the selector already guarantees. `_row(condition)` returns the
row whose condition column *starts with* `condition`; both tests pass
`"an issue branch, or a stop marked continued"` and then assert
`"an issue branch" in row` (L208) and `"a stop marked continued" in row` (L154).
Those substrings are inside the selector, so the assertions cannot fail once `_row`
returns — and if `_row` does not return, `pytest.fail` has already fired. The only
load-bearing assertion left in each is `"Do not ask" in row`, which
`test_a_handoff_on_a_branch_nobody_has_worked_on_starts_without_a_reply` (L84) and
`test_a_continued_stop_does_not_relax_the_quote_gate` (L165) both already make. This
file's own docstring is about exactly this failure mode — assertions that stay green
on a mutated artifact.
→ delete both tests and fold their reasoning into the existing row-1 test, or make
them non-vacuous by selecting on a *shorter* prefix (`_row("an issue branch")`) so
the remaining half of the cell is what the assertion checks.

**WARNING  tests/test_start_session_asks_conditionally.py:213** —
`test_a_trunk_branch_is_still_asked` is an exact duplicate of
`test_a_branch_that_has_ended_a_session_before_is_still_asked` (L88): same
`_row("a trunk branch whose last stop was not continued")`, same
`assert "Ask:" in row`, same `assert "Do not ask" not in row`. Only the docstring
differs, and this repo's convention is that the docstring names the failure the test
caught — two docstrings over one assertion pair means one of them names a failure
nothing is checking.
→ delete L213-222 and merge its `main`-accumulates-a-handoff paragraph into the
existing test, renaming that test if the old name has gone stale.

**NIT  tests/test_start_session_asks_conditionally.py:61** — `delete:` `_row` gained a
`section: str | None = None` parameter that no call site passes; the step-4 tests
assert against `_step_four()` directly. Unused flexibility added in this diff.
→ restore the one-argument signature and `for line in _step_nine().splitlines()`.

**NIT  wfctl/_session.py:484** — `shrink:` the `if NEXT_SESSION_TODO not in summary:
return True` guard is redundant against `str.partition`, which returns `""` for the
tail when the separator is absent — the loop then finds nothing and returns `True` by
the same path.
→ `body = summary.partition(NEXT_SESSION_TODO)[2]` and drop the guard and the
`index`/`len` arithmetic (−3 lines, identical behaviour).

**NIT  wfctl/_session.py:486** — the scan anchors on the *first* occurrence of
`## Next Session TODO` and treats the remainder of the heading line as section
content, so three near-misses read as *filled* and suppress the FR-013 warning:
a heading annotated `## Next Session TODO (updated)` (remainder `(updated)` counts
as content), a pluralised `## Next Session TODOs`, and a handoff that mentions the
heading text in prose above the real section (`index` finds the mention, the scan
runs over the prose, `## ` breaks it at the real heading). The break condition is
also `## ` alone, so an `# Appendix` or `### Sub` after the section leaks into it.
All narrow, all fail toward silence rather than toward a false warning, and the
canonical template is unaffected — worth a line in the docstring at least, since the
docstring currently claims the question is "whether the section was ever filled in".
→ anchor on a line-start match (`for line in summary.splitlines()`, find the line
whose `strip()` equals `NEXT_SESSION_TODO`, scan until the next line starting with `#`).

**NIT  wfctl/agents/skills/using-wfctl/SKILL.md:23** — the `wfctl end` row does not
mention `--continued`, and that table does document flags for every other command that
has one (`--force`, `--auto-approve`, `--branch`, `--agent`, `--yes`). This is the
agent-facing discovery surface for the flag; without it the only place `--continued`
is named in shipped text is `start-session`'s prose about a mark someone else writes,
and FR-009's "a person stopping a run themselves" has only `--help`.
`README.md:195` has the same gap in the human-facing surface.
→ append to the row: "`--continued` records that the work carries on rather than that
it was wrapped up — the next session reads it."

**NIT  wfctl/cli.py:829** — `summary_path.read_text()` is unguarded, so an
undecodable or concurrently-removed handoff raises *after* the stop has been appended
to the log: the stop is durably recorded and the command still exits non-zero with a
traceback, which is the opposite of the ordering the comment three lines above argues
for. `_session.auto_approve`'s docstring shows the repo already treats unreadable
state-dir files as a live case ("an invalid UTF-8 byte raises `ValueError`"). Against
it: the `summary_path.stat()` at L815 in the same function is equally unguarded and
predates this change, so the new line follows local precedent.
→ `read_text(errors="replace")`, or wrap the warning in `try/except OSError,
ValueError` and skip it — never fail the stop that already happened.

---

### What each pass checked

1. **Correctness** — traced `end_cmd` → `_session.end` → `_io.append_event`
   (`json.dumps` default separators produce `"event": "end"`, so step 4's grep
   pattern matches what is actually written); confirmed `_session.end` has exactly
   one call site, so the keyword-only no-default `continued` cannot silently default;
   confirmed `wfctl end --help` offers `--continued` and **no** `--no-continued`, as
   `contracts/wfctl-end.md` requires; confirmed `status --json` really emits
   `"issue"` with the literal `unknown` (`cli.py:428`, `extract_issue_key`) so step 4's
   instruction is not describing a field that does not exist; hand-traced
   `names_no_first_action` over the template, a missing section, an empty section, a
   filled section, a section filled below an empty one, a placeholder mixed with a
   real item, and the heading-variant cases above; checked `_stall._passes_this_sitting`
   and `opens_a_new_sitting` still match `("start", "end")`; walked the step-9 row
   table for exhaustiveness (the one hole is the WARNING above); verified
   `wfctl log`'s `{ts}  {event:<10}  {detail}` format makes
   `line.split()[2:3] == ["continued"]` the right index for the event column.
2. **Security** — no new external input; the only file read is one wfctl wrote into
   its own state dir; the FR-013 warning is a constant string with no interpolation,
   so no rich-markup injection path (the pre-existing `escape(observed.step)` is
   untouched and still correct); the flag reaches no outward-facing action and does
   not touch the `authority:notify` gate; `end` remains ungated, as it was.
3. **Architecture** — checked `stop-kind-is-a-field-not-an-event.md`'s central claim
   against the code: all four named readers do match on the kind and none reads the
   payload, so the record's argument holds and `test_the_payload_is_inert_to_every_existing_reader`
   is testing the right thing. `status: proposed` is this repo's norm for a new record
   (only 9 of 29 are `accepted`), so that is not drift. Constants live beside the
   renderer that writes them, per the record's own reasoning. FR-012 is satisfied by
   the rule living in package data under `wfctl/agents/` rather than in `.agents/`.
4. **Readability** — comments explain *why this shape* and do not narrate the diff,
   matching `AGENTS.md`. Names are accurate. No nesting or length problems; the one
   new function is 12 lines. The two prose nits are above.
5. **Performance** — nothing here is hot; one extra file read per `wfctl end
   --continued`, one grep per session start. No finding.
6. **Over-engineering** — findings above. The production code is lean; the fat is in
   the test file.

**net: −38 lines possible** (dead `section` param, one duplicate test, two vacuous
tests folded in, the `partition` shrink).

**Verdict: Request changes** (0 blockers, 4 warnings). Nothing here is a shipped bug
in `wfctl end` — that half of the change is correct, well tested and faithful to its
contract. The two routing warnings are both in `SKILL.md`, which is the artifact this
feature actually ships, and both fail in the direction #244 and SC-002 exist to
prevent: an unattended session starting work it was not pointed at. The two test
warnings matter because this file is the only check standing behind that text, and
its own docstring says an assertion the violating artifact passes is the rule's
absence.

# Review: `dc3387c..HEAD` (`8aeaa99`, `612d35b`) — r4

## Findings

WARNING  tests/test_shape.py:217 — `test_the_report_carries_the_finding_before_the_instruction` pins only one of the two routes `612d35b` says are load-bearing. Its comment states "What survives is the two routes, because they are not interchangeable: an agent without the slash command needs the path" — but the only route assertion is `"/conversation-response-shape" in message`. Deleting the `.agents/skills/...` half of the string leaves the suite green, and that half is the one the commit argues is non-substitutable. → add `assert ".agents/skills/conversation-response-shape/SKILL.md" in message`.

NIT  wfctl/cli.py:3688 — "the reader got the same seven lines twice per firing" is now a stale count twice over. The pre-fix report was 8 lines / 6 non-blank with one finding, and `612d35b` cut it to 6 / 4. The next reader counts `_SHAPE_REPORT` and gets a different number. The README's corrected paragraph already dropped the count. → "the same report twice per firing". Same string at tests/test_shape.py:202 ("seven-line report").

NIT  wfctl/cli.py:3568-3574 — the new comment paragraph narrates the diff rather than informing the next reader, which `CLAUDE.md` calls out by name ("If a comment only makes sense beside the diff, it belongs in the commit message"). "which is the part #298 changed" and the enumeration of the four deleted lines ("what the digest is, how long it is, what it omits") describe text that is no longer in the file. The load-bearing halves — the reader has already agreed to the rule so a pointer is enough; one string is billed on screen and in context both; keep both routes — survive a tightening to ~4 lines. Separately, "there is one string and it goes to both" lands two commits after `#298` removed a second emission and reads at first as the double print; "`additionalContext` is rendered on screen *and* carried into the next turn" says it without the collision.

NIT  README.md:486-487 — "It carries `|| true`" now follows a sentence whose subject is the finding ("It rode `systemMessage` as well until #298…"), so the antecedent reads as the finding rather than the `Stop` entry. The referent was already two sentences back before this edit; the rewrite moved a competing "It" in between. → "The entry carries `|| true` because…".

NIT  tests/test_shape.py:269 — "Its whole cost is that a reader who has not asked for it sees nothing new" states the absence of a cost as the cost. The intent is presumably that the flag's only cost *if it defaulted on* is noise for a reader who did not ask for it, which is why off is the half worth pinning. → say that.

NIT  tests/test_shape.py:277 — `assert result.stderr == ""` also passes if the command produced nothing at all, or died inside `CliRunner` (which routes the traceback to `output`, not `stderr`). The sibling echo-on test asserts on stdout as well; this one asserts on neither. → add `assert result.exit_code == 0` or reuse the stdout JSON assertion.

net: −4 lines possible (the cli.py comment paragraph)
Verdict: **Approve** — 0 blockers, 1 warning. The warning is a one-line test addition, not a defect in the shipped behaviour.

## Passes run, and what each checked

**Definition of done.** `uv run pytest -q` → 1139 passed in 64s. `uv run ruff check wfctl/ tests/` → clean. `uv run mypy wfctl/` → clean, 19 files. `uv run wfctl doctor` → `base:` and `claude:` skills current at 0.19.0; the three `ℹ issue N is claimed by 2 features` rows and the `⬆ claude: managed Stop hook … is gone` row are worktree/spec-tree state and predate this diff (nothing in it touches `.claude/settings.json` or the specs tree).

**1. Correctness — the six-case loop (attention point 1, mutation-verified).** Copied `wfctl/_shape.py` into the scratchpad, deleted each added alternation token in turn, and ran the six prompts through `findings("word " * 300, …)`. The tree is not touched.

| token deleted | case that then flags |
|---|---|
| `eli5` | `ELI5` |
| `plain` | `put it in plain english` |
| `simpl\w+` | `keep it simple` |
| `examples?` | `give me some examples` |
| `pm` | `how would you pitch it to a PM` |
| `examples?` → `example` (undo `8aeaa99`'s half) | `give me some examples` |

Every added token is pinned by exactly one case, including the `s?` this commit added — `\bexample\b` does not match "examples", so case 5 is a genuine pin on `8aeaa99` and not only on `dc3387c`. Unmutated, all six pass. Case 1 (the `#298` prompt) pins nothing on its own — it carries `plain`, `simpl`, `example` and `pm` at once — but it is the verbatim regression case and the docstring says so; that is documentation, not slack.

**The tautology claim checks out.** Against `dc3387c~1`'s gate all six new cases draw a Q3, and the old test's prompt (`explain this in plain terms…`) does not. The replaced test did pass on the unfixed code; the loop does not.

**2. Correctness — the conftest pop.** Reproduced the failure it prevents by re-exporting `WFCTL_SHAPE_ECHO=1` from a scratchpad pytest plugin fixture (which runs after `conftest.py`'s module-level pop): 5 failures in `tests/test_shape.py`, exactly 4 of them `JSONDecodeError: Extra data` — the count the comment claims. The fifth is `test_the_echo_is_off_until_the_flag_is_set` failing on its own assertion, which is the test doing its job. `runner.invoke(...).output` is confirmed to be the combined stream while `result.stderr` is separate, so both echo tests are asserting on what they say they are.

**3. The shrunk report (attention point 2).** Both routes resolve. `.agents/skills/conversation-response-shape/SKILL.md` exists in this worktree and is the layer `install-skills` writes unconditionally — an agent that got no `.claude/` layer (the `WFCTL_AGENT`-unset path `CLAUDE.md` describes) still has it, so the fallback is the universal one. Relative-path fragility if the agent's cwd is not the repo root is pre-existing: the old text carried the same path. What is lost against the old text is the explicit ordering cue ("Run … if you can; otherwise read …") and the "including the seven-question pre-send check at the end" pointer. The first is a mild readability loss against a bare comma-`or` (an agent could try the slash command as a shell command); the second is subsumed by "in full". Neither rises past preference, so neither is filed above.

Scope note, not a finding: `612d35b` is not one of the two defects `#298` names and the worktree brief does not authorize it. It is defensible as the same noise-reduction issue (the branch is named for it) and the reasoning is written down in both the commit and the code, so I am not asking for it to be split — but a reconciler should decide deliberately rather than by omission.

**4. Stale text sweep (attention point 3).** `grep` across the tree for `Re-read the skill`, `every-turn digest`, `500 characters`, `not wired for this event`, `systemMessage`, `WFCTL_SHAPE_ECHO`, `additionalContext`, `Stop hook feedback`. No survivor of the old instruction text or the old README claim anywhere in `wfctl/agents/`, `docs/`, `tests/` or `README.md`. The only `500 characters` hit is `wfctl/agents/skills/conversation-response-shape/SKILL.md:28`, about the digest's own cap — unrelated and still true. `.workmux/PROMPT-298-shape-hook-noise.md` describes the pre-fix state by design. The README env-var row's claim that "the installed hook entry redirects stderr" is verified against `STOP_HOOK_COMMAND` (`cli.py:1474`, `2>/dev/null || true`), and the row's column padding matches its neighbours.

**5. The kept measurement (attention point 4).** It still argues for text that exists. The measurement is that *invoking* the skill took the reply from a finding on four turns out of four to none on the two that followed, and the surviving string still tells the agent to invoke it — `/conversation-response-shape` is the first of the two routes. The closing sentence, "A pointer alone would have to be believed; a pointer under a finding has just been shown to be right," reads better after the shrink than before it, because the text is now purely a pointer. No orphaned argument. What is orphaned is the *description of the deleted lines* in the newly added paragraph, filed as a NIT above.

**6. Security.** Nothing user-controlled reaches a shell or a query. The env var is read with `os.environ.get(...) == "1"` — no truthiness surprise. The echoed report contains a 60-char excerpt of the reply, and the installed hook entry sends stderr to `/dev/null`, so the flag cannot leak transcript text into a log by default. `json.dumps` escapes the reply excerpt into `additionalContext`; there is no string-built JSON.

**7. Performance.** Unchanged. One added `re` alternation branch on a per-turn regex over a prompt string, one `os.environ.get` behind the `if found:` guard. No new I/O.

**8. Architecture.** The `_shape.py` owns *what broke* / `cli.py` owns *who is told* split the brief names is respected: the gate change is entirely in `_shape.py`, the report and echo entirely in `cli.py`. The conftest pop follows the `FORCE_COLOR` precedent immediately above it in the same file, same shape and same reasoning.

**9. Over-engineering.** Nothing to delete in the code. The one shrink candidate is the cli.py comment paragraph. Worth naming as accepted rather than flagged: `612d35b` spends +8 comment lines to remove 2 lines from a string, which is a good trade because the comment is billed once and the string on every firing — that is the argument the comment itself makes, and it holds.

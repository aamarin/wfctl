# Review: dc3387c — `fix(shape): let rule 3's plain-words row opt into depth, and say the finding once` (reviewer r3)

Target: `git show dc3387c` (HEAD). Three files, +64/−20.

## Findings

**WARNING  tests/test_shape.py:74-82 — the regression test for defect 1 passes on the unfixed code, so the regex half of the fix is unguarded → drop `explain this ` from the prompt (and loop the row's three phrasings, per the neighbouring `test_depth_is_asked_for_in_whatever_form_the_word_takes`).**

The prompt the test uses is `"explain this in plain terms, pretend you're talking to a PM. Provide a simple example as well"`. `explain` was already in `_ASKED_FOR_DEPTH` before this commit, and it matches at offset 0, so the gate opened for the old regex too. Proven two ways:

- `dc3387c~1:wfctl/_shape.py` — `findings("word " * 300, asked)` returns `[]`. Strip the leading `"explain this "` and the same pre-fix module returns the `Q3 — 300 words…` finding, i.e. the real bug.
- Whole file, pre-fix regex monkeypatched back in (`_shape._ASKED_FOR_DEPTH` reset to the old pattern via a pytest plugin, tree untouched): **32 passed**. Reverting the one changed regex line leaves the suite green.

The handoff (`.workmux/PROMPT-298-shape-hook-noise.md`) names the prompt to pin with: *"in plain terms, pretend you're talking to a PM… Provide a simple example as well"* — no `explain`. The docstring's first line, `"""The verbatim prompt from #298.`, is therefore also inaccurate as written.

Verified the corrected prompt does the right thing post-fix: `findings("word " * 300, "in plain terms, pretend you're talking to a PM. Provide a simple example as well")` → `[]`, and each of `"explain it simply"` / `"like a PM"` / `"ELI5"` → `[]` (all three returned `Q3` pre-fix except the first, which `explain` already covered).

**WARNING  README.md:481-484 — carries the exact claim this commit falsified; it is the same sentence the cli.py docstring was corrected for → update it alongside.**

> The `Stop` entry warns and never blocks. It reports to the agent that wrote the reply rather than to your terminal — `systemMessage` is the obvious channel and **is not wired for this event**, so the finding rides `hookSpecificOutput.additionalContext`…

The premise of #298 is that it *is* wired now. `cli.py`'s docstring was corrected; the README, which is the user-facing copy of the same paragraph, was not. A reader hitting the double print in an older install reads the README first.

**WARNING  wfctl/cli.py:3674 — the bolded claim is contradicted by its own docstring five lines later → reword to what is true now.**

L3674: *"**It reports to the model, not to the terminal.**"*
L3681-3683: *"it now prints under `Stop hook feedback:` while `additionalContext` prints under `Stop says:`, so the reader got the same seven lines twice."*

By the commit's own account `additionalContext` reaches the terminal too. The fix halves the report, it does not remove it from the reader's screen — and the sentence that follows the bold claim ("The reader cannot act on a finding about a reply that is already on screen") now argues against something the reader still sees. Concrete replacement for the paragraph head: *"**It reports to the model.** The channel is `hookSpecificOutput.additionalContext` — it reaches the agent that wrote the reply, in time to shape the next one. The harness surfaces it under `Stop says:` as well; once is the point, not zero."*

**NIT  wfctl/cli.py:3679-3685 — two paragraphs narrate the change rather than inform the next reader → compress to the one sentence that is a fence.**

CLAUDE.md: *"Comments inform the next reader of the file; they do not narrate the change to a reviewer. If a comment only makes sense beside the diff, it belongs in the commit message."* The `systemMessage` history and the seven-run measurement are already in the commit body near-verbatim, and no `systemMessage` appears anywhere in the file the reader is standing in. The part worth keeping is the fence — *don't re-add it* — which fits in one line: *"`systemMessage` is deliberately not emitted beside it: the harness renders both, so both is the same seven lines twice (#298)."* The handoff's "correct, don't delete, the measurement" instruction is satisfied by the commit message, which is where it now permanently lives.

**NIT  README.md:696-704 — `WFCTL_SHAPE_ECHO` is missing from the Environment Variables table, which is otherwise exhaustive (every `WFCTL_*` in `wfctl/*.py` plus `XDG_STATE_HOME`) → add a row.**

**NIT  wfctl/_shape.py:105-108 — the comment identifies code by physical position ("The second line"), and `example` is not in the row it says the line is → say which row, not which line.**

The SKILL.md rule-3 row is `"explain it simply", "like a PM", "ELI5"` (SKILL.md:161). `plain`, `simpl\w+`, `pm` and `eli5` trace to it; `example` does not — it comes from the #298 prompt. It is also the widest of the five (`for example` is ordinary in a bare instruction), which is fine under the deliberately-over-broad design but makes the sentence "the second line **is** the row" inexact. Also: re-wrapping the regex silently invalidates "second line".

**NIT  tests/test_shape.py:190 — the name `…_and_is_not_also_printed` overclaims, for the same reason as the cli.py bold claim → `…_and_the_report_is_emitted_once`.**

**NIT  tests/test_shape.py:243-258 — nothing asserts the echo is off by default → add `assert not result.stderr` to a default-path run.**

Flipping `== "1"` to `!= "0"` would reintroduce the duplicate-output class this commit fixes, and the suite would stay green. Low stakes because the wired `Stop` entry carries `2>/dev/null` (cli.py:1474 — docstring claim verified), so it is one assertion, not a new test.

## What each pass checked

- **Correctness.** Widening `_ASKED_FOR_DEPTH` can only suppress `Q3`, never create one, so no new false positive is reachable; confirmed the added tokens are boundary-safe against the obvious collisions (`npm`, `3pm`, `explain` vs `\bplain\b` — none match). Confirmed the echo is inside the `if found:` block, so a clean reply stays silent on both streams. Confirmed the surviving JSON shape (`hookSpecificOutput.hookEventName` + `additionalContext`) is unchanged. Traced `_last_exchange`, `_prose`, `_split_fences` and `_SHAPE_REPORT`; nothing else read `systemMessage` (grep across `wfctl/`, `tests/`, `docs/` — only README and the docstring).
- **Security.** New env var is read-only, compared with a strict `== "1"`, and writes only the already-computed report to stderr, which the installed hook redirects to `/dev/null`. No secret, no injection surface, no new input.
- **Architecture.** The split the handoff drew holds: `_shape.py` changed only the decision, `cli.py` only the delivery. Nothing crossed.
- **Readability.** Names are fine. The docstring is the one place that degraded (findings above).
- **Performance.** One extra `os.environ.get` per firing. Nothing.
- **Over-engineering.** `WFCTL_SHAPE_ECHO` duplicates data already on stdout (`… | python3 -c 'import json,sys;print(json.load(sys.stdin)["hookSpecificOutput"]["additionalContext"])'` gets the same string with no code), but the handoff explicitly specified the flag — noted, no action. Nothing else to cut.
- **Definition of done, re-run at HEAD:** `uv run pytest -q` → 1138 passed. `uv run ruff check wfctl/ tests/` → clean. `uv run mypy wfctl/` → clean, 19 files. (`wfctl doctor` and `wfctl verify` deliberately not run — shared worktree.)

net: −6 lines possible (cli.py:3679-3685 history → one line; the echo flag is spec'd and stays)
Verdict: **Request changes** — no blockers, but the headline defect ships without a working regression test, and the README still states the falsified premise. Both are one-line fixes.

## Review: r2 — commit `4910bf8` on `366-nested-chain-drawing-row` vs `origin/main`

Target: `git show 4910bf8` — two files, +15/−1.
`wfctl/agents/skills/conversation-response-shape/SKILL.md` (+1 table row),
`tests/test_response_shape_invariants.py` (+14 comment lines, `_LINE_CEILING` 490 → 491).

Verdict: **Approve** — no blockers. Two one-line wording fixes and one follow-up
to file.

---

### Findings

**WARNING  tests/test_response_shape_invariants.py:L74 — "the five above it" is
false; the row was inserted third.**
The comment says the new row "carries its form in the same two columns as the
five above it". The diff puts it after `one source, several destinations`, so
two rows are above it and three below. The whole point of this comment block is
that a paragraph arguing for its own exception "has to survive being checked" —
the sentence beside the arithmetic should not be the one that fails the check.
→ "the same two columns as the other five".

**WARNING  wfctl/agents/skills/conversation-response-shape/SKILL.md:L321 — the
row's two columns disagree on scope.**
`| a chain whose destinations fan out again | a nested tree, each level indented
under its caller |`. The left column is generic ("a chain" — of calls, of
dependencies, of reporting lines); the right column is call-specific ("its
caller"). A reader who matches a non-call chain on the left is then told to
indent under a caller that does not exist for their material — which is rule 4's
own failure, a noun that resolves only for the writer. The five existing rows are
each scope-consistent across their two columns, and "reads like the five already
there" is the acceptance criterion.

Everything else in the change says *calls*: the commit subject ("a nested call
chain"), the issue title, and the ceiling comment at L71 ("a chain of calls whose
destinations fan out again"). The row is the one place the word was dropped.
→ Left column to `a chain of calls whose destinations fan out again` (the
issue's own wording), or, if the generality is deliberate, right column to
`each level indented under the node it hangs from`. Either closes it; the first
matches the issue's Definition of Done.

**WARNING  (pre-existing, widened by one) — every line-number citation into
`SKILL.md` past the insertion point is now stale by 8 instead of 7.**
Inserting at L321 shifted everything below it. Measured against `origin/main`:

| Cited as | Actually at (main) | Actually at (HEAD) | Anchor |
|---|---|---|---|
| `SKILL.md:322` | 329 | 330 | "Tabular content goes in a table" |
| `SKILL.md:410` | 417 | 418 | "A reply is not a document" |
| `SKILL.md:429` | 436 | 437 | "This governs the reply only" |

Call sites: `wfctl/_shape.py:22`, `:66`, `:330`, `:358`; `wfctl/cli.py:4542`;
`tests/test_shape.py:44`, `:476`, `:480`, `:492`. `_shape.py:358` is the worst of
them — it builds a *user-visible finding string* `f"SKILL.md:322 — …"`, so the
checker points its reader eight lines off, landing inside the form-selection
table rather than at the rule being cited. `test_shape.py:480` asserts that
literal, so CI pins the stale number and can never catch the drift.

This diff did not cause it and should not fix it — the rubric's own rule is to
file cleanup rather than smuggle it in. → Follow-up issue: replace the numeric
citations with the quoted anchor text (which `_shape.py:330` already carries
alongside the number), or assert the anchor's line number in a test rather than
hardcoding it in prose.

**NIT  tests/test_response_shape_invariants.py:L77–82 — the closing paragraph
restates the commit body verbatim.**
"What the line buys is a shape that was being drawn by hand… the two nearest rows
each carry one layer… A reader who found neither invented a drawing" is the
commit message's first paragraph almost word for word, and the issue's *What went
missing* table before that. `CLAUDE.md`: a comment that only makes sense beside
the diff belongs in the commit message. The prior two exception blocks state what
the lines buy in one or two sentences (483: the Persistence pointer; 490: the
rule 2 / rule 3 contradiction); this one spends six lines re-arguing a row that
the table itself now shows. → Cut to one sentence; the argument survives in the
commit and the issue. Also: "the sentence about cutting rather than adding still
governs the seventh rule" is now the third statement of that same caveat inside
one comment.

**Observation, no action.** 483, 490, 491 — three consecutive disclosed
exceptions, and the ceiling has not yet forced a cut. Each grant costs ~13 lines
of argument for 1–7 lines of skill, so the test file now carries 60 lines of
ledger above a one-integer constant. That is the register the project chose and
this change stays inside it; if a fourth exception arrives, the mechanism is
worth a look rather than another paragraph.

---

### What each pass checked

**1. Correctness.** Verified by measurement, not by reading:
- `SKILL.md` is exactly 491 lines (`len(text.splitlines())` == 491, file ends in a
  newline), so `test_the_skill_stays_under_its_line_budget` passes at equality —
  zero slack, which is what the comment at L53–54 claims and intends.
- `test_the_form_selection_table_has_exactly_one_home`: `| The material is |`
  occurs in exactly one `.md` under `wfctl/agents/` (L317). No copy added.
- `test_the_first_three_rules_keep_their_numbers`: no `## N.` heading touched.
- `test_no_example_teaches_a_rule_in_wfctl_vocabulary`: the diff adds no fenced
  block, so the guilty set is unchanged.
- Commit-message claim "`digest.md` is re-measured at 493 of its 500 and
  unchanged" — confirmed: 493 characters, 493 bytes, not in the diff, and it
  projects the six rules with no table, so the row does not reach it.
- Issue DoD "the new row read back out of the installed copy" — both
  `.agents/skills/conversation-response-shape/SKILL.md` and
  `.claude/skills/…/SKILL.md` carry the row, so `install-skills` was run with the
  working tree's wfctl. That is the one DoD item the suite cannot check.
- Row placement: L321 is inside *Show: the drawing is the description* (L292–348),
  as the issue specifies. Inserting it directly under the fan-out row reads as an
  escalation from it, which helps the pick.
- Trailing prose still true after the insert: "Two columns is the most frequent
  row" still refers to row 1; "the selection table above" (L282) and "the
  selection table's job" (L372) are unqualified by count.

**2. Security.** Nothing applicable and nothing hand-waved: the diff is one
markdown table row and one integer constant plus comments. No input handling, no
secrets, no execution path. The only thing that *executes* near here is the
`UserPromptSubmit` hook that injects `digest.md`, which this change does not
touch.

**3. Architecture.** The single-owner invariant is the constraint this file
lives under, and it holds — the row went into the owner, not into a restatement.
The PR template's ASCII-vs-mermaid rules are untouched (both copies,
`.github/pull_request_template.md` and the bundled
`wfctl/agents/configs/github/…`, are byte-identical to each other and absent from
the diff), so the issue's *Out of scope* is respected. One adjacent thing checked
and deliberately not flagged: `wfctl/agents/skills/design-levels/SKILL.md:165–172`
carries its own shape picker ("what shape is the thing?"). It is not a copy of
this table — different surface (a design artifact, not a reply) and different
rows — so the new row does not belong there and the one-home test is not evaded
by it.

**4. Readability.** The row matches the five others' `<material> | <form>,
<detail>` shape and stays lowercase like them. Its 98-character line is not an
outlier — rule 3's table already runs to 222 characters, and a markdown row
cannot wrap. The one readability defect is the column scope mismatch, filed as a
WARNING above. Dropping the issue's third clause ("branches hanging off the node
that takes them") reads as a deliberate cut, not an omission: the sequence row
already owns that phrase.

**5. Performance.** One more line in a file the agent loads per invocation. That
cost is precisely what `_LINE_CEILING` exists to price, and the change pays for
it in the disclosed register. `digest.md`, the per-turn injection and the only
hot path here, is unchanged.

**6. Over-engineering.** No dead code, no speculative flexibility, no abstraction
— the diff is one row and one constant. The only shrinkable thing is the comment
ledger (NIT above).

net: −5 lines possible (trim the restated paragraph at L77–82)
Verdict: **Approve** — 0 blockers, 2 one-line fixes, 1 follow-up to file.

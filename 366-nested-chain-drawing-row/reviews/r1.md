# Review: 4910bf8 — `feat(skills): the form-selection table had no row for a nested call chain`

Reviewer **r1**. Target: the single commit `4910bf8` on `366-nested-chain-drawing-row`
against `origin/main` (2 files, +16 / −1). Rubric: `.agents/skills/code-review/SKILL.md`,
all six passes. Nothing in the tree was changed by this review.

## Findings

```
WARNING  wfctl/_shape.py:L358  — inserting SKILL.md:321 shifts every rule below it
                                 down one; five hard-coded `SKILL.md:<line>` pointers
                                 now miss by 8 instead of 7, one of them in a string
                                 shipped to a user → update to 330/418/437, or cite
                                 the section heading and stop the decay
WARNING  tests/test_response_shape_invariants.py:L83 — UNCOMMITTED in the tree:
                                 `_LINE_CEILING = 490` while the comment above it
                                 argues 491 and SKILL.md is 491 lines. The suite is
                                 red right now → restore 491 before any test verdict
                                 or commit (left untouched: not mine to revert)
NIT      tests/test_response_shape_invariants.py:L74 — "the same two columns as the
                                 five above it": the row lands third, two above and
                                 three below → "as the other five"
NIT      SKILL.md:L321 — left cell generalises to any chain, right cell says "its
                                 caller", which only decodes for a call chain →
                                 "a chain of calls whose…" or "under the node it
                                 hangs from"
NIT      SKILL.md:L321 — names a form with no medium, nine lines above "Reserve
                                 ASCII for flows and timelines", which reads as a
                                 whitelist a tree is not on → "a nested tree in
                                 ASCII, …" (costs no line)
NIT      tests/test_response_shape_invariants.py:L73-75 — shrink: the third ledger
                                 entry restates the second's "not a seventh rule /
                                 the sentence above still governs" near-verbatim →
                                 state it once at the head of the ledger
```

### WARNING — the line-number citations this row pushes further out of date

Five places cite `SKILL.md` by absolute line, and the new row at 321 moves all three
cited rules down one.

| Citation | Names | Actually at | Was at (origin/main) |
|---|---|---|---|
| `SKILL.md:322` | *Tabular content goes in a table* | 330 | 329 |
| `SKILL.md:410` | *A reply is not a document* | 418 | 417 |
| `SKILL.md:429` | *This governs the reply only* | 437 | 436 |

Sites: `wfctl/_shape.py:22`, `:66`, `:330`, `:358`; `wfctl/cli.py:4542`;
`tests/test_shape.py:44`, `:476`, `:480`, `:492`.

The drift is pre-existing — the 483→490 exception put all three off by 7 — but this
commit is what makes it 8, and `_shape.py:358` is not a comment: it interpolates
`SKILL.md:322` into a finding a user reads. Line 322 is now the table row
`| a value and what it causes | … |`, not the tabular-content rule it claims to
quote. `tests/test_shape.py:480` asserts the literal string `"SKILL.md:322"`, so
nothing resolves the number and nothing ever goes red. That is precisely the failure
class this feature's own test file opens by naming: *"the failure it catches is
silent — the file still installs, the suite still passes, and the cost lands on a
reader."*

Two ways to close it, A being in scope for this diff and B being the durable one:

- **A** — bump the five citations to 330 / 418 / 437. Mechanical, one commit, and it
  will rot again at the fourth exception.
- **B** — cite the section (`SKILL.md`, *Show: the drawing is the description*)
  instead of the line, in the emitted string too. Its own diff; file it.

Deferring is acceptable under the rubric only with a written follow-up. Silently
shipping an eighth line of drift on a pointer the tool prints is not.

### WARNING — the working tree is mutated and red

`git status` shows `M tests/test_response_shape_invariants.py`, and the diff against
`HEAD` is a single hunk reverting `_LINE_CEILING` from 491 to 490. `SKILL.md` is 491
lines, so `test_the_skill_stays_under_its_line_budget` fails as the tree stands. It
reads like a deliberate mutation probe by another panel member, which is why I left
it alone rather than reverting it — but it is uncommitted state that makes the tree
self-contradicting (the comment immediately above argues 491) and any `uv run pytest`
result untrustworthy until it is put back.

## Passes run, and what each checked

**1. Correctness** — arithmetic reproduced independently, not taken from the commit
message. `wc -l SKILL.md` = 491, ceiling 491, diff is +1/−0: the "third disclosed
exception, and one line" claim holds, and so does the 483 entry's invariant that the
ceiling equals the file exactly. `digest.md` is untouched in the diff and measures 493
bytes against its 500 cap — the commit's "493 of its 500" is accurate. Worth knowing:
grep found **no test enforcing the 500-character cap** anywhere in `tests/`; it lives
only in prose at `SKILL.md:28` and in the issue's Definition of Done, so a re-measure
disclosed in a commit message is the entire evidence chain. Pre-existing, not this
change's to fix.

All four tests in the file traced against the new content rather than assumed:
`test_the_first_three_rules_keep_their_numbers` is unaffected (insert at 321 is below
all three headings); `test_the_form_selection_table_has_exactly_one_home` globs
`files("wfctl")/agents`, not the gitignored mirrors, so the row now present at line
321 of both `.agents/` and `.claude/` cannot trip it;
`test_no_example_teaches_a_rule_in_wfctl_vocabulary` sees no new fenced block and no
`wfctl` token. I also traced the one consumer that parses this file's shape:
`_shape.py`'s `_TABLE_ROW` strips table rows before prose is counted, so the sixth row
cannot shift any `findings()` / `body_findings()` result.

**2. Security** — n/a. One markdown table row and one integer constant; no input, no
execution path, no secrets.

**3. Architecture** — scope honoured. The diff is two files; the PR template at
`wfctl/agents/configs/github/.github/pull_request_template.md` is untouched, as the
issue's *Out of scope* requires, and the single-owner arrangement it depends on
(template points at the table, table is not restated) is preserved. The ASCII-vs-
mermaid argument stays in the commit message where it belongs rather than becoming the
second copy the template forbids. The one architectural debt the change touches is the
line-citation coupling above.

**4. Readability** — the row is parallel with the five it joins: noun phrase on the
left, form plus a comma-qualifying participial phrase on the right. Right cell is 50
characters against an existing spread of 36–49, so it does not stand out. It is
distinct from `one source, several destinations` — a one-level fan still lands on the
older row, and "again" / "nested" is what separates them. The issue proposed "a chain
of calls, where destinations fan out again"; the shipped "a chain whose destinations
fan out again" is tighter and broader, which I read as an improvement, except that the
right cell kept "caller" and so no longer matches (NIT above). Placement third, after
the fan-out row it extends, is the right neighbour.

**5. Performance** — n/a.

**6. Over-engineering** — thirteen comment lines for a one-line table row is the
ledger's established convention, not bloat, and I am not flagging the ledger as such:
it is the only record of why the ceiling sits where it does, and the 483 entry's
insistence that an exception "has to survive being checked" is what let me verify this
one. The one `shrink:` is the duplicated "not a seventh rule / the sentence above still
governs" clause, now stated twice.

**Not run, per the review brief:** `pytest`, `ruff`, `mypy`, `wfctl doctor`,
`install-skills`. The ceiling assertion was verified by `wc -l` instead, and the
installed copies were read rather than reinstalled. Note that a suite run would have
been red anyway, for the unrelated working-tree reason above.

net: −4 lines possible (the ledger's duplicated clause). `SKILL.md` itself: lean already.

**Verdict: Approve** — the row is correct, in scope, well-formed against its five
neighbours, and the ceiling arithmetic checks out line for line. Two things to settle
before merge, neither of them the row: decide A or B on the `SKILL.md:<line>`
citations (and write the follow-up if B), and restore `_LINE_CEILING = 491` in the
working tree.

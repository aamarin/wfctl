## Review: `git diff main...HEAD`

No findings.

Passes run:

1. Correctness — traced `_counted_lead_in()` through sentence splitting and both regexes. Direct calls to `findings()` verified every claimed table outcome; the new closing-colon path correctly retains `both`, while inline/mid-sentence uses exclude it.
2. Security — reviewed untrusted reply/prompt handling and regex changes; no new input, execution, disclosure, or injection risk.
3. Architecture — preserves the existing pure-string detector and keeps the distinction local to the two regexes.
4. Readability & simplification — names reflect the distinction (`_COUNT` vs `_ANNOUNCING`); the rationale documents the otherwise non-obvious asymmetry.
5. Performance — fixed-size compiled regexes and line/sentence scans only; no changed asymptotic cost or meaningful added work.
6. Over-engineering — no speculative abstraction or unnecessary runtime mechanism added.

Verification: `git diff --check` passed. Direct behavioral checks passed for all supplied examples. `uv run pytest tests -q` and a targeted pytest fallback could not collect because this read-only sandbox forbids creation of uv-cache/temp files; this is an environment limitation, not a test failure.

Lean already.

Verdict: Approve
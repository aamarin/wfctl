## Review: `main...HEAD`

NIT [wfctl/_shape.py:146] — Comment says `_COUNT` “belongs to the coda,” but `_COUNTED_CLOSING` applies it to the opening sentence too. → Describe it as the vocabulary for a sentence-closing colon.

NIT [tests/test_shape.py:148] — Test name says an anaphoric count is “never” a lead-in, contradicted by the terminal-colon cases immediately below. → Scope the name to a mid-sentence colon.

Passes run:

- Correctness: traced `_counted_lead_in()` and both regex paths; direct probes confirmed all nine claimed examples.
- Security: reviewed untrusted reply handling and regex changes; no new injection or trust-boundary issue.
- Architecture: verified the change remains within the existing pure detector and preserves its sentence/colon distinction.
- Readability & simplification: found the two misleading descriptions above; otherwise the split vocabulary reflects a necessary behavioral distinction.
- Performance: added anchored regex check is bounded per scanned sentence; no material hot-path regression.
- Over-engineering: no speculative abstraction or dead flexibility; the two patterns encode distinct evidence levels.

Verification: `git diff --check` passed. `uv run pytest tests -q` could not start because the sandbox prohibits uv cache/temp-file creation; direct module probes succeeded.

net: Lean already.  
Verdict: **Approve**
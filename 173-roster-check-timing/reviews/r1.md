# Review: r1 — 81e39e7 `fix(skills): let the roster say *not yet*` (#173)

## Findings

WARNING  wfctl/_body.py:L392-397 — the finding text tells the reader that
`RUNNING` is "a failure" and "the silent reviewer this check exists to expose",
and attributes both to Step 3. The same commit's SKILL.md:L154 says the
opposite: *"Three words, three states, and only one of them is a failure"*, and
L158 classes `RUNNING` as *"not a result"*, not a failure. A `RUNNING`-only
roster renders:

> …the roster records ['RUNNING']: 'r1 ✓  r2 RUNNING'. fanning-out-code-review
> Step 3 calls that a failure rather than a pass — wait for it to come back,
> then read the roster again. A panel reported as incomplete is the silent
> reviewer this check exists to expose…

"the silent reviewer" is precisely the conflation #173 exists to end, restated
by the check that #173 added. → make the framing conditional on the marker, or
reword the shared sentence to the thing that *is* true of both ("neither word
belongs in a finished description; a panel recorded as complete while a reviewer
is out is the same untrue sentence"). Cite what the skill says, since the check's
authority is that it quotes the rule.

WARNING  wfctl/_body.py:L395 — the remedies are joined with `; ` and lose their
binding to the marker they answer. With both markers present the message reads
`— ask that reviewer again for its findings; wait for it to come back, then read
the roster again.` Two pronoun-led clauses, no antecedent, in sorted-key order
the reader has to reconstruct — and read as one sequence for one reviewer it is
the exact wrong instruction for a `RUNNING` one, which is the failure the commit
message says this dict prevents. Verified by running `panel_findings` on a
roster carrying both. → `'; '.join(f"{m}: {FAILURE_MARKERS[m]}" for m in
matched)`, so each remedy arrives labelled.

WARNING  tests/test_body.py:L432-454 — `test_each_failure_marker_carries_its_own
_remedy` asserts only the single-marker case (`RUNNING` present, `MISSING`
absent). The property its name states is the both-markers case, and that is the
one that degrades (above). → add a roster carrying both and assert both remedies
appear *and* that each is adjacent to its own marker.

WARNING  wfctl/_body.py:L91-93 — the comment still claims this is a vocabulary
the module "does not invent … quoted from the skill and pinned to it by test."
That is now true only of the keys. `"wait for it to come back, then read the
roster again"` appears nowhere in `fanning-out-code-review` (grepped; the
nearest is L158's "Read the roster again."), and
`test_the_named_states_are_the_skills_own_words` iterates the mapping, so it
pins keys and never looks at values. The comment promises a guarantee the tests
do not provide, which is the drift that comment was written to prevent. → either
extend that test to assert a distinctive phrase of each remedy appears in the
skill, or narrow the comment to "the markers are quoted; the remedies are this
module's."

NIT  wfctl/agents/skills/fanning-out-code-review/SKILL.md:L154 vs L173 —
"failure" carries two senses nineteen lines apart: "only one of them is a
failure" (a reviewer failed) and "Neither failure word may reach the
description" (the description fails). A reader arriving at L173 has just been
told `RUNNING` is not a failure word. → name the second sense: "Neither word may
reach the description Step 6 writes."

NIT  wfctl/agents/skills/fanning-out-code-review/SKILL.md:L129 — `RETURNED` is
the only input in Step 3 with no artifact behind it. The step's premise is that
the agent's recollection is what to disbelieve; "the ones you watched finish" is
recollection. The commit message's defence holds (a harness completion signal is
not the reviewer's own claim, and the skill's opening story — a false "I already
relayed above" — still lands as `MISSING` under the new loop), and both mistakes
fail loudly: forgetting an id prints `RUNNING`, which `check-body` now rejects;
listing one early prints `MISSING`, which costs a re-ask. Worth one sentence
saying so, so the next reader does not have to re-derive that it fails safe.

NIT  wfctl/_body.py:L391 — the marker test is a case-sensitive substring over
the whole roster field, and the body template invites narrating history into
that field (`[r1 ✓  r2 ✓  r3 ✓ — and which, if any, were re-asked]`). "r2 was
RUNNING at first read, ✓ on the second" is a truthful, complete roster this
check rejects. Pre-existing for `MISSING`; the change doubles the surface. → the
finding could say how to record a re-ask without the marker word, since it is
the only guidance the author gets.

NIT  wfctl/agents/skills/fanning-out-code-review/SKILL.md:L162-167 — `shrink:`
the paragraph restates the state table's own `RUNNING` row plus the three-
reports-in-three-minutes story that also lives in the commit message and in two
test docstrings (`_fixture`, `test_the_roster_check_tells_the_three_states_
apart`). Four copies. → keep the "reading early is not a mistake" sentence, drop
the retelling. Flagged with low confidence: prose density is deliberate in this
repo's skills, and this one is read by a model, not skimmed.

## Passes run

- **Correctness.** Traced the shell fence by hand and against the tests. The
  `case " $RETURNED " in *" $id "*)` form is right and better than it looks:
  quoting `" $id "` inside the pattern strips glob meaning, and the space
  delimiters make `r1` not match `r10`. Empty `RETURNED` yields all-`RUNNING`,
  which is the correct reading of "nothing back yet". An id in `RETURNED` but
  not in the `for` list is ignored; the `for` list stays authoritative. The
  zsh-word-splitting note at L145-148 is accurate (zsh does not split unquoted
  parameter expansions) and documents a fence the construct deliberately avoids
  — Chesterton's, kept on purpose. In `_body.py`, the `matched :=` walrus is
  correct and removes a duplicated comprehension; `sorted()` yields the same
  list repr the message printed before.
- **Tests.** Confirmed `test_a_roster_that_records_a_running_reviewer_is_not_a
  _pass` actually isolates the marker: the identical body with `r3 ✓` returns
  `[]`, so the truthy assertion is not passing on some unrelated finding. The
  merge of the two fanout tests into one three-state fixture is the right call —
  the states are now distinct outputs of one loop, so one fixture is the smaller
  check. `test_the_roster_check_survives_zsh` genuinely ran here (`/bin/zsh`
  present, PASSED — not the silent `return`-skip).
- **Architecture.** `FAILURE_MARKERS` changing from `frozenset[str]` to
  `dict[str, str]` is safe: grepped every reference, and all three surviving
  consumers (`m in FAILURE_MARKERS`-style iteration in `_body.py` and
  `test_body.py:L394`) iterate keys. No set algebra anywhere.
- **Security.** Nothing here reads untrusted input or executes it; the fence
  interpolates only ids the author typed.
- **Performance.** Not applicable — a two-key dict and a shell loop over a
  roster.
- **Over-engineering.** The code is lean. The only slack is prose (NIT above).
- **Verification.** `uv run pytest -q` → 1082 passed. `uv run ruff check wfctl/
  tests/` → All checks passed. `uv run mypy wfctl/` → Success, 17 files. Also
  diffed `.agents/skills/fanning-out-code-review/SKILL.md` against
  `wfctl/agents/…` — identical, so the installed tree carries this change.
  I did not run `wfctl install-skills`, `wfctl doctor` or `wfctl verify`
  (shared worktree, report-only).

net: −6 lines possible (SKILL.md prose only; the code is already lean, and the
two message fixes add ~1 line)

Verdict: **Approve** — the change definitely improves code health: the three-way
loop is correct, the fixture merge is the right shape, and `RUNNING` as a body
marker closes the hole the new word would otherwise open. The two `_body.py`
message findings should land first, though: this check's authority rests on
quoting the skill accurately, and today it tells the author `RUNNING` is a
"silent reviewer" that the same commit's skill says is working fine.

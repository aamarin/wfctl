# Review: origin/main..HEAD (0430698 — docs(skills): make the handoff tell its reader to run /start-session (#206))

Reviewer: r1
Target: `wfctl/agents/skills/worktree-handoff/SKILL.md`, one commit, +13/-8.

## Context gathered

- Read the full changed file (not just hunks), the issue (`gh issue view 206`),
  the commit message, `CLAUDE.md` (project AGENTS.md), `.agents/skills/start-session/SKILL.md`,
  `.agents/skills/end-session/SKILL.md`.
- Checked for tests covering this file: `tests/test_worktree_handoff.py` and the
  `worktree-handoff` mention in `tests/test_install_skills.py`. Neither asserts on
  this prose — they test `state-dir --branch` resolution and that shell commands
  named in the skill actually run. Consistent with `CLAUDE.md`'s own statement
  that the suite "checks that skills ship and cross-reference correctly, not that
  they read well" — no missing regression test here, this is a doc-only change.
- Checked for precedent on citing wfctl's own issue numbers inside a skill file
  that ships into other repos: `conversation-response-shape/SKILL.md` (#99),
  `speckit-orchestrate/SKILL.md` (#117, #200, #201) already do this. The new
  `(#206)` citation in this diff matches existing house style, not a new pattern.
- Confirmed no CHANGELOG exists in this repo to update.

## Passes run

**1. Correctness.** The stated bug: `worktree-handoff` reasoned about
`/start-session` four times but never instructed the handoff to tell its reader
to run it, so four worktrees skipped session init. The diff adds `Run
/start-session before you touch anything` as the literal opening of item 2's
handoff shape, with the reason inline, and updates Step 3's later paragraph from
"the first action in section 2" to "the first action after `/start-session`" —
correctly following the commit's own logic (section 2 now opens with a step
that's already done by the time `start-session` step 9 asks "what are we working
on today", so leaving the old wording would have offered `/start-session` back
as its own default answer). Verified this reasoning holds by re-reading
`start-session` step 9 and step 4. No defect found.

**2. Security.** N/A — prose-only change, no code path, no input handling.

**3. Architecture.** Fits the file's existing pattern of justifying each shape
requirement with a cited past failure (Step 3's "implement issue #N" anecdote,
the `.workmux/` vs. state-dir split, etc.) and of citing wfctl's own issue
numbers in shipped skill prose (see precedent check above). No new pattern
introduced.

**4. Readability.** Dense but consistent with the file's existing voice and
3-space list-continuation indentation. Read cleanly on its own.

**5. Performance.** N/A.

**6. Over-engineering.** None — net change is +5 lines, entirely load-bearing
for the stated bug. Lean already.

## Findings

WARNING `wfctl/agents/skills/worktree-handoff/SKILL.md:169-184` (Red flags
section) — the list of red flags enumerates a failure mode for nearly every
numbered requirement in Step 2 (route not stated → §3, worktree before prompt →
Step 3, decision without argument → §6, boundary without reason → §4, restating
the issue → §1, writing only to `.workmux/` → Step 3) but has none for the new
§2 requirement. The exact failure this diff exists to fix — "an agent already
holding a full handoff concludes a session-start step is redundant" — is
precisely red-flag material, and a future editor skimming just that list (a
plausible way to use it, since it's the file's own checklist section) won't be
reminded to check it. → Add one bullet, e.g. "Section 2 that never tells the
reader to run `/start-session` first — the inference that an already-complete
handoff makes it redundant is exactly what produced #206."

NIT (informational, not the diff's to fix) — the issue's own Definition of Done
says verification is "`current.json` existing in that branch's state dir — not
by the handoff containing the sentence," but the commit message documents that
`current.json` was removed as a session-start signal by #114 and is deleted on
sight by `_migrate_session_files`, so that literal DoD is no longer
satisfiable. The commit correctly substitutes `session_started()` /
`events.jsonl` as the live signal in its own reasoning, but issue #206 itself
still states the old, now-impossible verification method. Worth a one-line
comment or edit to #206 so a future reader doesn't try to verify against a file
that's deleted by design — not a defect in this diff.

net: −0 lines possible. Lean already.

**Verdict: Approve** (one WARNING — non-blocking, a real but small coverage gap
in the file's own checklist; the substantive fix is correct and matches the
issue's reasoning).

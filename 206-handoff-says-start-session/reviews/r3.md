# Review: origin/main..HEAD (0430698 — docs(skills): make the handoff tell its reader to run /start-session (#206))

Reviewer: r3
Target: `wfctl/agents/skills/worktree-handoff/SKILL.md`, one commit, one file
(+13/-8, confirmed via `git diff --stat origin/main..HEAD`).

## Context gathered

- Read the full changed file end to end (not only the hunks), `CLAUDE.md`
  (project AGENTS.md), `gh issue view 206`, the commit body (`git log -1
  --format=%B 0430698`), and both related skills named in the brief —
  `.agents/skills/start-session/SKILL.md` and `.agents/skills/end-session/SKILL.md`.
- Checked for tests coupled to this file's prose: `grep -rln
  "worktree.handoff|worktree_handoff" tests/` → `tests/test_worktree_handoff.py`
  (asserts on `wfctl state-dir --branch` behavior and that the skill's own shell
  invocations of it still run — untouched by this diff) and
  `tests/test_install_skills.py` (packaging, not content). Neither asserts on
  the two edited paragraphs. Ran `uv run pytest -q tests/test_worktree_handoff.py
  tests/test_skill_cross_references.py tests/test_skill_frontmatter.py` — 18
  passed, no regression.
- Checked whether anything else in the repo names "section 2" or references this
  skill's structure in a way the edit could silently break:
  `grep -rln worktree-handoff **/*.md` → only
  `wfctl/agents/commands/worktree-handoff.md` (a thin dispatcher that just reads
  the SKILL.md by path, unaffected) and `docs/architecture/mirror-supersedes-the-wrapper.md`
  (unrelated mention). No coupling found.
- Read `docs/architecture/` posture via `CLAUDE.md`'s pointer — this is a
  doc-only skill-prose change that adds no new state, matches `design-levels`'
  exclusion for changes drawing no new boundary, so no spec/plan artifacts are
  expected and none are missing.

## Passes run

**1. Correctness.** The diff does what issue #206 asks: item 2 of the handoff
shape now opens with `Run /start-session before you touch anything` plus the
reason inline ("say why on the same line" — matches the issue's explicit ask),
and Step 3's later paragraph is updated from "the first action in section 2" to
"the first action after `/start-session`". That edit is necessary, not
cosmetic: section 2 now contains two moves (run `/start-session`, then start
with the issue command), and `start-session` step 9 asks "what are we working
on today?" *during* `/start-session`'s own run — by which point running
`/start-session` is already done, so leaving the old wording would have handed
back `/start-session` itself as the default answer to that question. Verified
this against `start-session`'s step 9 and step 4 directly. No correctness
defect in the substantive change.

**2. Security.** N/A — prose-only edit to a markdown skill file, no code path,
no secrets, no external input.

**3. Architecture.** Matches the file's own stated pattern of citing a concrete
past failure for every requirement (Step 3's "implemented, committed and pushed"
anecdote; the `.workmux/` vs. state-dir split) and its explicit decision (echoed
in this branch's own `.workmux/PROMPT-*.md`) to extend section 2 rather than add
a new section — consistent with Step 2's own stated reason for omitting empty
sections. No boundary crossed, no new coupling.

**4. Readability & simplification.** Two friction points, both minor:

- `SKILL.md:L57` — "four concluded exactly that in one afternoon (#206)" has no
  in-file antecedent for "four." Every other quantified claim in this file names
  its noun at the point of use (`L14`: "Three of nine worktrees in this repo have
  a prompt"; `L104`: "the three prompts that worked"). This is also the file's
  only issue-number citation — a reader who hasn't opened #206 gets a bare count
  with nothing to attach it to, which cuts against the same sentence's own
  instruction to the handoff-writer ("say why... so a reader... will negotiate
  with all of them" is literally this file's stated failure mode for
  under-specified reasoning, at L62-63).
- `SKILL.md:L153` — the unedited "its answer is section 2, the whole first move"
  now sits directly before the edited "Say the first action after
  `/start-session` plainly enough to be that default." Section 2 is no longer
  a single move once it opens with the session-init instruction, so "the whole
  first move" and "the first action after `/start-session`" describe different
  scopes back to back. Reconcilable on a careful read, but this is precisely
  the kind of ambiguity the file elsewhere works to eliminate.
- `SKILL.md:L169-184` (Red flags) — every other numbered requirement in Step 2
  has a matching red flag (route unstated → §3; worktree before prompt → Step 3;
  decision without argument → §6; boundary without reason → §4; restating the
  issue → §1). The new §2 requirement has none, though the exact failure this
  diff fixes — an agent inferring a session-start step is redundant because it
  already holds a full handoff — is exactly the shape of evidence this section
  otherwise collects.

**5. Performance.** N/A.

**6. Over-engineering.** None. Net +5 lines, both hunks load-bearing for the
stated bug; no new section, no TODO list, no automation (the issue explicitly
rules out "making anything run it automatically" and the diff adds none).
`net: −0 lines possible. Lean already.`

## Findings

WARNING `wfctl/agents/skills/worktree-handoff/SKILL.md:L57` — "four concluded
exactly that in one afternoon (#206)" has no antecedent noun in-file, unlike
every other quantified claim in this document → say "four worktrees" or "four
sessions" explicitly.

NIT `wfctl/agents/skills/worktree-handoff/SKILL.md:L153` — "its answer is
section 2, the whole first move" was left as-is while the sentence right after
it was correctly narrowed to "the first action after `/start-session`",
producing a scope mismatch across two adjacent sentences → reword the first
clause too, e.g. "its answer lives in section 2, after `/start-session`", so
both agree.

NIT `wfctl/agents/skills/worktree-handoff/SKILL.md:L169-184` (Red flags) — no
entry for the failure this diff fixes, breaking the section's otherwise-total
one-red-flag-per-requirement coverage → add a bullet, e.g. "Section 2 that never
tells the reader to run `/start-session` first — the assumption that an
already-complete handoff makes a session-start step redundant is exactly what
produced #206."

Informational, not a defect in this diff: issue #206's own Definition of Done
says verification is "`current.json` existing in that branch's state dir," but
the commit body notes `current.json` was retired as the session-start signal by
#114 and is now deleted on sight — the commit correctly substitutes
`session_started()` / `events.jsonl` in its own reasoning, but #206 itself still
states a verification method that's no longer satisfiable. Worth a follow-up
comment on the issue, not a change to this file.

net: −0 lines possible. Lean already.

**Verdict: Approve** — no blockers. The substantive fix is correct, matches the
issue's proposed shape and DoD, and is grounded in a re-verified (not merely
inherited) claim about the observed failure. The three findings above are
wording/consistency polish an author can take or leave.

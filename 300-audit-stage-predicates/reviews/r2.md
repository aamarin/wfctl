# Review (r2): uncommitted working-tree diff — `wfctl/_pipeline.py` L78–115

Target: `git diff` at `300-audit-stage-predicates`, one hunk, 39 added lines, all
comment. Reviewed against issue #300, epic #100 scope 5, the handoff at
`.workmux/PROMPT-300-audit-stage-predicates.md`, `CLAUDE.md`, and the accepted
records in `docs/architecture/`.

## Findings

BLOCKER  wfctl/_pipeline.py:L87 — the `brainstorm` row says "some file under the
arch root was touched on this branch". The predicate excludes one subtree:
`design_block` (L650) calls `touched_on_this_branch(repo_root, arch, exclude=arch
/ "design")`. A level-3 record under `<arch-root>/design/` does **not** satisfy
the step, and `design_block`'s own docstring (L623–627) says that exclusion
exists because "#121 item 3 lands one in every such branch diff" — so the case
the line misdescribes is the common one, not a corner. A comment cannot cause
incorrect behavior, which is why this would normally be a WARNING; it is a
BLOCKER here because the diff's entire deliverable is the accuracy of eight
claims, and #240 and #309 are meant to be argued from them. → "some file under
the arch root outside `design/` was touched on this branch".

WARNING  wfctl/_pipeline.py:L87–104 — five of the eight rows exceed the one-line
budget (brainstorm 3, specify 2, clarify 2, decompose 5, implement 3). #300 scope
1 says "One line each — this is an audit, not a redesign", and the handoff
restates it with the rule for the overflow: "A step whose predicate needs a
paragraph to describe has found a gap — file the gap, keep the line." `decompose`
at five lines is the clearest case, and its gap already has an issue. → cut each
row to its predicate; move L99–101 to #240, L88–89 to a `#283` clause, L103–104
to the `implement` gap already filed as #308.

WARNING  wfctl/_pipeline.py:L78–81 — the ladder is restated with its left column
dropped (`file exists`, `sections present`, `no markers left`, `external objects
exist`, `verification passed`, `record accepted`, `approval recorded`) and no row
names the rung it sits on. Scope 1 asks for the audit "in the ladder's
vocabulary"; as written the ladder is quoted and then not used, and a reader has
to re-derive "`plan.md` is non-empty" → rung 1 for themselves. #309's own body
does this correctly — it carries a `| Step | Predicate | Rung |` table. → keep
both columns, and end each row with its rung name.

WARNING  wfctl/_pipeline.py:L88–89, L101, L103–104 — rationale duplicated from
the functions that own it, ~250–470 lines below, where it can drift out of
agreement with them:
- L88–89 "never checked to be about this change" ≡ `design_block` docstring
  L629–632 (which also cites FR-010a, dropped here).
- L101 "it stops blocking once the tasks close" ≡ the inline comment at L551–559.
- L103–104 "Where the repo declares no definition of done, the tasks reading
  closed is the whole predicate (FR-002)" ≡ `verification_block` docstring
  L311–313, same requirement ID.
The aggregate view is the deliverable and is worth having; the second copy of
each argument is not. → state the predicate, point at the owning function for the
why (`see design_block`, `see verification_block`).

WARNING  wfctl/_pipeline.py:L111–115 — the gap paragraph accounts for four of the
five `_AUTOMATIC` steps. `brainstorm` also carries `_AUTOMATIC`, its own row puts
it at "written, not accepted", and nothing says whether the audit considered it.
It was flipped by #283 on an argument about reversibility, not about its rung —
which is exactly the kind of thing a reader auditing the audit needs and cannot
recover. → one clause: "`brainstorm` sits at the same rung; its flag was settled
on reversibility rather than evidence (#283)."

WARNING  wfctl/_pipeline.py:L92–93, L97 — two `skipped` bypasses are omitted
while the same class of bypass is stated for `brainstorm` ("Not read at all once
`spec.md` exists") and `decompose` ("stops blocking once the tasks close"), so
the rows read as exhaustive:
- `clarify` — with `plan.md` present and no markers, the arm at L509–514 returns
  `skipped`, and the scan never ran.
- `decompose` — with no `delivery.md` at all and the tasks closed, L561–562
  returns `skipped`; the step is passed with zero evidence. This one is material
  to #240, which wants the flag flipped.
→ one clause per row, in the form the other two rows already use.

WARNING  wfctl/_pipeline.py:L87–104 — nothing routes a predicate's editor back to
this comment. The audited behaviors *are* pinned by tests — `test_storyctl.py`
has per-step state tests, including `test_tasks_done_when_no_checkboxes` (L227),
which is #308's exact reproduction — so strengthening a predicate does fail
something. But no test names #300 or this comment, so the author updates the test
and leaves L87–104 stale, and the file has no `REVIEW`-style check to catch it.
`a-rule-is-expressed-as-a-check` does not bind here (this is a description, not a
rule wfctl ships), so a new test is not the fix. → one line naming where the rows
are pinned (`pinned by tests/test_storyctl.py::TestInferPipeline`), or `#300` in
the docstrings of the tests that pin them.

WARNING  wfctl/_pipeline.py:L111–112 — "an audit that moved the thing it was
measuring could not be checked against it" argues this change's scope decision to
a reviewer. `CLAUDE.md`: "Comments inform the next reader of the file; they do
not narrate the change to a reviewer. If a comment only makes sense beside the
diff, it belongs in the commit message." The half that survives for the next
reader is "these gaps are filed: #308, #309, #240." → cut the clause, keep the
issue pointers, move the argument to the commit message.

NIT  wfctl/_pipeline.py:L83–85 — the placement argument re-derives a decision an
accepted record already makes. `knowledge-placement.md` says "a fact about one
file → that file", and `architecture-decisions` excludes "anything a code comment
already carries". Placement itself is correct — I checked it against both, plus
`design-levels` L249–257 ("Level 2 → one record per ownership decision"), and
this audit makes no ownership decision. → replace the argument with the pointer
(`knowledge-placement`) and keep the last sentence, which is the durable half.

NIT  wfctl/_pipeline.py:L79–80 — rung 3 is quoted as "known questions were
addressed"; #100 writes it "known questions were addressed, **syntactically**".
The dropped adverb is the whole distinction the `specify` and `clarify` rows then
have to make in prose ("its sections are never looked at", "Nothing under the
heading is read").

NIT  wfctl/_pipeline.py:L81 — "integration was approved" for #100's "approval
recorded → integration may proceed". A permission became a fact.

NIT  wfctl/_pipeline.py:L98–99 — "the plan carries an Issue Grouping Map" and
"read from the plan's own prose" mean `delivery.md`, four lines after a row where
"plan" means `plan.md`. → say `delivery.md` both times.

## What I verified and found clean

- **Correctness of the other seven rows** against `_infer_steps` (L403–602),
  `_implement_verdict` (L361–384), `verification_block` (L303–358), `_tasks_open`
  (L207–229), `_unkeyed_issues` (L245–300) and `design_block` (L604–654):
  `specify`, `clarify`, `plan`, `tasks`, `analyze`, `decompose`'s key scan, and
  `implement` all read as stated. `tasks` "not that it holds a single task" is
  right and is what makes #308 real.
- **"Two of the eight consult `blocks`"** — exactly two call sites, L340
  (`repo-declared`) and L654 (`ambient`). Correct.
- **"no inconclusive state" for the other six** — checked the one candidate:
  `decompose` reads `_tracker.configured_key_pattern`, and `load_manifest`
  (`_manifest.py:22`) raises rather than degrading on a malformed manifest, so
  there is no third state failing open. Claim holds.
- **Issue references all resolve.** #300 scope 1/2/3 as described; #100 scope 5
  carries the seven-rung ladder verbatim; #308 is `tasks`/`implement` cleared by
  a checkbox-free file; #309 is `specify`/`plan` automatic on file existence;
  #240 is decompose's unflipped flag. FR-002 is
  `69-machine-checked-done/spec.md:133` — "treat an absent file … as no
  verification configured" — which is what L103–104 claims. Unqualified `FR-002`
  points at a gitignored, out-of-tree spec, but that matches the existing
  convention at L312 and in `tests/test_verify.py:33`, so not a finding.
- **Scope discipline.** `_STEPS` is untouched, no predicate changed, no flag
  flipped — the handoff's "do not do #240's work here" is honored.
- **Security / performance:** nothing to review; zero executable lines.
- **Style:** longest new line 84 chars, in range for the file; ruff selects
  `E4,E7,E9,F` so no lint surface here either way.

net: −12 lines possible (rows to one line each, narration clause cut, placement
argument to a pointer, three duplicated rationales to pointers)

Verdict: Request changes (1 blocker — L87 misstates what the `brainstorm`
predicate reads)

## Review: main...HEAD on 111-response-shape-digest (commit c524e77)

**Passes run:**
1. **Correctness** — read `SKILL.md` in full (483 lines) and `digest.md` line by
   line, mapped every digest line back to its source sentence in `SKILL.md`,
   read `_digest_text`/`_hook_user_prompt`/`_DIGEST_MAX_CHARS` in `wfctl/cli.py`
   (unchanged in this diff, read for context), computed the flattened digest
   length by hand, verified `_LINE_CEILING`'s arithmetic against `git diff`
   and `git show main:...SKILL.md | wc -l`, and read both test files' new/changed
   assertions.
2. **Security** — no new input surface; `digest.md` is static shipped content,
   read through the existing symlink/whitespace/truncation guards from #85
   which this diff does not touch.
3. **Architecture** — checked the change fits the digest-per-skill pattern #85
   established (one `digest.md` sibling to `SKILL.md`) rather than inventing a
   new one.
4. **Readability** — checked test docstrings against `CLAUDE.md`'s "test names
   are sentences, docstrings say why" convention and the file's own
   dense-rationale comment style.
5. **Performance** — n/a, a 10-line static file and two test assertions.
6. **Over-engineering** — checked for anything to cut; ran the full verification
   suite.

Commands run: `uv run pytest -q` (823 passed), `uv run pytest -q
tests/test_response_shape_invariants.py tests/test_skill_cross_references.py`
(14 passed), `uv run ruff check wfctl/ tests/` (clean), `uv run mypy wfctl/`
(clean, 15 files). Nothing edited.

### Findings

**WARNING** `tests/test_response_shape_invariants.py:L46` — the new comment
claims this ceiling move is "neither adding a rule nor widening the budget,"
but `_LINE_CEILING` is the budget under discussion in the very sentence above
it, and it went from 480 to 483. `git show main:wfctl/agents/skills/
conversation-response-shape/SKILL.md | wc -l` → 478; the diff adds exactly 5
lines (4 prose + 1 blank separator) to `SKILL.md`; the file is now 483 lines,
and the ceiling was set to exactly 483 — zero headroom, and a number that was
in fact widened to match. The underlying argument is real and worth making:
the pointer is decay-mitigating infrastructure, not a new numbered rule with
its own example, so it isn't "adding a rule" in the sense the paragraph above
guards against. But "widening the budget" reads as a plain, checkable claim
about `_LINE_CEILING` and it's false on its face — the phrase is almost
certainly reused from the commit-message paragraph about the *other* rejected
candidate (raising `_DIGEST_MAX_CHARS`, a different constant), and it doesn't
fit pasted next to this constant. → Reword to state the actual distinction
(content that doesn't need the "rule + example" shape vs. content that does),
rather than asserting the budget wasn't touched when the diff shows it was.
This is exactly the kind of comment `CLAUDE.md` asks for ("dense with
rationale... explain why this shape") — its value depends on being literally
true, and a future contributor could cite "this didn't widen the budget" as
false precedent for the next bump.

**NIT** `wfctl/agents/skills/conversation-response-shape/digest.md:L3` —
"Plain words before identifiers" narrows SKILL.md rule 2 ("Frame in plain
language before mechanics") to one of its four named categories (identifiers,
file paths, requirement numbers, internal vocabulary — SKILL.md L119-121).
`mechanics` is the rule's own word, no longer than `identifiers`, and would
cover all four without narrowing the claim. Not a contradiction — a
projection is allowed to compress — but this compression drops information
for no length savings.

**NIT** `wfctl/agents/skills/conversation-response-shape/digest.md:L9` —
`"three things"` doesn't appear verbatim anywhere in `SKILL.md`; the source
examples for a counted lead-in are "two things worth naming", "three
decisions", "two questions left" (SKILL.md L247, L278-279). The digest's
composite phrase captures the pattern (count + plural noun) faithfully, so
this isn't a contradiction of the invariant under review, but it's worth a
second look given the commit message frames the digest as a strict
projection — a quoted-looking example that isn't a quote is the kind of drift
that's easy to miss in a 500-char file nobody reads end-to-end again.

### Invariant check (digest is a strict projection of SKILL.md)

Walked all 7 lines of `digest.md` against their source:
- L2-7 (rules 1-6) map onto the Precedence list (SKILL.md L34-39) using
  wording pulled from each rule's own bolded sentence or its Pre-send-check
  question, not invented language. No rule appears in the digest that isn't
  argued for in `SKILL.md`, and I found no digest line that contradicts its
  source.
- The trailing no-headers line is rule-adjacent content, not one of the six
  precedence rules — it's failure mode #7 of 8 (SKILL.md L452-463), correctly
  the more-frequent of the two failure modes (#7, #8) not already covered by
  rules 1-6. That choice matches the commit message's stated reasoning and
  checks out against the file.
- Flattened length: 479 chars (`" ".join(text.split())`), under
  `_DIGEST_MAX_CHARS` (500) with 21 chars of headroom — matches the commit
  message's "It lands at 479" and confirmed via the new
  `test_no_shipped_digest_is_truncated_by_the_hook_that_reads_it`, which
  passes.

### Other checks

- `test_the_first_three_rules_keep_their_numbers` still passes — this diff
  doesn't touch the numbered `## N.` headings, only the Persistence prose
  above them, so the three cross-referenced rule numbers are unaffected.
- Only one `digest.md` exists in the tree (`agents/skills/*/glob` finds
  exactly this one), so the new truncation test isn't silently skipping other
  skills' digests — there simply aren't any yet.
- `wfctl doctor` / `install-skills` intentionally not run per task
  instructions (shared worktree); `pytest`/`ruff`/`mypy` cover this diff's
  surface (docs + tests, no `wfctl/cli.py` changes) without needing them.

net: 0 lines possible — digest.md is already at the format's minimum; nothing
here is dead code, speculative, or a hand-rolled stdlib equivalent.

**Verdict: Approve** (0 blockers, 1 warning). The warning is about the
honesty of one sentence in a comment, not the mechanism it documents — the
`_LINE_CEILING` move itself is defensible on the merits laid out in the rest
of that same comment, and every functional check (tests, ruff, mypy, the
digest/SKILL.md invariant, the char-cap arithmetic) is clean. Worth a
one-line fix to the comment before or shortly after merge, not a blocker to
it.

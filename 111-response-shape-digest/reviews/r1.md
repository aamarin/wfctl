## Review: 111-response-shape-digest (`git diff main...HEAD`)

Scope: 4 files, ~43 lines — `wfctl/agents/skills/conversation-response-shape/{SKILL.md,digest.md}`,
`tests/test_response_shape_invariants.py`, `tests/test_skill_cross_references.py`.
Commit: c524e77 "feat(skills): ship the digest the per-turn hook was built to read".

### Passes run

1. **Correctness** — read `_digest_text`/`_hook_user_prompt` (`wfctl/cli.py:2360-2497`), traced
   the truncation/symlink/flatten guards, computed the digest's actual flattened length,
   confirmed packaging (`importlib.resources`) resolves the new file, ran the full suite +
   ruff + mypy.
2. **Security** — re-checked the symlink guard and newline-flattening guard in `_digest_text`
   still apply to the new content (they're unchanged code, just exercised by real data now).
3. **Architecture** — checked whether the digest is a strict projection of `SKILL.md` (the
   invariant the task called out): read both files in full and compared every rule.
4. **Readability** — read the amended `_LINE_CEILING` comment and the commit message's
   arithmetic for internal consistency.
5. **Performance** — n/a, static text file.
6. **Over-engineering** — checked the new `test_no_shipped_digest_is_truncated_by_the_hook_that_reads_it`
   against existing coverage in `tests/test_hook_user_prompt.py` for duplication.

### Findings

**WARNING** `tests/test_response_shape_invariants.py:L41,44` (and the commit body, same claim) —
the comment's arithmetic doesn't reconcile. It says "The four lines are the Persistence
section's pointer at `digest.md`" and "Paying four lines once to stop measuring the wrong
surface." The actual diff to `SKILL.md` adds **five** lines — four prose lines plus the blank
separator before `## Precedence` — confirmed both by the diff hunk (`+5` lines) and by
`wc -l`: `478` (main) → `483` (HEAD). Neither reading of "four" reconciles with the numbers
next to it: `478 + 4 = 482 ≠ 483`, and the ceiling itself only moved by 3 (`480 → 483`), using
2 lines of pre-existing headroom (`478` was already 2 under the old `480` ceiling) plus a
3-line raise to absorb the other 3 of the 5 added lines. So "four" is wrong under every
possible reading, and this is the one paragraph in the diff whose entire job is to convince a
future reader that this ceiling move was a narrow, accounted-for exception rather than a
budget give-up — the same standard the task asked me to check ("is the amended comment
honest?"). It isn't dishonest in intent, but it's factually wrong, on a file whose own project
convention (`CLAUDE.md`: "Comments inform... explain *why this shape*") holds comments to
exactly this kind of precision. → Fix: say "five lines" (four prose + the blank line), or spell
out the 2-line headroom / 3-line raise explicitly so the numbers add up.

**Verified — projection invariant holds.** Compared `digest.md` line by line against
`SKILL.md`'s six `## Precedence` rules and the "Persistence" section that describes the
mechanism: all six numbered rules are present, in the file's own numbering, and each is a
faithful compression of its source section (rule 1 ↔ "State the answer and stop.
Justification is a follow-up, not a paragraph."; rule 3 ↔ "point at the words that opted in.
If you cannot quote them, you are on the short rows."; rule 5 is a verbatim restatement of the
`SKILL.md` rule 5 header; rule 6 ↔ "The answer, plus at most one supporting block of prose.
Then stop." plus the counted-lead-in tell). No rule in the digest contradicts `SKILL.md`, and
no rule appears only in the digest. The trailing no-headers line is sourced from `SKILL.md`'s
"Three surfaces that are not prose" section (failure mode #7), which the commit message
discloses was picked despite being 7th, not top, on the frequency-ordered failure-mode list —
a disclosed editorial call, not a hidden one, so I'm not flagging it.

**NIT** `digest.md`'s rule 6 example — `("three things")` — doesn't literally appear in
`SKILL.md`; the source examples are `"three decisions"` and `"two things worth naming"`. It's
the same failure pattern (a counted lead-in), just a fabricated instance rather than a quoted
one, so it doesn't violate the "no rule invented" invariant. Cosmetic only.

**Verified — char budget.** Real flattened length of `digest.md` via the actual `_digest_text`
flattening rule (`" ".join(text.split())`) is 479 chars, under `_DIGEST_MAX_CHARS = 500` by 21
— matches the commit message's "It lands at 479." The new regression test
(`tests/test_skill_cross_references.py::test_no_shipped_digest_is_truncated_by_the_hook_that_reads_it`)
asserts this via the real reader function rather than a hardcoded length, so a future edit that
pushes it over gets caught here instead of silently truncating in a live session — this is not
duplicate coverage of `test_hook_user_prompt.py::test_an_oversized_digest_is_truncated`, which
only exercises the truncation *mechanism* against a synthetic fixture, not the shipped content.

**Verified — packaging.** `digest.md` is git-tracked (not `.gitignore`d) and resolves through
`importlib.resources.files("wfctl")`, so it ships without any `pyproject.toml`/`MANIFEST.in`
change — confirmed by loading it through the same path the hook and the new test use.

**Verified — build.** Full suite (823 tests), `ruff check wfctl/ tests/`, and `mypy wfctl/` all
pass clean on this branch as-is.

### Over-engineering

Lean already. The change is exactly its stated scope: one payload file, a four-line pointer in
the file it projects, one ceiling adjustment, one regression test for the exact failure mode
the commit describes hitting during drafting (silent truncation at 744 chars). Nothing here
invites a `delete`/`stdlib`/`native`/`yagni`/`shrink` tag.

net: 0 lines removable.

### Verdict

**Approve**, with the one WARNING above fixed or explicitly waived — it's a comment-only
inaccuracy (nothing functional breaks; the test still correctly asserts `lines <= 483` and the
file is exactly 483), but it sits in the paragraph whose entire purpose is arithmetic
credibility for a rule ("move it only by cutting a rule") that future changes will be judged
against, so getting the numbers right here is worth a one-line fix before merge.

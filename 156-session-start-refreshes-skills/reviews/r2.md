# Review: 156-session-start-refreshes-skills (three-dot vs 38-prune-orphaned-install-targets)

Target: `/Users/andremarin/Development/wfctl-specs/156-session-start-refreshes-skills/reviews/target.diff`
(3 files, +63/−13: `.workmux.yaml`, `AGENTS.md`, `wfctl/agents/skills/start-session/SKILL.md`)

Context read before judging: `AGENTS.md` (working tree, both halves of the diff),
`uv run wfctl arch context` (7 accepted records — `layer-model` and
`install-modes` are the ones this diff leans on), `wfctl/cli.py`
(`install_skills_cmd`, `doctor_cmd`, `_check_abandoned_entries`,
`_check_managed_hooks`, `_scan_owns`, `_interactive`), `wfctl/_workmux.py`,
`wfctl/agents/commands/start-session.md`, `wfctl/agents/configs/workmux/.workmux.yaml`,
the three tests that touch `start-session`, and the commit messages for
`0b2c4ce`, `c8e18d5`, `d549ef4`.

---

BLOCKER  wfctl/agents/commands/start-session.md:L4 — the command wrapper does not
permit the step the skill now requires. `allowed-tools` is
`Read Bash(wfctl start*) Bash(wfctl status*) Bash(wfctl arch context*)
Bash(wfctl state-dir*) Bash(wfctl doctor*) Bash(wfctl issue list*)
Bash(wfctl change list*) Bash(git …)`. Step 2 now mandates
`wfctl install-skills --prune --yes`, which matches none of those entries, so the
one step in `/start-session` that mutates the repo is the one step the wrapper
never pre-approved: it stops for a permission prompt in default mode, and is
simply unavailable wherever `allowed-tools` is the effective surface. Worse for
the diff's own thesis — every entry is spelled bare `wfctl …`, so in *this* repo,
the single repo the new "same wfctl" rule exists for, neither `uv run wfctl doctor`
nor `uv run wfctl install-skills` is covered either. The diff does not touch this
file. → add `Bash(wfctl install-skills*)` and `Bash(uv run wfctl*)` to L4. While
there, `tests/test_remaining_commands.py:L130` still documents the old contract —
`"""The path /start-session takes: report, never prompt, never write."""` — and
that sentence is now false; move it with the change rather than leaving the
suite asserting an intent the skill abandoned.

WARNING  wfctl/agents/skills/start-session/SKILL.md:L33-36 — the command block
precedes the rule that decides how to spell it. Agents execute a numbered skill
top-down. L26-27 run bare `wfctl start` / `wfctl doctor`; L34 runs bare
`wfctl install-skills --prune --yes`; the "run both through the **same** wfctl"
paragraph arrives at L49, thirteen lines and three paragraphs later. In this repo
the literal reading is exactly the failure the change was written to close — bare
doctor reports drift permanently, the agent "corrects" it with the wheel, the next
`uv run` session flips it back. Nothing in the skill even tells the agent to read
AGENTS.md, which is where L55-56 sends it. → hoist the same-wfctl rule directly
above the block and make the block name the choice rather than a spelling, e.g.
`<the wfctl this repo installs with — AGENTS.md says which> install-skills --prune --yes`.

WARNING  wfctl/cli.py:L3498, :L3279 — `doctor`'s own remediation lines contradict
the new rule, and the agent has just been told to act on `doctor`'s output.
Verified live in this worktree:

    $ NO_COLOR=1 uv run wfctl doctor
    ✓ wfctl 0.15.0 — latest
    ⬆ base: bundled skills changed since install
        update: wfctl install-skills
    ⬆ claude: bundled skills changed since install
        update: wfctl install-skills
    exit=1

`update: wfctl install-skills` (L3498) and `fix: wfctl install-skills --agent
{layer}` (L3279) both print the bare form. A change whose whole subject is "these
two commands must go through the same wfctl" leaves the tool telling every reader
to use the other one, on the line immediately above where the skill says to act.
→ either make the hint source-aware, or have the skill say outright that doctor
prints the bare form and it is not the one to copy in a repo with a source tree.

WARNING  wfctl/agents/skills/start-session/SKILL.md:L38-42 — the `--yes`
justification does not describe what `--yes` suppresses. The only prompt it skips
is `cli.py:L1916`, `if foreign_overwrites and not yes: … typer.confirm("Proceed?",
abort=True)`, and `foreign_overwrites` is built (L1878-1897) from paths that
**exist on disk and are not in the manifest** — by construction the set that is
*not* "wfctl's own output — generated, gitignored, never hand-edited". `_scan_owns`'
docstring (L3157-3163) documents the live case in the other direction: "a project
may commit its own skills and commands beside the installed ones". So the sentence
that authorizes `--yes` is true of the paths `--yes` does not affect and false of
the paths it does, and the skill ships to every repo. It is recoverable (originals
are copied to `.wf-skills-backup/` and one `ℹ Backed up N pre-existing file(s)`
line is printed) and `--yes` is genuinely needed for a non-tty run — that prompt,
unlike the tracker and spec-root ones, is not gated on `_interactive()`. → keep
`--yes`, narrow the claim to *recorded* paths, and make step 8 name any file the
run reports backing up instead of counting it.

WARNING  wfctl/agents/skills/start-session/SKILL.md:L124-127 — step 8's residual
report shrank. It was "anything `wfctl doctor` flagged as behind (tool / skills)";
it is now "skills you refreshed … plus anything `wfctl doctor` still flags as
behind (the tool)". `doctor` runs five checks and the refresh clears exactly one
of them (the content hash). The findings it cannot clear now fall outside that
parenthetical and go unreported while `doctor` still exits 1: the scanned-only
half of `_check_abandoned_entries`, whose own advice is "Delete the rest by hand"
(L3383-3388); an unwired `pre_remove` (`_check_workmux_hook`); a malformed
`wfctl.json` (`_check_verify_config`); a managed hook edited out of a settings
file (`_check_managed_hooks`). → "plus anything `wfctl doctor` still flags — the
tool version, and any finding the refresh could not clear."

WARNING  wfctl/agents/skills/start-session/SKILL.md:L54-55 — "refreshing with the
bare one would overwrite the skill edits the session exists to make" is not what
happens. `install-skills` copies from `_bundle.BUNDLE_ROOT` into `.agents/`,
`.claude/` and `.specify/`; `wfctl/agents/` — the source you edited, and the only
tracked copy — is never a destination in the plan (L1858-1897). What a bare
install destroys is the *installed mirror*, so the session stops exercising its
own edits; nothing in git is at risk. The AGENTS.md half of this same diff states
it correctly ("it installs *its* copy of the skill you just edited — the command
succeeds, the tree looks installed, and your change is nowhere in it"), so the
skill is the outlier, and it is the copy that ships to other repos. → use the
AGENTS.md wording.

WARNING  (whole diff) — no test, in a suite that already pins this file for this
exact reason. `tests/test_skill_cross_references.py` carries
`test_start_session_loads_the_in_force_set` and
`test_the_output_style_skills_are_both_loaded_at_session_start`, both of which
exist because a dropped line in `start-session` fails *silently*. A reworded or
dropped step 2 fails the same way, and the failure mode is the one #156 is about:
nobody finds out. → assert that step 2 runs `install-skills --prune`, and that
`commands/start-session.md`'s `allowed-tools` covers it. The second assertion
fails today — that is BLOCKER 1.

NIT  .workmux.yaml:L78 — `--prune` here can never do anything. `.wf-skills-manifest.json`
is gitignored (`.gitignore:L16`) and `files: {}` copies nothing into a new
worktree, so `_load_manifest` returns empty, `prior_layer_items` is empty, and
`orphans` is empty on every `post_create` run. Harmless, but unearned in a file
whose every other token carries rationale — the rewritten comment block above it
explains `uv run` and the linking hazard at length and never mentions the flag it
added. → drop it, or say why it is there.

NIT  wfctl/agents/skills/start-session/SKILL.md:L52-53 — "This only comes up in a
repo that develops wfctl itself" is narrower than the truth, and the commit
message repeats it ("Consumers are unaffected … with no source tree there is only
one wfctl to invoke"). Any project that declares wfctl as a dev dependency has a
`uv run` wfctl and a `uv tool` wfctl, and they disagree exactly the same way. The
rule above the sentence is universal and right; the scoping sentence is what
invites such a consumer to skip it. → "This bites hardest in a repo that develops
wfctl itself, where …".

NIT  wfctl/agents/skills/start-session/SKILL.md:L126-127 — "Omit if all current
and nothing was refreshed — a silent refresh is how a mirror gets stale again
without anyone noticing it had been wrong." The em-dash clause justifies *always
reporting a refresh*, but it is hung off the clause that says when to omit, so it
reads as justifying the omission. → attach it to the reporting half.

NIT  wfctl/agents/skills/start-session/SKILL.md:L31 — the trigger phrase does not
match doctor's strings. Doctor emits "skills stale — installed by wfctl X, running
Y", "bundled skills changed since install", "⚠ installed before content hashing —
re-run install-skills" (exit 0, and re-installing *is* the fix), and "⚠ N installed
paths are no longer shipped" (whose fix is precisely `--prune`). "behind or
drifted" covers the first two; the two the new command also repairs are the two it
does not name. → "any skills finding", or name all four.

NIT  .workmux.yaml:L78 — `post_create` still omits `--yes` while the skill's new
block requires it. `post_create` is non-tty, so if a fresh worktree ever does have
a colliding file, `typer.confirm` aborts, `|| true` swallows it, and the worktree
comes up with no skills at all — the failure `_warn_missing_bootstrap` and #137
exist to catch after the fact. Pre-existing, but this diff rewrote that exact line
and reasoned about `--yes` two files over.

---

## Verified, not findings

- **`~1s warm` (.workmux.yaml:L74) is accurate, and understated.** Measured a
  *cold* `uv run` into a throwaway `UV_PROJECT_ENVIRONMENT`: venv creation +
  19 packages from cache + run = **0.84s total**. The swap from a bare `wfctl` to
  `uv run` does not meaningfully slow worktree creation, and worktrees already
  carry a `.venv/` (3 of the 4 under `wt/` have one) so it introduces no new
  removal hazard.
- **"linking is what spread one orphaned file across twelve worktrees in #38"**
  checks out against `d549ef4`'s message ("#38's own evidence table found twelve
  worktrees linking installed paths in from a main checkout").
- **`post_create_wired` still matches.** `wfctl/_workmux.py:L171` tests
  `"install-skills" in ln`, so `uv run wfctl install-skills --prune …` keeps
  passing; `doctor` will not start reporting this repo's own `post_create` as
  unwired. Confirmed by `tests/test_workmux.py`.
- **`--agent <name>` covers base too** (`cli.py:L1858-1862`: the plan is
  base-layer targets, then the agent's, then the runtime), so the skill's
  parenthetical at L34-35 is correct — one command with `--agent` levels both
  layers, and a bare run leaves an agent layer stale, which is why the
  parenthetical has to be there.
- **`--prune` semantics as described at L41-42 are correct**: it deletes only
  paths recorded under a layer this install rewrote and did not re-write
  (`cli.py:L2055-2090`), restoring any backed-up original, and it does refuse
  paths outside the repo (`d549ef4`).
- **The shipped consumer template** (`wfctl/agents/configs/workmux/.workmux.yaml:L58`)
  was correctly left on bare `wfctl` — a consumer has no source tree to install
  from.
- **The bare-vs-`uv run` disagreement claim is real**, though not for the reason
  the prose gives here: both resolve to `wfctl 0.15.0`, so the divergence is the
  *content hash*, and doctor renders it as "bundled skills changed since install"
  rather than "stale". The AGENTS.md paragraph's framing ("compares against the
  released wheel … reports drift permanently") holds.
- `uv run pytest -q tests/test_skill_cross_references.py tests/test_workmux.py
  tests/test_remaining_commands.py tests/test_skill_frontmatter.py` → **74 passed**.
  Nothing in the diff breaks the suite; nothing in the suite covers the diff.

## Passes run

1. **Correctness** — traced every claim in the new prose to the code that
   implements it: `install_skills_cmd`'s plan/foreign-overwrite/prune paths,
   `doctor_cmd`'s five checks and their exit-code contract, `_interactive`,
   `post_create_wired`, `.gitignore`. Findings 1, 3, 4, 5, 6 and both `.workmux.yaml`
   NITs come from this pass.
2. **Security / safety** — the one privilege the diff grants is `--yes` running
   unattended in every repo at every session start; assessed against
   `foreign_overwrites`, the backup path, and AGENTS.md's own "`--yes` … is for
   CI" rule. Finding 4. No secrets, no injection surface; `${WFCTL_AGENT:+…}` is
   correctly quoted in the post_create line.
3. **Architecture** — checked against `layer-model` (generated trees are never
   hand-edited: this is the premise finding 4 shows is load-bearing and only
   half-true at the boundary) and `install-modes`. The skill/wrapper split is
   where the design broke down — finding 1.
4. **Readability** — ordering of step 2 (finding 2), the step-8 sentence (NIT),
   the trigger phrase (NIT). Comment density matches the repo's convention
   elsewhere; the `--prune` omission is the exception.
5. **Performance** — measured the one performance claim in the diff. Verified.
6. **Over-engineering** — the diff is prose that earns its length; the single
   removable token is `--prune` in `post_create`.

net: −0 lines possible (one unearned flag, `.workmux.yaml:L78 --prune`)
Verdict: **Request changes** (1 blocker, 6 warnings)

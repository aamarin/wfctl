# Review: `git diff main...HEAD` (5635747) — r3

Target: branch `213-upstream-skill-attribution`, one commit, 10 files.
Rubric: `.agents/skills/code-review/SKILL.md`, all six passes.

## Findings

WARNING  tests/test_skill_attribution.py:61 — "The direction that failed for
eleven releases" is verifiably wrong: the six shipped inside the wheel in three
releases, v0.15.0–v0.17.0. Evidence: `git ls-tree v0.14.0 -- wfctl/agents/…`
is empty, `MANIFEST.in` does not exist at v0.14.0, and the vendoring commit
8cadcdb (#47) carried `version = "0.14.0"`, i.e. it landed *after* v0.14.0 was
cut; `git tag --contains 8cadcdb` → v0.15.0 v0.16.0 v0.17.0. The file has no
earlier existence anywhere in history (`git log --diff-filter=A --all` finds one
add). → say "from v0.15.0 to v0.17.0". Note: the module docstring is already
being corrected in the working tree (uncommitted, `git status` shows
`M tests/test_skill_attribution.py`); line 61 still carries the wrong number.

WARNING  tests/test_skill_attribution.py:61-63 and
docs/architecture/vendor-upstream-skills.md:102 — both claim the pair catches
#213's actual failure, and it does not. Line 61-63: "a derived skill arrives,
the table is not touched, and nothing notices". Record line 102: "A skill
arriving from upstream without its line fails the suite, which is the part the
`license:` rule could not do." In #213 six files arrived with **no line and no
row** — against that state both checks are green, because both are set
differences between the file's line and the record's row. I verified this
directly: with the tree and the record as committed, adding an unattributed
skill changes nothing; only a line-without-row (test 2) or a row-without-line
(test 1) fails. What the pair actually guarantees is that the line and the
table cannot disagree; detecting an *undeclared* import stays manual, exactly as
the issue says ("the gap is invisible by construction"). → reword line 61 to "a
derived skill arrives *carrying its line*, the table is not touched…", and
record line 102 to "A pull that drops a listed skill's line fails the suite" —
then state plainly that an undeclared arrival is still only caught by a human.
A docstring that names a failure the test would not have caught is the opposite
of what `CLAUDE.md`'s "docstrings say *why the test exists*" is for.

WARNING  docs/architecture/layer-model.md:38-42 — now contradicted by the record
it cites. It reads: "a mark inside the file would sit in a file the project does
not own and the next upstream pull would drop it. See `vendor-upstream-skills`."
The amended record does exactly that — puts a mark inside those files — and
rejects that argument by name: "Keeping the annotation out of the file used to
be the answer to *an upstream pull would drop it*. The check is the better
answer." A reader following the pointer lands on a rebuttal of the sentence that
sent them. → one sentence in `layer-model` distinguishing the two marks (a
mirroring switch has nothing checking it, an attribution line has
`test_skill_attribution.py`), or drop the `See` pointer.

WARNING  docs/architecture/vendor-upstream-skills.md:38-56 — the diff deletes
"See `knowledge-placement` for the rule this is the exception to" at the moment
the exception gets larger. Before: one fact, one home (the record).
After: the same fact in two homes — the file's line and the record's row —
which is the state `knowledge-placement` Owns-truth rules out: "nothing is
copied between them, so there is no syncing to get wrong. A fact with two homes
has no owner." `test_skill_attribution.py:70` says the quiet part out loud
("Two homes for one fact only stay honest while they agree"). The record cites
`knowledge-placement` at line 55 as *support* for the table but never records
that it is now an exception on the file side. → restore a pointer with the
reason: the notice must travel inside the artifact, and the test supplies the
owner `knowledge-placement` assumes cannot exist. Cheapest is one sentence; the
alternative is a matching amendment to `knowledge-placement`.

WARNING  docs/architecture/install-modes.md:63, tests/test_skill_frontmatter.py:32,
tests/test_skill_cross_references.py:133 — all three cite the record for a rule
it no longer states. They say `vendor-upstream-skills` "forbids editing" a
vendored skill; the amended record says "Prefer layering to editing" and
explicitly records that six of the seven *were* edited in place and stay that
way. `test_skill_cross_references.py:133` is the sharpest case: it rests the
whole layering argument on a prohibition over
`finishing-a-development-branch` — the file the issue measured at 206 lines of
local drift. → replace "forbids editing" with "prefers layering over editing"
in the three, or restore the imperative in the record. Note the diff already did
this correction for `test_review_fanout_skill.py:107-112`; these three were
missed by the same sweep.

WARNING  wfctl/agents/skills/*/SKILL.md (all seven) — MIT asks for "the above
copyright notice **and this permission notice**"; the wheel now carries the
copyright notices and no permission notice for either upstream. Verified by
building: `uv build --wheel` in a scratch copy, unzipped — all seven lines are
present at `wfctl/agents/skills/*/SKILL.md` (acceptance criterion 2 holds), and
the only licence text in the wheel is `wfctl-0.17.0.dist-info/licenses/LICENSE`,
headed "Copyright (c) 2026 Andre Marin". → if you want the clause satisfied on
its own terms, `wfctl/agents/NOTICES.md` carrying both full MIT texts costs
nothing: `graft wfctl/agents` ships it, it is not a skill file (criterion 5
holds), and the in-file lines can point at it. Flagged rather than pressed: the
issue chose the `i-have-adhd` one-liner form deliberately and disclaims legal
advice, so this is the author's call — but the wheel currently asserts one
copyright holder over files belonging to three.

NIT      tests/test_skill_attribution.py:31 — `_LINE` requires the source and
the `©` but not the licence, so `Derived from [x](https://y) (© 2025 Z).`
passes while the record (line 24-26) says the line names three things. → require
a non-empty token before the comma: `\(([^),]+), [^)]*©[^)]*\)`.

NIT      tests/test_skill_attribution.py:32 — `_ROW` is whitespace-exact, so
re-formatting the table with aligned columns empties `_listed()` entirely. It
fails loudly today (test 2 reports all seven as unlisted), but a reformat
combined with a dropped line would pass test 1 silently. → allow padding:
`^\|\s*`([a-z0-9-]+)`\s*\|\s*`([^`]+)`\s*\|$`.

NIT      docs/architecture/vendor-upstream-skills.md — the record introduces
"derived" as the class name but keeps "vendored" in the title slug, in Owns
truth ("an untouched vendored file's contents"), and in every record and test
that points at it. The issue asked specifically that the fix "not deepen" the
`vendored` collision. → one sentence saying derived ⊇ vendored (a vendored copy
is the untouched case), or use one word throughout.

NIT      wfctl/agents/skills/brainstorming/SKILL.md:158 — pre-existing, not this
diff: the file tells the reader to read `skills/brainstorming/visual-companion.md`,
which does not exist in the repo or in the built wheel (`find` finds nothing).
Worth a separate issue; it is also evidence for the record's own drift argument.

## What each pass checked

1. **Correctness.** Verified both copyright notices against the sources, not the
   issue: `raw.githubusercontent.com/ayghri/i-have-adhd/main/LICENSE` → "Copyright
   (c) 2026 Ayoub Ghriss"; `api.github.com/repos/obra/superpowers/license` →
   "Copyright (c) 2025 Jesse Vincent" (`LICENSE`, not the `LICENSE.txt` the issue
   cites — that path 404s). Both lines are accurate. Exercised the new tests
   against mutated copies of the tree and the record in a scratch dir (worktree
   untouched): dropping a listed skill's line fails test 1; adding a line to an
   unlisted skill (`code-review`) fails test 2; changing a table row's upstream
   fails test 3; reverting `i-have-adhd` to the old `(MIT).` form fails test 1,
   so the `©` requirement is load-bearing. The pair fails in both directions as
   the issue requires — with the limit recorded in warning 2. Traced the copy
   path: `shutil.copy2`/`copytree` in `cli.py`, verbatim, so the lines reach
   `.agents/` and `MANIFEST.in`'s `graft` + `include_package_data` puts them in
   the wheel — confirmed by building one.
2. **Security.** No executable surface in the diff. The test reads two fixed
   paths and compiles two static regexes; no user input, no network, no secrets.
   Attribution URLs are `https://github.com/…` and are not fetched at runtime.
3. **Architecture.** Read `vendor-upstream-skills` as changed against
   `knowledge-placement`, `layer-model`, `install-modes`, `current-state.md`, and
   the three tests citing the record. Two contradictions and one dropped
   exception marker (warnings 3, 4, 5). Log entry format matches the convention
   in `install-modes` and `layer-model` (date, two-space verb column, continuation
   indent). `SKILLS_ROOT` resolution copies the documented
   `test_skill_frontmatter` pattern and correctly sidesteps conftest's autouse
   `bundle` fixture. Nothing moved into a layer, no new skill file — criteria 5
   holds; nothing in `wfctl/` reads the `license:` frontmatter key, so leaving it
   on `i-have-adhd` orphans no behaviour.
4. **Readability.** Helper names (`_attributed`, `_listed`) describe their
   contents; the docstrings carry rationale rather than restating assertions,
   matching the repo's convention. Comments explain *why* (the `©` in the pattern,
   the `SKILLS_ROOT` resolution) — nothing narrating the diff. Two accuracy
   defects, above.
5. **Performance.** 33 file reads and one record read per test run; the suite
   went 872 passed in 62s. `_attributed()` is recomputed by each of the three
   tests — 99 reads total, immaterial, not worth a fixture.
6. **Over-engineering.** Nothing to cut. Three tests where the issue asked for a
   pair, but the third is paid for by a real defect (`i-have-adhd` was listed
   *and* carried a line while the record still named the wrong source), and
   folding it into the others would lose that. 80 lines for three checks plus two
   parsers is proportionate.

Definition of done, re-run from the worktree: `uv run pytest -q` 872 passed,
`uv run ruff check wfctl/ tests/` clean, `uv run mypy wfctl/` clean.
`wfctl verify`/`doctor` not run per the review brief.

net: Lean already.
Verdict: **Request changes** — no blockers. Two verifiably false claims (W1, W2)
in a change whose entire purpose is accurate provenance, and three record-level
contradictions (W3, W4, W5) among the cross-references the change was supposed
to bring into line. All five are text fixes; nothing here needs redesign.

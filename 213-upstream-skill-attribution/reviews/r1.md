# Review: 213-upstream-skill-attribution (r1)

Target: `git diff main...HEAD` on `213-upstream-skill-attribution`, single commit
`5635747` ("fix(skills): attribute the six superpowers-derived skills in the
file"). Reviewed against issue #213's acceptance criteria and
`docs/architecture/vendor-upstream-skills.md` as changed, per `code-review`'s
six-pass rubric. No files were edited; three read-only verification steps were
run in addition to the passes (below).

## Passes run

**1. Correctness (adversarial).**
- Traced `_LINE` and `_ROW` regexes in `tests/test_skill_attribution.py` by hand
  against every line/row they're meant to match, including backtracking through
  `\(https://\S+\)` against a URL immediately followed by a space and a second
  `(...)` group — matches correctly for all seven skills.
- Verified the two copyright claims against upstream, not against the diff's own
  say-so: `curl`'d `raw.githubusercontent.com/obra/superpowers/main/LICENSE`
  (© 2025 Jesse Vincent) and `raw.githubusercontent.com/ayghri/i-have-adhd/main/LICENSE`
  (© 2026 Ayoub Ghriss). Both match the six new attribution lines and the
  corrected `i-have-adhd` line exactly.
- Checked for the "vendored" terminology collision the issue calls out by name
  (`_MIRRORED_SKILLS` in `wfctl/cli.py`, `wfctl/cli.py:1274`, `test_install_skills.py`)
  — confirmed it's an unrelated concept (native-discovery mirroring) that this
  diff correctly leaves untouched.
- Checked every other reference to `vendor-upstream-skills.md`
  (`knowledge-placement.md`, `layer-model.md`, `install-modes.md`,
  `test_skill_cross_references.py`) — none depend on the old `license:`-key
  wording or the record's now-changed title; no dangling reference.
- `tests/test_review_fanout_skill.py`'s updated docstring ("it lists two of the
  three as superpowers-derived and not the third") is accurate against the new
  table: `requesting-code-review` and `receiving-code-review` are listed,
  `code-review` (wfctl's own) is not.
- Ran `uv run pytest -q`: **872 passed**. `uv run ruff check wfctl/ tests/`:
  clean. `uv run mypy wfctl/`: clean.

**2. Security.** N/A — no user input, no secrets, documentation and a static
regex check over local files only.

**3. Architecture.** Follows the existing pattern of the other `test_skill_*`
suites (`test_skill_frontmatter.py`, `test_skill_cross_references.py`): read the
shipped tree via `Path(wfctl.__file__)`, assert against the record. The record
edit replaces the identification rule rather than patching around it, as
required — confirmed no other code path reads the `license:` frontmatter key for
identification (`grep` for `"license"` reads in `wfctl/*.py` returns nothing).

**4. Readability.** Attribution lines match the existing `---` + line convention
`i-have-adhd` already used. Record prose is dense but consistent with the
file's established style (matches the rest of `docs/architecture/`).

**5. Performance.** N/A — one glob over ~32 skill directories, two regex passes
over a single markdown file.

**6. Over-engineering.** Nothing to cut. Three tests (not two) cover the
record↔file relationship — `test_the_file_and_the_record_name_the_same_upstream`
looks redundant against the "fails in both directions" requirement but isn't:
it's the exact failure mode the issue names (`i-have-adhd` was listed, carried a
line, and the record still named the wrong repo for it before this diff), which
the union/difference checks in the other two tests cannot catch on their own.

## Additional verification (beyond the six passes, evidence over assertion)

- Built the wheel (`uv run --with build python -m build --wheel -o <scratch>`)
  and inspected it directly: all six attribution lines are present inside
  `wfctl-0.17.0-py3-none-any.whl` (unzipped and `tail`'d each `SKILL.md`).
  Satisfies acceptance criterion 2 and the issue's own "Automated"/"Manual"
  wheel-build verification step.
- Ran `uv run wfctl install-skills --agent claude --yes` into a scratch repo
  under the scratchpad directory (not the worktree) and confirmed the
  attribution lines land in both `.agents/skills/*/SKILL.md` and
  `.claude/skills/*/SKILL.md` in the installed tree. Satisfies the issue's other
  manual verification step.
- `git status --porcelain=v1` in the worktree is empty before and after all of
  the above — no files in the worktree were touched.
- Did not run `wfctl doctor` or `wfctl verify`, per this review's scope.

## Findings

NIT `wfctl/agents/skills/i-have-adhd/SKILL.md:5` — the frontmatter still carries
`license: MIT`, which was the old identification mechanism the record explicitly
says it "replaces" in favor of the attribution line. Nothing in `wfctl/*.py`
reads this key (confirmed by grep) and the new record/table no longer mentions
it as meaningful for any of the seven skills. It's accurate, so it's not wrong,
but it's now orphaned metadata sitting next to the exact class of problem this
issue exists to fix (a signal nothing checks, that a future reader could
mistake for still mattering). Non-blocking — author's call whether to drop it
or leave it as inert-but-true.

net: −1 line possible (the stale `license:` key, if the author agrees it's dead)
Verdict: **Approve**

All six acceptance criteria checked and satisfied: each of the six skills names
its source and carries a copyright notice in the `i-have-adhd` form (verified
against upstream LICENSE files, not just internal consistency); the notice ships
inside the wheel (verified by building it); the record's table matches the tree
and its `license:`-key rule is replaced, not patched; `i-have-adhd` is listed
with its real source; nothing was moved into a layer and no new skill file was
created; the test added fails in both directions plus the specific
already-observed failure mode (record and file disagreeing on which upstream).

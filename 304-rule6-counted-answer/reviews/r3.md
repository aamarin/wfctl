## Review: 7be7bea (`git diff origin/main...HEAD`) — r3

Target: `wfctl/_shape.py`, `tests/test_shape.py`. Context read: issue #304, the
whole of `_shape.py` (not only the changed lines), the whole of `test_shape.py`,
`CLAUDE.md`. All six passes run; every finding below was reproduced by running
the code, and the reproduction is quoted with it.

Gate state at review time, for the record: `uv run pytest -q` 1552 passed,
`uv run ruff check wfctl/ tests/` clean, `uv run mypy wfctl/` clean.

---

WARNING  `wfctl/_shape.py:77,201-208` — #304's defect survives intact whenever
the first sentence ends in a character `_SENTENCE`'s `[a-z)\]\`]` prefix class
does not cover, or in `?`/`!`. The issue's repro line 2 with two words added
still fires:

| line | rule 6 |
|---|---|
| `Both routes resolve. The tests now pin each one:` | ok (fixed) |
| `Both routes resolve in CI. The tests now pin each one:` | **FIRES** |
| `Three reviewers reported the same bug in PR 304. Here is what each said:` | **FIRES** |
| `Both routes resolve? The tests now pin each one:` | **FIRES** |
| `Both routes resolve! The tests now pin each one:` | **FIRES** |
| `Three reviewers called it "noisy." Here is what each said:` | **FIRES** |

A sentence ending in an acronym, a digit, an issue number, a version, a closing
quote, or a question mark is not an exotic shape in this repo's own replies —
`in CI`, `over #304`, `on 3.11` are ordinary endings. The fix removes the
subset of #304 whose first sentence happens to end in a lowercase letter.

→ Give the reply surface its own boundary rather than borrowing the body's.
`[A-Za-z0-9)\]\`"']\s*[.?!]["')\]]?\s+[A-Z]` clears every row above and leaves
`Three things worth flagging:` and the run-on case firing. Do **not** widen
`_SENTENCE` in place to get there — see the architecture finding below.

WARNING  `tests/test_shape.py:70-71` — the third pinned case passes by accident
of the sha's last character, and the docstring presents it as the case that
justified the design. `dc3387c` ends in `c`, which is in `_SENTENCE`'s prefix
class; a hex sha ends in a digit more often than not, and then the same
observed line fires:

| sha in the pinned line | rule 6 |
|---|---|
| `dc3387c` (as committed) | ok |
| `dc3387a` | ok |
| `dc33870` | **FIRES** |
| `dc33871` | **FIRES** |
| `1a2b3c4` | **FIRES** |

A regression test whose verdict flips on a character irrelevant to the shape
under test does not pin the shape. It is also the case most likely to be
copy-edited later — someone normalising a sha to `abc1234` silently deletes
the coverage.

→ Either fix the boundary (finding above), which makes the case pass for its
stated reason, or pin the sha shape explicitly with a second line ending in a
digit. Leaving it as-is pins luck.

WARNING  `tests/test_shape.py:65-67` — the docstring's stated reason for the
third case is not supported by the code. It says the line "is the reason the
boundary is a sentence rather than the period the first two happen to share",
but a naive `line.split(".")[0]` clears all three pinned lines identically
(verified). Nothing in the third case distinguishes a sentence boundary from a
bare period. Its actual distinguishing property is the opposite of the claim:
it is the line where the prefix class is load-bearing, and where a bare period
split would have been *more* robust than `_SENTENCE`.

The convention in `CLAUDE.md` is that a docstring says why the test exists.
Saying why, incorrectly, is worse than the restatement the convention is
written against — the next reader trusts it and preserves the wrong property.

→ Replace with the reason that is true: the third line is a live observation
whose first sentence ends in a sha, which is where the boundary regex is
weakest. Then pin that, per the finding above.

WARNING  `wfctl/_shape.py:82-88` — the comment overclaims what the code does.
"The discriminator is a colon in the sentence the count is in" is not the
implemented rule; the implemented rule is "a colon before the first boundary
`_SENTENCE` can see", and the gap between the two is the whole first finding.
This file's comments are its primary artifact and are accurate elsewhere; a
reader who takes this one at face value will not think to check `?`, `!`, or a
period after a digit.

→ Say what is implemented and name the limit: the boundary is `_SENTENCE`'s,
which sees a period after a lowercase letter or `)]`` and nothing else.

WARNING  `wfctl/_shape.py:95-100` — the out-of-scope note names a class that
does not cover the issue's own second live observation, so #304 closes with one
of its two cited live cases still firing and nothing in the file saying so.
`Both of r1's top findings reproduce: the gate and the corpus.` still FIRES.
That is not "a count that is itself the answer" — no question was asked whose
answer is a set; it is a counted fact whose colon introduces the evidence for
it. The note as written describes `Three start today with zero overlap: #324,
#305, #335` and stops there.

I am not reporting the accepted boundary as a missed case. I am reporting that
the note misstates it: the unreachable class is wider than "a count that is
itself the answer" — it is *any* count and colon inside one sentence — and the
issue's own `Why it matters` section is the evidence.

→ State the class by its shape (one sentence, count, colon) rather than by one
intent it can carry, and name `Both of r1's top findings reproduce:` as the
second of the issue's two live cases that this does not reach.

WARNING  `wfctl/_shape.py:231-235` — the narrowing removes true positives of a
shape this file's own comment, four lines above, names as the observed one. The
comment says the shape is a coda: "the answer lands, then `One thing I couldn't
finish:` starts a second block." When the answer and the coda share a line and
the line opens with a count word, the coda is now invisible:

| line | before | after |
|---|---|---|
| `Both landed. Three things worth flagging: a, b, c.` | fired | **silent** |
| `Two tests pass. One thing I couldn't finish: the migration.` | fired | **silent** |
| `Done. One thing I couldn't finish: the migration.` | silent | silent (the `^` anchor, unchanged) |

The window is narrow — the line must open with a count word — and #304 says a
missed hit costs less than a false positive, so this may well be the right
trade. But the issue also said explicitly that narrowing "can only remove real
hits, so it needs the corpus behind it", and no corpus measurement is cited for
this direction. It is asserted nowhere and mentioned nowhere.

→ At minimum, one line in the comment saying the first sentence is the only one
scanned and what that gives up. Better: scan the first sentence *of each
sentence that starts the line*, which is a no-op here, or accept and document.

NIT  `wfctl/_shape.py:77` — architecture: `_SENTENCE` now serves two surfaces
whose error costs point in opposite directions, and the comment says they share
it without saying they want different things from it. On the body surface a
missed boundary suppresses a finding (safe); on the reply surface a missed
boundary *produces* one, which is exactly #304. Tuning it for either moves the
other silently. Verified concretely: widening it enough to clear the first
finding's table keeps both body verdicts correct but changes which cell
`body_findings` quotes, failing `test_the_drawing_the_reader_rejected_is_flagged`
at `tests/test_shape.py:418`.

→ This is why the first finding's fix should be a reply-local boundary
(`_REPLY_SENTENCE`, or a widened pattern used only inside `_first_sentence`)
rather than a widened `_SENTENCE`. Worth one sentence in the comment either way.

NIT  `wfctl/_shape.py:208` — `line[: boundary.start() + 1]` returns the first
sentence *without* its terminal period, while the docstring says "up to its
first sentence boundary". Harmless for a colon search and not worth changing
the slice; the docstring could say "up to, not including, the period that ends
it" so the next reader does not assume the period is there.

---

**Existing tests that silently changed meaning: none found.** Checked every
test in `tests/test_shape.py` that reaches `_COUNTED` or `_SENTENCE`:
`test_a_counted_lead_in_is_flagged_and_a_counted_fact_is_not` (both lines are
single sentences, unaffected), `test_a_colon_inside_inline_code_does_not_make_a_lead_in`
(still passes for its original reason: after `_QUOTED.sub` the line holds no
colon at all, and `_first_sentence` is the identity on it — verified),
`test_every_finding_names_the_check_the_reader_already_agreed_to` (`Three
things:` is one sentence), `test_a_fenced_block_is_content_not_voice` and
`test_a_quoted_fence_inside_a_wider_one_is_still_inside_it` (never reach the
counted scan). The `_SENTENCE` literal is byte-identical across the move, so
`body_findings` and its four tests are untouched.

**Coverage asserted nowhere**, in priority order: a first sentence ending in a
digit, an acronym, a closing quote, `?` or `!` (the first finding — this is the
gap that matters); the lost coda (`Both landed. Three things worth flagging:`);
`_first_sentence`'s no-boundary branch is exercised only incidentally, through
`Three things worth flagging:`, and is fine as such.

**Pass notes.** *Security*: nothing — pure functions over strings, no
filesystem, no subprocess; the only new input path is a slice of a string the
caller already had. Regex safety checked: `_SENTENCE` has no nested quantifier
and `_first_sentence` adds one `search` per prose line, so no catastrophic
backtracking is reachable. *Performance*: one extra `re.search` per prose line
on a per-turn hook — immeasurable against `_md.walk`, which already walks the
same text. *Over-engineering*: `_first_sentence` has one caller but is named,
documented and testable, which is this file's established shape, not an
abstraction ahead of need. The 29-line comment on `_COUNTED` is at the upper
end even for this file, but the file's convention is dense rationale and three
of its four paragraphs carry an argument that is not in the code.

net: Lean already.
Verdict: **Request changes** (0 blockers, 6 warnings). The change definitely
improves code health and the three named cases are genuinely fixed. What has to
move before merge is the pair that will rot silently: the sha-dependent pin at
`test_shape.py:70-71`, which asserts luck, and the comment at `_shape.py:82-88`,
which tells the next reader the rule is stricter than it is. Fixing the boundary
per the first finding resolves both and costs one regex.

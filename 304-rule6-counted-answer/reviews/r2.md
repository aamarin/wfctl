## Review: 7be7bea (`git diff origin/main...HEAD`) — reviewer r2

Target: `wfctl/_shape.py`, `tests/test_shape.py`. Local gates re-run read-only:
`uv run pytest tests/test_shape.py -q` → 36 passed; `uv run ruff check` and
`uv run mypy wfctl/_shape.py` → clean. Every finding below was reproduced by
running `_shape` in this worktree, not read off the diff.

The core fix is right. `_COUNTED.match` on a *prefix* of the line can only ever
remove matches (the pattern's `[^\n]*:` needs a colon that a prefix can only
lack), so the narrowing direction the issue warns about is bounded by
construction, and both cases in the issue's Reproduction block stop firing while
`Three things worth flagging:` still fires. Ordering is also right: `_QUOTED.sub`
runs *before* the split, so the slice indices belong to the string being matched
and a colon inside a code span cannot survive into the sentence. Moving
`_SENTENCE` above the `--- the PR-body surface ---` divider changes nothing for
`body_findings` — same compiled object, same call site at L279, and it uses the
match only for truthiness.

---

WARNING  wfctl/_shape.py:L95-100 — the comment names the residual class more
narrowly than it is. It says the out-of-reach case is "a count that is itself
the answer". The issue's own *Why it matters* names two live false positives;
this diff fixes one (`Three reviewers dispatched over dc3387c. …`, pinned) and
leaves the other, `Both of r1's top findings reproduce: …`, firing — and that
one is not a count-as-answer: it is a counted *subject* whose colon opens an
elaboration inside one sentence. The true residual is "one line, one sentence,
count and colon together"; count-as-answer is an instance of it, not the whole.
As written, a reader of the closing commit believes the issue's live cases are
covered. → State the residual as its shape, and name the issue's own case beside
`Three start today with zero overlap:` as a second instance of it.

WARNING  wfctl/_shape.py:L77, L201-208 — an emphasised first sentence defeats
the boundary, so the #304 shape survives whenever the count is bolded.
Reproduced: `**Both routes resolve.** The tests now pin each one:` and
`_Both routes resolve._ The tests now pin each one:` both still fire, because
`_SENTENCE` requires `\.\s+[A-Z]` and the closing `**`/`_` sits between the
period and the space. This is not an exotic input — `_COUNTED` carries
`(?:\*\*|_)?` precisely because the observed corpus emphasises counted openings,
so the fix misses the emphasised half of the class it was written for. A closing
quote (`resolve." The …`) fails the same way. → Let the boundary skip one closer:
`(?<=[a-z)\]`])\.(?:\*\*|\*|_|["')\]])?\s+(?=[A-Z])` — verified to clear all
three cases while `Three things worth flagging:` and the run-on lead-in keep
firing. Note it widens the *shared* regex: on the PR-body surface it newly sees
`**done.** Next` as a boundary. Inside fenced blocks that shape is unlikely, but
if that risk isn't wanted, widen a rule-6-local pattern and leave `_SENTENCE` to
`body_findings`.

WARNING  wfctl/_shape.py:L114, L234 — `_QUOTED` erases the boundary that
`_first_sentence` then needs. `\bhttps?://\S+` swallows the period that ends the
sentence, so `Three of these came from https://x.com/a. Here is the list:`
substitutes to ``Three of these came from `` Here is the list:`` — one
"sentence", colon found, still fires. That is exactly #304's shape, on a line
whose form the `_QUOTED` comment itself names as observed. Pre-existing in
`_QUOTED`, but it is this change that depends on the boundary surviving the sub.
→ Stop the URL before trailing sentence punctuation:
`` `[^`]*`|\bhttps?://\S*[^\s.,;:!?)\]]` ``. Verified: the case stops firing, the
code-span case (`Two unrelated branches show `[origin/…: gone]``) is unchanged,
and as a bonus a genuine lead-in colon that *follows* a URL stops being eaten.

NIT  tests/test_shape.py:L61-73 — the third case's stated reason isn't
demonstrated. The docstring says it "is the reason the boundary is a sentence
rather than the period the first two happen to share", but `line.split(".")[0]`
gives the identical verdict on all three cases (verified), so nothing here
separates a sentence boundary from a bare period split. Under this repo's
convention ("docstrings say *why the test exists*"), the reason has to be true.
→ Keep the case — it is the live observation — and say that; or add the case
that does discriminate: `Three of these live in wfctl/_shape.py: the pattern,
the gate and the quote strip.` fires under `_SENTENCE` (no space+capital after
the period) and is missed by a period split.

NIT  wfctl/_shape.py:L207-208 — `boundary.start() + 1` hard-codes that
`_SENTENCE`'s lead-in is exactly one character. Widen that char class — which
the emphasis fix above would do — and the slice silently cuts a character short.
Harmless today (a shorter prefix can only drop a match, never add one), but it
is a silent coupling between a helper and a regex now shared by two surfaces.
The docstring is also a shade off: it returns the sentence *minus* its period,
not "up to its first sentence boundary". → Put the terminator outside the match:
`_SENTENCE = re.compile(r"(?<=[a-z)\]`])\.\s+(?=[A-Z])")` and slice at
`boundary.start()` — verified identical on both surfaces, since `body_findings`
only tests truthiness.

NIT  wfctl/_shape.py:L30 — the module docstring still says "Each function
carries the rules for its own surface and neither borrows the other's", and
`_SENTENCE` is now read by both. The move above the divider is the right call
(a shared primitive should not sit under "the PR-body surface"), and
"Two surfaces read it" carries most of the weight; it is the docstring line that
now overstates. → One clause, either place: a retune of this boundary lands on
both surfaces and has to be checked against both corpora.

NIT  process, not code — `.workmux/PROMPT-304-rule6-counted-answer.md:L62-71`
asked that the count-as-answer instance be added to #304 *before* starting.
`gh issue view 304` shows 0 comments and no mention, and the commit says
`Closes #304`. The commit message and the code comment do record it, so nothing
is lost if a PR body repeats it — but the issue itself will close without it.
→ Comment on #304, or carry it in the PR body, before merge.

---

Passes run, and what each checked:

- **Correctness** — traced `_prose` → `_QUOTED.sub` → `_first_sentence` →
  `_COUNTED.match`. Verified the narrowing is monotone (prefix match implies
  line match); the no-boundary path (`""`, whitespace, a line with no `. X`)
  returns the line unchanged; tab as the separator works (`\s+`); the `+1`
  arithmetic lands on the period and excludes it, which is inert for a
  colon search. Probed 17 lines against the built module: three residual false
  positives found (emphasis, closing quote, bare URL — above), one false
  negative found and judged not worth a finding (`… e.g. Foo … :`, an
  abbreviation boundary — the same weakness every period-based splitter has, and
  no evidence it occurs in a counted lead-in). Checked the "two thirds of the
  corpus" claim's transplant from *closes the line* to *closes the sentence*: it
  survives, because a colon can never be a `_SENTENCE` terminator, so
  "closes the first sentence" and "closes the line" name the same set. The
  corpus itself is not in-tree, so the figure stays unverifiable — pre-existing.
- **Security** — pure regex over local strings, no I/O, no untrusted sink.
  Checked for catastrophic backtracking: `[^\n]*:` now runs over a shorter
  string than before, `_QUOTED` is linear. Nothing.
- **Architecture** — `_SENTENCE` promoted to shared primitive: correct place,
  one docstring line now overstates (NIT above). No new dependency, no cycle,
  `body_findings` untouched in behaviour.
- **Readability** — `_first_sentence` is well-named and the comprehension reads;
  the rewritten `_COUNTED` comment explains *why this shape* and states what was
  rejected, matching the file's existing density and this repo's convention. It
  does not narrate the change to a reviewer (the change story is in the commit
  message, where the prompt asked for it). Markdown bold inside a comment has
  precedent at L164.
- **Performance** — one extra `re.search` per prose line; the regex is anchored
  by nothing but is linear and the lines are short. Irrelevant at this scale.
- **Over-engineering** — `_first_sentence` has one caller, but inlining it into
  the comprehension would bury the fix; the docstring is the explanation. The
  comment grew 14 lines and every one of them carries rationale the file's
  style demands. Nothing to delete.

net: Lean already.
Verdict: **Approve** — with the three WARNINGs answered before merge (the
comment's residual class is the one that matters; the emphasis and URL cases can
be fixed here or deferred with a follow-up issue, but should not be left
silent).

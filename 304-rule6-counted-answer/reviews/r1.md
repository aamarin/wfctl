## Review: 7be7bea (r1) — `git diff origin/main...HEAD`, `wfctl/_shape.py` + `tests/test_shape.py`

Six passes run. Gates re-run read-only: `uv run pytest -q` 1552 passed, `uv run
ruff check wfctl/ tests/` clean, `uv run mypy wfctl/` clean. `wfctl verify` not
run, per instruction; nothing under the worktree was modified.

The change is right in direction and the confinement works: measured over the
166 wfctl transcripts in `~/.claude/projects/-Users-andremarin-Development-wfctl*`
(7,995 terminal replies — assistant messages carrying no `tool_use`), rule 6
goes from 1,359 line-hits to 1,160. Of the 199 removed, ~171 are the counted
*facts* #304 is about (`Both confirmed. Fixing:`, `Three reds, all
AttributeError. Now the fix:`). That is a real improvement and the findings
below do not undo it.

WARNING  wfctl/_shape.py:L79-93 — the narrowing removes real hits too, and the
change ships no corpus number, which is the one thing #304 asked for before a
narrowing lands ("it needs the corpus behind it that the original tuning had").
28 of the 199 removed lines match the skill's *own* named tell — SKILL.md:285,
`<count> things / questions / reasons / answers / decisions`. ~17 are
unambiguous list announcements, not facts:

  - `Three things I'd want to argue before this ships. Pick one:`
  - `Two clarification questions left. Next one:`  (SKILL.md names "two questions left" verbatim)
  - `Two things worth your attention beyond the fix. …`
  - `Two things worth knowing before you pick. …`
  - `Three concrete things. The strongest is #2 …:`
  - `Four things from their spec are worth taking. Applying them:`

  The shape they share is the one the fix cannot see: the announcing sentence
  ends in a **period**, and the colon that used to catch it sat in the next
  sentence. Detection of that tell falls 571 → 543, −4.9%; the class is also the
  one `MEMORY.md` records as the user's most-broken rule. → Two ways to close
  it, either is fine: (a) record the trade in the comment the way this file's
  other tuning is recorded ("drops two thirds of the real hits in the corpus",
  "the only false positive across ninety transcripts") — `−199 hits, of which 28
  match the skill's named tell; −4.9% on that shape, −86% on the counted-fact
  class`; or (b) recover it, since the lost sentences are separable without any
  colon at all: a counted first sentence of the form `<count>
  (things|questions|reasons|answers|decisions|points)` is an announcement, and
  `Both confirmed` is not. (b) widens past the old behaviour and would need its
  own measurement, so (a) is the cheaper honest answer for this diff.

WARNING  wfctl/_shape.py:L72-77 — #304's own reproduction still fires in bold.
`_SENTENCE` needs `\.\s+[A-Z]`, so a sentence that ends inside bold is invisible:

      Both routes resolve. The tests now pin each one:        → fixed
      **Both routes resolve.** The tests now pin each one:    → still FIRES

  `_COUNTED` accepts a `**`/`_` prefix precisely because the observed lead-ins
  are bold, so the emphasised form is the one this module expects most, and it
  is the one the boundary cannot read. Same blind spot, same corpus: 24
  still-firing lines carry an undetected period break inside the matched span
  (~2% of what remains, ~11% of the class the fix targets) —
  `One assumption broke. ``gh --json`` returns objects, not strings:`,
  `Both done. ``export WFCTL_AGENT=claude`` is in ~/.zshrc …:`,
  `Five other worktrees, all marked done. #212 mentions PR bodies …:`,
  `Both siblings merged — #314's restructure and #308. My handoff anticipated this:`.
  The causes are enumerable: the next sentence opens with inline code, `#`, `**`
  or a digit, or the previous one ends in a digit or an acronym — house style in
  a repo whose prose is issue numbers and backticked commands, not edge cases.
  → The regex change is not free (see the next finding), so the minimum here is
  to name the blind spot beside `_SENTENCE`. The comment as written reads as if
  the colon is now confined to the counted sentence, full stop; it is confined
  to the counted sentence *as `_SENTENCE` can see it*.

WARNING  wfctl/_shape.py:L72-77 — `_SENTENCE` now serves two surfaces whose
tuning pulls opposite ways, and nothing says so. `body_findings`' evidence
(L263-267) is a *precision* claim — "the rejected block is the only one of the
five carrying a sentence boundary" — which holds only because the regex is
narrow. The reply surface now wants it wider. Concretely: the obvious widening
for the finding above, allowing a digit before the period, turns `1. Run the
gate` inside a fenced aligned block into a sentence boundary, and
`body_findings` starts firing on numbered lists in ASCII drawings. So the next
person tuning this for replies silently re-tunes the PR-body rule against a
corpus they are not looking at. → Keep the constant shared — it is the same idea
— and add one line to its comment: widening it has to be re-checked against PR
#208's four accepted drawings. The move above the surface divider is otherwise
right.

NIT  wfctl/_shape.py:L95-100 — the accepted-scope paragraph is narrower than the
class it accepts. (Flagging the wording, not the exemption.) It frames the
survivor as a count that "answers a question whose answer is a set". What
actually survives is wider: `Both of r1's top findings reproduce: the first
reproduces on a clean tree.` still fires, and that is one of the two *live
observations* #304 cites as the defect — a counted fact whose colon introduces
elaboration inside its own sentence, not an answer that is a set. With `Closes
#304` on the commit, half the issue's cited live evidence closes as fixed while
still reproducing. → Widen the sentence to "a count in the same sentence as its
colon, whatever that colon introduces", and say in the PR body that #304's
second observation is deferred rather than fixed.

NIT  tests/test_shape.py:L61-83 — the accepted boundary is argued in prose and
pinned by nothing. This file's habit is to pin what it argues for
(`test_a_counted_lead_in_is_flagged_and_a_counted_fact_is_not` pins the issue's
pair). → One line, `assert _shape.findings("Three start today with zero
overlap: #324, #305, #335.", BARE)`, makes the boundary a fact a later change
has to move deliberately.

NIT  wfctl/_shape.py:L201-208 — `_first_sentence` returns the sentence without
its period (`line[: boundary.start() + 1]` on `Both routes resolve. …` gives
`'Both routes resolve'`). Harmless — nothing downstream looks at the
punctuation — but the docstring's "`line` up to its first sentence boundary"
reads as including it. → `+ 2`, or half a clause in the docstring saying the
terminal period is dropped because no reader of it cares.

**Passes with nothing to report.** *Security*: pure regex over in-process
strings, no I/O, no untrusted sink; `[^\n]*` has no nested quantifier and one
literal after it, so no backtracking blowup, and it now runs over a shorter
string than before. *Performance*: one extra `_SENTENCE.search` per prose line,
against strictly less work in `_COUNTED` — not measurable. *Architecture*
otherwise: `_SENTENCE` moving above the `--- the PR-body surface ---` divider is
the correct response to it having two readers. *Readability*: the comment
explains why this shape and cites the failure it prevents, which is this file's
convention and not change-narration; the tests are sentences and their
docstrings name the failure, per AGENTS.md.

net: Lean already. (`_first_sentence` has one caller and a three-line body, but
the name is what makes the `_COUNTED` call site readable — inlining it would
cost more than it saves. No dead code, no speculative flexibility, no
hand-rolled stdlib.)

Verdict: **Approve** (0 blockers, 3 warnings). The change definitely improves
code health — 199 removals, ~171 of them true false positives, 1552 tests green.
The one warning worth acting on before merge is the first: #304 set the bar at
corpus evidence for a narrowing, the measurement above supplies it, and it
belongs in the comment rather than in a review file.

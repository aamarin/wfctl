# Review: commit `6f13497` (`373-mirrored-skill-wrappers`) — reviewer r2

Target resolved per `code-review` Step 1: a specific commit, so BASE=`6f13497~1`,
HEAD=`6f13497`, read with `git show 6f13497`. Nine files, +65/−17.

Context gathered before reading the diff (Step 2): the repo's `CLAUDE.md`
(comments, testing conventions, code style, the skills tree, `install-skills`
safety), `docs/architecture/mirror-supersedes-the-wrapper.md`,
`wfctl/cli.py:2355-2575` (`_MIRRORED_SKILLS`, `_claude_native_skill_mirror`,
`_AGENT_SKILL_EXTRAS`, `_mirror_supersedes_wrapper`, `_copy_command_for_bob`),
`docs/architecture/vendor-upstream-skills.md`, and the tests around the change —
`tests/test_install_skills.py:286-430`, `tests/test_skill_cross_references.py`,
`tests/test_review_fanout_skill.py:286-298`, `tests/test_skill_attribution.py`.
Tests read first, as the skill asks.

Evidence run (read-only): `uv run pytest -q` → **1598 passed in 128s**;
`uv run ruff check wfctl/ tests/` → clean; `uv run mypy wfctl/` → clean.
Nothing in the worktree was modified.

---

## Findings

**WARNING** `tests/test_skill_cross_references.py:436-449` — `test_the_design_record_skill_is_model_invocable` went from two assertions to one, and the one it kept does not test what its name claims. It now asserts only `"software-design-decisions" in _MIRRORED_SKILLS`. Its own docstring says "same shape as `test_the_panel_skill_is_model_invocable`", and that test (`tests/test_review_fanout_skill.py:294-298`) asserts membership **and** `"disable-model-invocation" not in _arch._frontmatter(...)`. Its nearer sibling in the same file, `test_the_design_method_skill_is_model_invocable:145-152`, spells out why membership alone overreaches — "`disable-model-invocation` on this SKILL.md would refuse the skill on the discovery path membership just put it on, leaving no route at all" — and kept its frontmatter assertion through this diff. The record test did not, and the gap is now live rather than theoretical: on the Claude layer `#373`'s wrapper is suppressed, so a `disable-model-invocation:` copy-pasted into `wfctl/agents/skills/software-design-decisions/SKILL.md` leaves **no** route on that layer with the suite green. → add the sibling's two lines:
```python
from wfctl import _arch
...
assert "disable-model-invocation" not in _arch._frontmatter(
    (_AGENTS / "skills" / "software-design-decisions" / "SKILL.md").read_text()
)
```

**WARNING** `wfctl/cli.py:2366-2368` and `wfctl/cli.py:2408-2412` — the membership rationale for two of the six names now states a fact the change falsified, in present tense, in the comments a future reader consults before touching `_MIRRORED_SKILLS`. `:2366` opens the `architecture-design` entry with "Same case as `software-design-decisions` below — no wrapper under `agents/commands/`"; `:2408` opens the `software-design-decisions` entry with "No wrapper under `agents/commands/`, so the mirror is the only route: a skill absent from both is reachable only by an agent already reading `design-levels` as text". Both files now exist and both are typed routes on `.agents/` and bob. The commit message presents "`_MIRRORED_SKILLS` and `_mirror_supersedes_wrapper` are untouched" as a virtue, and the code is — but the argument for keeping those two entries is not, and CLAUDE.md § Code style makes these comments load-bearing ("explain *why this shape*"). → reword both to the reason that survives: the trigger is a moment nobody types a command, the wrapper is suppressed on the mirroring layer, so membership is still what puts the skill on the discovery path. Keep the #150/#198 provenance; drop the "no wrapper" premise.

**WARNING** `tests/test_skill_cross_references.py:442-444` — inside a docstring this commit rewrote, the surviving sentence "Without membership the skill is reachable only by an agent already reading `design-levels` as text" is now false by this very commit: without membership, `_mirror_supersedes_wrapper` stops firing, `software-design-decisions.md` un-suppresses, and `/software-design-decisions` is typeable on every layer including Claude. The sibling docstring the same commit wrote (`:138-142`) reaches the opposite conclusion — "what the wrapper bought is a typed route on `.agents/` and bob" — so the two edited docstrings now disagree about what the wrapper did. CLAUDE.md § Testing conventions: a docstring says *why the test exists*, and this one now argues from a premise the diff removed. → restate as what membership still buys: model discovery at a moment nobody types a command, the wrapper being suppressed on the layer that mirrors.

**NIT** `tests/test_install_skills.py:361-363` — "a loop over the pinned seven would never see the addition it exists to catch" is now a pinned fourteen. The diff fixed the same-count staleness two docstrings up (`:319` "Deleting five of these seven" → "Deleting five of them") and left this one, in the same test function it edited. → "a loop over the pinned set" keeps the argument and needs no count.

**NIT** `docs/architecture/mirror-supersedes-the-wrapper.md:61,69` — Considered still reads "Delete the seven wrappers from the bundle" and "Drop `disable-model-invocation` from the seven wrappers", two paragraphs after the Context the diff refreshed to fifteen-and-fourteen. Defensible as a record of what was weighed at #170 — that is a past deliberation, not a present-tense claim — but the commit message's own justification for refreshing Context ("#204's panel found the same sentence wrong for the same reason, so it is carried rather than left") applies here too. → either leave both and accept them as history, or say "the seven wrappers suppressed at the time".

---

## Passes run, and what each covered

**1. Correctness** — traced the mechanism end to end rather than the changed lines. `_mirror_supersedes_wrapper` keys on `item.stem in _MIRRORED_SKILLS` with `layer == agent` and `agent in _AGENT_SKILL_EXTRAS`, so each of the six new `<name>.md` files is suppressed on claude and lands on `.agents/` and bob — the claim the commit message makes, and it holds. Verified the counts the record now asserts by parsing the set against the commands directory: **15 names in `_MIRRORED_SKILLS`, 14 with a same-stem wrapper, `speckit-brainstorm` the only one without** (its wrapper is `speckit.brainstorm.md`, which differs by a dot and never trips the predicate). Verified the pointer bodies resolve on the non-mirroring layers: `.bob/commands/<name>.md` → `../skills/<name>/SKILL.md` = `.bob/skills/...`, identical to the eight existing wrappers. Checked each new frontmatter for a YAML-hostile `": "` in the description — none, all six parse. `test_no_suppressed_wrapper_carries_more_than_a_pointer` passes because none carries a third key. Packaging checked, since a bundle addition invisible to an editable install is the repo's own known blind spot: `MANIFEST.in` uses `graft wfctl/agents`, so the six files ship without any manifest edit. Also checked the drift story — `doctor` hashes `_bundle.BUNDLE_ROOT` via `_bundle.content_hash`, so unlike the record's documented consequence for a bare `_MIRRORED_SKILLS` addition, six new files *do* move the hash and existing installs are told to reinstall. No correctness finding.

**2. Security** — none of the six wrappers carries `allowed-tools`, so no grant is widened on any layer, and `test_no_suppressed_wrapper_carries_more_than_a_pointer` keeps it that way; the `allowed-tools`-revoked-by-suppression consequence the record documents for `end-session.md` is not reachable here. All six carry `disable-model-invocation: true`, the narrower default, and `_copy_command_for_bob` strips it for bob exactly as for the existing eight. `using-superpowers` is upstream-derived, so checked `docs/architecture/vendor-upstream-skills.md` and `tests/test_skill_attribution.py`: the attribution obligation attaches to `SKILL.md` and the specify scripts, not to a wfctl-authored wrapper — the existing wrappers for `i-have-adhd`, `receiving-code-review` and `verification-before-completion` carry no line either, so the new one is consistent, not an omission. No security finding.

**3. Architecture** — the change adds no mechanism; it uses the one `mirror-supersedes-the-wrapper` already owns, which is the right call and the commit message argues it correctly (a different stem would give one workflow two names, and the dotted-name escape stays available for a real collision). The record amendment is in the right place, the Log line's date/verb/column shape matches the two above it, and `status: proposed` is untouched — consistent with this repo holding proposed records deliberately. Findings 2 and 5 above are architecture-adjacent: the *record* was refreshed and the *code comments that duplicate its reasoning* were not. No structural finding.

**4. Readability & simplification** — the six wrappers are byte-for-byte the established shape: two-key frontmatter, one pointer sentence with the `../skills/` fallback, and for `using-superpowers` the "apply it for the rest of the session" + "Confirm activation in one line" tail that `i-have-adhd.md` and `conversation-response-shape.md` already use for session-persistent skills. Descriptions are trigger-first and faithful to each skill's own description. Applied Chesterton's Fence to the two dropped assertions before judging them: both asserted a file's *absence*, both were genuinely invalidated, and dropping them was right — the defect is what did not replace them (finding 1) and the rationale left standing beside them (finding 3). Findings 3, 4 and 5 are this pass.

**5. Performance** — six additional file copies per `install-skills` run and six more paths in the content hash. Sub-millisecond against a bundle that already walks the whole `wfctl/agents` tree. Nothing hot, nothing unbounded, no finding.

**6. Over-engineering** — nothing to delete, stdlib-ify, or shrink. Six files × six lines is the minimum a pointer wrapper can be, and the convention they join is eight files old. The one alternative that would have been shorter — suppressing at the bundle instead of per layer — is the regression the record and `tests/test_install_skills.py:450-462` exist to prevent. The `all six at once rather than one` choice is correctly argued in the commit message: `architecture-design` and `software-design-decisions` have the identical gap, so splitting them would be three diffs of one file each.

---

net: Lean already.

Verdict: **Request changes** (3 warnings, no blockers — one is a coverage regression this commit introduced, two are rationale the commit falsified and left standing; each is a one-to-three-line fix)

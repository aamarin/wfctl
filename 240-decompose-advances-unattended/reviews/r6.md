## Review: ab6bfa9 — feat(pipeline): let decompose advance without a prompt

Target resolved as `git show ab6bfa9` (BASE `ab6bfa9~1`). Rubric:
`.agents/skills/code-review/SKILL.md`, all six passes.

### Findings

WARNING  wfctl/agents/commands/speckit.decompose.md:8 — `allowed-tools: Read Glob`
names neither the Bash the guard needs nor the `Write` the step's own artifact
needs, and this commit is what makes that reachable unattended. Step 5 of
`speckit-delivery-plan` writes `delivery.md`; step 6 runs `wfctl status --json`.
This repo reads `allowed-tools` as a hard ceiling — `tests/test_skill_cross_references.py:167`,
"a step added to the prose without its command is a step that reads correctly and
cannot run. Silent: the agent reports the tool refusal, not a missing rule" — and
`speckit.brainstorm.md`, the precedent the new `_predicates.py` comment names, carries
`Bash(wfctl status*)` with `test_brainstorm_allows_the_commands_its_records_need`
pinning it. Two consequences, and only the first is benign: the step-6 read fails
closed ("Treat any other answer — the command failing — as `false`"), so no issues
are created; but `delivery.md` is not written either, decompose stays `pending` and
unblocked, and `next_step_content` therefore answers `auto: true` again. `speckit-orchestrate`
step 5 re-emits `EXECUTE_COMMAND` with no no-progress guard, so the loop spins. The
`auto: false` this commit removes is exactly what halted it there before.
→ Add `Write Bash(wfctl status*) Bash(wfctl issue create*) Bash(wfctl notify*)` to
that line, pinned by a test in the shape of `test_brainstorm_allows_the_commands_its_records_need`.
**Escalate to BLOCKER** if the ceiling reading in that docstring is what the harness
actually does; it stays WARNING only if `allowed-tools` is a pre-approval list, in
which case the cost is a permission prompt per unattended run rather than a spin.
The frontmatter block edited by this commit is the one that carries the stale line.

WARNING  wfctl/agents/skills/speckit-delivery-plan/SKILL.md:128 — step 7 names no
command for creating the issues, so nothing routes them through the check that
already exists. `wfctl issue create` is gated by `_tracker._refuse_notifying`, which
cites `a-rule-is-expressed-as-a-check` and refuses an ungranted run whether or not
the agent read step 6; `gh issue create` reaches the tracker with no gate at all.
Before this commit a human sat at the `auto: false` stop and at the tool prompt;
after it, step 6's prose is the only thing steering the agent onto the gated route.
→ Name `wfctl issue create` in step 7. That converts the commit's safety argument
from prose into the check the repo already ships, at no cost to the flip.

WARNING  tests/test_decompose_issues.py:309 — `test_a_fully_keyed_plan_leaves_decompose_behind`'s
docstring claims "the failure would show up here and nowhere else". Refuted: patching
`_current_step_name` to re-enter decompose once it reads `done` fails 12 tests, 11 of
them elsewhere — `test_end_reports_observations` (4) and `test_pipeline_commands` (7,
including `test_a_blocked_implement_routes_to_verification` and both
`test_the_file_an_agent_reads_names_the_command_status_prints` cases). The test also
passes unchanged with the flag reverted to `_REVIEW_REQUIRED`, so it pins nothing
about this change. Under this repo's own rule — a test whose prose only restates its
assertion is not carrying its weight — a justification that is false is worse than a
weak one. → Correct the docstring to what it actually guards, or delete the test.

WARNING  tests/test_decompose_issues.py:4 — the module docstring still says the issues
are created only when something "waits for a human": *"the file is written before the
issues exist by design — creating them is notifying and waits for a human."* That is
the same claim this commit corrected in `_unkeyed_issues`, in `speckit.decompose.md`
and in the delivery-plan checklist, left standing in the file the commit added three
tests to — and it now contradicts the predicate docstring rewritten in the same diff.
→ Same wording as the predicate: what it waits on is the notify grant (#280), not
this step.

NIT  tests/test_decompose_issues.py:284 — `_use_tracker(tmp_path)` is dead setup in
`test_a_feature_that_has_analyzed_enters_decompose_without_a_prompt`: the fixture
writes no `delivery.md`, so `decompose` returns `Reading("pending")` before
`configured_key_pattern` is ever reached. Defensible as the file-wide precondition
its module docstring declares — author's call.

NIT  tests/test_decompose_issues.py:266 — `test_an_unkeyed_map_stops_the_loop_the_flag_now_lets_run`
does not parse as a sentence. → `..._stops_a_loop_the_flag_now_lets_through`. Its
separate existence *is* justified: verified that it passes with the flag reverted, so
it is the guard its docstring says it is — the only test that would fail if the
blocked-step-is-never-automatic rule were dropped — rather than a pin on this change.
Folding `report.auto` into `test_a_half_decomposed_feature_is_not_routed_to_implement`,
which uses the identical fixture, would be the shorter form if the author disagrees.

NIT  docs/architecture/a-human-grants-outward-facing-authority.md:2 — still
`status: proposed`, so `wfctl arch context` does not project the record that
authorizes the grant this flip now depends on. #280 shipped the code and left the
record proposed; not this commit's to change, named so the panel does not rediscover it.

### Passes

1. **Correctness** — Traced the flip through its only consumer. `continuation` is read
   in exactly one place (`_pipeline.py:345`), so the flag's blast radius is
   `next_step_content`'s second return value. Confirmed empirically that the blocked
   branch precedes the table read, so an unkeyed map still answers `auto: False`.
   Verified the three claims the commit rests on: `_predicates.decompose` returns
   `in_progress` + reason while `tasks_open`, `build_report` hands that reason to
   `next_step_content` as `blocked`, and `speckit-delivery-plan` step 6 genuinely reads
   `notify`/`notify_source` and fails closed on any other answer. Walked the
   ungranted unattended path: writes `delivery.md` with placeholders → next iteration
   blocked → `auto: false` → loop stops. It terminates. The one non-terminating path is
   WARNING 1.
2. **Security** — This is the pass the change lives or dies on, since it widens what
   runs with no human. The outward-facing action is gated by a real check
   (`_tracker._NOTIFYING_VERBS` / `_refuse_notifying`), not by the flag and not only by
   prose, which is the right shape and is what makes the flip defensible. The gap is
   that nothing routes the agent to the gated verb (WARNING 2) and the command's own
   frontmatter cannot run the read that would (WARNING 1). No secrets, no input handling,
   no new surface.
3. **Architecture** — `pipeline-state-is-one-payload`: untouched; the flip changes a
   table value, no view computes anything new, both new report assertions read the one
   payload. `a-rule-is-expressed-as-a-check`: satisfied on the half that matters — the
   grant is a check — and WARNING 2 is where that record's own question ("is a violation
   visible in an artifact the work already produces?") is answered yes and left to prose
   anyway. No declaration record needed: three exist for many changes, and a flag flip
   draws no boundary.
4. **Readability** — The `_predicates.py` comment rewrite informs the next reader (why
   the rung sits below the flag) rather than narrating the diff, and matches the
   surrounding density. Both new docstrings say why the test exists. WARNING 3 and 4 are
   this pass's findings. No console-output assertions added, so `NO_COLOR` does not apply.
5. **Performance** — Nothing. One extra `wfctl status --json` per unattended decompose;
   no disk or network read added to `_infer_steps`.
6. **Over-engineering** — `delete:` `test_a_fully_keyed_plan_leaves_decompose_behind`
   (tests/test_decompose_issues.py:309) — passes identically before and after the flip,
   and the failure it claims to own is caught by 11 other tests. Nothing else in the diff
   is speculative; the comment rewrite is net +9 lines of rationale that a reader needs.

### Definition of done (run in this worktree, tree unmodified)

- `uv run pytest -q` → 1376 passed in 90.44s
- `uv run ruff check wfctl/ tests/` → All checks passed
- `uv run mypy wfctl/` → Success: no issues found in 22 source files
- `uv run wfctl doctor` → exit 0; both layers current at 0.19.0. The three `ℹ` lines are
  pre-existing duplicate issue-key claims in the shared spec root, unrelated to this diff.

Two experiments, both run from a scratchpad pytest plugin with no write to the worktree:
reverting `_STEPS["decompose"]` to `_REVIEW_REQUIRED` fails exactly 3 tests — the two
table tests and `test_a_feature_that_has_analyzed_enters_decompose_without_a_prompt` —
which is the one new test that pins the change; and the `_current_step_name` mutation
behind WARNING 3.

net: −18 lines possible
Verdict: **Request changes** — no blocker, and the flip itself is correct and pinned.
The two one-line fixes (WARNING 3, 4) are inside this commit's own new and edited text.
WARNING 1 and 2 sit on the safety argument the flip depends on and are #280 debt this
change makes load-bearing: fix them here, or defer them to a filed issue in writing.

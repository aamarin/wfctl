# Review: 241-digests-for-decaying-rules (origin/main...HEAD) — reviewer r3

Target resolved via `git diff origin/main...HEAD` (also read from the saved
`target.diff`). Two commits: `77e8403` (the feature — two `digest.md` files plus
one red flag in `worktree-handoff/SKILL.md`) and `46ce498` (an unrelated
`uv.lock` version-key sync, discussed under Architecture below).

## Context gathered

- `gh issue view 241` — full argument, acceptance criteria, and the measured A/B
  read in full.
- `.agents/skills/conversation-response-shape/SKILL.md` — the contract a
  `digest.md` is under: "It projects this file and never sources it: growth goes
  here, cuts there," capped at 500 chars (`_DIGEST_MAX_CHARS`), truncated to an
  ellipsis past that with no build failure.
- `wfctl/cli.py:2839-2868` (`_digest_text`) — confirms the truncation/whitespace-
  flattening/symlink-safety contract the new files must fit inside.
- `tests/test_skill_cross_references.py::test_no_shipped_digest_is_truncated_...`
  and `tests/test_hook_user_prompt.py` — the generic tests that automatically
  cover any new `digest.md` via `glob("skills/*/digest.md")`; confirmed no new
  test was needed or added, matching the issue's own claim.
- Read `worktree-handoff/SKILL.md` and `opening-a-change/SKILL.md` in full, and
  checked every numbered line of both new `digest.md` files traces to text
  actually present in its own skill (the "projects and never sources" rule).
- `CLAUDE.md` at repo root — comments/code-style/spec-pipeline sections.

## Verification run (read-only, nothing written)

```
$ uv run pytest -q                          1033 passed
$ uv run ruff check wfctl/ tests/           All checks passed!
$ uv run mypy wfctl/                        Success: no issues found in 17 source files
$ uv run wfctl doctor                       ✓ base / ✓ claude, both "skills current" — no drift
$ uv run wfctl hook user-prompt             3 bullets, none ending in "…"
```

`.agents/skills/{worktree-handoff,opening-a-change}/{digest.md,SKILL.md}` were
already byte-identical to the `wfctl/agents/...` source and `doctor` reports zero
drift, so the tree was already reinstalled with `uv run` — the acceptance
criterion "install-skills then a fresh session shows all three under the hook's
header" is independently confirmed without me running `install-skills` myself.

## Pass 1 — Correctness

Checked each digest line against its own `SKILL.md`, since a line that isn't
actually stated there breaks the contract in `conversation-response-shape`:

- `worktree-handoff/digest.md` lines 1-5 each match text added or already present
  in `worktree-handoff/SKILL.md` — line 1 matches the new red flag verbatim in
  spirit ("New work gets a worktree before it gets a commit"), line 2 matches the
  "write it, then create the worktree" red flag, line 3 matches "Both
  destinations, and neither is optional" / the `.workmux/` red flag, line 4
  matches the decision/boundary red flags, line 5 matches Step 2's shape section
  3 and the "section 2 that never says to run `/start-session`" red flag.
- `opening-a-change/digest.md` lines 1, 3, 4, 5 match Step 1, Step 2/4, Step 5,
  and Step 6/red-flags respectively, verbatim in substance.
- One imprecision, not incorrect: line 2 ("A body without one is what a skipped
  panel looks like") drops the SKILL's actual point that an empty body is
  *ambiguous* between "skipped" and "ran clean" ("a body carrying no panel is
  also what a clean pass looks like"). The digest states it as certainty. It's a
  defensible compression under a 500-char budget and the actionable rule (always
  include the table) survives, so this is a NIT, not a defect.
- Both new files are plain UTF-8 text ending in a single newline, no symlinks, no
  embedded newlines that would forge a second bullet — confirmed live via
  `wfctl hook user-prompt`, all three digests render as single flat lines with no
  truncation ellipsis.
- Character counts: `opening-a-change/digest.md` 471, `worktree-handoff/digest.md`
  491, both under `_DIGEST_MAX_CHARS` (500) with margin against future edits.

## Pass 2 — Security

Not applicable to this diff. No user/external input, no new code path — `_hook_
user_prompt`, `_digest_text`, symlink/binary/loop handling are all pre-existing
and untouched. The new files are static prose read through the same hardened
path every other skill's digest already goes through.

## Pass 3 — Architecture

- Fits the existing pattern exactly: this is the second and third `digest.md` in
  the tree, using the same "numbered, imperative, no prose" shape as the sole
  precedent (`conversation-response-shape/digest.md`).
- The `worktree-handoff/SKILL.md` red flag is added *before* the digest that
  projects it, in the same commit, in the same order the issue's own argument
  requires ("a digest line with no home in its skill is a rule with no reasoning
  behind it"). Checked: no digest line in either file projects a rule its
  `SKILL.md` doesn't state.
- Scope: `46ce498` ("chore(lock): sync uv.lock to the 0.18.0 version key") is
  bundled into this branch's diff against `origin/main` but has nothing to do
  with digests — it's a lock-drift fix for the *previous* release commit
  (`cef6d5b`, already the tip of `origin/main`). Harmless and independently
  correct, but it's scope creep in the diff being reviewed: a reader diffing this
  branch for "what did the digests change" gets an unrelated `uv.lock` hunk. NIT,
  not a blocker — doesn't touch behavior and CLAUDE.md's "not every change needs
  the spec pipeline" already covers a chore this size.
- Spec pipeline: no `specs/241-digests-for-decaying-rules/{spec,plan}.md` exist
  (only this `reviews/` dir). Per `CLAUDE.md`'s `design-levels` exclusion ("bug
  fixes, copy edits and other trivial changes that introduce no new state"), and
  given issue #241 itself carries the full argument, acceptance criteria and
  measured A/B that a spec would otherwise hold, skipping the pipeline here is
  consistent with the project's own carve-out, not a gap.

## Pass 4 — Readability & simplification

- Both digests follow the shipped model's voice and structure; no comment
  narrates the change to a reviewer (the new `SKILL.md` red flag explains *why*
  the rule exists, citing `#241` the same way sibling red flags in the same file
  already cite `#206` — consistent with existing convention, not a new one).
- Nothing to shrink — content is already at the density the format demands.

## Pass 5 — Performance

Fixed, bounded cost stated in the issue and reproduced here: three digests now
total 1455 chars/turn (493 + 471 + 491) against the issue's own "~1400" estimate
— close enough that the issue's stated budget claim holds. No loop, no N+1, no
unbounded growth: the hook still globs a fixed, small set of installed skills.

## Pass 6 — Over-engineering

Nothing to cut. Two data files and a five-line doc addition; no new
abstraction, no config, no speculative flexibility. `Lean already.`

## Verdict

net: 0 lines removable — this diff is lean already.

No BLOCKERs, no WARNINGs. Two NITs (author's discretion):
- NIT `opening-a-change/digest.md:2` — states "skipped panel" as certain where
  the source skill treats an empty-panel body as ambiguous (could also be a
  clean pass). Acceptable compression; reword only if a future edit gives it
  room.
- NIT `uv.lock` (`46ce498`) — unrelated lock-sync chore riding along in this
  branch's diff against `origin/main`. Correct and harmless; would read cleaner
  as its own change against `main` directly.

**Approve.**

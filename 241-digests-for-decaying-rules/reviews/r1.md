## Review: 241-digests-for-decaying-rules (origin/main...HEAD)

Passes run, against `git diff origin/main...HEAD` (4 files: `uv.lock`,
`opening-a-change/digest.md` new, `worktree-handoff/SKILL.md`,
`worktree-handoff/digest.md` new). Context read: issue #241 (`gh issue view
241`), `CLAUDE.md`, `conversation-response-shape/SKILL.md` (the digest
contract), both full skills' `SKILL.md`, `_digest_text`/`_DIGEST_MAX_CHARS` in
`wfctl/cli.py`, and the digest-related tests.

**1. Correctness** — Checked: does each digest bullet correspond to a rule
actually stated in its `SKILL.md` (the contract: "It projects this file and
never sources it: growth goes here, cuts there"). Traced all 5 bullets in each
new digest back to specific sections/red-flags in both `SKILL.md` files — all
10 have a home (worktree-handoff rule 1 is backed by the red flag this same
diff adds, which is why that red flag exists). Verified char budget by running
the actual flattening logic (`" ".join(text.split())`): `opening-a-change`
459 chars, `worktree-handoff` 479 chars, both under `_DIGEST_MAX_CHARS = 500`,
neither truncates. Ran `uv run wfctl hook user-prompt` against this worktree's
already-installed `.agents/` (confirmed byte-identical to the source under
review, so this was read-only) — all three digests print, none ends in `…`.
Ran the full suite: `uv run pytest -q` → 1033 passed, including
`test_no_shipped_digest_is_truncated_by_the_hook_that_reads_it` and all of
`test_hook_user_prompt.py`.

Checked against issue #241's acceptance criteria, since that's the diff's
stated spec:
- Two digests under 500 chars — done.
- Rules not prose, numbered/imperative like the existing digest — done, same
  shape as `conversation-response-shape/digest.md`.
- Per-turn budget stated with the number — done, but only in the commit body
  (493 → 1431 chars), not in any file. Fine per this project's convention
  (reasoning lives in commit bodies), just noting where it lives.
- "No digest on the remaining 31, and the change says why not" — done in the
  commit body (a digest on every skill is a second copy of the skill tree).
- The behavioral AC — **not evidenced anywhere in the diff or the commit
  message**. See WARNING below.

**2. Security** — N/A. No user input, no secrets, no new attack surface. The
existing hardening in `_digest_text` (path-confined resolve, symlink-loop
guard, UTF-8 guard, whitespace flattening) already covers arbitrary
`digest.md` content, and these two files are wfctl-authored, not
consumer-supplied.

**3. Architecture** — Fits the existing one-`digest.md`-per-skill pattern
exactly; no new mechanism introduced. `worktree-handoff/SKILL.md`'s red flag
is placed first in its list, ahead of the pre-existing flags — reasonable
given the commit frames it as the failure the skill was written for.

**4. Readability** — Both digests match the existing digest's voice (numbered,
imperative, no first person). `SKILL.md governs; these break first.` header
line is copied verbatim across all three digests now, keeping the format
literal rather than each skill inventing its own framing — good.

**5. Performance** — N/A (static text, hook already bounds the read/flatten
cost per skill).

**6. Over-engineering** — Nothing to cut. Two small, capped content files and
one paragraph. `net: 0 lines possible. Lean already.`

---

WARNING `wfctl/agents/skills/worktree-handoff/digest.md` (and the commit that
adds it) — Issue #241 states its own priority order explicitly: *"The
behavioural one first, because the others can all pass while the digest does
nothing... The negative arm is the whole test; an arm that was never run
without the digest has measured nothing."* Neither the diff nor commit
`77e8403`'s message (which reads "Closes #241") contains or references that
A/B result, and no PR is open yet (`gh pr list --head
241-digests-for-decaying-rules` is empty) to check for it there either. The
same commit message is also silent on the issue's second AC — whether a
subagent actually receives the `UserPromptSubmit` re-injection at all, which
the issue flags as a separate claim from "the content is right." → Before
this closes #241, either link/record the A/B run (clean-context agent with
digest reaches for a worktree; without it, doesn't) and the subagent-delivery
check, or soften the "Closes #241" claim to the subset actually verified here
(content correctness, budget, char cap).

NIT `uv.lock` — This hunk (`0.17.0` → `0.18.0` for the `wfctl` self-entry) is
commit `46ce498`, a standalone lockfile-drift fix unrelated to the digest
feature (`cef6d5b`'s release commit bumped `pyproject.toml` but not the
lock's own record). It's legitimate and independently justified, but riding
along in this branch means a reviewer of the "feat(skills)" diff sees an
unrelated version-key change with no connection to the stated subject. →
Land it on `main` directly (or note in the PR body that it's carried
incidentally from a rebase), so the feature diff doesn't need `uv.lock`
explained.

NIT `wfctl/agents/skills/opening-a-change/digest.md:L8` — `Closes #N` picks
the GitHub-specific keyword as the shorthand for a rule the `SKILL.md`
deliberately states tracker-agnostically (`Closes #123` *or* Jira's `Fixes
PROJ-45`, Step 4). Fine as a terse example under a 500-char cap, but a reader
on a Jira-tracked repo could read it as the literal required syntax. → Not
worth spending chars on, just flagging the narrowing; author's call.

net: 0 lines possible. Lean already.
Verdict: Approve, with the WARNING resolved (or explicitly deferred with the
A/B result linked) before the commit's "Closes #241" claim stands.

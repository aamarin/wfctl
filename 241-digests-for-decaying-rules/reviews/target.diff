diff --git a/uv.lock b/uv.lock
index bfdfb37..1c9f933 100644
--- a/uv.lock
+++ b/uv.lock
@@ -356,7 +356,7 @@ wheels = [
 
 [[package]]
 name = "wfctl"
-version = "0.17.0"
+version = "0.18.0"
 source = { editable = "." }
 dependencies = [
     { name = "rich" },
diff --git a/wfctl/agents/skills/opening-a-change/digest.md b/wfctl/agents/skills/opening-a-change/digest.md
new file mode 100644
index 0000000..456f6c8
--- /dev/null
+++ b/wfctl/agents/skills/opening-a-change/digest.md
@@ -0,0 +1,9 @@
+SKILL.md governs; these break first.
+1. Run the review panel first, on every change — in an unattended run nobody
+   says "review this".
+2. Its disposition table goes in the body. A body without one is what a skipped
+   panel looks like.
+3. Read the repo's template before writing a line. Every section, in its order;
+   "N/A" over deletion, and Review Panel takes no "N/A".
+4. `--body-file`, never `--body`.
+5. An open PR is not done: sidebar, reviewers, `Closes #N`.
diff --git a/wfctl/agents/skills/worktree-handoff/SKILL.md b/wfctl/agents/skills/worktree-handoff/SKILL.md
index 7333fe4..b71feb5 100644
--- a/wfctl/agents/skills/worktree-handoff/SKILL.md
+++ b/wfctl/agents/skills/worktree-handoff/SKILL.md
@@ -168,6 +168,11 @@ conversation.
 
 ## Red flags
 
+- Doing the work in the checkout you are standing in. This skill's triggers are
+  all phrases a human says, and the failure it was written for is the turn where
+  nobody says one: a session on `main` that identified the next piece of work and
+  offered to take it there (#241). New work gets a worktree before it gets a
+  commit, and the session that spots the work is the one that owes the handoff.
 - Opening with "implement issue #N" on work that needed a spec first. The verb
   in line 1 is the route the child takes when nothing else names one, and by the
   time `/start-session` disagrees the code is written.
diff --git a/wfctl/agents/skills/worktree-handoff/digest.md b/wfctl/agents/skills/worktree-handoff/digest.md
new file mode 100644
index 0000000..c9f6a16
--- /dev/null
+++ b/wfctl/agents/skills/worktree-handoff/digest.md
@@ -0,0 +1,9 @@
+SKILL.md governs; these break first.
+1. New work gets its own worktree. A branch in the main checkout is the
+   failure, not a shortcut.
+2. Write the handoff to a file before `wm add`, never after — the prompt is
+   only injected at add time.
+3. Both slots: `--prompt-file`, and the state dir's `session-summary.md`.
+   `.workmux/` alone dies with the worktree.
+4. Every decision carries its argument; every boundary carries its reason.
+5. Name the route, and say to run `/start-session`.

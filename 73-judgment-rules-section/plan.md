# Implementation Plan: Judgment rules section for conversation-response-shape

**Branch**: `73-judgment-rules-section` | **Date**: 2026-08-23 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/Users/andremarin/Development/wfctl-specs/73-judgment-rules-section/spec.md`

## Summary

Add a "judgment rules" section to `conversation-response-shape` for rules whose
compliance can't be checked by inspecting the output (enumerate real states,
sections repeat one shape, the drawing leads), separate from the existing
checkable "Show, to simplify the description" rules from #72. Retitle that
existing section so it states the drawing *is* the description, and amend rule
3's architecture row to require a leading diagram over tables for structured
content. Doc-only content change to one already-existing skill file; no new
abstractions, dependencies, or state.

## Technical Context

**Language/Version**: Markdown (skill content). Repo tooling that validates it:
Python 3.11+ via `uv run`.
**Primary Dependencies**: None new. Uses the existing `wfctl` skill-packaging
pipeline (`wfctl/agents/` → package data → `wfctl install-skills`).
**Storage**: N/A
**Testing**: `uv run pytest -q` (skill packaging / cross-reference checks —
`tests/test_skill_cross_references.py` asserts `conversation-response-shape` is
loaded and cross-referenced, not its section wording, so the rename is safe),
`wfctl install-skills` + `wfctl doctor` (confirms the edit reaches `.agents/`
install output), manual read-through diff against FR-001…FR-009. No live agent
behavioral test (per spec Clarifications — doc-only diff is the validation bar).
**Target Platform**: Ships to every downstream repo via `wfctl install-skills`
(base bundle, cross-agent).
**Project Type**: Single project (CLI + packaged skill content); this feature is
a content-only change within it.
**Performance Goals**: N/A
**Constraints**: Minimal-complexity bias. Edit only the source file — see
Project Structure below; never edit `.agents/` directly (gitignored install
output, per `AGENTS.md`'s layer model — a change there ships nothing). Never
edit `i-have-adhd` (vendored, upstream-owned). Leave the existing wfctl-specific
example in the #72 checkable section untouched (out of scope, flagged as a
follow-up per spec).
**Scale/Scope**: One skill file: one new section, one section retitle, one rule
row amendment.

## Constitution Check

_GATE: Must pass before Phase 0 research. Re-check after Phase 1 design._

No `.specify/memory/constitution.md` exists in this repo. Gates below are
substituted from `AGENTS.md`'s documented conventions (Definition of done, the
layer model, skill-authoring rules) — recorded here per the plan template's
instruction, since a gate with no source is decorative.

- [x] Validation plan exists: `uv run pytest -q`, `wfctl install-skills` +
      `wfctl doctor`, and a manual FR-by-FR read-through diff — see Testing
      above.
- [x] Complexity is justified: no added abstraction, infrastructure, or
      dependency — this is a content edit to one existing file.
- [x] Ownership is stated: this feature introduces no new state or derived
      value (documentation content only) — N/A.

## Project Structure

### Documentation (this feature)

```text
/Users/andremarin/Development/wfctl-specs/73-judgment-rules-section/
├── spec.md               # Feature specification (/speckit.specify)
├── plan.md               # This file (/speckit.plan)
├── research.md           # Phase 0 output
└── (no data-model.md / contracts/ / quickstart.md — see Phase 1 below)
```

This repo (`wfctl`) records a spec root *outside* the working tree — `specs/` in
the worktree is gitignored scratch; `wfctl feature-paths` resolved the durable
location above. The `/speckit.specify` skill's own hardcoded path predates that
resolver and pointed at the worktree-local `specs/` dir; artifacts were moved to
the durable location during `/speckit.clarify`.

### Source Code (repository root)

```text
wfctl/agents/skills/conversation-response-shape/SKILL.md   # edit target — the source of truth (committed)
.agents/skills/conversation-response-shape/SKILL.md         # install output — gitignored, regenerated by
                                                              # `wfctl install-skills`, never edited directly
tests/test_skill_cross_references.py                        # existing test; asserts the skill loads and
                                                              # cross-references — checked, does not pin
                                                              # section titles or rule wording, so unaffected
```

**Structure Decision**: Single-file content change. No new files, no new test
files — the existing cross-reference test already covers what could break.

## Complexity Tracking

> No Constitution Check violations. Table intentionally omitted.

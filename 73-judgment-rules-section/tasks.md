# Tasks: Judgment rules section for conversation-response-shape

**Input**: Design documents from `/Users/andremarin/Development/wfctl-specs/73-judgment-rules-section/`
**Prerequisites**: plan.md, spec.md, research.md (no data-model.md/contracts/ — none apply, see research.md)

**Tests**: This is a doc-only content edit (per spec Clarifications — no live agent
behavioral test). Verification is the existing skill-cross-reference test plus
grep-based structural checks and a manual read-through against each FR — see each
phase's Verification block.

**Organization**: Grouped by user story. All edits land in the same file
(`wfctl/agents/skills/conversation-response-shape/SKILL.md`) but at disjoint
regions; US3 is independent of US1/US2, US2 depends on US1 (it appends into the
section US1 creates).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files/regions, no dependencies). Since
  every task here edits the same file, `[P]` is omitted throughout — treat all
  tasks as sequential within a session, but US3 may be done before or after
  US1/US2 without changing the outcome.
- **[Story]**: US1, US2, US3 — maps to the user stories in spec.md
- Line numbers below are pre-edit, verified against
  `wfctl/agents/skills/conversation-response-shape/SKILL.md` as of 2026-08-23.
  Re-locate by heading/grep after the first edit shifts line numbers:

  ```bash
  grep -n "^## \|tables over prose" wfctl/agents/skills/conversation-response-shape/SKILL.md
  ```

- **Edit target**: `wfctl/agents/skills/conversation-response-shape/SKILL.md` —
  the committed source (per plan.md's layer-model research). Never edit
  `.agents/skills/conversation-response-shape/SKILL.md` — gitignored install
  output, regenerated by `wfctl install-skills`.
- **Rule text**: The three judgment rules' wording is already drafted in issue
  #73 and is settled content, not open for rewording (per spec Assumptions) —
  reuse it verbatim rather than re-deriving it.
- **Examples**: Each of the three rules gets one **after-only illustration** —
  a small table or mini diagram sized to what the rule needs (not a literal
  one-liner, not a ✗/before counterpart, not a narrative build-up). The
  illustration carries the structure so the annotation stays to one line,
  same principle as the existing "Show" rule applied to itself. Rich
  before/after narratives (the PR #72 style) belong in the PR description, not
  the skill file.

## Path Conventions

Flat single-package layout: `wfctl/` and `tests/` at repository root. No `src/`.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Establish a known-green baseline so any later failure is
attributable to this feature rather than inherited.

- [X] T001 Confirm baseline: `git status --short wfctl/agents/skills/conversation-response-shape/`
      is clean, `diff wfctl/agents/skills/conversation-response-shape/SKILL.md .agents/skills/conversation-response-shape/SKILL.md`
      reports identical, and `uv run pytest -q tests/test_skill_cross_references.py`
      passes before any edit

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: None required.

This feature is additive content in one existing file with no shared module,
schema, or abstraction. US3 touches a disjoint region and blocks nothing; US2's
dependency on US1 (appending into the section US1 creates) is captured directly
in that story's phase below, not as separate foundational work. Recorded
explicitly rather than filled with invented scaffolding.

---

## Phase 3: User Story 1 — Judgment rules get a documented home (Priority: P1) 🎯 MVP

**Goal**: A "Judgment rules" section exists, framed as requiring re-derivation
rather than inspection, containing "enumerate real states" and "sections repeat
one shape."

**Independent Test**: Read the skill file; confirm the new section header, its
framing sentence, and both rules are present and distinct from the checkable
section.

**Verification**:

- Automated: `uv run pytest -q tests/test_skill_cross_references.py`
- Manual: `grep -n "^## Judgment rules" wfctl/agents/skills/conversation-response-shape/SKILL.md`
  returns one match; read-through against FR-001, FR-002, FR-003
- Evidence: the section's framing sentence lets a reader sort a new candidate
  rule into checkable vs. judgment without consulting issue #73 (SC-001)

### Tasks

- [X] T002 [US1] Insert a new `## Judgment rules` section into
      `wfctl/agents/skills/conversation-response-shape/SKILL.md`, placed after
      the retitled "Show" section and before `## Two surfaces that are not
      prose` (currently line 208) — open with one framing sentence stating
      these rules are verified by re-deriving the judgment behind them, not by
      inspecting the rendered output (FR-001); verify with
      `grep -n "^## Judgment rules" wfctl/agents/skills/conversation-response-shape/SKILL.md`
- [X] T003 [US1] Add the "enumerate real states" rule under the new section
      (drafted text, issue #73: a property that varies across every row is a
      column, not a row; two states that leave identical output are one state
      reached two ways), illustrated with a small after-only table (2–3 rows,
      each a genuinely distinct state — no ✗ counterpart, no narrative) —
      depends on T002; verify by reading the added text against FR-002
- [X] T004 [US1] Add the "sections repeat one shape" rule under the new section
      (drafted text, issue #73: sections are named concepts, and every section
      in a set holds the same slots in the same order), illustrated with a
      small after-only skeleton showing two sections sharing the same slot
      order — depends on T002; verify by reading the added text against FR-003
- [X] T005 [US1] Validate Phase 3 with `uv run pytest -q tests/test_skill_cross_references.py`
      and `wfctl install-skills && wfctl doctor` (confirms the edit reaches
      install output cleanly) — merge gate

**Checkpoint**: The judgment-rules section exists and is independently
readable/testable — MVP for this feature.

---

## Phase 4: User Story 2 — Structured subjects get a leading drawing (Priority: P2)

**Goal**: "The drawing leads" rule is filed in the judgment section, and rule
3's architecture row requires a leading diagram over tables for structured
content.

**Independent Test**: Read the rule text and the amended architecture row;
confirm the wording requires a diagram before prose for structured subjects. No
live agent prompt is required (spec Clarifications).

**Verification**:

- Manual: read-through against FR-004, FR-005; `grep -n "tables over prose" wfctl/agents/skills/conversation-response-shape/SKILL.md`
  returns no match (old phrasing fully replaced)
- Automated: `uv run pytest -q tests/test_skill_cross_references.py`

### Tasks

- [X] T006 [US2] Add "the drawing leads" rule to the `## Judgment rules` section
      created in T002 (drafted text, issue #73: when the subject has
      structure — a pipeline, a control plane, a routing decision, a layered
      architecture — the drawing goes before the explanation; the prose then
      covers only what the picture cannot say), illustrated with a mini ASCII
      diagram (same shape as the `/end-session` example in the #72 checkable
      section) plus one annotation line — no ✗ counterpart — depends on T002;
      verify by reading the added text against FR-004
- [X] T007 [US2] Amend rule 3's architecture row in the table at
      `wfctl/agents/skills/conversation-response-shape/SKILL.md:142` from
      "Structured and complete; tables over prose" to "a leading diagram, then
      named sections; tables where the content is genuinely tabular" (FR-005);
      verify with `grep -n "tables over prose" wfctl/agents/skills/conversation-response-shape/SKILL.md`
      returning nothing
- [X] T008 [US2] Validate Phase 4: the two greps above pass and
      `uv run pytest -q tests/test_skill_cross_references.py` passes — merge
      gate

**Checkpoint**: Structured-subject responses have a documented, judgment-graded
rule and an amended architecture row consistent with it.

---

## Phase 5: User Story 3 — The section title stops undercutting the rules (Priority: P3)

**Goal**: "Show, to simplify the description" is retitled to state the drawing
*is* the description, not a follow-on illustration.

**Independent Test**: Read the new title; confirm it does not describe the
drawing as something that "simplifies" prose written first.

**Verification**:

- Manual: read the title against FR-006
- Automated: `uv run pytest -q tests/test_skill_cross_references.py` (does not
  pin this title — confirmed in research.md)

### Tasks

- [X] T009 [US3] Retitle `## Show, to simplify the description` at
      `wfctl/agents/skills/conversation-response-shape/SKILL.md:175` to a
      title stating the drawing is the description and prose annotates it —
      exact wording is an editorial choice per spec Assumptions, must satisfy
      FR-006's framing; verify by reading the new title against FR-006
- [X] T010 [US3] Validate Phase 5 with `uv run pytest -q tests/test_skill_cross_references.py`
      — merge gate

**Checkpoint**: All three user stories land; the skill now has a judgment-rules
home, a drawing-leads rule with a consistent architecture row, and a title that
doesn't undercut it.

---

## Phase 6: Polish & Cross-Cutting Concerns

- [X] T011 Confirm FR-007/SC-002: no wfctl-specific example strings were
      introduced in the new judgment section — `sed -n '/^## Judgment rules/,/^## Two surfaces/p' wfctl/agents/skills/conversation-response-shape/SKILL.md | grep -iE "wfctl |implement [0-9]/[0-9]"`
      returns nothing (the pre-existing example in the #72 checkable section is
      untouched by design, per Clarifications)
- [X] T012 Run the full definition of done from `AGENTS.md`: `uv run pytest -q`,
      `uv run ruff check wfctl/ tests/`, `uv run mypy wfctl/`, then
      `wfctl install-skills && wfctl doctor` — all green — final merge gate

**Implementation note**: the plain `wfctl install-skills`/`wfctl doctor` (the
globally `uv tool install`-ed binary) reported clean without ever picking up
this session's source edits — it runs a frozen package-data copy from whatever
git ref it was last installed from, not this working tree. `uv run wfctl
install-skills` / `uv run wfctl doctor` (the local dev entrypoint) is what
actually reads `wfctl/agents/` live and is what T005/T008/T010/T012 above must
use. `uv run mypy wfctl/` also failed to spawn; `uv run --extra dev mypy wfctl/`
is correct. Worth a follow-up: `AGENTS.md`'s own Definition of Done section
doesn't say `--extra dev`, and neither it nor the task template flags the
global-vs-local install split — both cost a detour here.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — start immediately
- **Foundational (Phase 2)**: None required (see Phase 2 note)
- **User Stories (Phase 3–5)**: US1 (Phase 3) has no dependency; US2 (Phase 4)
  depends on US1's T002 (appends into the section it creates); US3 (Phase 5) is
  independent of both and may be done in any order relative to them
- **Polish (Phase 6)**: Depends on all three stories being complete

### User Story Dependencies

- **User Story 1 (P1)**: No dependencies — can start after Setup
- **User Story 2 (P2)**: Depends on US1 (T002 creates the section T006 appends
  into)
- **User Story 3 (P3)**: No dependencies on US1/US2 — different region of the
  same file

### Within Each User Story

- Rule-text additions before the phase's validate/checkpoint task
- Story complete before moving to the next priority (or interleave US3 freely,
  since it's independent)

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup
2. Complete Phase 3: User Story 1 — the judgment-rules section exists
3. **STOP and VALIDATE**: run Phase 3's Verification block independently
4. This alone resolves issue #73's core ask; US2/US3 refine it

### Incremental Delivery

1. Setup → Phase 3 (US1) → validate → the section exists (MVP)
2. Phase 4 (US2) → validate → drawing-leads rule + consistent architecture row
3. Phase 5 (US3) → validate → title stops undercutting the rules
4. Phase 6 → full `AGENTS.md` definition-of-done sweep

## Logical PR Boundaries

All three stories are small enough, and tightly enough related (one issue, one
skill file, one section), to ship as a single PR — matches this repo's "one PR
closes one issue" convention (`AGENTS.md`). Do not split into three PRs; there's
no independent deployment value in landing "the section exists" without its
third rule and the title fix that makes it consistent.

---

## Notes

- No `[P]` markers: every task edits the same file. Parallelism here means
  logical independence (US3 vs. US1/US2), not concurrent execution.
- Commit after each phase's validate task, not after every rule addition —
  matches the PR-boundary note above.
- The pre-existing wfctl-specific example in the #72 checkable section
  (`wfctl end` / `implement 3/8`) is deliberately untouched — flagged as a
  follow-up outside this issue's scope (spec Assumptions), not a task here.

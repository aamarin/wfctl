# Review: `git diff main...chore/ponytail-audit-cuts` (r1)

One commit, `eb61482 chore(ponytail): apply nine audit cuts, and say why two are held`.
19 files, +325 / −1419.

## Findings

```
WARNING  docs/architecture/views/current-state.md:L36 — `_io … 0 out · 4 in` and
         the importer annotation drops `_tracker`, which still imports `_io`.
         Correct count is 5 → restore `_tracker` and write `0 out · 5 in`.
WARNING  docs/architecture/views/current-state.md:L197 — "Four functions:
         write_json_atomic and write_md_atomic … load_agentconfig" names three
         symbols this diff deletes → rewrite for `write_atomic` + `append_event`.
WARNING  docs/architecture/io-owns-durability-not-domain-files.md:L14,L37,L48 —
         record (status: proposed) still diagrams the three deleted names and
         reserves `load_agentconfig`'s deletion for #149 phase 2 → record that
         half of its Decision has landed early, or leave the function alone.
WARNING  PONYTAIL-DEBT.md:L31 — "6 markers" but the tree carries 9 → harvest
         docstring markers too, or say the harvest is #-comments-only.
WARNING  wfctl/_shape.py:L136 — `_blocks` now emits the tail of an *unclosed*
         fence as a block; `_split_fences` dropped it → add a test + a docstring
         sentence, or restore the drop.
WARNING  wfctl/_shape.py:L152 — `_prose` docstring claims "counting it inflated
         the word count"; the old code excluded delimiters too, so that never
         happened → state it as the walk's contract, not a fixed bug.
WARNING  wfctl/_md.py — new module, no `tests/test_md.py`; the four closing
         rules it now owns for `_arch`/`_body` are pinned by no test → one
         table-driven test over `walk`.
NIT      wfctl/_arch.py:L339 — new `_unfenced` docstring omits the two rules
         that actually changed for this caller (4-space indent, full run length
         + no info string on close) → one sentence naming them.
NIT      wfctl/_arch.py:L313 — comment "`_unfenced` can truncate" is now false;
         `_md` keeps the whole delimiter run → drop the contrast.
NIT      wfctl/_shape.py:L141 — `_blocks` no longer emits `(n, [])` for an empty
         fence pair. Inert; recorded so it is not later read as a regression.
NIT      PONYTAIL-DEBT.md:L21,L24 — `cli.py:1747` / `_shape.py:111` are off by
         one against the tree (`1748` / `112`).
NIT      PONYTAIL-DEBT.md:L14 — "plus one marker added the same day and not yet
         committed" describes a pre-commit state → and root placement is a new
         convention; `docs/` is where this repo's prose lives.
NIT      tests/test_skill_attribution.py:L155 — docstring says the prose below
         the copyright line names `github-issue-template.md` "in order to say it
         is *not* derived". It now names four removed files instead. The bound
         is still needed; the stated reason is gone.
```

`net: −1094 already cut; −0 further lines possible.` Lean.

**Verdict: Request changes** (0 blockers, 7 warnings — two of them factual
errors introduced in a file this diff edits).

---

## Passes run, and what each checked

### Definition of done (CLAUDE.md)

| Command | Result |
|---|---|
| `uv run pytest -q` | 1136 passed in 65.78s |
| `uv run ruff check wfctl/ tests/` | All checks passed |
| `uv run mypy wfctl/` | Success, 20 source files |

`uv run wfctl doctor` and `wfctl verify` deliberately **not** run — the review
brief forbids writing outside the worktree. CLAUDE.md's "653 tests" is itself
stale against 1136; not this diff's problem, noted in passing.

### 1. Correctness — the fence-walker consolidation

The three call sites had genuinely different rules on `main`. I reimplemented
each pre-change function verbatim from `main` and ran it against the post-change
code over 13 synthetic fence documents plus the repo's own corpora.

| Case | old → new |
|---|---|
| `_shape._prose` | identical on all 13 cases |
| `_shape._blocks` | **differs**: unclosed fence, empty block |
| `_body._blank_fences` | **differs**: close carrying an info string |
| `_arch._unfenced` | **differs**: 4-space indent, nested `` ``` `` in a `` ```` ``, info-string close, short close |

Corpus differential (the check that decides whether any of this is live):

- `docs/architecture/**/*.md`, 23 files, through `_arch._unfenced` — **0 files differ**.
- `wfctl/agents/`, `docs/`, `wfctl/specify/`, 104 `.md` files, through
  `_body._blank_fences` and `_shape._blocks` — **0 files differ**.

So the divergences are latent, not active. Three of the four are the intended
strictening (CommonMark's closing rule), and are correct: a close now needs the
same character, a run at least as long, and no info string. The two I am
reporting are the ones nothing in the diff claims:

**`_blocks` and the unclosed fence (WARNING).** `_split_fences` appended to
`blocks` *only* on the closing branch, so a fence that never closed contributed
no block. `_blocks` groups on `Line.opened`, which is set for exactly those
lines, so the tail is now a block and `body_findings` can emit
`SKILL.md:322 — the fenced block at line N …` against it. That is arguably the
better reading, but the mirror case for prose is pinned by
`tests/test_shape.py:144 test_an_unclosed_fence_drops_its_tail_rather_than_scanning_it`,
so the module now reads as if both halves drop the tail and only one does.

**`_arch`'s two new rules (NIT).** `_unfenced` on `main` matched a fence via
`stripped[:3]`, so *any* indentation opened one and the marker was truncated to
three characters. It now inherits `^ {0,3}` and full-run comparison. Both are
more correct; neither is mentioned in the docstring the diff rewrote, which
discusses only the index conversion and the delimiter exclusion. A record with a
fence indented four or more spaces — a fence nested inside a numbered list item
is the realistic shape — would now have its contents scanned for `## Decision`.
Nothing in the corpus does this today.

`walk_lines`' keepends handling checks out: `.` does not cross `\n` and `$` sits
before it, so `` ```\n `` and `` ```\r\n `` both yield an empty info string and
close. `_arch` calls `_unfenced` with keepends lines (`_arch.py:410` → `_log_bounds`)
and without (`_arch.py:251`), and both go through the same primitive.

### 1b. Correctness — the `_io` merge

Nothing lost, verified line by line:

- `write_json_atomic` was `os.fdopen(fd, "w")` + `json.dump(data, f, indent=2)`;
  the three JSON call sites (`_session.grant_auto_approve`, `_verify.write_record`,
  `cli._write_settings`) now pass `json.dumps(data, indent=2)` into
  `write_atomic(..., newline=None)`. Byte-identical output — neither form ever
  emitted a trailing newline, and `_write_settings` still appends its own.
- Identical `mkstemp` / `os.replace` / `unlink`-on-failure path and identical
  `FileNotFoundError` precheck; `_write_settings` still resolves the target and
  restores the mode after replace.
- `newline=""` reaches `_arch.supersede` unchanged.
- The atomicity tests moved with the rename and still exercise the
  `os.replace`-raises and no-orphan-`.tmp` paths.

The only real cost is `json.dumps(data, indent=2)` spelled at three call sites
instead of once. That is the trade the audit's finding 8 names, and it is the
right one — nothing now forces a JSON writer through a signature that cannot
take `newline`.

`load_agentconfig` and `_body._RESULT_CELLS`: grep across `wfctl/` and `tests/`
confirms zero readers of either. Chesterton's fence applies to `load_agentconfig`
and the fence has a note on it — `io-owns-durability-not-domain-files` already
argued for its deletion. That record is why WARNING 3 exists, not a reason to
keep the function.

### 1c. Correctness — deleting bundled files

- Whole-repo grep for all four basenames: the only hits are `PONYTAIL-DEBT.md`,
  `docs/architecture/vendor-upstream-skills.md`, `wfctl/specify/templates/NOTICES.md`
  and one test docstring. No skill, command, hook, `common.sh`,
  `check-prerequisites.sh` or `setup-plan.sh` invokes any of them.
- `git log -S create-new-feature -- wfctl/agents/` is empty, so no shipped
  command ever named it either — a consuming repo's older `.claude/commands/`
  cannot be holding a call to it that wfctl put there.
- Packaging is unaffected: `MANIFEST.in` uses `graft wfctl/specify`, not an
  enumeration.
- **Already-installed repos:** the four paths stay on disk and fall out of the
  bundle, so `_check_abandoned_entries` (`cli.py:4475`) reports them and
  `install-skills --prune` removes them. `PONYTAIL-DEBT.md:60` states exactly
  this and points at #183 for the shape. Correct, and worth saying out loud that
  the consequence is a standing `doctor` finding in every consuming repo until
  someone prunes — `/start-session` runs `install-skills --yes` then `doctor`, so
  it will surface on the next session in each of them rather than quietly.

### 2. Security

Nothing touches input parsing at a trust boundary, credentials, subprocess
construction or path resolution. The deletions *remove* two shell scripts from
what `install-skills` seeds into a repo, which reduces the surface CLAUDE.md's
Safety section warns about. `write_atomic` still writes through `mkstemp` in the
target's own directory, so no tmpdir-symlink change. No finding.

### 3. Architecture

`_md` imports nothing from `wfctl` and is imported by `_arch`, `_body`, `_shape`
— no cycle, and the durability band's stated membership test ("would be correct
in a program that was not wfctl") holds for it. `tests/test_architecture_view.py`
re-derives the graph and passes, which covers the three fenced blocks at the foot
of the view. It does **not** cover the ASCII drawing or the prose, which is how
WARNING 1 and WARNING 2 got through green.

`Line` carries `opened` for one caller and `number` for one caller. The module
docstring pre-empts the yagni objection ("a walker that returned any one of them
would have the other two building it back from a lossy view") and I accept it —
`_shape` genuinely needs the opening line number in its finding text
(`_shape.py:231`), and `_arch` genuinely needs an index into its own list.

### 4. Readability

Comment accuracy is where this diff is weakest, and CLAUDE.md sets a high bar
for it ("Existing comments are dense with rationale — match that"). Three new
paragraphs explain excluding the fence delimiters as a decision this change
makes; in all three call sites the pre-change code already excluded them. Two
phrase it conditionally and are fine. `_prose`'s is past tense and asserts a
failure that never occurred — WARNING 6. `_arch.py:313`'s existing comment is
now simply false — NIT.

`_blocks`' `dict` + `sorted` round-trip is fine: `walk` yields in order and
openings are unique, so the sort is a formality, but it costs two lines and
documents the invariant.

### 5. Performance

`_md.walk` is one pass with one compiled regex per line, same as each of the
three it replaces; three walkers became one walker called three times, so no
change in asymptotics or constant factor. `json.dumps` in memory instead of
`json.dump` streaming, on payloads of a few hundred bytes. Nothing to quantify.

### 6. Over-engineering

The diff *is* the over-engineering pass, and it lands −1094 net. I found nothing
further worth cutting. Two things I checked and am deliberately not flagging:

- The `ponytail:` marker added at `cli.py:1748` for `_AGENT_SKILL_EXTRAS` is the
  right disposition — `_mirror_supersedes_wrapper` keys on membership, so an
  inlined call does lose something, and the marker names the trigger.
- Findings 5 and 6 held with the reason written down (four `.workmux.yaml` files
  still wiring `wfctl archive-story` into `pre_remove` under `|| true`) is the
  correct call, and holding them *with* the evidence is better than either
  cutting or silently skipping.

### Tests read first

`tests/test_io.py` and `tests/test_json_atomic_no_corruption.py` are renamed and
re-pointed, keeping every failure path. `tests/test_shape.py` and
`tests/test_body.py` are untouched and still pass, which is evidence the two
public entry points are unchanged on the shapes they cover — but nothing was
added for the rules that changed, hence WARNING 5 and WARNING 7.
`tests/test_skill_attribution.py` passes because `_template_claims` bounds each
section at its `    Copyright` line and the new NOTICES prose naming the removed
templates sits below it; its docstring rationale for that bound is now stale
(final NIT).

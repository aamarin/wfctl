# Review: `git diff main...HEAD` on `216-speckit-attribution` (reviewer r3)

Target: commit `863a089`, 17 files, +342/−56. Rubric: `.agents/skills/code-review/SKILL.md`,
all six passes. Nothing in the worktree was modified; no `wfctl` command was run.

## Findings

WARNING  docs/architecture/vendor-upstream-skills.md:L67-71 — the record now reads as an
exhaustive account of what wfctl derived from `github/spec-kit`, and eight files in the same
grafted tree are derived and undeclared: `wfctl/agents/commands/speckit.{analyze,checklist,
clarify,constitution,implement,plan,specify,tasks}.md`. Measured with the author's own
`score.py` against `templates/commands/*.md` at upstream main, the substantial lines that are
verbatim upstream are 8/11 (`speckit.tasks.md`), 7/10 (`plan`), 6/9 (`specify`), 4/7
(`clarify`), 4/8 (`constitution`), 2/5 each (`analyze`, `checklist`, `implement`). What is
copied is not interface: the whole `description:` string, the whole `handoffs:` block
(labels, agents and prompt text), and `You **MUST** consider the user input before proceeding
(if not empty).` `speckit.tasks.md`'s frontmatter is upstream's `tasks.md` frontmatter minus
the `scripts:` key. `MANIFEST.in`'s `graft wfctl/agents` ships them and `_BASE_TARGETS`
installs them into `.agents/commands/` and `.claude/commands/`, so this is the same
obligation on the same artifact as the eight skills the change does attribute.

This is not a defect the diff introduces — it is one the diff stops disclosing. `main` carried
"The table is not yet the whole answer, and says so rather than letting silence imply one …
A record that omits a known exception is read as denying it." The diff deletes that paragraph
and replaces it with one that explains every absence by name (`speckit-delivery-plan` and
`speckit-orchestrate` "are absent because they are wfctl's own"). After that edit, absence
from the table asserts wfctl's ownership, and for these eight files that assertion is false.
`speckit.decompose.md` is genuinely clean (1/9, and its one hit is the boilerplate line), so
the fix is not "attribute everything with a speckit prefix".

Fix, minimally and in scope: one sentence after L71 naming `wfctl/agents/commands/speckit.*.md`
as derived-but-not-yet-declared with the follow-up issue number, restoring the hedge the diff
removes. #218 does not cover this (it is about five unvendored sibling files); no open issue
does. The larger fix — a line and a row for the eight — is a separate change.

WARNING  docs/architecture/vendor-upstream-skills.md:L177-181 — the "Considered" list rejects
`wfctl/specify/NOTICES.md` because it "would ship in the wheel and never reach a project", and
the record keeps `wfctl/agents/NOTICES.md`, which has exactly that property. The diff's own new
test says so out loud: `tests/test_skill_attribution.py:L204` — "`wfctl/agents/NOTICES.md`
ships in the wheel and stops there — a top-level file in a grafted tree belongs to no install
layer". Verified: `_BASE_TARGETS` is `agents/skills` and `agents/commands` only, so a project
that runs `install-skills` receives `.agents/skills/speckit-*/SKILL.md` carrying "(MIT, ©
GitHub, Inc.)" and no permission notice anywhere, while `.specify/templates/NOTICES.md` does
arrive. A record that rejects a placement for a flaw its accepted counterpart has is
internally inconsistent. Fix: a sentence in Consequences stating that the skills tree's notice
reaches the wheel and not an installed project, and that this is accepted (or tracked) — the
asymmetry is now a decision, not an oversight, and should read as one.

WARNING  wfctl/specify/templates/NOTICES.md:L38 and docs/…/vendor-upstream-skills.md:L95-97 —
"a command spec-kit does not have" / "which spec-kit has no counterpart for" is false as
stated. Upstream ships `templates/commands/taskstoissues.md`, a tasks-to-GitHub-issues command,
at the commit this change pins and at main. The conclusion still holds — I scored
`github-issue-template.md` against it and got 0% — but the stated reason is a claim about
upstream's command set that a reader can check and find wrong, in a file whose whole purpose is
to be checkable. Fix: say what was measured. "scores 0% against every upstream template,
`templates/commands/taskstoissues.md` included."

WARNING  tests/test_skill_attribution.py:L199-214 —
`test_every_derived_template_that_ships_reaches_a_project` does not test that. It asserts
`SPECIFY_NOTICES.exists()` and that `wfctl/specify/NOTICES.md` does not, which is a statement
about two paths. The property it names depends on `cli._RUNTIME_TARGETS` still containing
`("specify/templates", ".specify/templates")` and on the copy loop still using `src.iterdir()`
(`wfctl/cli.py:L1129-1132`, `L2138`) — narrow that to a named-template list, or drop the
templates target, and the notice silently stops reaching any project while this test stays
green. One line closes it: assert the pair is in `_RUNTIME_TARGETS`. Its own docstring
("This asserts the placement rather than the file's existence") shows the author knew placement
was the point; the missing half is what makes placement sufficient.

NIT  docs/architecture/vendor-upstream-skills.md:L147 — "`common.sh` is 20% upstream" is not
reproducible against the tree it now describes. Running the author's own `score.py`: the
pre-change file scores exactly 20% against upstream main and the committed file scores 19%,
because the attribution line this diff adds is itself a substantial non-upstream line. Same
for the 82% at NOTICES.md:L41 — pre-change 82%, committed 81%. Also, the two figures are
against different refs: 82%/100% are against `fc3d124`'s parent (the ref the record pins), 20%
is against main, where the same file scores 39–40% against the pinned parent. Either name the
ref beside each number or round to "a fifth" / "four fifths"; a digit that the reader cannot
reproduce from the file in front of them is the kind of claim this record exists to prevent.

NIT  wfctl/agents/NOTICES.md:L38 — "whose notice travels with it in
`specify/templates/NOTICES.md`". Every other path in that file is relative to the file's own
tree or the distribution root, so `specify/` reads as `wfctl/agents/specify/`, which does not
exist. `../specify/templates/NOTICES.md` resolves from where the file sits.

NIT  docs/architecture/vendor-upstream-skills.md:L9 — the title, the class and both tables now
cover scripts and templates, but the Context still opens "Some skills wfctl ships come from
outside the project." First sentence of the record, and it is narrower than the record.

NIT  docs/architecture/vendor-upstream-skills.md:L103 — "#216 and #218 are what it missed"
reads stale the moment this merges, since #216 is what merges. "#213 is what that pass costs
when it is deferred; #216 and #218 are what it had missed by then" keeps the history true.

NIT  docs/architecture/vendor-upstream-skills.md:L191 — reflowed line overruns the file's
~79-column wrap ("… which is what let the"). Cosmetic, but it is the one line in the diff that
does it.

NIT  knowledge-placement — the rationale "a template carries no line because `cp` puts it in
the reader's own document" now lives in four places: the record's Decision (L73-79), the
record's Considered bullet (L172-176), `wfctl/specify/templates/NOTICES.md:L9-13`, and
`tests/test_skill_attribution.py:L14-17`. The record earns its exception for line-vs-row (they
are different facts); these four are the same fact four times and will drift. The NOTICES.md
copy is the one that has to be there — it ships. Consider making the two record passages one.

NIT  tests/test_skill_attribution.py:L84-102 — `shrink:` `_listed()` and `_listed_templates()`
each read `RECORD` and re-run `_ROW.findall`. One `_rows()` helper feeding both is shorter and
removes the chance of the two parses disagreeing after a regex edit.

NIT  tests/test_skill_attribution.py:L63 — `_attributed()` globs `scripts/**/*.sh`. Upstream
now ships `scripts/python/`, and wfctl's runtime target is the whole `specify/scripts`
directory, so a non-`.sh` runtime file added later carries no line-check at all. Widening to
`scripts/**/*` and matching the line only where it is found would keep the check honest as the
tree changes.

NIT  wfctl/specify/scripts/bash/*.sh:L2 — the comment is markdown inside a shell script, so a
reader of the script sees `[github/spec-kit](https://github.com/github/spec-kit)` raw. The
record's stated reason for the position is "where a reader of a script looks", and the raw
markup slightly undercuts it. Keeping one sentence across both formats is a defensible
trade-off; flagging it so the choice is explicit rather than inherited.

net: −10 lines possible (shared row parsing in the test; two of the four copies of the `cp`
rationale)

**Verdict: Request changes** (4 warnings — one record claim that the tree contradicts, one
internal inconsistency in the same record, one false statement about upstream, one test that
does not test its own name). No blockers: no behaviour changes, no security surface, and the
acceptance criteria are otherwise met.

## What each pass checked, and what it found clean

**Context.** Read `CLAUDE.md`, the code-review skill, `docs/architecture/vendor-upstream-skills.md`
before and after, issue #216 and #218 via `gh`, and the tests before the implementation. The
spec dir `/Users/andremarin/Development/wfctl-specs/216-speckit-attribution/` holds only
`reviews/` and `target.diff` — no `spec.md`/`plan.md`, which `design-levels` permits for a
change that draws no new boundary.

**1. Correctness.** Re-implemented every function in `tests/test_skill_attribution.py` in a
scratch script against the worktree's absolute paths (no bytecode or cache written into the
repo) and ran all seven checks: 27 rows parse, 21 attributed files, and all six directions are
empty — no listed file missing a line, no line without a row, no disagreement, no upstream
without a notice in its own tree, and the six template rows and the six names in
`wfctl/specify/templates/NOTICES.md` are identical sets. `wfctl/specify/NOTICES.md` does not
exist. The regex widening to `[a-z0-9./-]` matches the new path rows and still matches every
skill row; `_TEMPLATE_ROW` partitions the 27 rows cleanly; `_noticed_templates()`'s
`partition` + `split("\n    Copyright")` correctly excludes the prose that names
`templates/github-issue-template.md` in order to deny it — I confirmed a whole-file scan would
have picked it up and produced a false row.

**Acceptance criteria, each verified independently rather than assumed:**
- All eight `speckit-*` skills carry the line in #213's form; `speckit-delivery-plan` and
  `speckit-orchestrate` do not. Scored every skill in the tree against every upstream command
  template: the eight score 59–94%, and all 24 others score 0% — including the two the record
  says are wfctl's own. The record's "0% against every spec-kit command template" is exact.
- `speckit-specify` confirmed to carry no `metadata.source` key (only `compatibility:`), and to
  be 59% derived from `templates/commands/specify.md` — the file a frontmatter rule misses.
- All five scripts carry the line on the line after the shebang; all six derived templates are
  named in the new notice; `github-issue-template.md` is in neither. 5 + 6 = the eleven the
  issue asks for.
- The MIT permission notice ships inside both grafted trees. I diffed both `NOTICES.md` bodies
  against `LICENSE` line by line: byte-identical, 15 lines each, so "byte-identical to wfctl's
  own `LICENSE`" holds. `Copyright GitHub, Inc.` is upstream's own wording, not a paraphrase —
  spec-kit's `LICENSE` has no `(c)` and no year, so reproducing it without them is correct and
  the inconsistency with the other three entries is upstream's, not wfctl's.
- The two previously-unverified files: fetched `github/spec-kit@fc3d124` from the API. It
  removes `scripts/bash/update-agent-context.sh` and `templates/agent-file-template.md`
  (83 changed paths, parent `518dc9d`). Against the parent tree, downloaded fresh, I score them
  81% and 100%. The record's "removed in `github/spec-kit@fc3d124`" and "100%" are correct;
  "82%" is the pre-change file (see the NIT).

**Behaviour.** The five script edits are `#` comments on line 2; no mode change in the diff
(all five stay 100755, which `ci.yml:L78` checks). No shellcheck in CI. The new
`NOTICES.md` lands in `.specify/templates/` because the copy loop iterates
`src.iterdir()` — nothing in `wfctl/` or in the installed runtime globs that directory
(`create-new-feature.sh` and `setup-plan.sh` name `spec-template.md` and `plan-template.md`
literally; the skills name their templates literally; wfctl's `check-prerequisites.sh` has no
`--template` flag), so the extra file is inert and cannot be mistaken for a template. It is
added to `gitignore_targets` and to the manifest like every other mirrored item, so `doctor`
and `uninstall-skills` account for it. No test asserts a bundle file count that this would
break.

**2. Security.** No executable behaviour added. The notice is data, installed into a
gitignored mirror path, never read by a script or interpolated into a prompt. No secrets, no
input handling, no new shell. The one thing worth naming: the file does travel into consumer
repositories, so its text is content wfctl publishes into other people's trees — it is
licence text plus paths, which is appropriate.

**3. Architecture.** The change fits the pattern #213 established rather than inventing one:
same line, same table shape, same paired checks. Two genuinely new decisions — a second
`NOTICES.md` per grafted tree, and the notice standing in for the line where a line would
travel into the reader's own document — and both are recorded with their rejected alternatives.
The `templates/` placement is the right call and the reasoning is verified: `_RUNTIME_TARGETS`
mirrors `specify/scripts` and `specify/templates` and nothing above them. Findings 2 and 4
above are architectural.

**4. Readability.** Comments explain why, not what, and match the file's existing density.
The test docstrings name the failure each check exists to catch, per the project's convention.
`_TEMPLATE_ROW`'s comment ("A third 'where the notice is' column would be derived data in a
hand-kept table") is the strongest one in the diff — it forecloses a design a reader would
otherwise propose. Naming is consistent; the renamed tests read as sentences. Minor prose
findings are NITs above.

**5. Performance.** Not applicable. The checks read at most 40 small files once per test;
nothing in the shipped code path changed.

**6. Over-engineering.** The `partition`/`split` in `_noticed_templates()` looks elaborate for
a regex scan, but it defends against a real, present failure — the notice names
`github-issue-template.md` below the copyright line specifically to deny it, and a whole-file
scan would read that denial as a claim. Not bloat. `_TEMPLATE_ROW` as a prefix rather than a
third table column is the smaller design, correctly argued. The two shrink opportunities are
the duplicate record parse and the four copies of the `cp` rationale, both NITs above.

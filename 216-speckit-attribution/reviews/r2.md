# Review: `863a089` (`git diff main...HEAD`, branch `216-speckit-attribution`) — reviewer r2

Six passes run per `.agents/skills/code-review/SKILL.md`. Evidence for every
claim below is a command I ran, not a reading of the diff.

## Findings

WARNING  docs/architecture/vendor-upstream-skills.md:L36-L60 — the diff deletes the record's own hedge ("The table is not yet the whole answer… absence from this table means 'not yet recorded' for those and 'wfctl's own' for everything else") while a third spec-kit-derived population in the same grafted tree, `wfctl/agents/commands/speckit.*.md`, stays undeclared. `speckit.analyze.md` is 14 of its 15 lines verbatim `github/spec-kit@fc3d124:templates/commands/analyze.md` — the `description:` frontmatter, `## User Input`, the fenced `$ARGUMENTS`, and "You **MUST** consider the user input before proceeding (if not empty)."; only the closing "Read `.agents/skills/…`" line is wfctl's. All eight of `speckit.{analyze,checklist,clarify,constitution,implement,plan,specify,tasks}.md` carry a `description:` byte-identical to upstream's (checked against `fc3d124` via `gh api`), and nine command files carry the upstream MUST-sentence (`speckit.decompose.md` and `idea-refine.md` included). `graft wfctl/agents` ships all of them. Before this diff the record told a reader those were pending #216; after it, the record's silence is an ownership claim — which is the exact failure its own line names ("A record that omits a known exception is read as denying it"). → one-sentence fix, either direction: add the eight command files to the first table (a footer line is safe there — commands are not `cp`'d into a user's documents the way templates are), or add a sentence saying why a shim reproducing upstream's description and preamble is not derived, the way `specify/templates/github-issue-template.md` gets one.

WARNING  tests/test_skill_attribution.py:L158-L177 — `test_every_upstream_named_in_a_file_has_its_notice_in_the_same_tree` cannot fail on a hollow notice, and this diff is what extends it to a second tree. The check is `repo not in text`, and the slug `github/spec-kit` also appears inside the section's own URL, so the section can lose everything that makes it a notice and the suite stays green. Verified by mutation against a scratch copy: deleting `Copyright GitHub, Inc.` from `wfctl/agents/NOTICES.md` → **7 passed**; renaming the heading `## github/spec-kit` → `## nothing` → **7 passed**. Only deleting the whole section (heading, URL and copyright line together) fails it. The docstring claims "`NOTICES.md` is the licence", and the check tests for a substring that a stray mention satisfies — the same shape of defect as the `license:` key the record retired. → tighten to require the heading *and* a copyright line inside it, e.g. `re.search(rf"^## {re.escape(repo)}$.*?^ {{4}}Copyright", text, re.M | re.S)`. The section-partition idiom is already in the file — `_noticed_templates()` does exactly this partition for the specify tree.

NIT  docs/architecture/vendor-upstream-skills.md:L146-L149 — "`specify/templates/constitution-template.md` is all of it" names no revision, and nothing in the repo records which spec-kit revision the specify tree was copied at (`_bundle.BUNDLE_SOURCE` pins `aamarin/wf-skills`, not spec-kit). Measured against `github/spec-kit@fc3d124` it is 68% shared lines (50/73). The two files the diff *does* resolve properly both name `fc3d124`, so the asymmetry is visible in the same paragraph. → name the revision the percentage was taken against, or drop the number and say "wholly derived".

NIT  wfctl/agents/NOTICES.md:L33-L37 — path frame changes mid-file. The header writes paths relative to the agents tree ("skills under `skills/`"), the new section writes "The rest of that upstream is under `specify/`", which is relative to `wfctl/`. From inside the tree this file lives in, there is no `specify/`. → `../specify/`, or "under `wfctl/specify/` in the wheel".

NIT  wfctl/cli.py:L2141 (behaviour the new file inherits) — `install-skills` appends every planned dest to `gitignore_targets`, so a consumer repo gets `.specify/templates/NOTICES.md` on disk and gitignored. The record's reach argument holds for arrival, which is what matters, but "reach a repository" and "be committed in that repository" are different, and the record reads as claiming both. → one clause, or nothing; the obligation attaches to wfctl's wheel and the wheel carries it.

NIT  tests/test_skill_attribution.py:L64-L71 — `_attributed()` globs `scripts/**/*.sh`. `fc3d124` removed `scripts/powershell/update-agent-context.ps1` upstream, and spec-kit ships a `scripts/powershell/` tree generally; if one is ever restored here the pair goes silent for it rather than failing. → glob `scripts/**/*` and skip by suffix, or say in the comment that `.sh` is the whole population by construction.

## What each pass checked

**1. Correctness.** Ran the acceptance criteria one at a time rather than reading
the diff for them.

- Skills: 10 `speckit-*` skills exist; the 8 listed each carry the line as their
  literal last line; `speckit-delivery-plan` and `speckit-orchestrate` are absent
  from both file and table. `speckit-specify` is in, with no `source:` key.
  Matches the issue exactly.
- Specify tree: 11 derived files = 5 scripts (line after the shebang) + 6
  templates (named in `wfctl/specify/templates/NOTICES.md`);
  `github-issue-template.md` is the only file excluded and gets a written reason.
- The two "previously unverified" files, resolved independently rather than
  trusting the diff: `gh api repos/github/spec-kit/commits/fc3d124` shows it
  removed `scripts/bash/update-agent-context.sh`,
  `scripts/powershell/update-agent-context.ps1` and
  `templates/agent-file-template.md`. `diff` against `fc3d124^` returns **exit 0**
  for `agent-file-template.md` — the "100%" is literally byte-identical.
  `update-agent-context.sh` shares 666/800 lines (83%) with `fc3d124^`; the record
  says 82%, honest and slightly conservative.
- Shebang question: `bash -n` clean on all five scripts; mode `100755` unchanged
  on all five in the tree and in the built wheel; nothing in the repo or the
  installed runtime reads line 2 of a script. `common.sh` is sourced, and a
  comment is inert there.
- Template-enumeration question: nothing globs `.specify/templates/`. Every
  reference in the scripts and the skills is a named file
  (`plan-template.md:setup-plan.sh:41`, `agent-file-template.md:update-agent-context.sh:81`,
  `spec-template.md:create-new-feature.sh:284`, and three named reads in
  `speckit-constitution/SKILL.md`). `NOTICES.md` cannot be picked up as a
  template by anything. The record's `cp` claim is real — `create-new-feature.sh:285`.
- `install-skills` / `doctor` / `uninstall-skills`: the plan builder iterates
  `src.iterdir()` over `specify/templates` (cli.py L2139), so the new file is an
  ordinary tracked runtime item — manifested, gitignored, backed up and
  uninstalled like the templates beside it. No per-kind or per-extension
  branching applies to it. `content_hash` walks both trees, so the change reads
  as ordinary drift and `install-skills` is the remedy, as intended.
- Wheel: built `wfctl-0.17.0-py3-none-any.whl` from a scratch copy (never in the
  worktree). It contains `wfctl/agents/NOTICES.md` **and**
  `wfctl/specify/templates/NOTICES.md`, 14 files under `specify/`, and
  `common.sh` line 2 is the attribution comment. The claim holds.

**2. Can the new checks fail.** Eleven mutations against a scratch copy, each run
in isolation and reverted:

| Mutation | Result |
|---|---|
| drop the line from `speckit-plan/SKILL.md` | fails `…record_lists_carries_its_attribution_line` |
| drop the comment from `common.sh` | fails the same |
| drop `specify/templates/plan-template.md` from the record | fails `…notice_names_no_template_the_record_does_not_list` |
| drop `plan-template.md` from the specify notice | fails `…record_lists_is_named_in_the_specify_notice` |
| move the specify notice to the tree root | fails 4, including `…reaches_a_project` |
| add the line to `speckit-orchestrate` | fails `…claims_an_upstream_the_record_does_not_list` |
| drop the `## github/spec-kit` heading from the specify notice | fails `…named_in_the_specify_notice` |
| delete the whole spec-kit section from `agents/NOTICES.md` | fails `…notice_in_the_same_tree` |
| **rename that heading, keep the body** | **7 passed** ← WARNING 2 |
| **drop `Copyright GitHub, Inc.` from `agents/NOTICES.md`** | **7 passed** ← WARNING 2 |
| baseline | 7 passed |

Both directions of both pairs fire on real regressions. The one hole is the
notice-content check, above. The placement assertion in
`test_every_derived_template_that_ships_reaches_a_project` is not decorative —
moving the file is caught, and by four tests.

**3. Security.** Nothing executable changed. The five inserted lines are `#`
comments in bash — no expansion, no command substitution, nothing sources line 2.
`NOTICES.md` landing in `.specify/templates/` is inert data; no code path
executes or evaluates a file from that directory (the scripts name their three
templates explicitly). No secrets, no input handling, no new external reads.

**4. Architecture.** Fits the #213 pattern rather than inventing one: same
sentence, same three facts, notice-per-grafted-tree, same paired check. The one
new idea — a notice standing in for a per-file line, because templates are `cp`'d
into a reader's own documents — is argued in three places that agree with each
other (the record's Rejected section, the notice's own prose, the test module
docstring), and the placement inside `templates/` is justified by an install
target rather than by symmetry. `_TEMPLATE_ROW` keying off the path instead of a
third table column is the right call and the comment says why.

**5. Readability.** Matches house style — comments carry rationale, docstrings
name the failure the test exists to catch. `test_every_derived_template_that_ships_reaches_a_project`'s
docstring ("moved to the tree root it would still ship, still pass every check
above, and arrive nowhere") is the best line in the diff and my mutation confirmed
it. `uv run ruff check wfctl/ tests/` and `uv run mypy wfctl/` both clean on a
copy; `uv run pytest -q` → **942 passed in 48s** (CLAUDE.md still says 653 — stale,
not this diff's problem).

One wobble: `docs/architecture/vendor-upstream-skills.md:L189-L192` leaves a
187-character line after the edit ("close it. What they close is the drift…"),
where the rest of the file wraps at ~78. Cosmetic; mentioning it so it isn't
mistaken for intent.

**6. Over-engineering.** Lean. The one shrink available: `_listed()` and
`_listed_templates()` each read and re-scan the record independently, so
`_ROW.findall(RECORD.read_text())` runs four times per session for a partition
into two sets — one helper returning both would be ~6 lines shorter. Not worth a
change on its own. Nothing else here is speculative: every new function has a
caller, every new constant has a comment earning it, and the notice file is the
artifact the licence requires rather than an abstraction over it.

net: −0 lines possible (the two WARNING fixes each *add* a line or two).

**Verdict: Request changes** — one warning, one sentence to fix. Not a rework:
the change is a clear improvement and every acceptance criterion in #216 is met
as written. But the record's own rule is that omitting a known exception reads as
denying it, and this diff removes the sentence that kept
`wfctl/agents/commands/speckit.*.md` from being denied — while those files
demonstrably carry verbatim upstream text and ship in the same graft. Either list
them or say why they are not derived, and tighten the notice check that currently
passes on a notice with its copyright line deleted.

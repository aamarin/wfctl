# Review: d17986e (`origin/main...HEAD`) — r1

Target: the whole of `origin/main..HEAD`, one commit, −1141/+110.
Rubric: `.agents/skills/code-review/SKILL.md`, all six passes.
Context read: issue framing, `CLAUDE.md`/`AGENTS.md`, `docs/architecture/a-rule-is-expressed-as-a-check.md`,
`docs/architecture/the-scan-is-attested-where-the-reviewer-reads.md`,
`docs/architecture/required-sections-are-wfctls.md`, the `architecture-decisions` skill,
the deleted `wfctl/_body.py` and `tests/test_body.py`, and the tests that survive.

Checks run: `uv run pytest -q` (1516 passed, 2m10s), `uv run ruff check wfctl/ tests/`
(clean), `uv run mypy wfctl/` (clean),
`uv run wfctl check-body` by hand on a body with no panel section — it now reports only
`verification: stale …` and exits 1, so the panel rule is gone from the command and the
verification source still fires. `uv run wfctl verify` deliberately not run.

The argument is sound and the mechanical removal is clean: no Python or test file still
imports `wfctl._body`, `docs/architecture/views/current-state.md`'s machine-checked
```layers``` block was updated (`tests/test_architecture_view.py` would have failed
otherwise), and the box art's `_md` importer list was corrected to `_arch _shape
_predicates` — which fixes a pre-existing error, since `_predicates` has imported `_md`
all along and `main` listed `_body` in its place while claiming `3 in`.

What it did not finish is the prose. A rule removed from one shipped skill is still
asserted verbatim in the sibling skill that produces the thing it was about.

---

BLOCKER  wfctl/agents/skills/fanning-out-code-review/SKILL.md:104-108 — the removed rule
survives in the other half of the pair. The paragraph still reads "…so this skill starts
from a step in another file rather than from a phrase, and **the reconciled table from
Step 6 is content for the description that step goes on to write**." That is the exact
sentence `opening-a-change` Step 1 now contradicts ("The panel's output is not content for
the description"), and it is the one an agent reads at the moment it has just finished a
panel and is about to open a PR — the fanout skill is what runs, `opening-a-change` Step 1
is what dispatched it. Two shipped skills now give opposite instructions about the same
artifact, and the more proximate one gives the old one. Nothing in the suite pins it:
`tests/test_review_fanout_skill.py` guards the roster fence, the three layered references
and rubric non-restatement, none of which touch this line. The same text is in the
installed mirror (`.agents/skills/fanning-out-code-review/SKILL.md:108`), so the defect is
live in this worktree.
→ Fix: rewrite the clause to name the table's real destination — the reader the panel
reports to — and say that what reaches the description is the rejected findings under
Additional Context, per `opening-a-change` Step 1. Same paragraph is the natural place to
keep the #187 point about why the step exists, which is still correct.

WARNING  README.md:165 and README.md:525-529 — two stale claims that `check-body` checks
the panel, in the file a reader outside the repo reads first. Line 165's command table:
"Check a PR description's drawings **and review panel**, and whether the definition of
done has passed". Line 527: "It reads **two** more things while it has the branch in front
of it: **the review panel's disposition table**, and whether a `wfctl verify` record
covers this tree." It now reads one. The diff touched neither.
→ Fix: drop the panel clause from line 165; at 527, "It reads one more thing while it has
the branch in front of it: whether a `wfctl verify` record covers this tree."

WARNING  wfctl/agents/skills/opening-a-change/SKILL.md:66-68 — a sentence inside the
rewritten Step 1 still stands on the premise the same block deleted: "…leaves the PR
opening from the commit that still has the defect **while the description says it was
applied**." Eight lines above, the skill now says the description does not say that.
→ Fix: end the clause at "…the commit that still has the defect." The reason to commit
before pushing survives without the trailing half.

WARNING  wfctl/agents/skills/opening-a-change/SKILL.md:60-62 — the load-bearing
justification overstates, and the panel skill contradicts it. "Nothing checks this and
nothing can: whether a panel ran is not visible in any artifact the work produces." The
panel *does* produce an artifact: `fanning-out-code-review` Step 3's roster check greps
reviewer reports on disk, and this review is one of them. A check could read that
directory exactly the way `_verification_finding` reads the verify record — feasibility is
not what is wrong with it. What is actually wrong is two other things, and the record the
change invokes turns on them: the reports land in `FEATURE_DIR`, which resolves outside
the working tree and is gitignored here, so no reviewer can reach them; and a fabricated
report is as cheap as fabricated prose, so a check over them counts files rather than
observing a panel. That is the same argument
`the-scan-is-attested-where-the-reviewer-reads` makes one stage earlier, and stating it
correctly is what stops a future session from reading "nothing can" as wrong — noticing
the reviews directory — and reopening the decision.
→ Fix: "No artifact a reviewer can reach records that a panel ran, and a report a run
writes about itself is the self-report `a-rule-is-expressed-as-a-check` refuses. So it is
written here as a rule and not as a check."

WARNING  docs/architecture/the-scan-is-attested-where-the-reviewer-reads.md:32-38 — a
`proposed` record's `Direct baseline` is rewritten with no `## Log` line. The record is
`proposed`, so `architecture-decisions` permits the body edit — but this record's own Log
carries a dated line for each of its two prior body edits ("amended — …", including one
explicitly "Amended while `proposed`"), and this edit changes the very section the
decision is compared against.
→ Fix: append `- 2026-09-11  amended     — #347 withdrew the shape this baseline cited one
stage later; the section now says so.`

WARNING  .github/pull_request_template.md:151 + wfctl/agents/configs/github/.github/pull_request_template.md:151
vs SKILL.md:56-59 and template:167-170 — the new rule is written in two vocabularies that
do not cover the same set. The checklist says "anything it raised that **I did not
apply**"; the skill and the Additional Context comment say "**rejected**" / "decided
against". `fanning-out-code-review` Step 6 defines three dispositions — applied,
**accepted**, rejected — so a finding accepted but deferred to a follow-up is inside the
checklist's net and outside the prose's, and the author who reads the prose omits it.
→ Fix: use "did not apply" in all three places; it is the wording that covers both
non-applied dispositions and it is already the checklist's.

NIT  docs/ponytail-debt.md:61,65 — two rows cite `wfctl/_body.py:169` and
`wfctl/_body.py:118` in a file that no longer exists. It is a historical ledger and both
rows are `**applied**`, so these are dead pointers rather than wrong claims; the file's own
header says rows carry symbols because line numbers rot. Optional: leave them, or note the
module was deleted in #347.

NIT  docs/architecture/views/current-state.md:88 — "`cli` is 3627 lines because it is the
one place **all thirteen** meet". The domain band went 15 → 14 with this diff; the count
was already wrong, as is the 3627 against `cli 5266` at line 17. The view's Staleness
section says a reader who finds a wrong count should fix it, and this diff is the one
editing that band.

NIT  wfctl/cli.py:4469 — the docstring's first line is the `--help` one-liner. "Check a PR
description against what a machine can see in it" now under-describes a command whose
second finding is explicitly *not* in the file. Something like "…in it, and against the
branch it describes" costs four words. (Same for the success string at :4540, "the
drawings look right", which stays silent about the verification source it also cleared —
pre-existing shape, worth a word only while the line is open.)

---

Passes with nothing to report: **security** (no input boundary, no secret, no new
execution path; the deletion removes a regex-heavy parser and shrinks the surface),
**performance** (`check-body` now runs one fewer pass over the body), **over-engineering**
(the diff *is* the deletion — `_body.py` 399 lines, `test_body.py` 560, and the tests that
kept the fixtures alive; `_md.py` still has three callers, so the extraction it was part of
does not become a one-caller abstraction). **Correctness**: the survivors are right —
`_verification_finding`'s "second source", `_change.py`'s two pointer updates,
`_CLEAN_BODY`, and the `_PANEL` fixture removal all land where they should, and the two
rewritten test docstrings say why the test exists rather than restating the assertion.

net: −4 lines possible (the two stale clauses above; the rest are rewordings)
Verdict: **Request changes** (1 blocker, 5 warnings)

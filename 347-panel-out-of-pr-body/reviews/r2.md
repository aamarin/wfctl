## Review: d17986e — `origin/main...HEAD` (#347, the panel's table leaves the PR description)

Reviewer r2. Suite, lint and types confirmed green on this tree
(`uv run pytest -q` 1516 passed; `ruff check wfctl/ tests/` clean; `mypy wfctl/`
clean). Both copies of `pull_request_template.md` are byte-identical. The
deletion of `wfctl/_body.py` is clean: `panel_findings`, `PLACEHOLDERS`,
`FAILURE_MARKERS` and `BARE_VERDICTS` had no importer outside `tests/test_body.py`,
which goes with it. The `_md` caller list in `current-state.md` is now *more*
accurate than it was on main — main drew `_arch _body _shape` and claimed `3 in`
while `_predicates` was already a fourth caller; this diff lands on the true set
of three. `test_architecture_view` agrees.

The findings below are all in prose the suite does not read.

---

BLOCKER  wfctl/agents/skills/fanning-out-code-review/SKILL.md:L105-108 — the rule
this change exists to remove is still stated, in full, in the other skill that
ships. "…so this skill starts from a step in another file rather than from a
phrase, **and the reconciled table from Step 6 is content for the description
that step goes on to write.**" `opening-a-change` Step 1 now says the exact
opposite ("The panel's output is not content for the description"). An agent that
enters through the panel skill — which is the path Step 1 sends it down — reads
the withdrawn rule as current and writes the table into the body anyway. The
commit message asserts this file was reasoned about ("`fanning-out-code-review`
keeps Step 6's table — … the table stops being content for a description without
stopping being that"), and the sentence that says it *is* content survived. Two
neighbouring paragraphs in the same file (L192, L179-region) were edited for this
change, so the omission is a miss rather than a decision.
→ Replace the trailing clause: the panel is reached as Step 1 of
`opening-a-change`, and Step 6's table is the reconciliation this skill owns —
what reaches the description is only what the diff cannot show, which that skill
defines.

WARNING  wfctl/agents/skills/opening-a-change/SKILL.md:L60-62 — the justification
for demoting the rule to prose is factually wrong, and it is the load-bearing
sentence. "whether a panel ran is not visible in any artifact the work produces".
It is: `fanning-out-code-review` Step 3 requires every reviewer to write
`$FEATURE_DIR/reviews/<id>.md`, and that step's whole point is that the roster is
read "against the disk, not against what you remember receiving" — an empty
`reviews/` *is* a violation visible in an artifact the work produces.
`a-rule-is-expressed-as-a-check` (accepted, in force) asks exactly that question,
so a next reader applying the record to the sentence as written gets "yes → express
it as a check" and re-opens #347. The true argument is reach, not existence: the
feature dir resolves outside the working tree and `specs/` is gitignored, so
nothing a reviewer of the *pull request* can read records that a panel ran — which
is the argument `the-scan-is-attested-where-the-reviewer-reads` already makes one
stage earlier.
→ Say reach: the reports exist, on a path the PR's reader cannot see and the repo
does not carry, so no check over the description can settle it.

WARNING  wfctl/agents/configs/github/.github/pull_request_template.md:L151 vs
wfctl/agents/skills/opening-a-change/SKILL.md:L57-59 and template:L167-169 — the
three places that state the new rule disagree about its scope, and the panel
skill's own vocabulary falls in the gap. The checklist says "anything it raised
that I **did not apply**"; Step 1 says "a finding you **rejected**"; the Additional
Context comment says "anything a review panel raised that you **decided against**".
`fanning-out-code-review:L244` defines *three* dispositions — applied, **accepted**,
rejected — and an accepted-but-not-applied finding satisfies the checklist while
being excluded by the other two. That is precisely the finding the diff cannot
show and the one most worth a line.
→ Pick one wording and use it in all three: "anything the panel raised that this
change does not apply" covers accepted and rejected both.

WARNING  wfctl/agents/skills/opening-a-change/SKILL.md:L117-121 — the no-template
path lost the panel's destination and did not gain the new one. Step 1 sends
rejected findings "under Additional Context"; Step 3's default shape is now
"Summary · What changed · How it was tested · Issue links" — four sections, none
of them Additional Context. Before this change the default shape named "Review
panel" explicitly, so the no-template repo had somewhere to put it; now Step 1's
instruction dangles for every repo that ships no template, which Step 3 exists to
serve. The skill is installed into consuming repos, where that case is the common
one.
→ Add Additional Context to the default shape (five sections), or have Step 1 say
where the line goes when the shape has no such section.

NIT  wfctl/cli.py:L4511-4517 — the new docstring paragraph gives a reason that
proves too much. The bolded claim is right ("A rule about the body that no
artifact can settle does not belong here"), but the closing sentence grounds it in
the description rather than the rule: "the artifact the rule was being read off is
the description, and the description is written once at the end, editable
afterwards, and produced by no step of the work." `a-rule-is-expressed-as-a-check`
says the opposite in as many words — "*Already produces* … includes the reply, the
**PR body**, the diff and the tree" — and the same sentence would rule out the
`conversation-response-shape` drawing check that stays in this very function,
which reads the same file. (The identical claim is already in
`the-scan-is-attested-where-the-reviewer-reads`, but that record is `proposed`;
the accepted one wins.)
→ Ground it in the rule: a check over the body can see that prose was written and
never that a panel ran. Drop the clause about what the description is.

NIT  wfctl/agents/skills/fanning-out-code-review/SKILL.md:L179 — stale in the same
way as the blocker, one paragraph up from an edit this diff made. A fabricated
`MISSING` "spends that reviewer's work twice and puts an untrue sentence in a
description (#173)". The roster no longer reaches a description.
→ "…and puts an untrue sentence in the report this panel hands back."

NIT  docs/architecture/the-scan-is-attested-where-the-reviewer-reads.md:L33-37 —
the body is amended (legitimately: the record is `proposed`, so not frozen) but no
`## Log` line is appended. That record logs each of its three prior amendments by
date and reason, and this one changes its `Direct baseline` — the section a human
reads when deciding whether to accept it.
→ Append `- 2026-09-11  amended  — #347 withdrew the one-stage-later shape this
baseline cited; the citation now names it as withdrawn.`

NIT  docs/ponytail-debt.md:L61,L65 — two ledger rows cite `wfctl/_body.py:169` and
`wfctl/_body.py:118`, a file this change deletes. `current-state.md` was swept for
`_body` and this was not, so a grep for `_body` still lands on a path that does not
exist. Arguably it is evidence about what a past audit found and stays as written —
if so, that is the answer, but it is the one remaining reference in the tree.
→ Mark the rows as citing a since-deleted module, or leave with a one-word note.

NIT  wfctl/cli.py:L4540 — the green line names one of the two sources the command
composes: "the drawings look right". Exit 0 also means the verification record
covers this tree (or that there is no repo / no definition of done), which is the
finding the author most wants to have cleared. The old wording named two of three,
so this is not a regression, but the rewrite was the moment to fix it.
→ `f"{path}: drawings and verification look right"`.

---

net: `Lean already.` — the diff is −1031 lines and deletes a whole module; nothing
here is removable. The four non-NIT findings are additions of a sentence each.

Verdict: **Request changes** (1 blocker)

The change itself is right and the deletion is clean — `_body.py` had exactly one
consumer, the tests that go with it were the only importers of its constants, and
the drawing it touched came out more accurate than it went in. What is not
finished is the prose sweep: the panel skill still ships the removed rule as
current, and the replacement rule is stated three times in three different scopes
with no destination in the no-template path. Those are the artifacts that install
into other repositories and the ones the suite has no opinion about.

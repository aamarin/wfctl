# r3 — Review: `d17986e` (`origin/main...HEAD`, #347)

Rubric: `code-review` (six passes). Ran `uv run pytest -q` (1516 passed),
`uv run ruff check wfctl/ tests/` (clean), `uv run mypy wfctl/` (clean).
Did not run `wfctl verify`. Exercised `check-body` by hand against the edited
template (exit 0, `the drawings look right`).

## Findings

WARNING  wfctl/cli.py:L4512–L4517 — the new rationale contradicts the accepted
record it names as its authority. `a-rule-is-expressed-as-a-check` (status:
accepted) says, in its Decision: *"It excludes a check that needs an artifact
invented so that it can be checked, and it includes the reply, **the PR body**,
the diff and the tree — which is where every violation above was observed."*
The docstring's clause — "the artifact the rule was being read off is the
description, and the description is written once at the end, editable
afterwards, and produced by no step of the work" — is the argument from
`docs/architecture/the-scan-is-attested-where-the-reviewer-reads.md`, which is
`status: proposed` and disagrees with the accepted record on exactly this point.
Citing the accepted record for a sentence the proposed one supplies leaves the
next reader believing the accepted record says the body is not an artifact.
→ Delete the clause after the em dash. The sentence before it already carries
the valid argument and is the one the issue actually makes: the check could see
only that prose had been written, never that a panel ran, so a violation is
visible in **no** artifact and the rule falls on the record's prose side.
`wfctl/agents/skills/opening-a-change/SKILL.md:L60–62` phrases this correctly
already ("whether a panel ran is not visible in any artifact the work
produces") — reuse that wording.

WARNING  wfctl/agents/skills/opening-a-change/SKILL.md:L66–67 — stale sentence
inside the step this change rewrote. "…an uncommitted fix leaves the PR opening
from the commit that still has the defect **while the description says it was
applied**." The description no longer says any finding was applied; the same
step now says applied findings are commits and belong nowhere in the body.
→ End the sentence at "…that still has the defect.", or replace the clause with
"while the reviewer reads a diff the fix is not in."

WARNING  README.md:L165 and L525–L528 — user-facing docs still describe the
removed check. L165: "`check-body` | Check a PR description's drawings and
review panel, and whether the definition of done has passed". L527: "It reads
two more things while it has the branch in front of it: the review panel's
disposition table, and whether a `wfctl verify` record covers this tree."
The handoff's ninth-file sweep (`grep -rln … wfctl/ tests/ .github/ docs/`) does
not cover README.md, which is why both survived.
→ L165: "Check a PR description's drawings, and whether the definition of done
has passed". L527: "It reads one more thing while it has the branch in front of
it: whether a `wfctl verify` record covers this tree."

WARNING  wfctl/_md.py:L1–L10 — the docstring rewrite replaced a true history
with a present-tense caller list that is incomplete. `_predicates` imports `_md`
(`wfctl/_predicates.py:28`) and reads markdown through `_md.walk`
(`wfctl/_predicates.py:368`); it is a third caller and is not named, so "`_shape`
and `_arch` both need to know which lines … sit inside a fenced code block"
under-counts the module's own callers. The original sentence was past tense and
stayed true after `_body` was deleted — what forced the edit was the dangling
name, not the claim.
→ Keep the count and the tense without the dead name: "Three modules each
needed to know which lines of a markdown document sit inside a fenced code
block, and each answered it differently: `_shape` implemented CommonMark's
closing rule, the other two matched a prefix."
Same edit and same reason at `tests/test_md.py:L1–L10` ("made the
implementations disagree for a year" → "made three implementations disagree")
and `docs/architecture/views/current-state.md:L115–L117` ("those that each had
their own fence walker" → "the three that each had their own fence walker").
`docs/ponytail-debt.md:61` records the count as three, with the three line
totals, so the softened wording is now the only place that disagrees with it.

NIT  tests/ — nothing asserts the shipped template now **passes** `check-body`.
`test_the_template_as_shipped_does_not_pass_its_own_check` was the only test
that ran the command over
`wfctl/agents/configs/github/.github/pull_request_template.md`, and it was
deleted with the rule. The template still carries fenced blocks that the
surviving drawing rules read, so a future `_shape` change could start rejecting
the repository's own unfilled template with nothing to catch it. I confirmed by
hand that it exits 0 today.
→ One assertion in `tests/test_shape.py`, the inverse of the deleted one:
`assert _shape.body_findings(_TEMPLATE.read_text()) == []`.

NIT  wfctl/cli.py:L4537 — `check-body` is now a two-term sum
(`_shape.body_findings(body) + _verification_finding()`) and no test exercises
both terms firing in one run: `tests/test_shape.py` points `WFCTL_REPO_ROOT` at
an empty dir to silence the verification finding, and `tests/test_verify.py`
uses a body with nothing wrong with it. Pre-existing, but the composition is now
the whole of the function.
→ One case asserting a body with a rejected drawing, in a repo with an
unverified definition of done, prints both lines.

NIT  wfctl/cli.py:L4540 — the success string changed from "drawings and panel
look right" to "the drawings look right" and is asserted nowhere in the suite.
→ Add `assert "drawings" in result.output` to
`test_check_body_exits_one_on_a_finding_and_zero_without`, or leave it; the
exit code is the contract.

NIT  wfctl/agents/skills/opening-a-change/SKILL.md:L183 — "**Its second finding**
is not about the file" is a hand-maintained ordinal (it was "third" before this
diff) that nothing checks. The sibling references in this file *are* pinned by
`test_the_in_file_step_references_point_at_the_step_they_name`.
→ Reword to "The other finding is not about the file", which stops being an
ordinal.

## Checked and clean (so it is not re-derived)

- `docs/architecture/views/current-state.md` still passes
  `tests/test_architecture_view.py`: only the three fenced blocks are parsed,
  and the ```layers block drops `_body` correctly (14 domain modules, matching
  the drawing's box). The drawing is also **more** correct than before — main
  claimed `_md … 3 in` while listing `_arch _body _shape` and omitting
  `_predicates`, which was already a caller; `◄── _arch _shape / _predicates`
  now matches the import graph. `_io`'s `5 in` is unchanged and correct
  (`_body` never imported `_io`). Box widths and the continuation-line
  indent (col 62) are preserved.
- The two `pull_request_template.md` copies are byte-identical, so
  `tests/test_install_config.py:L414` still covers the drift this change could
  have introduced.
- The surviving `check-body` tests assert what their docstrings claim.
  `tests/test_verify.py`'s `_CLEAN_BODY` lost its panel section and the comment
  above it was corrected to match ("`_shape` owns the other source"), and every
  assertion in that block is about the `verification:` line alone, as stated.
  `tests/test_shape.py`'s `_PANEL` removal leaves `ACCEPTED`/`REJECTED` as the
  only inputs, which is what those two tests were always about.
- The 77 deleted assertions all exercised `_body.panel_findings`, which has no
  other caller. No surviving behavior lost its only test except the template
  case above.
- `docs/architecture/the-scan-is-attested-where-the-reviewer-reads.md`: only
  `## Direct baseline` is touched; status, Decision and Log are untouched, and
  the new cross-reference ("the argument this section reaches below") is true —
  the paragraph below it makes exactly the #347 argument.
- `docs/ponytail-debt.md:61,65` still cite `wfctl/_body.py` line numbers. That
  file is a log of findings marked **applied**; leaving it is right.
- No packaging reference to `_body.py`/`test_body.py`. No security surface:
  `escape()` on the finding line is unchanged, and the diff only removes code.

net: −1031 lines already; the over-engineering pass found nothing further to
delete. Every finding above is an edit, not a deletion.

Verdict: **Request changes** (0 blockers, 4 warnings — all prose; none change
behavior, and the largest is that the durable justification for deleting a check
misstates the accepted record it cites).

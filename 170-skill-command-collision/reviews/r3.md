# Review: `git diff main...HEAD` (50adee5) — #170, r3

Rubric: `.agents/skills/code-review/SKILL.md`, all six passes. Read-only review;
nothing in the worktree was modified.

## Context established

- No `spec.md` / `plan.md` for this branch (`wfctl feature-paths` →
  `/Users/andremarin/Development/wfctl-specs/170-skill-command-collision/`, which
  holds only `reviews/`). Reviewed against the stated requirement — both the
  Skill-tool path and the human-typed `/name` path must survive — plus
  `CLAUDE.md` and the record set under `docs/architecture/`.
- Verified the host behaviour the whole change rests on, against Claude Code's
  own docs (`https://code.claude.com/docs/en/skills.md`): a project skill at
  `.claude/skills/<name>/SKILL.md` is typeable as exactly `/<name>` (since
  v2.1.101); `disable-model-invocation: true` bars only model invocation and
  leaves the typed route intact; and when a command and a skill share a name,
  the documented precedence is skills first. So the change's premise is correct
  and the human path does survive on the Claude layer.
- Definition of done: `uv run pytest -q` → **831 passed**; `uv run ruff check
  wfctl/ tests/` → clean; `uv run mypy wfctl/` → clean.
  `uv run wfctl install-skills` / `doctor` were **not** run — they write into the
  shared worktree, which this review was barred from touching. Treat the
  `wfctl/agents/` half of the DoD as unverified by me.

## Findings

WARNING  docs/architecture/mirrored-skills-ship-no-wrapper.md:L36 — the decision is
justified entirely by Claude Code's behaviour ("Claude Code creates `/name` for a
skill exactly as it does for a command") but the deletion removes the wrapper from
**every** layer, and one of them had a reason to keep it. `_AGENT_TARGETS`
(`wfctl/cli.py:L1093`) gives `bob` both `agents/skills → .bob/skills` and
`agents/commands → .bob/commands`, and only the second copy goes through
`_copy_command_for_bob` (`wfctl/cli.py:L1355`, dispatched at `wfctl/cli.py:L2031`)
— the skills copy is a plain `shutil.copytree` at `wfctl/cli.py:L2029`, no
frontmatter transform. `i-have-adhd`'s vendored `SKILL.md` carries
`disable-model-invocation: true`, and this repo's own comment at
`_CLAUDE_ONLY_FRONTMATTER_KEYS` (`wfctl/cli.py:L1316`) records what that key does
to Bob Shell: "causes Bob Shell to skip model invocation entirely — the skill body
never executes. That is the bug where /end-session and other commands do nothing
when invoked in Bob Shell." Before this diff, `/i-have-adhd` on bob resolved to a
wrapper whose flag had been stripped on the way in; after it, the only bob-side
file is the unstripped skill. The same gap, less sharply, covers the other six:
the record asserts nothing about whether bob or copilot surface `.bob/skills` /
`.github/skills` as a typed name, and `layer-model` (accepted) still says of
non-mirrored skills "Everything else reaches the agent as a slash command only".
→ Either extend the Claude-only-key strip to the bob **skills** copy (so the
mirrored-skill rule is layer-safe), or state the layer scope in the record's
Consequences and say what bob/copilot/codex lose. Do not leave the argument
Claude-only while the deletion is global.

NIT  docs/architecture/mirrored-skills-ship-no-wrapper.md:L36 — the load-bearing
claim ("a skill creates `/name` for a human exactly as a command does") has a
version floor: it is true from Claude Code v2.1.101 (Apr 2026), and nothing in
this repo records a minimum Claude Code version (`grep` over `README.md`,
`wfctl/agents/`, `docs/` finds none). On an older host the seven skills lose the
typed route with no test and no doctor finding to say so. → Name the version in
the record's Context, one clause.

NIT  tests/test_install_skills.py:L313 — the docstring wraps a test identifier
across the line break (`test_every_declared_mirror_names_a_` / `shipped_skill`),
so a reader grepping the referenced name does not find this citation. Every other
cross-test citation in the suite keeps the name intact. → Reflow so the identifier
sits on one line.

NIT  wfctl/cli.py:L1283 / docs/architecture/mirrored-skills-ship-no-wrapper.md:L86
— the paired obligation is stated twice ("un-mirroring one is a decision to write
it back", "the wrapper that used to carry the typed route has to be written back
with it") and tested for only three of the ten names: `i-have-adhd` and
`conversation-response-shape` via
`test_each_output_style_skill_is_typeable_by_name`, `fanning-out-code-review` via
`tests/test_review_fanout_skill.py:L143`. Dropping `worktree-handoff`,
`opening-a-change`, `receiving-code-review`, `verification-before-completion`,
`architecture-decisions`, `design-levels` or `using-superpowers` from the set
leaves the skill with neither route and a green suite. I checked whether the
symmetric invariant is testable and it is not — 12 shipped skills have neither a
mirror nor a wrapper (`agent-brief`, `architecture-design`, `brainstorming`,
`software-design-decisions`, the `speckit-*` set, `using-wfctl`, `using-wm`), so
"every skill is mirrored or wrapped" is false today. → Author's call; the honest
options are a named allowlist of "reachable by design" skills or leaving the
asymmetry as the comment already admits it.

NIT  docs/architecture/mirrored-skills-ship-no-wrapper.md:L64 — the "Rename the
wrapper" alternative is rejected on cost without noting that the tree already
does exactly this: `wfctl/agents/commands/wfctl.md` is a wrapper named `wfctl`
pointing at the `using-wfctl` skill. That is a precedent the reader will find, and
a rejected alternative reads stronger when it names the case that went the other
way. The cost argument itself (a second referent for a name #149 already used in
prose) still holds — this is a completeness note, not a reversal.

NIT  docs/architecture/mirrored-skills-ship-no-wrapper.md:L2 — the record ships
`status: proposed` alongside the implementation, so `wfctl arch context` prints
"2 records not shown (2 proposed)" and the rule is absent from the in-force
contract an agent reads. The only in-tree statements of it are the comment at
`wfctl/cli.py:L1277` and the new test. Precedent exists
(`wfctl-runs-the-verification` has been proposed since 2026-08-27) and the
`architecture-decisions` skill explicitly allows "proposed while a human still has
to agree", so this is a flag, not an objection.

## Checked and sound (no finding)

- **Record format.** `Context` / `Decision` / `Owns truth` / `Considered` /
  `Consequences` / `Log` all present; the `Log` line matches the set's
  `- YYYY-MM-DD  status  — reason` shape; `Owns truth` carries both halves and
  states the owned thing as a question in quotes (`_MIRRORED_SKILLS` owns "may an
  agent invoke this skill without being asked?"), which is the checklist item
  records most often miss; filename is a decision slug with no sequence number.
  `_arch.validate` has nothing to say about it (it checks supersession links only,
  and this record supersedes nothing).
- **Every factual claim in the record.** "Ten skills mirrored, seven with a
  wrapper" — confirmed by cross-tabulating `_MIRRORED_SKILLS` against
  `wfctl/agents/commands/`. "Persistence is already in both SKILL.md bodies" —
  confirmed, both carry a `## Persistence` section. "`conversation-response-shape`
  sits exactly on its tested line ceiling" — confirmed, `_LINE_CEILING = 483` and
  the file is 483 lines, so the dropped "Confirm activation in one line" genuinely
  had nowhere to go. "`disable-model-invocation` is not an Agent Skills key and
  `test_every_shipped_skill_declares_only_spec_keys` rejects it" — confirmed,
  including the `i-have-adhd` exemption at `tests/test_skill_frontmatter.py:L46`.
- **Migration for repos that already installed the wrappers.** This was my main
  correctness worry: deleting from the bundle does not delete from a consumer's
  `.claude/commands/`, which is the copy that actually collides. It holds up —
  `install-skills` flags dropped recorded paths as `orphaned`
  (`wfctl/cli.py:L2171`), reports them by path with a `--prune` instruction
  (`wfctl/cli.py:L2194`), and `start-session` already runs
  `wfctl install-skills --prune --yes` (and `--prune --yes --agent claude`) as its
  freshness repair (`wfctl/agents/skills/start-session/SKILL.md:L44`). So the
  first session after the upgrade removes both the `.agents/commands/` and
  `.claude/commands/` copies. The record's Consequences names only the
  `.agents/commands/` half, which is what `_scanned_dirs` reports on
  (`wfctl/cli.py:L3182` explains why the agent roots stay out) — accurate as
  written, just narrower than the actual repair.
- **Dangling references.** Grepped the whole tree for `/i-have-adhd`,
  `/conversation-response-shape`, `/fanning-out-code-review`, `/opening-a-change`,
  `/receiving-code-review`, `/verification-before-completion`,
  `/worktree-handoff`: no skill, README table, test or doc points at a deleted
  file. README's `.agents/commands/` row already scoped itself to `/speckit.*`
  wrappers, so it needed no edit.
- **No remaining collision on the Claude layer.** The seven wrappers that still
  share a name with a skill (`code-review`, `end-session`,
  `finishing-a-development-branch`, `idea-refine`, `requesting-code-review`,
  `scaffold-tracker`, `start-session`) name skills that are **not** in
  `_MIRRORED_SKILLS`, so nothing lands in `.claude/skills/` for them. The
  invariant the new test asserts is the complete one for Claude.
- **The regression test earns its place.** `test_mirrored_skills_ship_no_wrapper`
  fails on the exact one-line change that reintroduces the bug (adding a name to
  the set while its wrapper stands), resolves through `Path(wfctl.__file__).parent`
  for the documented reason (conftest's autouse `bundle` fixture repoints
  `BUNDLE_ROOT`), and asserts on a sorted list so the failure names the offending
  skills rather than printing `False`. The rewritten
  `test_each_output_style_skill_is_typeable_by_name` keeps the property and
  changes only the file that delivers it, which is what its docstring claims.
- **Security / performance.** Nothing to report: no input handling, no secrets, no
  I/O in a hot path. The diff removes files from a bundle and adds one directory
  listing per mirrored name at test time.
- **Style.** Comments explain why-this-shape rather than narrating the diff, per
  `CLAUDE.md`; test docstrings name the failure they catch; ruff's narrow rule set
  and mypy's `disallow_untyped_defs` are both satisfied without new annotations
  being needed (the new test is `-> None`).

net: Lean already. (46 lines of shipped wrapper deleted; the 89-line record and
28-line test are the house shape for a boundary decision and its guard, and I
found nothing in either to cut.)

Verdict: **Approve** — 0 blockers. Resolve the WARNING (bob-layer scope) before
merge or write the deferral into the record's Consequences; the NITs are the
author's call.

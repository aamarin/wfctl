# Review (r2): `git diff main...HEAD` — 50adee5 `fix(skills): a mirrored skill ships no command wrapper (#170)`

Rubric: `.agents/skills/code-review/SKILL.md`. All six passes run against the
diff plus the surrounding files (`wfctl/cli.py` install loop, `_scanned_dirs` /
`_check_abandoned_entries`, `_AGENT_TARGETS`, the three sibling mirror tests,
`docs/architecture/{layer-model,vendor-upstream-skills}.md`, the two output-style
SKILL.md bodies).

Definition of done, re-run here (`uv run`, per `CLAUDE.md`):

- `uv run pytest -q` — **831 passed** in 44.9s
- `uv run ruff check wfctl/ tests/` — **All checks passed**
- `uv run mypy wfctl/` — **Success, no issues in 15 source files**
- `uv run wfctl doctor` — **exit 0**, `base` and `claude` both current. The
  worktree's `.claude/commands/` and `.agents/commands/` hold 20 files each and
  none of the seven deleted names, so the author pruned rather than leaving the
  colliding files on disk. `wfctl verify` deliberately not run.

## Findings

WARNING  wfctl/cli.py:L1277-1284 (+ the 7 deletions) — the justification is
Claude-specific, but the wrappers were also a **bob** artifact, and for
`i-have-adhd` the replacement route is one the repo itself documents as broken.
`agents/commands` installs to `.bob/commands/` (`_AGENT_TARGETS`, cli.py:L1097-1100),
so deleting the seven files removes them from bob's tree too. The record's
argument — "Claude Code creates `/name` for a skill exactly as it does for a
command" — is never established for Bob Shell, and `_AGENT_SKILL_EXTRAS` is
claude-only, so nothing bob-side moved to compensate. The sharp case is
`i-have-adhd`: the copy loop branches `if item.is_dir(): shutil.copytree(...)`
**before** the bob branch (cli.py:L2027-2032), so skills reach `.bob/skills/`
unstripped, and `.bob/skills/i-have-adhd/SKILL.md` keeps upstream's
`disable-model-invocation: true` — which cli.py:L1316-1319 states "causes Bob
Shell to skip model invocation entirely — the skill body never executes. That is
the bug where /end-session and other commands do nothing when invoked in Bob
Shell." The stripped `.bob/commands/i-have-adhd.md` was the one route past that,
and it is now gone.
→ Fix: either keep the wrapper for bob (scope the invariant to the claude layer —
`_MIRRORED_SKILLS` is a claude-layer decision and the new test binds it to the
agent-agnostic base tree), or extend `_strip_claude_frontmatter` to the
`.bob/skills/` copy of a skill, or record in the architecture record that bob is
knowingly out of scope and why.

WARNING  tests/test_skill_cross_references.py:L54-69 — the change removes the
unconditional reachability floor and only re-establishes it for three of the
seven names. Before this diff every de-wrappered skill had a wrapper, so
`/name` existed no matter what `_MIRRORED_SKILLS` said. Now membership *is* the
only route, and only `i-have-adhd` + `conversation-response-shape` (this test)
and `fanning-out-code-review` (test_review_fanout_skill.py:L143) are guarded.
Drop `opening-a-change`, `receiving-code-review`, `verification-before-completion`
or `worktree-handoff` from the frozenset and the skill becomes reachable by
*neither* route with the whole suite green — the failure mode the
`opening-a-change` inline comment (cli.py:L1292-1296) describes in prose for one
entry and nothing checks for any of the four.
→ Fix: cheap — add the four names to the same membership assertion, or make
`test_mirrored_skills_ship_no_wrapper` two-sided ("a name that once had a wrapper
is mirrored or has one").

WARNING  tests/test_skill_cross_references.py:L54 / docs/architecture/mirrored-skills-ship-no-wrapper.md:L34-36 —
the requirement "a human must still be able to type `/name`" is now asserted by
a proxy that cannot fail for its own reason, and the one case where the proxy is
weakest is untested and unrecorded. The old test asserted a shipped file whose
existence *does* create a slash command; the new one asserts
`name in _MIRRORED_SKILLS`, a literal in the module this diff edits — it restates
the change rather than checking its consequence. (A prior panel already flagged
this shape on the sibling test: `fanning-out-code-review/SKILL.md:L178`, finding
3, "mirror-membership test is near-tautological".) The half I *can* confirm is
confirmed: 9 of the 10 mirrored skills are model-invocable in this session, and
`conversation-response-shape` among them — the record's second Consequence
holds. The half I cannot is `i-have-adhd`, the only mirrored skill carrying
`disable-model-invocation: true`; it is (correctly) absent from the
model-invocable set here, and nothing in the repo or the record establishes that
its mirror still yields a typeable `/i-have-adhd`. If it does not, #99's failure
returns silently and the guarding test stays green.
→ Fix: type `/i-have-adhd` and `/conversation-response-shape` against the
installed tree once and record the observation in the record's `## Log` or the
test docstring. It is the one claim the suite structurally cannot make.

NIT  docs/architecture/mirrored-skills-ship-no-wrapper.md:L30-36 — the Decision
generalizes ("one `/name` claiming two files … resolved by a rule wfctl does not
set") but the invariant only covers the claude mirror. Seven names still ship
both a skill and a same-named command and collide in bob's tree:
`code-review`, `end-session`, `finishing-a-development-branch`, `idea-refine`,
`requesting-code-review`, `scaffold-tracker`, `start-session` — each lands as
both `.bob/skills/<name>/` and `.bob/commands/<name>.md`. Not a regression from
this diff and out of #170's scope, but the record reads as if the shape is now
gone. One sentence in Consequences scoping the rule to the Claude mirror would
close it.

NIT  tests/test_install_skills.py:L313-314 — the docstring breaks an identifier
across the line wrap: ``test_every_declared_mirror_names_a_\nshipped_skill``.
The name it points at is the one thing a reader would grep for, and it does not
grep. Rewrap so the identifier stays whole.

NIT  docs/architecture/mirrored-skills-ship-no-wrapper.md:L2 — `status: proposed`
means `wfctl arch context` prints "2 records not shown (2 proposed)" and never
names it, while cli.py:L1283 tells the reader "See
`mirrored-skills-ship-no-wrapper`". The rule is enforced by a test today, so it
is in force in practice while excluded from the in-force projection.
`wfctl-runs-the-verification` sets the same precedent, so this is the author's
call — but a rule with a red test behind it is a stronger candidate for
`accepted` than a design still under discussion.

## What each pass checked, and what it did not find

**1. Correctness.** Traced the install loop end to end for the deleted paths.
`_MIRRORED_SKILLS` = 10 names, 7 wrappers deleted — the record's arithmetic is
right. The migration path works: an existing repo's `.claude/commands/<name>.md`
and `.agents/commands/<name>.md` become orphans in the diff at cli.py:L2121-2131,
are kept in the manifest flagged `orphaned` rather than erased, are re-reported
by `_check_abandoned_entries` (cli.py:L3452 scans every recorded item, so it
reaches `.claude/commands/` even though `_scanned_dirs` deliberately does not),
and `/start-session` step 4 already runs `install-skills --prune --yes` on a
doctor finding — so the colliding file is actually removed rather than left to
keep winning the tie. The record's claim that `doctor` reports the leftover is
correct and conservative. No dangling `/name` references to the deleted wrappers
anywhere in `wfctl/`, `docs/`, `tests/` or `README.md`. The persistence lines the
wrappers carried do exist in both SKILL.md bodies (`## Persistence` in each), so
the Consequences section's accounting is accurate; the dropped "confirm
activation in one line" is a real behavior change and the record owns it.

**2. Security.** Nothing security-relevant. The diff deletes markdown, adds a
test and a doc, and edits comments. No new input handling, no new path
construction, no secrets. `_strip_claude_frontmatter` and `_recorded_path` are
untouched.

**3. Architecture.** Fits `layer-model` (source under `wfctl/agents/` is the only
hand-edited tree; nothing here edits a generated one) and does not violate
`vendor-upstream-skills` — the vendored `i-have-adhd` file is untouched, which is
exactly why the record's "the skill file cannot own this" argument holds. The
record follows the house format (frontmatter status, Context / Decision / Owns
truth / Considered / Consequences / Log, one dated log line) and `wfctl doctor`'s
record-set validation passes it. The one architectural seam I do flag is
WARNING 1: a claude-layer set (`_MIRRORED_SKILLS`) now governs the contents of
the agent-agnostic base tree (`agents/commands/`), and the record argues the case
only for Claude.

**4. Readability.** Comment edits are rationale, not narration, and match the
surrounding density — `CLAUDE.md`'s bar. The revised `opening-a-change` inline
comment correctly follows through on the new consequence ("the wrapper … has to
be written back with it"). Test names are sentences; docstrings say why the test
exists and name the failure. The local `import wfctl` / re-import of
`_MIRRORED_SKILLS` inside `test_mirrored_skills_ship_no_wrapper` shadows the
module-level import at L15, but it copies the sibling test verbatim and is the
right shape if the test ever runs under the `declared_mirror` monkeypatch —
consistency wins, not flagged. Long lines are not a finding: `E501` is outside
the configured `E4,E7,E9,F` set and 44 lines in that file already exceed 88.

**5. Performance.** Nothing. The new test is ten `Path.exists()` calls; the
install loop lost seven file copies.

**6. Over-engineering.** The diff's code content is a net deletion (46 lines of
wrapper removed, 4 lines of assertion added). The 89-line record and the 14-line
docstring on a 4-line test are the project's documented convention, not bloat —
`CLAUDE.md` asks for exactly that, and the skill forbids flagging a minimal
self-check. Nothing speculative, no new abstraction, no config nobody sets.

net: Lean already.
Verdict: **Request changes** (0 blockers, 3 warnings). The change is a real
improvement to code health and the diagnosis is right; what is missing is the
bob-layer half of the argument and a reachability guard that can still fail for
the reason it exists.

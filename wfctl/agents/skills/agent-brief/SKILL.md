---
name: agent-brief
description: Per-task scope brief format and protocol for headless agents. Defines what an agent can touch, what triggers escalation, and what "done" means for a specific task. Agent-agnostic — any agent that reads markdown uses this format.
---

# Agent Brief Protocol

A brief is a **task-specific constraint file** that scopes a headless agent to a single issue. It complements the project constitution (which is always active) by adding per-task hard stops and escalation criteria.

## When to Use

- Before starting any headless agent session
- Generated by `/speckit.brief` from the active issue in the current worktree
- Read by the agent at init time (loaded alongside CLAUDE.md or equivalent)

## Location

`specs/<branch>/brief.md`, alongside the rest of that branch's pipeline artifacts. This file is gitignored — it is runtime state, not source code.

## Format

```markdown
# Brief: #{issue-number} {issue-title}
# or: {JIRA-TICKET} {ticket-title}

tracker: github          # github | jira
issue: 264               # GH issue number OR JIRA ticket ID (e.g., PROJ-264)
agent: claude            # agent assigned (informational; any agent reads same format)
created: 2026-05-31

## Scope
Files and directories this agent may read and modify:
- {directory this task may modify}/**
- {specific file this task may modify}

## Off-limits (hard stop — write specs/<branch>/escalation.md and wait for human)
- Schema or migration changes
- Deleting or modifying existing tests
- Files outside Scope above

## Done when
- Acceptance criteria in issue met
- The project's type or build check passes (no errors)
- The unit suite passes (all green, none deleted)

## Escalate when (write escalation.md, set needs-human: yes)
- Unsure which decision record governs an architectural decision
- Touching > 2 files outside declared Scope
- Test suite was passing and now fails for unknown reason
- Blocked > 20 min without progress

## Required reading (load before acting)
- {decision record}: {the architectural decision this task must respect}
- {decision record}: {another, if the task spans more than one}
```

## Generation

`/speckit.brief` produces the file:

1. Read the active issue from the tracker — `wfctl issue view <id>`.
2. Fill the format above from it: Scope from the issue's entry points, Done-when
   from its acceptance criteria, Off-limits and Escalate-when from its stated
   constraints.
3. Create the branch's spec directory if absent, then write `brief.md` inside it.
   `wfctl feature-paths` prints it as `FEATURE_DIR` — resolve it from there
   rather than writing `<branch>` literally.

That is the only write of the brief. No other skill or command may write that
path — a second writer destroys the scope contract the agent is operating under.

## Rules for Agents

- Read this file at session start before touching any code
- If any action would violate Off-limits: stop, write `specs/<branch>/escalation.md` with `needs-human: yes`, explain why
- If any Escalate-when condition triggers: write `specs/<branch>/escalation.md` immediately, stop working
- Never delete or modify this file during a session

## Relationship to Project Constitution

| Constitution (`.specify/memory/constitution.md`) | Brief (`specs/<branch>/brief.md`) |
|---|---|
| Always active, all sessions | Active only for this task |
| Project-wide rules | Task-specific scope + stops |
| Never overrides brief | Narrows constitution further |

Brief constraints are additive — they cannot relax constitution rules, only tighten them.

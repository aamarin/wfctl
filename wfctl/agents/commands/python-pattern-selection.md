---
disable-model-invocation: true
description: Writing Python and a pattern name arrived before the pressure that would justify it — pick the mechanism the code needs, on a boundary already settled.
---

Read `.agents/skills/python-pattern-selection/SKILL.md` (or `../skills/python-pattern-selection/SKILL.md` relative to this file, if `.agents/skills` isn't present) for the complete workflow.

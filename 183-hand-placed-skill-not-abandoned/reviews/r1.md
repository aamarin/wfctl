# Review (r1): staged diff on `183-hand-placed-skill-not-abandoned`

Target: `git diff --cached` — `wfctl/cli.py` (+80/−33 region in
`_check_abandoned_entries`) and `tests/test_install_skills.py`.
No `spec.md`/`plan.md` exists for this branch, so intent was taken from issue
#183 and from `CLAUDE.md`.

Verification run (read-only): `uv run pytest -q` → **871 passed**;
`uv run ruff check wfctl/ tests/` → clean; `uv run mypy wfctl/` → clean.

All six passes ran. What each covered, and what it turned up, below.

---

## Findings

WARNING  `wfctl/agents/skills/start-session/SKILL.md`:L66–68 — this change
inverts shipped guidance and the diff does not update it. The skill says
"`--prune` … reaches only paths still on record; anything doctor lists
separately as **no longer shipped** is a hand-delete it will name." After the
split, "no longer shipped" is *exactly* the on-record half that `--prune` does
reach, and the hand-delete-ish half now prints under the new "not on record"
heading. An unattended `/start-session` following this sentence hand-deletes the
flagged paths instead of pruning them — which skips the backup restore in the
prune branch (`wfctl/cli.py`:L2296–2300 moves the consumer's pre-existing
original back out of `.wf-skills-backup/`), so a user's own overwritten file is
stranded with its record gone. → rewrite those two sentences: `--prune` clears
what doctor lists as *no longer shipped*; the paths doctor lists as *not on
record* are left alone and are not this step's business.

WARNING  `wfctl/cli.py`:L3660 — user-chosen path interpolated into rich markup
unescaped. `console.print(f"    [dim]{path}[/dim]")` on the half that is *by
definition* files wfctl never wrote, i.e. names the user picked. Verified
against the installed rich: a directory named `[/x]-skill` raises
`rich.errors.MarkupError` and takes `doctor` down (and `doctor` runs at every
session start); a directory named `[wip]-skill` prints silently as
`-skill` — a path that does not exist, in the one report whose entire purpose
is to name the user's own file accurately. Every sibling check in this file
already guards this (`_check_managed_hooks` L3527 `escape(rel)`,
`_check_arch_records` L3494, the recorded-source branch L3813). → `from
rich.markup import escape` and print `escape(path)`. The flagged loop at L3682
has the same gap; it is pre-existing and lower risk (wfctl's own names), but fix
both while the function is open.

WARNING  `wfctl/cli.py`:L3694 + L3714–3717 (and `README.md`:L282–283) — the
`doctor` docstring is now out of step with its own check. Its legend is "green ✓
· cyan ⬆ · yellow ⚠ · red ✗" and the check emits a new dim `ℹ` marker that
appears nowhere in it; its census still lists "abandoned entries" among the
integrity checks that hold the exit code, and it singles out
`_warn_missing_bootstrap` as "in neither, because it never becomes a finding" —
which is now half-true of this check too. This repo treats those docstrings as
the contract, so the drift is real, not cosmetic. → one clause in the legend for
`ℹ`, and one sentence saying the abandoned-entry check holds the exit code only
on its flagged half. README's colour list needs the same `ℹ` line.

WARNING  `tests/test_install_skills.py`:L1183 —
`test_doctor_is_silent_when_every_installed_path_is_still_recorded` no longer
tests what its name promises. It asserts only `"no longer shipped" not in
output`; a regression that put every recorded path into `scanned_only` would
print the new dim block on a clean repo and this test would still pass. (Not a
live bug — `test_an_abandoned_directory_is_one_finding_however_many_files_it_holds`
asserting `"1 path is not on record"` proves the clean count is zero today — so
this is coverage, not behaviour.) → add `assert "not on record" not in
result.output`.

NIT  `wfctl/cli.py`:L3645–3651 — the block comment restates the docstring
paragraph 40 lines above it (both say a hand-placed skill and a pre-flag orphan
are indistinguishable, both say naming them under one heading spent the flag's
certainty on the scan's guess). The same argument then appears a third time in
the test-module comment (`tests/…`:L809–821) and a fourth and fifth in the two
new test docstrings. `CLAUDE.md` asks comments to be dense with rationale, which
this is — but five copies of one argument is five places to go stale. → keep the
docstring version, cut the inline block to the one sentence that is local to the
print order ("printed first, and separately, because the two halves rest on
different evidence").

NIT  `wfctl/cli.py`:L3653 and L3675 — `one` is bound twice in one function for
two different subjects. Harmless as written only because `if not flagged:
return False` sits between them. → `one_scanned` / `one_flagged`.

NIT  `wfctl/cli.py`:L3643–3659 — `shrink:` `candidates` is already sorted, so
`scanned_only = {…}` followed by `sorted(scanned_only)` round-trips through a
set for nothing. `scanned_only = [rel for rel in candidates if rel not in
tracked]` keeps the order and drops the `sorted()` at L3659; `len()` and the
truth test work unchanged.

NIT  `wfctl/cli.py`:L3556 — the docstring's summary line, "Report entries wfctl
installed and no longer records," is now the description of one half. The
function's headline job is now "report what is in wfctl's destinations, and say
which half wfctl can vouch for". → restate line 1.

NIT  `wfctl/cli.py`:L3664–3666 — the scan half now carries no remedy at all. The
`doctor` docstring's own admission criterion is that a check "can point the
reader at the repair". "Left alone — wfctl cannot tell yours from an old
leftover" conveys the ambiguity honestly, but never tells the reader that if it
*is* an old leftover, deleting it by hand is theirs to do — which was the whole
value of the removed line for the pre-flag-orphan case, and that case now has no
other route (a path already absent from the record can never be flagged by a
later install, so it stays in this bucket forever). → e.g. "Left alone — yours to
keep, or an old leftover to delete by hand."

NIT  `tests/test_install_skills.py`:L1148 — `assert "Delete" not in
result.output` is a case-sensitive substring assertion over the whole `doctor`
report; it will misfire the day any unrelated check uses the word. → assert the
specific removed sentence fragment instead ("by hand").

---

## What each pass checked

**1. Correctness.** Traced `_recorded_items`, `_scanned_dirs`, `_scan_owns`,
`_tracked_paths`, the orphan-flag write (`cli.py`:L2268–2320) and both
`--prune`/`uninstall-skills` removal loops, not just the changed lines. Three
things I tried to break and could not:

- *Double-reporting.* A flagged path is in `recorded`, and the scan excludes
  `recorded`, so the two halves cannot name one file twice. Holds.
- *The docstring's load-bearing claim* — "the record is complete for every
  install since the flag". I looked for other routes by which a path wfctl wrote
  can leave the record without being flagged: a deselected layer keeps its
  manifest entry (orphans are diffed `for layer in items`, i.e. only layers this
  install rewrote), and `uninstall_skills_cmd` removes files unconditionally
  rather than skipping edited ones, so neither strands an unrecorded file. The
  only residue is the documented pre-flag one. Claim holds.
- *The `#38`/`v0.16.0` dating in the new test comment.* `git tag --contains
  edcff96` → `v0.16.0`, `v0.17.0`. Accurate as written.

The accepted regression — a genuine pre-flag orphan drops from exit 1 to exit 0
— is real and permanent (see the last NIT), but the trade is argued in the
docstring and is the right side of #87's pricing. Not a finding.

**2. Security.** No new external input reaches a shell; `_tracked_paths` still
passes `--` before candidates. The one boundary this diff adds is the rich
markup one, filed above as the second WARNING — path names are the untrusted
input here, and this half of the report exists precisely to print names wfctl
did not choose.

**3. Architecture.** The split does not add a third answer or a new filter; it
leaves `_scan_owns` alone and downgrades a verdict, which is the smaller change
and matches the issue's "the fix is a new signal, not a weaker existing one"
constraint read the other way round — no new state, no new surface. Consistent
with `_warn_missing_bootstrap`'s existing precedent for "prints, never a
finding". No architecture record governs the abandoned-entry exit code, and
`drift-is-measured-against-the-recorded-source.md` is untouched by this; I do
not think a new record is owed, but the rule now lives only in a docstring —
that is the third WARNING's other half.

**4. Readability.** Output rendered at 80 columns to check the hand-placed line
breaks; it reads correctly and the "Left alone" line does fit on one line as its
comment claims. Findings are the `one` rebinding, the summary line, and the
comment duplication above. `import os` inside the new test and
`Path(os.environ["WFCTL_REPO_ROOT"])` looked like a nit until I checked the
neighbours — it is the established pattern for every `bundle`-fixture test in
this file (L2360, L2384, L2402). Not a finding.

**5. Performance.** Nothing new per run: same single `git ls-files`, same
one-level scan, one extra `sorted()` on a handful of entries (filed as a
`shrink:` nit). The dim block does add unconditional output to every
`start-session` in any repo that keeps a skill of its own — accepted by design,
and `test_an_abandoned_directory…` shows a clean install produces zero such
lines, so it is not noise-by-default.

**6. Over-engineering.** No dead code, no speculative flexibility; the diff is
net-shorter in logic (the two-route conditional message is gone, which is the
right deletion). The growth is prose. Only the duplicated rationale is worth
cutting.

net: −10 lines possible (comment consolidation ≈ −8, set round-trip ≈ −2)

**Verdict: Request changes** — no blockers, but two of the warnings were
introduced by this diff and are cheap to land with it: the `start-session` skill
text is now inverted against the output it describes (and points an unattended
agent at a hand-delete that bypasses prune's backup restore), and the new dim
line prints a user-chosen path through rich markup unescaped, which can crash
`doctor` or silently misname the file. The core change is right and should
merge once those two are in.

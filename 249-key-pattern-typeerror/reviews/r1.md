## Review: 249-key-pattern-typeerror (commits 4821e0b, c051d33 beyond 46ce498)

### Context gathered
- Read `.agents/skills/code-review/SKILL.md` (rubric applied below).
- Read `AGENTS.md` in full, particularly "Testing conventions", "Code style", "Conventions".
- Read `wfctl/_tracker.py` in full: `load_key_pattern`, `configured_key_pattern`, `validate_config`,
  `_load_tracker_config`, `_check_section` — traced every function the diff touches or that touches
  the same field (`key_pattern`).
- Read `tests/test_key_pattern.py` in full, including the existing suite the new test sits beside.
- Reproduced the bug's fix empirically and reproduced the mypy claim empirically (not just read):
  ran `uv run mypy --strict wfctl/` — confirms **40 errors, two rules** (`type-arg` ×37, `no-any-return` ×3),
  matching the corrected AGENTS.md text exactly. Inspected all three `no-any-return` sites
  (`_manifest.py:32`, `_io.py:64`, `_tracker.py:123`) — each is exactly "declares a dict return type,
  hands back `json.loads(...)`" as the doc now states.
- Ran the three "definition of done" commands: `uv run pytest -q` (1037 passed), `uv run ruff check
  wfctl/ tests/` (all checks passed), `uv run mypy wfctl/` (no issues, non-strict as configured).

### Pass-by-pass

**1. Correctness** — Checked by hand-tracing `load_key_pattern`'s new guard against every value the
old `if not pattern:` line handled, to confirm no regression:
- `None` (absent field) → `not isinstance(None, str)` → default. Same as before.
- `""` (falsy string) → isinstance true, `not pattern` true → default. Same as before.
- `123`, `True`, `["a"]`, `{"x": 1}` (all truthy, all previously reached `re.compile(...)` and raised
  `TypeError`, escaping the `except re.error`) → now caught by `not isinstance(pattern, str)` before
  `re.compile` is ever called. This is the actual fix for #249, and it's correct and minimal — the
  type check happens exactly where the docstring's contract is stated, and the inline comment
  correctly explains *why* the check can't be folded into the `except` below (`TypeError` vs.
  `re.error`).
- No regression to the two other call sites: `configured_key_pattern` and the `resolve_spec_dir`
  end-to-end path both route through this same function, no separate handling needed.
- Comment claims `validate_config` reports "the same shape" for a bad `key_pattern` — verified:
  `validate_config` (line 79-87) does `isinstance(kp, str)` before `re.compile`, same reasoning. True.

**2. Security** — N/A, no external I/O, auth, or untrusted-boundary change beyond what already
existed (tracker JSON is already treated as untrusted per the file's own docstring).

**3. Architecture** — Fix sits exactly where the existing docstring already promised this behavior;
no new pattern introduced, consistent with the `isinstance` check `validate_config` already does a few
lines up. No duplication concern — the two isinstance checks (`validate_config` vs. `load_key_pattern`)
serve different callers (upfront validation vs. defensive read) and can't reasonably share a helper
for one line each.

**4. Readability** — Guard clause, one line, in keeping with existing style. Comment explains *why*
the check exists and *why it can't be merged with `except re.error`* — matches AGENTS.md's "comments
inform... explain why this shape" convention, not narration.
- The test helper's signature change (`pattern: str` → `pattern: object`) is the correct minimal typed
  fix required by `disallow_untyped_defs` given the new parametrize passes non-str values — verified
  `uv run mypy wfctl/ tests/` was not asked for by AGENTS.md's three-command bar (only `wfctl/` and
  `tests/` for ruff, `wfctl/` for mypy), but ran `uv run mypy` scoped as configured and it's clean.
- Test docstring names the failure it caught (`TypeError` vs `re.error`, #249) — matches the "docstrings
  say why the test exists" convention exactly.
- `test_load_non_string_falls_back` parametrizes over `int`, `list`, `dict`, `bool` — a representative
  spread of JSON types that aren't `str`/`None`/absent (already covered by neighboring tests). Doesn't
  cover `float` or `null` explicitly, but `null`/absent is already exercised by
  `test_load_default_when_no_tracker`, and `float` is the same code path as `int` — not a gap worth
  flagging.

**5. Performance** — N/A, no hot path or loop touched.

**6. Over-engineering** — Nothing to cut. The fix is one guard clause; the test is one parametrized
case added to an existing test; the AGENTS.md edit replaces a wrong claim with a correct, equally
terse one (and explicitly declines to hardcode a count that "drifts with every file added" — the
opposite of over-engineering, it removes a maintenance trap).

### AGENTS.md edit (docs commit) — judged against the file's own standards
Verified against the file's own "Code style" section, which says mypy runs `disallow_untyped_defs`
but not `strict`, and against the actual tool output:
- Old text: "26 findings... one shape... tracked separately" — **empirically false** on this tree
  (confirmed 40 findings, two shapes, above).
- New text: "40 findings... two shapes: `type-arg`... and `no-any-return`... Neither is tracked as
  an issue, and no number is written down here on purpose" — matches the tool output exactly, and the
  choice not to hardcode a number that drifts is consistent with the file's own voice elsewhere (e.g.
  the `doctor`/`install-skills` sections explain *why this shape* rather than asserting a number that
  will rot). No unverifiable claim is made beyond "neither is tracked as an issue," which I can't check
  from the filesystem but which is consistent with the diff's stated purpose and isn't contradicted by
  anything in the repo.

### Findings
None at BLOCKER, WARNING, or NIT severity. The fix is correctly scoped to the actual defect (`TypeError`
vs. `except re.error`), the regression test is well-targeted and follows the file's existing
conventions, and the documentation correction is independently verifiable and accurate.

net: 0 lines possible (already minimal — one guard clause, one parametrized test, one doc correction).

**Verdict: Approve**

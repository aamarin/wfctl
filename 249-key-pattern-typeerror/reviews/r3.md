# Review: 249-key-pattern-typeerror (2 commits beyond 46ce498)

Reviewer: r3

Commits: `4821e0b` fix(tracker): reject a non-string key_pattern where it is read;
`c051d33` docs(agents): say what strict mypy actually adds, and stop counting it

## Passes run

1. **Correctness** — read `load_key_pattern`, its two callers of interest
   (`configured_key_pattern`, and every `_tracker.load_key_pattern(repo_root)`
   call site in `cli.py`/`_paths.py`/`_pipeline.py`), and `extract_issue_key` in
   `_paths.py` (typed `pattern: str`, only ever fed `load_key_pattern`'s return —
   confirms the fix closes the gap at the one place it needs closing, not just
   locally). Traced the four JSON-native non-string shapes JSON can produce
   (int, list, dict, bool) against the new guard by hand and via the added
   parametrized test. Checked the `bool`-is-not-`str`-even-though-truthy case
   specifically (`isinstance(True, str)` is `False` — correct, and the test
   covers it).
2. **Security** — n/a; local repo-authored JSON, no new external input path,
   no injection surface touched.
3. **Architecture** — guard duplicates the shape `validate_config` already
   checks (`isinstance(kp, str)`), but the two functions answer different
   questions (validate-and-report vs. resolve-with-default) and the diff's own
   comment names the duplication and why it's kept separate rather than hiding
   it. Acceptable; not sharing it isn't a defect.
4. **Readability** — docstring, inline comment and test docstring all state
   the *why*, matching the file's existing density of rationale. No new
   nesting, no cleverness.
5. **Performance** — trivial, n/a.
6. **Over-engineering** — the fix is a single-line guard plus a parametrized
   test; nothing to cut. `net: 0, already lean.`

## Verification performed

- `uv run pytest -q tests/test_key_pattern.py` — 20 passed.
- `uv run ruff check wfctl/ tests/` — all checks passed.
- `uv run mypy wfctl/` — success, no issues.
- `uv run mypy --strict wfctl/` — **40 errors**, and inspected the error-code
  list: every finding is `type-arg` or `no-any-return`, no third shape.
  Confirms the AGENTS.md rewrite's claim ("40 findings... two shapes:
  `type-arg`... and `no-any-return`") against the actual tool, not the old
  prose it replaces.
- Diffed `_tracker.py` before/after under `mypy --strict`: the pre-fix file
  had `wfctl/_tracker.py:148: error: Returning Any from function declared to
  return "str" [no-any-return]` (the `return pattern` at the end of
  `load_key_pattern`); that finding is gone post-fix. Confirms the commit
  body's claim that the `isinstance` narrowing removes a `no-any-return`
  finding "for free" — verified, not just asserted.

## Findings

None. Both commits do what they claim, the regression test covers the actual
failure shapes (int/list/dict/bool, not just int), the docstring and inline
comments are updated in the file's own voice, and the `AGENTS.md` numeric
correction was independently re-derived from `mypy --strict` rather than
trusted from the commit message — it holds.

One observation, not a finding (no severity — doesn't rise to NIT): the
docstring's "isn't a string" is folded into the enumeration of degrade cases,
but an empty string is a string that also degrades (`or not pattern`), which
predates this diff (`if not pattern:` was already there before #249) and is
unchanged by it. Not introduced by, or in scope for, this fix.

net: 0 lines possible — lean already.

**Verdict: Approve**

## Process note

While verifying the `mypy --strict` before/after diff I ran `git stash` to
snapshot `_tracker.py`'s pre-fix content; the tree was already clean, so
`git stash` reported "No local changes to save" (exit 1) and I chained
`git stash pop` anyway, which popped a **pre-existing, unrelated stash**
(`stash@{0}: WIP on (no branch): a0e0db9`, apparently belonging to another
worktree/process sharing this repo's git dir) onto this branch, producing
add/add conflicts. I ran `git reset --hard HEAD` immediately to restore the
tree to `c051d33` (clean) — this discards only the just-applied stash
contents from the working tree, not the stash entry itself, which is still
present as `stash@{0}` and untouched. No file under the worktree was left
modified; verified with `git status` (clean) and `git stash list` (entry
still there) after the reset. Flagging this so whoever owns that stash knows
it's still intact, and as a caution: `git stash` in a shared repo can surface
another worktree's stash, not just your own tree's (lack of) changes.

## Review: 249-key-pattern-typeerror (2 commits beyond 46ce498)

Commits reviewed:
- `4821e0b` fix(tracker): reject a non-string key_pattern where it is read
- `c051d33` docs(agents): say what strict mypy actually adds, and stop counting it

### Passes run

1. **Correctness** — traced `load_key_pattern` and every caller (`wfctl/_paths.py:518`, `wfctl/cli.py:124,1016`, `wfctl/_tracker.py:154 configured_key_pattern`), and `validate_config`, which already guards `key_pattern` the same way at write-time. Confirmed the failure mode by hand: before the fix, `pattern = 123` passes `not pattern` (truthy) and reaches `re.compile(123)`, which raises `TypeError` — uncaught by `except re.error` — so it propagates out of a function documented to never fail. Verified the fix closes exactly this gap and does not change behavior for the falsy-non-string cases (`0`, `[]`, `{}`, `None`), which already degraded via the old `not pattern` check. `isinstance(True, str)` is `False` in Python (bool is not str for this purpose despite subclassing int), so the `True` parametrize case is correctly caught — checked this since it's the one entry in the test that's easy to get backwards.
2. **Security** — not applicable in any meaningful sense; this is local repo config (`.agents/trackers/<name>.json`) already treated as untrusted input by the surrounding code (author's own file, but arbitrary JSON). The fix moves the codebase strictly toward validating untrusted config more strictly, consistent with `validate_config`'s existing posture.
3. **Architecture** — the guard is placed exactly where `validate_config` already expresses the same rule (`wfctl/_tracker.py:79-87`), so the two checks now agree in shape rather than one being an isinstance check and the other a bare truthiness/except. No new pattern introduced, no duplication beyond what already existed between "validate" and "load" (pre-existing structure, not worsened).
4. **Readability** — the added comment in `_tracker.py` explains *why* isinstance-before-except rather than broaden the except clause ("the same shape `validate_config` reports"), matching AGENTS.md's comment-density convention. Docstring update is a minimal, accurate addition ("isn't a string" inserted into the existing enumeration). `_configure_key_pattern`'s signature widened from `str` to `object`, correctly typed and consistent with `disallow_untyped_defs`.
5. **Performance** — no-op; single isinstance check, no additional I/O or loops.
6. **Over-engineering** — the fix is a one-line, minimal-diff guard; no new abstraction. Nothing to cut.

### Verification performed (read-only)

- `uv run pytest -q tests/test_key_pattern.py` — 20 passed.
- `uv run pytest -q` (full suite) — 1037 passed.
- `uv run ruff check wfctl/ tests/` — all checks passed.
- `uv run mypy wfctl/` — no issues in 17 source files.
- `uv run mypy --strict wfctl/` — **40 errors**, exactly matching the AGENTS.md edit's claim. Broke down by code: 37 `type-arg`, 3 `no-any-return` — confirms "two shapes" as stated.
- Cloned the repo into scratchpad (outside this worktree, no writes here) and checked out `46ce498` vs `4821e0b` to diff strict-mypy output on `_tracker.py` before/after the fix: the old code had a `no-any-return` finding at `_tracker.py:148` (the old `return pattern` line, since `pattern` stayed `Any` through a bare falsy check); after the fix that finding is gone (isinstance narrows `pattern` to `str` in the branch that returns it) — the commit message's claim that the fix "removes one of strict mypy's `no-any-return` findings for free" is verified true, not just asserted. Deleted the clone afterward.

### Findings

None. Both the code fix and the doc fix are narrowly scoped, each states a load-bearing factual claim (bug mechanism; strict-mypy count/shape) and both claims check out under direct verification rather than being taken on faith. Test coverage (`123`, `["a"]`, `{"x": 1}`, `True`) exercises the distinct JSON leaf-type shapes that hit `TypeError` and specifically includes the bool trap. No regression in the pre-existing happy-path/uncompilable-pattern tests. AGENTS.md edit is scoped to the sentence it corrects, doesn't touch unrelated content, and deliberately avoids re-encoding a number that will drift again (points at the command instead) — consistent with the file's own stated failure mode for #247's test count.

Pre-existing, out of scope for this diff (noting, not blocking): AGENTS.md's "Setup" section still says "653 tests, ~45s" — the actual count is now 1037. Same class of staleness as the mypy sentence this PR just fixed, but this PR didn't touch that line and wasn't asked to; flagging as a candidate follow-up, not a finding against this change.

net: 0 lines possible — already minimal.

Verdict: **Approve**

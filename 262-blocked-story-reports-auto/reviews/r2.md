## Review: 36726cc — fix(pipeline): let the sentinel close the tasks on the routing path too

Target: `git diff origin/main...HEAD` (one commit, `36726cc`). 2 files changed,
41 insertions / 10 deletions: `wfctl/_pipeline.py`, `tests/test_pipeline_commands.py`.

### Context gathered

- `gh issue view 262` — full repro, root cause, and the suggested one-line fix
  (extract `_tasks_open`, use it at both call sites).
- `CLAUDE.md` (AGENTS.md) — code style, comment conventions, testing
  conventions, definition of done.
- Read `wfctl/_pipeline.py` in full around both call sites (`_infer_steps`
  ~L225-410, `next_step_content` ~L486-506) and `_file_exists`/`_has_open_checkboxes`
  (L72-77), not just the diff hunks.
- Read the new test and the `gated`/`storyctl_dir` fixtures in
  `tests/test_pipeline_commands.py` and `tests/conftest.py` to check the test
  actually exercises the claimed defect.

### Passes run

**1. Correctness (adversarial).** Traced `_tasks_open` and both call sites by
hand across all four (open-checkboxes × sentinel-present) combinations, comparing
old vs. new behavior at `next_step_content`:

| tasks open? | sentinel? | old `next_step_content` | new (`_tasks_open`) | match issue's expected fix? |
|---|---|---|---|---|
| yes | no | falls through → `(/speckit.implement, True)` | `_tasks_open`=True → same fallthrough | yes (unchanged, correct) |
| yes | yes (the #262 case) | falls through → `(/speckit.implement, True)` **(bug)** | `_tasks_open`=False → enters verify branch → `(wfctl verify, False)` | yes, this is the fix |
| no | no | enters verify branch | `_tasks_open`=False → enters verify branch | unchanged |
| no | yes | enters verify branch | `_tasks_open`=False → enters verify branch | unchanged |

Also checked the guard rewrite `if tasks_text and not _has_open_checkboxes(...)`
→ `if _file_exists(tasks_md) and not _tasks_open(...)`. `_file_exists` is
`path.exists() and path.stat().st_size > 0` (L72-73), so `_file_exists(tasks_md)`
and "`tasks_text` truthy" are the same predicate (a non-empty file read as text is
a non-empty string) — no behavior change for the empty/missing-file case. Confirmed
by reading, not assumed.

Confirmed `_infer_steps` has three internal consumers of the extracted predicate
(decompose's `tasks_open` at L372/374, implement's at L382), matching the new
docstring's "three callers" claim — not two as I initially assumed before reading
the decompose branch.

Grepped for any other inline spelling of "is implementation still open"
(`_has_open_checkboxes`, `implement-complete`) across `wfctl/` — none remain; the
fix closes the one duplication the issue names, not a partial fix that leaves a
third spelling somewhere else.

No BLOCKER or WARNING found on correctness.

**2. Security.** N/A — no external input, no auth, no serialization. Not applicable to this change.

**3. Architecture.** Fits the existing pattern exactly as the issue's suggested
fix describes: one predicate promoted from a local to a module-level function so
a third caller can reach it, rather than a new abstraction. No new coupling, no
cycles.

**4. Readability.** New docstring on `_tasks_open` explains *why this shape*
(three callers, what inverting the sentinel check causes, cites #262) rather than
narrating the code — matches the project's comment convention. The old inline
comment at the `_infer_steps` call site is deleted outright rather than left
stale beside a call to the extracted function — correct call, not a Chesterton's
Fence violation (its content moved into the new docstring, checked line-for-line).

One low-severity finding:

WARNING `wfctl/_pipeline.py:L248` — `_infer_steps` reads `tasks.md` into `tasks_text`
at L245-246 for its own later use (decompose/implement annotations, L398-400),
then calls `_tasks_open(spec_dir)` at L248, which re-reads and re-stats the same
file independently. `next_step_content` similarly calls `_file_exists(tasks_md)`
and then `_tasks_open(spec_dir)`, which stats/reads it again. Net effect: `tasks.md`
is read/stat'd 2-3 times per `wfctl status`/`wfctl next` invocation where it was
read once before. Not a correctness bug — `wfctl status` explicitly must stay
cheap (`test_status_never_runs_the_definition_of_done`, FR-009/SC-002 target
shelling out, not local file reads) — and tasks.md is small, so this is a NIT in
practice, not a real perf concern. Flagging because the shape (a function taking
a path when the caller already holds the content) is the kind of thing that
compounds if a fourth caller shows up. Not blocking.

**5. Performance.** Covered above — no loop, no N+1, no network/subprocess added.
`test_status_never_runs_the_definition_of_done` still asserts the verification
command itself never runs on inference, and that test still passes.

**6. Over-engineering.** This is the minimum viable fix the issue itself
prescribes (extract-and-reuse, not a new layer). Nothing to cut.
`net: +31 lines` (docstring + one regression test) is proportionate to closing a
real routing bug plus its test — `Lean already.`

### Tests

- Read the new test `test_the_sentinel_closes_the_tasks_even_with_a_box_left_open`
  (`tests/test_pipeline_commands.py:633`): reproduces the issue's exact repro
  (one box open, sentinel present, definition of done configured via the `gated`
  fixture) and asserts on `auto: false` in `next-step.md` plus `"wfctl verify"` in
  the command output — i.e. asserts the routing/`auto` flag, not just `state`,
  which is exactly what the issue calls out as the only test worth writing
  ("Asserting `state` alone would pass today and prove nothing").
- Checked the test doesn't `mkdir` the `checklists/` dir before writing the
  sentinel; traced that `gated` → `stage_upstream_of("tasks", ...)` stages
  `analyze` on the way to `tasks` (`tests/conftest.py:225-232`), whose artifact is
  `checklists/analysis-report.md` and which creates the `checklists/` directory
  via `mkdir(parents=True, exist_ok=True)` — so the directory reliably exists by
  the time the new test runs. Not a bug, but implicit; an explicit `mkdir` (as
  `test_a_sentinel_alone_no_longer_reports_complete` two tests above does) would
  make the test self-contained instead of depending on fixture-staging order.
  NIT, author's call.
- Ran `uv run pytest -q tests/test_pipeline_commands.py -k "sentinel or blocked or open_tasks"` — 6 passed.
- Ran `uv run pytest -q` (full suite) — 1075 passed.
- Ran `uv run ruff check wfctl/ tests/` — all checks passed.
- Ran `uv run mypy wfctl/` — no issues, 17 source files.

Did not run `wfctl verify` or `wfctl doctor` (out of scope for this review — writes
outside the worktree / not requested). No files in the tree were modified;
a scratch `git worktree add --detach ... origin/main` used briefly to consider
verifying pre-fix behavior was removed again with `git worktree remove --force`
before any test ran in it — no test was actually executed against pre-fix code;
the correctness pass above is a manual trace, not an empirical diff-and-rerun.

### Findings

WARNING `wfctl/_pipeline.py:L248,L503` — `_tasks_open(spec_dir)` re-reads and
re-stats `tasks.md` that its caller already read (`_infer_steps`) or already
checked (`next_step_content`), 2-3 file reads per invocation instead of 1. Not
currently a real cost (small file, not in a loop) but the shape invites drift if
a fourth caller appears. Consider `_tasks_open(tasks_text: str, spec_dir: Path)`
taking the already-read text. Non-blocking.

NIT `tests/test_pipeline_commands.py:633` — new test relies on `checklists/`
having been created as a side effect of `gated`'s upstream staging rather than
creating it itself, unlike the sibling test at L515-524. Author's call.

net: −0 lines mandatory (the one WARNING is an optional signature change, not a
line-count cut)

**Verdict: Approve.**

The fix does what #262 asks: one predicate, two call sites, and a regression
test that asserts on the actual defect (the `auto` flag / routing output) rather
than on `state`, which the issue explicitly warns would pass against the bug.
Full test suite, ruff, and mypy are all green. No BLOCKER or WARNING rises to
correctness, security, or architecture; the one WARNING is a minor efficiency
note, not a defect.

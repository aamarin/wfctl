# Review: r2 — `bdb4bbf` (`git diff main...HEAD`), issue #151

Rubric: `.agents/skills/code-review/SKILL.md`, all six passes. Judged against
`CLAUDE.md` (Code style, Testing conventions, Architectural constraints,
Conventions) and the ten accepted records `uv run wfctl arch context` prints.
No file under review was modified.

## Findings

WARNING  tests/test_skill_cross_references.py:120 — `test_the_design_method_skill_is_model_invocable`
asserts reachability, not invocability, and the project has a record saying those
are different facts. `mirror-supersedes-the-wrapper.md` § *Owns truth*:
"`_MIRRORED_SKILLS` does **not** own 'may the model invoke this skill
unprompted' … Membership decides reachability; the file decides invocability."
The test name claims the second and checks only the first. The precedent that
checks it is 148 lines further down the same file —
`test_the_session_gates_remedy_is_reachable_without_a_human`
(tests/test_skill_cross_references.py:268), whose docstring names this exact
failure: "`disable-model-invocation` is the second way to lose the route
silently: the mirror puts the skill on the discovery path and that key would
refuse it there as well, leaving none. `i-have-adhd` is mirrored and unreachable
for exactly that reason." `architecture-design` ships no wrapper, so the diff's
own argument is that the mirror is one of only two routes in; a later
`disable-model-invocation: true` on that SKILL.md — a plausible copy-paste from
any of the four wrappers carrying it — kills the mirror route with the whole
suite green.
→ add the third assertion the sibling test already models:
`assert "disable-model-invocation" not in _arch._frontmatter((_AGENTS / "skills" / "architecture-design" / "SKILL.md").read_text())`
(`from wfctl import _arch` is already the import that test uses).

WARNING  wfctl/cli.py:2372 — the comment justifies the mirror entry with a
scenario the shipped ceilings refuse. It reads: "Its description triggers on a
boundary that may be moving, which surfaces mid-implementation as readily as
inside a design pass — and an agent that arrives there without having read the
gate has no other route." Mid-implementation inside the pipeline means
`/speckit.implement`, whose ceiling is
`allowed-tools: Read Glob Bash(.specify/scripts/bash/check-prerequisites.sh*) Bash(git rev-parse*) Bash(wfctl feature-paths*)`.
`architecture-design` § *Authority* makes `wfctl arch context` its first
obligation and holds that "a record found any other way is not in force", and its
step 6 handoff needs `wfctl arch-root` plus `git add`/`git commit`. None of those
are in that ceiling — which is precisely the defect item 4 of this change fixed
for `speckit.brainstorm.md`, and the test docstring added at
tests/test_skill_cross_references.py:209 states the argument in those words.
`speckit.plan`, `speckit.tasks`, `speckit.specify`, `speckit.clarify` and
`speckit.analyze` are the same shape. The claim does hold for a session running
outside a wrapper, which is why this is a warning and not a blocker — but the
wrapped half of it is not delivered.
→ pick one: raise the ceiling on the wrapper(s) where the mid-run route is
actually claimed (`Bash(wfctl arch context*)`, and `Bash(wfctl arch-root*)` if
the handoff is meant to complete there), or narrow the comment to the unwrapped
session it is true of. Note that moving `allowed-tools` onto the SKILL.md — the
`start-session` remedy `mirror-supersedes-the-wrapper.md` records — does **not**
solve this one: a wrapper's `allowed-tools` is a ceiling on the turn, so a
skill's own grant cannot exceed it.

NIT  wfctl/cli.py:2360-2366 — the comment cross-references
`software-design-decisions`' entry ("this is `software-design-decisions`' case
one level up") and then restates that entry's whole argument anyway — no wrapper,
description-triggered, no other route. The cross-reference is doing the work; the
restatement is four lines of it twice in one literal.
→ keep the first clause and the `#150 … expired` paragraph (that one is
Chesterton's-fence information a future reader needs, and it matches the
`opening-a-change` entry's shape); drop the re-derivation.

NIT  wfctl/agents/skills/design-levels/SKILL.md:128 — "Skip it only when the
boundary is already settled in an accepted record or an existing spec" is
narrower than the next paragraph six lines down, which grants "If the level
introduced no boundary, declare that — 'no boundary drawn, no record' — and
descend." A reader of the new paragraph alone is told there are exactly two
exits and that a no-boundary feature must run the six-step method to discover it
has nothing to decide. `architecture-design`'s own *When to use* has the cheap
exit ("If the change is local and preserves every existing boundary and
contract, declare **no record** and return to `design-levels`"), so this is the
gate's wording, not a real disagreement between the skills.
→ add the third case to the skip clause, or point at the paragraph that carries
it.

NIT  docs/architecture/mirror-supersedes-the-wrapper.md — its *Context* says
"Eleven skills are mirrored"; the set is 13 after this change. It was already
stale at 12 (its own Log records #204 adding `start-session`), and records are
not rewritten in place, so this is an observation rather than a request — raised
only because the count is the kind of number a reader treats as current.

## What each pass checked, and what it cleared

**Correctness / completeness of the wiring.** Traced a `_MIRRORED_SKILLS` entry
end to end: `_claude_native_skill_mirror` (wfctl/cli.py:2465) is reached only
through `_AGENT_SKILL_EXTRAS`, from the `src_rel == "agents/skills"` branch of
the install plan loop (wfctl/cli.py:3569); the mirror is appended to `plan` under
the *agent* layer, its relative path recorded in the manifest and added to
`gitignore_targets`, so `uninstall-skills --agent claude` removes it (covered by
`test_uninstall_removes_native_skill_mirror`). Nothing else needed touching:
packaging is `graft wfctl/agents` in `MANIFEST.in`, not a glob list, so a skill
directory needs no packaging entry; `_scanned_dirs` derives `.claude/skills` from
`_CLAUDE_NATIVE_SKILL_ROOT` rather than a hand-kept list, and `_scan_owns`
already gates an unrecorded mirror on the base copy existing; doctor's staleness
check is `_bundle.content_hash` over the whole bundle, and this change edits
three files under `wfctl/agents/`, so — unlike the pure-suppression case
`mirror-supersedes-the-wrapper.md` § *Consequences* warns about — an
already-installed repo *will* be told it is behind and get the mirror on the
refresh `/start-session` runs. No `MANIFEST.in`, `pyproject.toml`, doctor or
prune change is missing.

**Side effects of mirroring.** `wfctl/agents/commands/` holds no
`architecture-design.md` (verified by listing, and now asserted by the new test),
so `_mirror_supersedes_wrapper` suppresses nothing and no `allowed-tools` or
`disable-model-invocation` is silently revoked on the Claude layer. The pinned
`_SUPPRESSED_ON_A_MIRRORING_LAYER` (tests/test_install_skills.py:290) needed no
entry, and its `<= _MIRRORED_SKILLS` subset assertion still holds.
`test_no_suppressed_wrapper_carries_more_than_a_pointer` derives its loop from
`_MIRRORED_SKILLS` and passes vacuously for this name. Correct.

**Frontmatter on the native discovery path.** `architecture-design/SKILL.md`
carries `name` and `description` only — no `disable-model-invocation` (so the
mirror route works today; see the first WARNING for why nothing pins that) and no
`allowed-tools` (so the mirrored skill carries no grant of its own; see the
second WARNING). `_CLAUDE_ONLY_FRONTMATTER_KEYS` stripping applies to command
files via `_copy_command_for_bob`, not to the skills `copytree`, so it is not in
play here either way.

**Prose-vs-check (`a-rule-is-expressed-as-a-check`).** The new rule at
design-levels:123 ("answered by running the method") is on the *prose* side of
that record's own test: `wfctl arch check` validates link integrity only
(`_arch.validate` — VR-002/003/004), nothing reads a record's `Considered` or
`Direct baseline` for content, and a method skipped on a change that produced no
record leaves no artifact at all. `speckit.brainstorm.md` already carries the
same rule as prose ("A record whose `Considered` is empty has skipped its gate
more quietly"), so this is consistent, not a gap. No finding.

**Other accepted records.** `layer-model`: the change edits only committed
package data under `wfctl/agents/` plus the installer that owns the generated
trees — no dotted directory touched. `knowledge-placement`: the routing claim
lives in the two SKILL.md files it constrains, the mirror rationale beside the
literal it explains, and nothing was added to AGENTS.md that belongs in a file.
`vendor-upstream-skills`: `design-levels` and `architecture-design` are
wfctl-authored, not derived, so the in-place edits are legitimate — no
upstream-derived skill (`brainstorming`, `idea-refine`) was edited, and the
`speckit.brainstorm.md` wrapper is the layering point the record prescribes.

**Tests.** Both new tests assert behavior a change could silently break, and the
docstrings say why the test exists and name the failure (`#150 shipped the method
and only the second reference"), per the project's convention. The third edit —
adding `"wfctl arch context"` to an existing loop — extends a test rather than
duplicating one, which is right. `test_every_referenced_skill_ships` picks up the
new `.agents/skills/design-levels` reference in `architecture-design/SKILL.md`
for free. Ran `uv run pytest tests/test_skill_cross_references.py
tests/test_install_skills.py tests/test_architecture_design_skill.py -q` → 182
passed. `uv run ruff check wfctl/ tests/` and `uv run mypy wfctl/` both clean.
(`wfctl verify` and `install-skills` deliberately not run — shared worktree.)

**Security.** No untrusted input, no secrets, no new execution path. The one
permission widening is `Bash(wfctl arch context*)`, a read-only projection
command, in the same un-colonned prefix form every other entry in that file
already uses — consistent with existing practice, so not raised as a finding.

**Performance.** Not applicable; no runtime code path changed. The mirror adds
one `copytree` of one skill directory per install.

**Over-engineering.** No new abstraction, no new flexibility, no speculative
config. The only shrinkable thing is the duplicated rationale in the
`_MIRRORED_SKILLS` comment.

net: −4 lines possible
Verdict: **Approve** (0 blockers, 2 warnings). Both warnings are follow-ups the
diff can absorb cheaply: one test assertion, and one decision about whether the
mid-implementation claim is delivered or narrowed.

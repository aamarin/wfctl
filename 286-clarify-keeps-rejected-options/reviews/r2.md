## Review: 9da5440 `feat(speckit): clarify keeps every option it rejected, not one` (r2)

Target: `git diff origin/main...HEAD` — one commit, three files, +89/−1.
Rubric: `.agents/skills/code-review/SKILL.md`, all six passes run. Read as
context: `CLAUDE.md`, `the-scan-is-attested-where-the-reviewer-reads.md`,
`design/307-the-coverage-map-is-the-evidence.md`,
`.agents/skills/writing-a-scan-file/SKILL.md`,
`wfctl/agents/skills/speckit-clarify/SKILL.md` (steps 3–4),
`wfctl/agents/commands/speckit.brainstorm.md`, issue #286's full body.

WARNING  wfctl/agents/commands/speckit.clarify.md:L95 — the rule counts an option the upstream step renders but nobody weighed, and its only available reason is invented. `speckit-clarify/SKILL.md:156` renders a sixth table row alongside A–E: `| Short | Provide a different short answer (<=5 words) (Include only if free-form alternative is appropriate) |`. That row is, literally, "an option the question offered and the answer did not take", so the rule demands `Decided against **Short**: <why it lost>` — and no true reason exists, because `Short` is an escape hatch rather than a candidate. The agent's cheapest exit is to make one up, which is precisely what L106–110 forbids. The instruction contradicts itself on every question where upstream judged a free-form alternative appropriate. → scope the count to the lettered options: "one line per lettered option (A–E) the question offered and the answer did not take; the `Short` escape row is not one of them."

WARNING  wfctl/agents/commands/speckit.clarify.md:L112 — the exemption keys on the question's type, and upstream's third case is a multiple-choice question answered free-form. `speckit-clarify/SKILL.md:157` tells the answerer they may "provide your own short answer" to a multiple-choice question, and `:165` validates that path. That question *did* offer options, so `A short-answer question offered no options and writes none` does not reach it; the L95 rule does, and asks for the true reason each of 2–5 options lost — which the answerer never gave. Same failure as W1 reached from the other side: nothing honest to write, so something gets invented. → one sentence covering it, e.g. "a multiple-choice question answered free-form rejected every option; the reason each lost is the answer that displaced it, named once."

WARNING  wfctl/agents/skills/writing-a-scan-file/SKILL.md:L107 — the shared skill still states the singular rule this change exists to replace, and it is the file both wrappers are told to follow first. It reads "**Findings** — what was found, and for each, the alternative the answer was decided against." The clarify wrapper now says one line per option; the skill says one alternative; nothing states which wins. The skill already owns the idiom for this exact deferral twice ("The wrapper that sent you here names the rows"; "is the wrapper's to declare") and it was not applied here. Fixing it also gives #286's third open question — *does `analyze` want the same treatment?* — a durable home: that answer currently exists only in the commit body, while `speckit.analyze.md:88` keeps the singular `Decided against <the alternative>` with no statement of why. → "**Findings** — what was found, and for each, what the answer was decided against. How many the step names is the wrapper's to declare."

NIT      tests/test_scan_files.py:L~294 — `test_the_clarify_wrapper_refuses_a_straw_option` asserts a subordinate clause, `"a weakness the option does not have is never one"`, where the other three new tests each assert their rule's bolded lede. That clause is also the one piece of borrowed prose in the section (adapted from `architecture-decisions/SKILL.md:158`), so an ordinary rewrite of the sentence around it drops the test with a message reading as though the rule were deleted. → assert `"The reason an option lost is the reason it actually lost"`, keeping the clause as a second assertion if the borrowed standard is what the test is really pinning.

NIT      wfctl/agents/commands/speckit.clarify.md:L81-86 — the `### Findings` template shows only the multiple-choice shape, so the exact string an agent must emit for the exempt case (`No options offered — short answer.`) appears nowhere in the block it copies from; it is prose three sections further down. → add a second bullet to the template showing that line under a short-answer finding.

NIT      tests/test_scan_files.py:L46 — the new rule lives in a *sibling* top-level heading (`## The options the answer was chosen over`), so the four new tests reach it through `_section()`, whose contract is `split("## Write the scan file")[1]` — "everything after", not "that section". It works only because nothing else follows. → make the new block `### The options the answer was chosen over` under `## Write the scan file` (where its reader already is), or rename the helper to say it returns the tail.

### What each pass checked

**1. Correctness.** Traced every factual claim the new prose makes against the upstream skill it cites. `2–5 distinct, mutually exclusive options` — accurate (`speckit-clarify/SKILL.md:131`). `Step 4's other branch takes a free-form answer of five words or fewer` — accurate (`:132`, `:162`). `the workflow discards it once the answer is recorded` — consistent with `:166` recording only the answer in working memory. `speckit.brainstorm.md`'s auto-approve branch fires only when nobody is there to be asked — accurate (`speckit.brainstorm.md:17-39`). The two gaps are W1 and W2, both on the option-set boundary the rule is drawn across. Checked the change touches no `wfctl/agents/skills/speckit-*/SKILL.md`: `git diff origin/main...HEAD -- wfctl/agents/skills/` is empty, so `vendor-upstream-skills` holds and `test_no_review_skill_carries_the_instruction` still passes.

**Issue coverage.** #286 asks for four things. Rejected alternatives per resolved question — present. The true reason each lost, to `architecture-decisions`' standard — present, quoted almost verbatim with `alternative`→`option`. *Extended bullet or a file of its own?* — answered by #307's destination, and the declaration says so; the skill's two validation rules the issue warned would collide (`exactly one bullet per accepted answer`, `only allowed new headings`) are correctly untouched, because a separate file is outside their reach. *Attended vs unattended?* — answered explicitly at L118-122, which is what the issue asked for. *Does `analyze` want the same?* — the issue marks it "Not assumed here", so leaving it is defensible; the reason it stays singular is not written down anywhere durable (W3).

**2. Security.** No executable code. `allowed-tools` on the wrapper is unchanged and needs no addition — the new rule adds content to a file the existing `Write`/`Edit` grants already cover, and `test_each_review_wrapper_allows_the_commands_the_scan_file_needs` still passes. The instruction directs no new command, path or network call. Nothing secret, nothing untrusted, no new destination outside `<arch-root>/scans/`.

**3. Architecture.** The rule is placed in the command wrapper, not the spec-kit-derived skill — correct per `vendor-upstream-skills` and per `writing-a-scan-file`'s own split ("what *this* step scanned stays in the wrapper"). The `arch none` declaration is accurate and matches the format of the three existing ones: no new destination, no change to what a scan file is, one content rule tightened inside an artifact #307's records already drew. The one boundary problem is W3, where the shared half now under-states what the wrapper half requires.

**4. Readability.** The four sub-rules each lead with a bolded imperative and follow with why, matching the density `CLAUDE.md` asks for. Test names are sentences; all four docstrings name the failure the test exists to catch rather than restating the assertion, and none is a paraphrase of its `assert`. Prose-substring assertions are this file's established house style (`"A run that found nothing still writes the section"`, `"never discards an earlier session"`, `"Do not fall back to a repository-local path"`), so the new ones are not a new brittleness — only the straw-option one picks a less canonical anchor than its siblings (NIT).

**5. Performance.** Not applicable — markdown instructions and four string assertions; `tests/test_scan_files.py` runs in 0.74s for 28 tests.

**6. Over-engineering.** Nothing to delete. Four sub-rules, four tests, one declaration — no speculative flexibility, no abstraction with one caller, no restated rule. The two paragraphs of justification at L102-104 and L118-122 are the *why this shape* the project asks instruction files to carry, not narration of the change.

### Verification run

- `uv run pytest tests/test_scan_files.py -q` → 28 passed
- `uv run ruff check wfctl/ tests/` → All checks passed
- `uv run mypy wfctl/` → Success, 22 source files
- (`wfctl verify` deliberately not run — shared worktree.)

net: Lean already.
Verdict: **Approve** — no blockers; the change is a clear improvement over `main`, where no rule exists at all. W1 and W3 should be fixed before merge: W1 makes the shipped instruction ask for the straw record it bans one paragraph later, and W3 leaves the file both wrappers read first stating the superseded singular.

## Review: origin/main..HEAD (`wfctl/agents/skills/start-session/SKILL.md`, 1 commit, docs-only)

### Passes run

- **Correctness (adversarial):** Read the full file (not just hunks). Verified
  every factual claim against reality rather than trusting the prose:
  - Built `wfctl` 0.18.0 via `uv run --project <worktree>` against a throwaway
    git repo under `/tmp` and ran `install-skills --yes --prune` then `doctor`
    with `WFCTL_AGENT` unset. Reproduced the exact scenario step 8 argues for:
    `✓ wfctl 0.18.0 — latest`, `✓ base: skills current`, exit code **0**, and
    still a printed `ℹ no agent layer — .agents/ only, which every / agent
    reads. Set WFCTL_AGENT to your agent's name / to install its own paths in
    new worktrees too.` — matches the quoted string verbatim and confirms the
    change's central claim ("a run with every layer ✓ and exit 0 still tells a
    developer to set WFCTL_AGENT").
  - Added a hand-placed file under `.agents/skills/` and re-ran `doctor`:
    reproduced `ℹ 1 path is not on record under a directory wfctl installs
    into:` / `Left alone — wfctl cannot tell yours from an old leftover.`
    verbatim, exit code still 0 alongside the other ℹ block.
  - Grepped `wfctl/cli.py` for both strings (`4333-4345`, `4619-4623`) and the
    docstring on `doctor_cmd` (`4400`: "dim ℹ named, not a finding — it does
    not reach the exit code") — matches the skill's "Not reaching the exit
    code is all they share."
  - Checked issue attribution: `git log -S"not on record under a"` →
    `c29dd02` (2026-09-05, `Closes #183`); `git log -S"no agent layer — .agents/
    only"` → `1d68774` (2026-09-06, `Closes #178`). #183 predates #178, so
    "since #178 there are two of them" is chronologically correct — #178 is
    what turned one dim block into two.
  - The #178 commit message itself contains "The line carries no repair
    command, unlike every finding above it in `doctor`... The variable is a
    setting to apply, not a line to execute" — the new SKILL.md text ("Doctor
    prints no repair command beside it on purpose... it is a setting you apply
    and carry to step 8, not a line to execute") is a faithful paraphrase of
    the commit's own stated rationale, not an invented justification.
  - Confirmed `1d68774` touched only `wfctl/cli.py`, not this SKILL.md — so the
    file was genuinely left stale after #178 landed, which is the premise the
    task states and this commit now fixes.
  - Cross-checked the `no-hardcoded-agent` architecture record
    (`docs/architecture/no-hardcoded-agent.md`): its 2026-09-06 amendment says
    "`doctor` names an absent agent layer (#178) without calling it wrong —
    dim, no repair line, exit code untouched" — matches the skill's claim that
    installing an agent choice on someone else's behalf is what that record
    forbids.
  - Step 8's new omission rule ("Omit only when doctor printed nothing but ✓
    and nothing was refreshed") correctly handles the case doctor actually
    produces (⬆ upgrade line, ⚠ findings, or either ℹ block) — none of those
    are "nothing but ✓", so none are silently dropped. Verified against real
    doctor output shapes, not just the prose.
  - No contradiction found between step 2 (execution-time guidance: don't act
    on the #183 block, do act on the #178 one by setting an env var) and step 8
    (report-time guidance: surface both, but don't let repeating the #183 block
    read as license to delete).

- **Security:** N/A — prose-only change, no code, no input handling.

- **Architecture:** Fits the file's existing pattern (dense, causally-argued
  prose citing issue numbers and named records). No new pattern introduced.
  Confirmed the stated constraint is honored: `git diff origin/main..HEAD
  --stat` shows exactly one file changed, and `git diff origin/main..HEAD --
  wfctl/cli.py` is empty — `cli.py` (owned by another in-flight branch) was not
  touched.

- **Readability & simplification:** Backtick and `**bold**` markers in the
  changed hunks are balanced (16 backticks, 4 `**` pairs — both even). The two
  ℹ blocks are told apart by quoting their distinguishing lead text, as the
  task asked. Sentences are long but match the file's established voice
  throughout (e.g. the existing `--from`/`--prune` paragraphs a few lines
  above use the same causal-chain construction).

- **Performance:** N/A.

- **Over-engineering:** None found. The diff is a net addition (+25/-9) because
  it's splitting one (now-wrong) claim into two correctly-scoped ones, not
  padding — no single-implementation abstractions, no restated logic. Nothing
  to shrink.

- **Verification commands:**
  - `uv run pytest -q tests/test_skill_cross_references.py
    tests/test_start_session_asks_conditionally.py tests/test_install_skills.py`
    → 169 passed
  - `uv run pytest -q` (full suite) → 1080 passed in 75.29s
  - `uv run ruff check wfctl/ tests/` → all checks passed
  - `uv run mypy wfctl/` → no issues in 17 source files
  - (all as expected for a `.md`-only change; run per the repo's definition of
    done, not because this diff could plausibly fail them)

### Findings

None at BLOCKER or WARNING level. One NIT:

NIT `wfctl/agents/skills/start-session/SKILL.md:82-83` — "this worktree got the
base layer and none of your agent's own paths, because `WFCTL_AGENT` was unset
when it was created" names worktree-creation-time as the cause, but `doctor`'s
actual check (`not _agent_keys(manifest) and not chose_a_layerless_agent`,
`cli.py:4618`) fires on the *current* manifest state regardless of how it got
that way — e.g. a manual `install-skills` run without `--agent` in an existing
checkout hits the same line. Given this skill only ever runs inside a worktree
and `AGENTS.md`'s own Worktrees section frames the identical scenario the same
way ("with it unset the worktree gets `.agents/` and no `.claude/`"), this is a
reasonable simplification for the documented common case, not a misstatement —
flagging only because it's the one sentence in the diff that's narrower than
the code it describes. Author's call.

net: 0 lines possible. Lean already — the added length is two correctly-scoped
claims replacing one wrong one, not padding.

**Verdict: Approve.**

# Review: wfctl/agents/skills/start-session/SKILL.md (origin/main..HEAD, commit 07175aa)

Reviewer: r1. Read-only — no files edited, no `wfctl verify`, no commits, no GitHub actions.

## Context gathered

- `git diff origin/main..HEAD` — one commit, one file, 25 insertions / 9 deletions.
- Read the full current `SKILL.md` (244 lines), not just the hunk, for step 2 and step 8 in context.
- Read `CLAUDE.md`/`AGENTS.md` at repo root (worktrees, session-state, code-style sections).
- Read `docs/architecture/no-hardcoded-agent.md` in full — the record the new prose cites.
- Read the `doctor` implementation in `wfctl/cli.py` (the `--prune`/unknown-paths block ~L4290-4345, and the no-agent-layer block ~L4570-4623) to check every quoted string and every behavioral claim against the actual code, not against the prose's say-so.
- Ran `uv run pytest -q` (full suite): 1080 passed. Ran the two most directly relevant files individually first (`test_skill_cross_references.py`, `test_start_session_asks_conditionally.py`): 17 passed.
- Built a throwaway git repo under `/private/tmp/.../scratchpad/doctor-test`, ran `uv run wfctl install-skills --yes` then `wfctl doctor` against it (no `WFCTL_AGENT` set) to observe real output, then added an untracked file under `.agents/skills/` and re-ran `doctor` to trigger the second block.

## Empirical verification of the change's central claim

The change's load-bearing claim is: *a `doctor` run with every layer ✓ and exit code 0 can still print the actionable `no agent layer` line*, which is why step 8 must key off what doctor printed rather than the exit code. Reproduced directly:

```
✓ wfctl 0.18.0 — latest
✓ base: skills current (wfctl 0.18.0)
ℹ no agent layer — .agents/ only, which every
  agent reads. Set WFCTL_AGENT to your agent's name
  to install its own paths in new worktrees too.
---DOCTOR EXIT 0---
```

Confirmed in the source too: the no-agent-layer print (`cli.py:4620`) sits after the layer-loop and is gated only on `not _agent_keys(manifest) and not chose_a_layerless_agent` — it never sets `exit_code`, which is otherwise seeded from `_check_wfctl_version()` and only bumped to 1 by actual findings (`cli.py:4439,4460,4481,4558,4583`). The claim is correct, not just plausible.

Also reproduced the other block for the same repo:

```
ℹ 1 path is not on record under a directory wfctl installs into:
    .agents/skills/my-own-skill
    Left alone — wfctl cannot tell yours from an old leftover.
```

Both are the only two `[dim]ℹ` lines `doctor_cmd` can emit (grepped `[dim]ℹ` across `cli.py`; the other three dim-ℹ occurrences belong to `install_skills_cmd`/`install_config_cmd`, not `doctor`) — so the prose's "two of them" is exhaustive, not an undercount.

Cross-checked issue numbers: `cli.py:4327` ("the whole of #183") backs the first block, `cli.py:4604` and `docs/architecture/no-hardcoded-agent.md` L74/L90 (#178) back the second and its "no repair line on purpose" reasoning. Both citations in the SKILL.md are attributed to the right issue.

## Pass 1 — Correctness

No defect found. The rewritten step-2 disambiguation and step-8 "keys on what doctor printed, not exit code" rule both match the actual `doctor` behavior verified above, including the edge case the issue calls out (green exit code, actionable line still printed). Step 8's "Omit only when doctor printed nothing but ✓ and nothing was refreshed" correctly forces inclusion whenever either dim block appears, which is the whole point of the fix.

## Pass 2 — Security

N/A — prose-only change, no code, no secrets, no input handling.

## Pass 3 — Architecture

Consistent with project conventions:
- Does not touch `wfctl/cli.py`, per the stated constraint (verified: diff touches exactly one file).
- Edits the source (`wfctl/agents/skills/...`), not a generated tree (`.agents/`, `.claude/`), matching AGENTS.md's "Only `.agents/` is overwritten, never `wfctl/agents/`" rule.
- The "no repair command, by design" framing in step 2 matches `no-hardcoded-agent.md`'s own reasoning almost verbatim, including citing #178 the same way the architecture record does — the skill isn't inventing a rationale, it's restating the accepted one.
- No spec/plan exists for #254 (`wfctl feature-paths` shows no `spec.md`) — appropriate per AGENTS.md's exclusion for changes that "introduce no new state."

## Pass 4 — Readability & simplification

Matches the file's existing dense, argument-carrying prose style (consistent with the project's "comments are dense with rationale" convention, applied here to skill prose an agent executes). Line lengths stay in the ~74-81 char band the rest of the file uses. No trailing whitespace introduced. The two `#178` mentions (step 2 and step 8) serve different local arguments (introducing the split vs. justifying the printed-not-exit-code rule) rather than being redundant restatement.

One judgment call, not a defect: "because `WFCTL_AGENT` was unset when it was created" (step 2) describes the common cause but the check is actually against the recorded manifest (`_agent_keys(manifest)`), so the block would also persist if `WFCTL_AGENT` is set now but the layer was simply never installed. This is the same simplification AGENTS.md itself uses ("with it unset, the worktree gets `.agents/` and no `.claude/`") and doesn't change what the reader should do (`install-skills --agent <name>`), so I'm not raising it above NIT.

- NIT `wfctl/agents/skills/start-session/SKILL.md:181` — "licence" (British spelling) vs. "licenses" used in `conversation-response-shape/SKILL.md`. Not actually an inconsistency: this same file already uses British spelling elsewhere ("to honour", L100), so it's internally consistent — author's call, not worth changing.

## Pass 5 — Performance

N/A — prose-only.

## Pass 6 — Over-engineering

Net addition of 16 lines for a two-way disambiguation plus one exit-code-vs-printed-output rule correction, each backed by a real behavioral distinction (verified above) rather than restating one idea twice. Nothing to cut.

net: Lean already.

## Tests / build

`uv run pytest -q` — 1080 passed (0 failed). No lint/type check run (doc-only change to a Markdown skill file; `ruff`/`mypy` don't touch `wfctl/agents/`).

## Verdict

**Approve.** No BLOCKER, no WARNING. One NIT (spelling), author's discretion, not worth a revision cycle.

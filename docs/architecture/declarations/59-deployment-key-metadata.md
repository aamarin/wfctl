# No new boundary — 59-deployment-key-metadata

Moves the mirror switch from skill frontmatter into install-skills, which is where layer-model already assigns authority for what lands in each layer. The boundary is not new — the code was contradicting a record that already drew it. layer-model is amended to drop two sentences the change makes false; no record is added.

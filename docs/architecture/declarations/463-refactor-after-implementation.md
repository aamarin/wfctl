# No new boundary — 463-refactor-after-implementation

Adds guidance text to the clean-code skill and one paragraph to the /speckit.implement wrapper. No new state, no status row, no ownership moved. Placement is already answered by a-repo-concern-earns-a-step-hook-or-method: a post-implementation refactor pass changes nothing downstream can detect, so it is a method, and a method lives inside the skill that owns the practice (clean-code).

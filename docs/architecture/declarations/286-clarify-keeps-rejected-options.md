# No new boundary — 286-clarify-keeps-rejected-options

The destination, the session rule and the per-finding shape were all settled by #307's records; this change tightens one content rule inside the artifact they already draw — clarify's findings name every rejected option rather than one. No new file, no second destination, no change to what a scan file is.

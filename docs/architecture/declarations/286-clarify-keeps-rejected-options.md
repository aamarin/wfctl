# No new boundary — 286-clarify-keeps-rejected-options

The destination, the session rule and the per-finding shape were all settled by #307's records; this change tightens one content rule inside the artifact they already draw — clarify's findings name every rejected option rather than one. No new file, no second destination, no change to what a scan file is. #286's third open question, whether analyze wants the same treatment, is answered in writing-a-scan-file: the count belongs to the wrapper, because analyze reaches a finding with no option set to discard. The one credible alternative weighed — inheriting speckit.brainstorm.md's mode-conditional shape rather than making the rule unconditional — is argued in the wrapper prose a reviewer reads in this diff, so the structural choice needs no level-3 record of its own.

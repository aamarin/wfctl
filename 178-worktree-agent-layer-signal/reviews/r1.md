# Review: `7ee6a05` — fix(doctor): name an agent layer that was never installed (r1)

Target: `git show 7ee6a05` (the single commit on `178-worktree-agent-layer-signal`),
+103 / −0 across `wfctl/cli.py`, `tests/test_install_skills.py`,
`docs/architecture/no-hardcoded-agent.md`, `AGENTS.md`, `README.md`.
Nothing in the working tree was modified by this review.

## Findings

WARNING  wfctl/cli.py:4567 — the notice can never be cleared by a Codex user, and fires forever on every session start. `_agent_keys` skips agents with no targets (`none`, `codex` → `_AGENT_TARGETS[k] == []`, cli.py:1389-1401), and `install_skills_cmd` writes no manifest entry for a layer with no items (cli.py:2483-2490). So `wfctl install-skills --agent codex` — the supported, correct call for that agent, which prints *"Installing the base layer only; Codex reads `.agents/` and AGENTS.md directly"* (`_AGENT_NOTICES`, cli.py:1211-1218) — still leaves `_agent_keys(manifest) == []`, and `doctor` then tells that developer to install an agent layer and export `WFCTL_AGENT`, forever. Exporting `WFCTL_AGENT=codex` does not help either: `post_create` passes `--agent codex`, which installs nothing and records nothing. Reproduced live in a scratch repo:
```
$ wfctl install-skills --agent codex --yes && wfctl doctor
✓ base: skills current (wfctl 0.18.0)
ℹ no agent layer — .agents/ only, which every agent reads.
  `wfctl install-skills --agent <name>`, and WFCTL_AGENT=<name> in your shell
  profile so `workmux add` installs it in new worktrees too.
EXIT=0
```
This is verbatim the anti-goal the diff's own second test names — *"telling a developer who already made the choice to make it, on every session start, forever"* (tests/test_install_skills.py:766-773). The guard the test asserts only covers agents that happen to write files. → Either record the chosen agent as a manifest sibling the way `tracker` is (cli.py:2513-2517) and suppress on it, or, minimally, gate the notice on `_AGENT_TARGETS` having nothing to offer beyond what is installed and add a clause naming the agents that need no layer ("Codex and anything else that reads `.agents/` directly needs none"). Add a test for `--agent codex`; there is none.

WARNING  wfctl/agents/skills/start-session/SKILL.md:72 — the consumer this change is aimed at was not updated, and it currently licenses dropping the new line. The commit's argument is that `doctor` is *"the one command a session start always reads"*. That reader is step 2/step 8 of the shipped `start-session` skill, and it says two things that work against the new line: (a) SKILL.md:72-76 — *"Doctor's dim `ℹ` block is the other thing entirely: paths in wfctl's destinations that it cannot show are its own… **Delete nothing on the strength of that block** — it does not affect the exit code"*. There are now **two** dim `ℹ` blocks in `doctor` output (cli.py:4290 and cli.py:4569) with opposite intended reactions, and the singular *"the* dim ℹ block" maps the ignore-it guidance onto the new one. (b) SKILL.md:157-161 (step 8 Freshness) — *"Omit only when doctor is green and nothing was refreshed"*. The notice deliberately keeps doctor green, so an agent that follows step 8 literally is entitled to omit it, which is the exact silence #178 is about. → One clause in step 2 distinguishing the two `ℹ` blocks, and one in step 8 that the agent-layer notice is surfaced even on a green run. Per AGENTS.md, that edit also needs `uv run wfctl install-skills` and an exercise, not just the suite.

WARNING  docs/architecture/no-hardcoded-agent.md:73-81 — the body of an `accepted` record was edited, which the skill wfctl itself ships forbids without qualification. `.agents/skills/architecture-decisions/SKILL.md:106-116`: *"Once a record is `accepted`, exactly two things ever change: `status: accepted → superseded | retired`; `Log`: one line appended. **The body is never edited.** Not to fix the reasoning, not to add a consideration that came up later, not to soften a claim."* There is no `amended` status in that skill's status table and no amend allowance anywhere in the skill directory. The repo has normalised the practice anyway — 8 `amended` log lines across `install-modes`, `layer-model`, `vendor-upstream-skills`, `mirror-supersedes-the-wrapper` and this record's own 2026-08-29 entry — so the diff is consistent with local precedent and inconsistent with the binding rule it installs into every consumer project. Flagging because the reviewer's job is to name the contradiction, not because this diff invented it. → Either write the clarification as its own record, or amend `architecture-decisions/SKILL.md` to license `amended` for a scope clarification that does not change the decision. Leaving both as they are means wfctl ships a rule its own repo breaks ten times.

WARNING  wfctl/cli.py:4558-4573 — the block is inline where every other thing `doctor` prints is a named helper, and the docstring that enumerates them was not updated. `doctor_cmd`'s docstring (cli.py:4370-4380) names its checks deliberately — *"Named rather than counted: a numeral here has gone stale three times"* — and singles out the one non-finding: *"`_warn_missing_bootstrap` is in neither, because it never becomes a finding."* The new notice is the second member of that category and is unnamed, in a docstring that says in its own text why that goes stale. It is also the sibling of `_warn_missing_bootstrap` (cli.py:3752-3789) in every respect — never a finding, prints a remedy, and is about a worktree coming up short of skills; that function even prints the `${WFCTL_AGENT:+--agent "$WFCTL_AGENT"}` line this issue is about. → Extract `_warn_missing_agent_layer(manifest)` beside it and add it to the docstring's "in neither" clause.

NIT  README.md:366-377 — the insertion orphans the antecedent of the paragraph below it. README.md:379 reads *"It refuses to overwrite an existing file unless you pass `--force`"*, where "It" was `install-config`. The new paragraph now sits between them and ends on `wfctl doctor`, so "It" reads as doctor or `install-skills`. → Move the new paragraph below the `--force` block, or open README.md:379 with `install-config` by name.

NIT  wfctl/cli.py:4569 — `"ℹ"` escape where every other info marker in the file is the literal `ℹ` (cli.py:143, 644, 1114, 2636, 2642, 2664, 3083, 4290). → Use the literal.

NIT  wfctl/cli.py:4570 — the notice hard-wraps at 77 columns and degrades below ~78, losing the two-space continuation indent that marks it as part of the `ℹ`. Verified at `COLUMNS=70`:
```
  `wfctl install-skills --agent <name>`, and WFCTL_AGENT=<name> in
your shell
  profile so `workmux add` installs it in new worktrees too.
```
The comment at cli.py:4296-4298 pins exactly this rule for the neighbouring block (*"short enough not to wrap at 80… a sentence broken across two lines beside a path list reads as the next path"*), and the pasteable lines in this function carry `soft_wrap=True`. → Shorten the second line below ~72 chars, or pass `soft_wrap=True`. Same shape at cli.py:2725-2726 (77 chars).

NIT  tests/test_install_skills.py:766-780 — `test_doctor_is_silent_once_an_agent_layer_is_installed` asserts only an absence, so it passes vacuously if `doctor` blows up entirely (CliRunner catches the exception and the string is absent either way). → Add `result.exit_code == 0` and a positive assertion such as `"claude: skills current" in result.output`.

NIT  tests/test_install_skills.py:744 — the section header omits the trailing `---` that all seven other `# ---` headers in the file carry (790, 857, 1362, 2326, 2784).

NIT  wfctl/cli.py:4558-4566 — nine comment lines that restate the commit message's third paragraph nearly verbatim, and the same argument now also lives in `no-hardcoded-agent.md:73-81`. AGENTS.md: *"Comments inform the next reader of the file; they do not narrate the change to a reviewer. If a comment only makes sense beside the diff, it belongs in the commit message."* The load-bearing half (dim + no exit code, because `no-hardcoded-agent`'s "Silence here is intended" governs the flag) informs; *"one dim line in `install-skills` output that had already scrolled past before the session began"* is the issue narrative. → Trim to the rule and point at the record.

net: −10 lines possible (comment trim at 4558-4566, one output line, offset by the helper extraction)

Verdict: **Request changes** — no blockers; the two that should land before merge are the Codex/no-op-agent false positive (WARNING 1, a permanent un-clearable notice on a supported agent) and the un-wired `start-session` consumer (WARNING 2, which is what makes the new line actually get read). Both are small. The change is otherwise a clear improvement and the reasoning behind it is unusually well recorded.

## Passes run

1. **Correctness** — traced `_agent_keys` → `_layer_keys` → `_AGENT_TARGETS`, and the manifest-writing path in `install_skills_cmd` (cli.py:2483-2517) to establish which agents ever produce a key. Checked the notice's placement against `doctor_cmd`'s two early exits (not-a-git-repo at cli.py:4397-4400, `Nothing installed` at cli.py:4421-4424 — correctly not reached, the bigger message wins there) and against the `exit_code` accumulation (the notice touches neither; confirmed `EXIT=0` live, and the notice still prints last when a real finding sets exit 1). Exercised the real binary in a throwaway git repo for base-only, `--agent claude`, and `--agent codex`; the codex case is WARNING 1. Verified the `${WFCTL_AGENT:+…}` claim in AGENTS.md/README against both `.workmux.yaml` and the seeded template (`wfctl/agents/configs/workmux/.workmux.yaml:58`) — accurate.
2. **Security** — nothing user-controlled reaches either new string; both are literals, so the `escape()` discipline the neighbouring blocks need (cli.py:4276-4280 — an unescaped `[/x]` raising `MarkupError` at session start) does not apply here. No secrets, no shell, no file writes added. Confirmed the two `[dim]…[/dim]` strings contain no other bracket sequence rich would parse.
3. **Architecture** — read `docs/architecture/no-hardcoded-agent.md` in full plus the `architecture-decisions` skill; findings 3 and 4. The decision itself (dim, last, exit code untouched) is the right side of the record: nothing infers an agent and no flag is guessed. The rejected alternatives that carry real evidence — reading workmux's `agent:` key, which does not exist on the filing machine — live only in the commit message; the record's `## Considered` is where a future reader will look before re-proposing it, and it is the section the skill's own checklist names.
4. **Readability** — findings on the `ℹ` escape, the hard wrap, the comment length, the README antecedent, and the test section header. Names and control flow are otherwise plain; the guard is a single `if not _agent_keys(manifest)` with no nesting.
5. **Performance** — nothing: one list comprehension over a dict with a handful of keys, on a command that already hashes a bundle tree.
6. **Over-engineering** — no new abstraction, no new flag, no config. The one `yagni`-adjacent note is that the doctor notice restates in three lines what `install-skills` already prints in five; both are justified (different commands, different moments), but the doctor half could be one line, which matters because the `doctor_cmd` docstring makes brevity on a green run an explicit design constraint (*"Split them when a green run stops fitting on a screen"*, and *"each arrival costs the exit code some of its meaning"*).
7. **Tests read first** — the three new tests are behavioral, name the failure they exist to catch, and the docstrings say why (AGENTS.md's convention, met). Coverage gaps: no `--agent codex` case (WARNING 1), no case where the notice coexists with a real finding, and the silence test can pass vacuously.

**Definition of done, re-run read-only:** `uv run pytest -q` → 1050 passed in 55s. `uv run ruff check wfctl/ tests/` → clean. `uv run mypy wfctl/` → clean, 17 files. `uv run wfctl doctor` in this worktree → green, exit 0 (base + claude current), so the amended record passes `_check_arch_records`. Working tree still `git status --porcelain`-clean after the review.

# Review: 153-cut-0-16-0 (main...HEAD) — reviewer r2

## Passes run

1. **Correctness** — verified every factual claim in the two commit messages and
   the PR description against the actual repo state (see checks below). No
   behavioral code in the diff, so no edge cases apply; the substantive question
   for a release-cut diff is "are the claims backing the version number true,"
   and that's where I spent the review.
2. **Security** — N/A. No user input, no secrets, no auth surface. Diff is two
   version-string lines.
3. **Architecture** — compared this diff's shape (bump commit + separate lockfile
   commit) against the two prior release cuts in history (0.13.0, 0.14.0) to
   check it's actually following precedent and not inventing one.
4. **Readability** — read both commit messages and the PR description in full
   for internal consistency and factual accuracy (this is where the findings
   below came from).
5. **Performance** — N/A.
6. **Over-engineering** — diff is already minimal (2 files, 1 line each). Nothing
   to trim.

## Checks performed (with results)

- `git log --oneline --no-merges v0.15.0..main | wc -l` → **37**. Matches the
  commit message, issue #153, and PR #154 exactly.
- `feat!: remove wfctl checkpoint (#68)` is at `4ca1604`, inside `v0.15.0..HEAD`,
  and is genuinely marked breaking (`feat!:`, "Breaking for anyone scripting the
  command" in its own body). The minor bump is justified.
- Scanned `v0.15.0..HEAD` for anything else that would change the version
  decision (another breaking change, a reason for patch instead of minor) —
  found nothing else marked `!` or otherwise breaking in the range.
- `.github/workflows/ci.yml` — the `tag` job: `needs: [test, wheel, lint]`,
  `if: github.ref == 'refs/heads/main'`, reads the version via `tomllib` (not a
  grep, so no false match from a version key in a different table), keys
  tag-existence and release-existence independently so a partial prior failure
  is retried rather than silently skipped. This will fire correctly on merge to
  main and produce `v0.16.0`. Matches what both commit message and PR claim it
  does.
- `uv.lock` diff is exactly the one line for the `wfctl` package's own
  `version` field, nothing else touched. `git status --porcelain` is clean
  after the two commits — no further drift from what `uv sync`/`uv run` would
  regenerate.
- Searched for anything else in the tree coupled to the version key:
  `wfctl/cli.py` resolves the running version via
  `importlib.metadata.version("wfctl")` — dynamic, not hardcoded, nothing to
  bump there. No `__version__` literal anywhere in `wfctl/`. No `CHANGELOG`
  file. `tests/test_install_skills.py:611` mentions "0.15.0" but only inside a
  docstring's prose example; the actual assertion uses
  `version('wfctl')` dynamically — not a hardcoded dependency, doesn't need
  touching.
- README.md prose examples: PR description says these are deliberately left
  alone (would drift again at 0.17.0, "worth its own issue"). Correctly out of
  scope for a release-cut diff — but see WARNING below on the count.
- Compared this branch's two-commit shape (bump, then separate `sync uv.lock`)
  against the two real prior release cuts: `2232f35`/`dd946be` (0.13.0: bump
  alone, then separate lockfile sync — identical shape to this branch) and
  `d8688f6` (0.14.0: bump and lockfile combined in one commit — different
  shape, but the commit message doesn't claim that one supports the "kept
  separate" reasoning, only that it independently "carried" the lockfile change,
  which it did). No misrepresentation of precedent.
- Ran the three verification commands read-only:
  `uv run pytest -q` → 809 passed; `uv run ruff check wfctl/ tests/` → all
  checks passed; `uv run mypy wfctl/` → no issues. Matches the PR's stated
  results exactly.
- `git log --oneline main..HEAD` / `HEAD..main` — branch is exactly 2 commits
  ahead of main, 0 behind. Clean, no rebase needed, nothing else riding along.

## Findings

WARNING  d186de6 (commit msg), issue #153, PR #154 — "v0.15.0 was tagged
2026-08-19" is false. `git for-each-ref refs/tags/v0.15.0 --format='%(creatordate:iso-strict)'`
→ `2026-08-18T13:43:00-05:00`. The tag was created 2026-08-18, one day before
the claimed date, in every one of the three places this diff's rationale is
recorded (commit message, issue, PR description). It doesn't change the
substance of the decision — 37 commits and the breaking change in the range
are both independently verified correct — but a wrong date repeated three
times in the work product that justifies a release is exactly the kind of
claim this project's own rule ("a claim from prior context is not evidence...
test before repeating") exists to catch, and here it wasn't tested before
being written three times. → Fix by correcting the date in the PR description
before merge (commit history is immutable at this point; not worth a
force-push/amend for a date in prose, but the still-open PR description should
say 08-18).

NIT  PR #154 description ("What is deliberately not here" section) — claims
"README.md carries 0.15.0 in eight places (lines 195, 205, 262, 264, 274, 300,
317)." `grep -c` on those exact lines finds 7 occurrences, and 7 line numbers
are listed, not 8. Doesn't change the underlying decision (leaving README
prose alone is still correct and well-reasoned), but the PR miscounts its own
evidence by one. → Fix the word "eight" to "seven" in the PR description.

## Over-engineering

Diff is two files, one changed line each. Nothing to cut.

net: −0 lines possible. Lean already.

## Verdict

**Approve.**

Both findings are prose-accuracy nits in the commit message / PR description,
not defects in the change itself. Every substantive claim underpinning the
version decision — commit count, the breaking change forcing a minor bump, the
CI tag job's wiring, the lockfile scoping, the test/lint/type-check results —
checked out exactly as claimed. The diff does exactly what issue #153 asks
and nothing else; it correctly avoids bundling the README's stale version
strings into a diff whose entire value is being reviewable as a one-line
decision. Recommend fixing the two prose inaccuracies in the PR description
before merge (cheap, and the PR is still open), but neither blocks it.

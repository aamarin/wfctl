# Review: `153-cut-0-16-0` (`git diff main...HEAD`) — reviewer r1

Target: 2 files, +2 −2. Commits `d186de6` (pyproject.toml) and `e28f63d` (uv.lock).
Context read: issue #153, PR #154 (incl. the Copilot review), `AGENTS.md`
(`CLAUDE.md` is a symlink to it), `.agents/skills/design-levels/SKILL.md`,
`.github/workflows/ci.yml`, `wfctl/cli.py` version paths, `wfctl.json`.
No file in the worktree was modified; `wfctl verify` was not run.

## Findings

```
WARNING  (PR #154 body, "Why now" / d186de6:L4) — "`doctor` and `uv tool install`
         both resolve releases by tag" is false for `uv tool install`. Nothing in
         this repo installs by tag.
         → reword to "doctor's release check resolves by tag"; file a follow-up to
           fix the same sentence in AGENTS.md ("Releasing") and ci.yml:L143.

WARNING  e28f63d:L3 — "`uv run` — the first of the three verification commands —
         regenerates this line". The three verification commands are wfctl.json's,
         and all three pass `--frozen`, which runs *without* updating the lockfile.
         → name the command that actually does it: the bare `uv run pytest -q` in
           AGENTS.md "Setup". Message-only fix; the commit itself is correct.

NIT      (PR #154, "One thing found and not fixed") — "eight places (lines 195,
         205, 262, 264, 274, 300, 317)" is seven line numbers, and
         `grep -o '0\.15\.0' README.md | wc -l` → 7.
         → say seven.

NIT      README.md:L274 — after v0.16.0 is tagged, the sample line
         `✓ wfctl 0.15.0 — latest release` illustrates the "current" verdict with a
         version that is no longer current. Deferring is right; the follow-up is not.
         → open the placeholder-version issue the PR declines to open (`X.Y.Z`).

NIT      (PR #154, "How Has This Been Tested?") — the post-merge steps (confirm the
         tag, confirm the release, reinstall and re-run doctor) live only in the PR
         body, which stops being anyone's worklist the moment it merges — and the
         merge auto-closes #153.
         → paste those two steps onto #153 before merging, or reopen it until the
           tag exists. Rationale under "Correctness", item 6.

NIT      AGENTS.md:L37 — "653 tests, ~45s"; the suite is 809. Not coupled to the
         version key and out of scope here (skill Step 6: file it, don't smuggle it in).
         → separate `docs:` commit.
```

**net: −0 lines possible. Lean already.**
Both changed lines are load-bearing and neither can be shortened. The two-commit
split for a two-line change is the one thing that could read as process for its
own sake, and it isn't: the "release decision is one line" distinction has nowhere
else to live, and this repo merges with merge commits (`f03910f`), so the split
survives the merge.

**Verdict: Approve** (0 blockers; 2 warnings, both commit-message/PR-body accuracy,
neither requiring a code change).

---

## Pass 1 — Correctness

Adversarial checks run, each with the command and its result.

1. **Is `0.16.0` the right number?** Yes.
   `git log --oneline v0.15.0..HEAD | grep -iE '!:|BREAKING'` returns exactly one
   subject: `4ca1604 feat!: remove `wfctl checkpoint` (#68)`.
   `git merge-base --is-ancestor 4ca1604 HEAD` → in range;
   `git merge-base --is-ancestor 4ca1604 v0.15.0` → not in v0.15.0. Pre-1.0, a
   breaking change takes the minor. Nothing else in the range changes the answer —
   there is no second `!` subject and no candidate for 1.0.

2. **Range size claim.** `git log --oneline --no-merges v0.15.0..HEAD | wc -l` → 39,
   i.e. 37 before this branch's two commits. Matches the issue and the PR.

3. **Tag collision / ordering.** No `v0.16.0` locally or on `origin`
   (`git ls-remote --tags origin`). `v0.15.0` is the newest tag by creator date.
   The comparison in `_check_wfctl_version` (`wfctl/cli.py:L2698`) uses
   `_parse_semver` (`wfctl/cli.py:L2560`), which parses to an `int` tuple — so
   `(0,16,0) > (0,15,0)` numerically. A lexical compare would already be broken at
   `0.9.0` vs `0.16.0`; it isn't. No off-by-one, no regression.

4. **Does the bump reach everything derived from it?** Yes, because there is only
   one consumer. The CLI gets the version solely through
   `_wfctl_version()` → `importlib.metadata.version("wfctl")`
   (`wfctl/cli.py:L67-L77`), used at L83, L1859, L1936, L2416, L2675, L3269. No
   `__version__` constant, no second literal, no CHANGELOG file, no PyPI publish
   step. `.wf-skills-manifest.json` records `wfctl_version` but is generated and
   untracked (`git ls-files` confirms).

5. **Does publishing 0.16.0 flip every consumer repo's `doctor` red?** No — and this
   was the most plausible unintended consequence. Per-layer skill staleness is keyed
   on `content_hash`, not on `wfctl_version` (`wfctl/cli.py:L1946-L1950`), and
   `tests/test_install_skills.py:L606-L621` pins exactly that: equal versions with a
   changed tree still reports stale, and the test asserts the version comparison is
   *absent* from the output. A version bump alone therefore causes no false stale
   report. What does change — correctly — is the release line: builds at 0.15.0 start
   seeing `⬆ wfctl 0.15.0 → 0.16.0 available`. That is the intent.

6. **Will the tag job actually fire, and has it ever?** It will fire; it has never
   taken its write path. `.github/workflows/ci.yml` `tag:` — `needs: [test, wheel,
   lint]`, `if: github.ref == 'refs/heads/main'`, `permissions: contents: write`,
   `fetch-depth: 0` (so `git rev-parse refs/tags/v$v` sees existing tags), version
   read with `tomllib` rather than a grep, and tag-push and `gh release create` keyed
   on their own existence so a half-failure retries. On merge it reads `0.16.0`,
   finds no `refs/tags/v0.16.0`, tags and pushes.
   But: the job landed in `1055330` on **2026-08-19 04:14**, and `v0.15.0`'s tag
   (tagger `Andre Marin`, 2026-08-18) and its GitHub release (author `aamarin`)
   both predate it. Every main run since has hit the `v0.15.0 already tagged` /
   `gh release view` no-op path — run `33828654030`'s `tag` job finished in 4s.
   So this merge is the first execution of `git tag -a`, `git push origin`, and
   `gh release create` in this repo. I checked the preconditions and they hold:
   `gh api repos/aamarin/wfctl/rulesets` → `[]`, `repos/aamarin/wfctl/tags/protection`
   → 404, `actions/permissions/workflow` → `default_workflow_permissions: "write"`.
   Nothing should block it. The PR already owns this risk with a post-merge
   checklist; my only ask is the NIT above — move the checklist somewhere that
   outlives the merge.

7. **Suite as evidence** (green, but not proof — no test reads the version key for
   its value):
   ```
   809 passed in 48.41s
   All checks passed!                            # uv run ruff check wfctl/ tests/
   Success: no issues found in 15 source files   # uv run mypy wfctl/
   ```
   Matches the PR's claimed numbers exactly.

8. **`uv.lock` correctness and scope.** `uv lock --check` → `Resolved 20 packages in
   7ms`, no drift, so the lockfile is genuinely in sync at 0.16.0. The diff moves one
   line inside the `[[package]] name = "wfctl"` entry (`uv.lock:L359`); no hash, no
   `requires-dist`, no dependency line, no unrelated package moved. `grep -n '0\.16\.0'
   uv.lock` returns that single line. Correctly scoped, and the separate commit matches
   the repo's own precedent (`d8688f6 chore: release 0.14.0`, `dd946be chore: sync
   uv.lock to 0.13.0`).

9. **The motivating claim, checked against the trees rather than the prose.** The
   stale-mirror story holds: `git ls-tree -d --name-only v0.15.0:wfctl/agents/skills`
   has neither `opening-a-change` nor `worktree-handoff`; `HEAD` has both. (Small
   imprecision in the issue: at v0.15.0 there was no `_MIRRORED_SKILLS` set at all —
   mirroring was driven by a `deployment: skill` frontmatter key
   (`v0.15.0:wfctl/cli.py:L821`). The substance — those two skills are unreachable
   from a v0.15.0 build — is correct, and that is what matters.)

## Pass 2 — Security

Nothing in the diff handles input, holds a secret, or touches auth. What I checked
anyway, because merging this publishes:

- The tag job uses `GH_TOKEN: ${{ github.token }}` — the job-scoped token, not a PAT
  — with `permissions: contents: write` declared at the job, so the elevated scope
  does not leak to `test`/`wheel`/`lint`.
- `gh release create --generate-notes` builds notes from merged PR titles. No build
  log, no environment, no secret material enters the release body.
- `git ls-remote --symref -- <url>` in `_remote_state` (`wfctl/cli.py:L2648`) already
  terminates options before the URL from `direct_url.json`, which is the argument-
  injection hole a release would otherwise ship. It is closed and commented.
- The release does make the shell-executing hooks bundled since v0.15.0 (#129's
  cross-worktree Bash guard, #85's merge install mode) reachable by `uv tool install`.
  That is the documented posture in AGENTS.md "Safety", each change was reviewed at
  its own merge, and `install-skills` still lists and prompts. Not a new exposure.

No finding.

## Pass 3 — Architecture

The version has exactly one home — `pyproject.toml [project].version` — and every
other appearance derives from it (`importlib.metadata` at runtime, `tomllib` in CI,
`uv.lock` at lock time). The diff does not add a second home, does not introduce a
constant to be kept in step, and does not create a cycle. The two-commit shape
(decision / derived artifact) is the shape the repo already used at 0.13.0 and
0.14.0, so it fits the existing pattern rather than inventing one.

**On the absent spec** — evaluated, not waved through. `design-levels` SKILL.md:L22
excludes "bug fixes, copy edits and other trivial changes that introduce no new
state". A version bump introduces no state, draws no boundary, and moves no
ownership; there is nothing for a level-1 or level-2 pass to decide. The one
decision here — *when* the key moves — is a release judgement, and it is written
down where AGENTS.md "Conventions" says it belongs: in the issue and in the commit
body. Correct call. The out-of-scope note (deriving the bump from conventional
commits) is also correctly refused: that one *would* need a design pass, and it is
named without being smuggled in.

No finding.

## Pass 4 — Readability & simplification

Two literal replacements; there is no logic to read. Judging the record instead,
which is the work product here:

- Subjects are statements, conventional-ish, correctly scoped `chore(release):`.
  `d186de6`'s subject is 71 chars and says what changed *and* why.
- Both bodies explain what the diff cannot show — why the minor, why the split, why
  the lockfile — which is what AGENTS.md "Conventions" asks for.
- Two accuracy defects in those bodies are filed above as WARNINGs. Chesterton's
  fence applies to the README literals the PR left alone: they are illustrative
  output, nothing generates them, and editing them here would put non-release
  content in a release-cutting diff. Agreed with the author.

## Pass 5 — Performance

No code path changed, so there is nothing to measure. The only runtime cost the
release alters is `doctor`'s: builds at 0.15.0 now take the `⬆` early-return branch
in `_check_wfctl_version` (`wfctl/cli.py:L2698-L2703`), which returns *before* the
second `_remote_state` call — so a stale build does one `git ls-remote` instead of
potentially two. Marginally cheaper, not more expensive.

No finding.

## Pass 6 — Over-engineering

Nothing to `delete:`, `stdlib:`, `native:`, `yagni:` or `shrink:`. The diff is two
lines, both required: one is the release decision, one is the artifact `uv` derives
from it and would otherwise rewrite under the next contributor. The PR explicitly
declines the speculative version of this work (auto-deriving the bump from
conventional commits) and declines the drive-by README edit. Both refusals are
correct.

**net: −0 lines possible. Lean already.**

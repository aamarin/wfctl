# Review: 153-cut-0-16-0 (r3)

Target: `git diff main...HEAD` — two commits, `d186de6` (pyproject.toml) and
`e28f63d` (uv.lock). Reviewed against `CLAUDE.md` (Releasing, Conventions),
issue #153, and PR #154's description, per the code-review skill's six passes.

## Passes run

1. **Correctness** — verified the version bump is well-formed TOML/lock syntax,
   verified the commit-count and breaking-change claims against actual git
   history (not just trusted the prose), verified the CI tag job's trigger
   conditions and its behavior line by line, verified `uv.lock` is actually in
   sync with `pyproject.toml` after the change. Found one factual error (below).
2. **Security** — n/a. No user input, no secrets, no auth surface in a two-line
   version bump. Nothing to report.
3. **Architecture** — checked for anything else in the repo derived from or
   coupled to the version key (CI scripts, `__version__` constants, CITATION
   files, README badges, hardcoded version strings in source). Found none
   outside what the PR description already discloses (README prose examples).
   The two-commit split (bump / lock-sync) matches project convention
   (`dd946be`, `d8688f6`) and CLAUDE.md's instruction not to bump the version
   as part of unrelated work — this diff is *only* the bump.
4. **Readability** — n/a, no code.
5. **Performance** — n/a.
6. **Over-engineering** — the diff is already the minimum possible (2 lines,
   2 files, 2 commits, each with one purpose). Nothing to cut.

## Verification performed (not just read)

- `git log --oneline --no-merges v0.15.0..main | wc -l` → 37. Commit-count claim holds.
- `feat!: remove \`wfctl checkpoint\` (#68)` is in that range (`4ca1604`), and
  `wfctl checkpoint` is in fact gone from `wfctl/cli.py` (only stale doc
  mentions remain in unrelated skill templates, pre-existing, out of scope).
  0.16.0 (minor bump for pre-1.0 breaking change) over 0.15.1 is justified.
- `uv lock --check` passes on the branch — `uv.lock`'s version line and
  `pyproject.toml`'s version key are in sync. The diff is correctly scoped:
  `git diff main...HEAD --stat` shows exactly `pyproject.toml` and `uv.lock`,
  1 line each.
- Read `.github/workflows/ci.yml`'s `tag` job in full: gated on
  `needs: [test, wheel, lint]` and `github.ref == 'refs/heads/main'`, reads the
  version via `tomllib` (not a fragile grep), tags `v$v` only if the tag
  doesn't already exist, and separately guards `gh release create` on the
  release's own existence (not the tag's) — so a failed `gh release create`
  after a successful tag push is retried on the next main push rather than
  silently staying missing. This will fire and produce `v0.16.0` on merge.
- Ran the three commands `CLAUDE.md` names as the definition of done:
  `uv run pytest -q` → 809 passed; `uv run ruff check wfctl/ tests/` → all
  checks passed; `uv run mypy wfctl/` → no issues. Matches what PR #154 claims
  in "Test Details" (it reports 809 passed too, different wall-clock time,
  irrelevant).
- Searched the repo for anything else keyed to the version string: no
  `__version__` constant, no CI script reads `0.15.0`/`0.16.0`, no
  CITATION/badge file. `README.md` has the string `0.15.0` in exactly 7 lines
  (195, 205, 262, 264, 274, 300, 317), each one occurrence — all inside
  illustrative `doctor`/`install-skills` sample output, none of them the
  actual installed-version source. `tests/test_install_skills.py:611` also
  mentions `0.15.0` but as a hypothetical example string in a docstring/test
  name, not a pinned assertion — confirmed it does not assert against the
  real package version.

## Findings

NIT `d186de6` commit message — "v0.15.0 was tagged 2026-08-19" → the tag was
actually created 2026-08-18 13:43:00 -0500 (`git for-each-ref
refs/tags/v0.15.0 --format='%(taggerdate)'`; the tagged commit `65d48c2` is
also dated 2026-08-18). Same sentence is repeated verbatim in issue #153 and
PR #154. It's off by one calendar day and doesn't change the substance of the
argument (37 untagged commits either way), but it's a checkable factual claim
in a commit message this project treats as part of the release's reasoning
record — worth a one-word fix (`2026-08-18`) if anyone touches this again, not
worth blocking on.

No BLOCKER or WARNING findings. The diff does exactly what #153 and the commit
messages say it does, touches nothing else derived from the version key,
correctly reproduces the project's established two-commit release pattern, and
the CI job that's supposed to act on it will fire as described.

net: −0 lines possible. Lean already — it's a two-line version bump split
across two one-line commits; there is nothing left to cut.

**Verdict: Approve.**

One process note, not a defect in the diff: PR #154's own checklist correctly
flags that the tag/release job firing is *unverified until after merge*
("This will not be reported done until step 1 is confirmed"). That's outside
this diff's review — file it as a post-merge follow-up, not a reason to hold
the PR.

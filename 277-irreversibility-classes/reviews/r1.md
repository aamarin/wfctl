## Review: origin/main..HEAD (17fd129 — docs(arch): give the three authority classes a home, and a criterion)

Target: two new/changed files, both documentation.
- `docs/architecture/wfctl-classes-the-action-not-the-command.md` (new, `status: proposed`)
- `docs/references/README.md` (new section appended)

### Context gathered before reviewing

- Read `.agents/skills/code-review/SKILL.md` (this rubric), `.agents/skills/architecture-decisions/SKILL.md` + `record-template.md`, `docs/references/README.md`'s opening, and the accepted records the new record cites or that the task named: `install-modes`, `a-rule-is-expressed-as-a-check`, `knowledge-placement`, `layer-model`, plus `approval-mode-is-stored-intent` (proposed) and `wfctl-runs-the-verification`.
- Read `wfctl/_arch.py` in full (the `decision_text` projection, `validate`, `load_records`) rather than trusting its docstring.
- Read `wfctl/_pipeline.py` (line 122) and `wfctl/agents/skills/start-session/SKILL.md` (line ~258) for the two quoted files.
- Read `tests/test_install_hook_merge.py`.
- Pulled issues #37, #40, #100, #101, #127, #131, #240, #277 via `gh issue view` (GitHub MCP was down — auth rejected — so I used the `gh` CLI directly).
- Fetched the eight external sources cited in the new research-index entry via WebFetch/WebSearch to check quotes against source text.

### Pass 1 — Correctness (here: factual accuracy, since there's no executable code)

Verified, and confirmed accurate:
- `_pipeline.py:122` — `"creating them is outward-facing and waits for"` a human — exact match.
- `start-session/SKILL.md` — `"Beginning implementation is local and reversible"` — exact match (line 258-259).
- Confirmed by grep these are the **only** two files in `wfctl/` using either phrase — the record's "two files" claim is exactly right, not an undercount.
- `install-modes.md` — "gives the consumer that half of `.claude/settings.json`" matches its `Owns truth` section ("the consumer owns the file and wfctl owns exactly the entries...").
- `test_install_hook_merge.py::test_install_preserves_foreign_permissions_and_hooks_and_adds_one_entry` docstring literally says *"The acceptance criterion for the whole mode: everything the consumer had is byte-for-byte where they left it..."* — matches the record's claim precisely.
- #37, #40 are real, open, and are exactly the "uncommitted work destroyed" issues the record cites for why commit is reversible-by-default.
- #100/#101 do hold merge/integration authority out of scope of any agent loop — confirmed from both issue bodies ("Integration authorization → human, PR, or tracker").
- #131→#127 fold-in history matches exactly what #277's body says, which the record's Context section restates correctly (`gh issue view 127 --json body` genuinely has no hit for "irreversib" or "outward", consistent with the record's claim).
- `approval-mode-is-stored-intent.md` — *"There is no wfctl command an agent cannot run unprompted, so choosing one for its inaccessibility is choosing an illusion."* — exact quote, correctly attributed.
- `layer-model.md` is real and accepted, and does say every dotted directory at the repo root is "generated, gitignored, and never edited by hand" — the record's paraphrase ("generated and writable") is a fair synthesis, not a misquote.

**`Owns truth` — both halves checked, present:**
`wfctl owns "what class is this action, and who has to be asked?"` is followed by two separate "other side cannot compute it" arguments (the harness: command-string matching correlates but isn't the question; the agent: classing its own action is the self-report `wfctl-runs-the-verification` already rejects). Both halves are there, and the second half — the one the skill warns gets dropped — is present for *two* candidate owners, not stopped at one. This is a strong pass of the hardest checklist item.

One readability wrinkle, not an error: the same section opens with "wfctl owns [the whole question]" and later says "Row three is owned by the human and is not delegated to wfctl at all." On a careful read these don't contradict (wfctl owns the *classification*, the human owns row-3's *grant*), but a fast reader can misread it as inconsistent. Worth a clarifying half-sentence — NIT, not a blocker.

**`Decision` projects correctly through `wfctl arch context`** — verified by calling `decision_text()` directly (not by reading the docstring), since the record is currently `proposed` and won't appear in a default `arch context` run:

```
$ uv run python3 -c "from wfctl._arch import parse_record, decision_text; from pathlib import Path; r = parse_record(Path('docs/architecture/wfctl-classes-the-action-not-the-command.md')); print(decision_text(r))"
```
returned the lead paragraph plus the full 3-row table verbatim — the colon-then-block carry-through works as designed. `validate(load_records(...))` also reports zero findings (no dangling `supersedes`, no orphaned `superseded`).

**Scope respected.** `git diff origin/main...HEAD --name-only` shows exactly the two doc files. No touch to `decompose`, `_STEPS`, or `end-session`'s prompts, matching the stated exclusion.

**Architecture-decisions checklist**, walked item by item: slug has no sequence number and is never renamed elsewhere in the diff; `Owns truth` phrases the question in quotes (see above); `Direct baseline` is concrete (one shipped skill, prose, no other change) and comparable to the decision; `Considered` carries five alternatives, and at least two of them (the `PreToolUse` hook, the fourth "security posture" class) are explicitly conceded as sound/defensible and rejected on fit or reach rather than given a manufactured weakness — this is exactly the pattern the skill's own example calls out; `Log` has one dated line matching the current `status: proposed` and matches the commit date (`git log -1 --format=%ad 17fd129` → 2026-09-07). All required sections present, in template order, `Consequences` used appropriately.

### Pass 2 — Security

N/A (no code, no secrets, no auth surface). Skimmed for accidentally-pasted tokens/URLs with credentials — none.

### Pass 3 — Architecture

Correctly placed: this is a constraint on the system ("who decides an action's class, and who is asked"), which `knowledge-placement` routes to `docs/architecture/`, not `AGENTS.md`. The research citations are correctly kept in `docs/references/README.md` rather than under `docs/architecture/`, consistent with that file's own "placed outside the arch root on purpose" rule (#171) — a record with an argument mixed into a citations file would wrongly satisfy a level-2 gate about boundaries.

### Pass 4 — Readability / research-index accuracy

Citation-by-citation verification (task item 4):

| Source | Verified |
|---|---|
| Claude Code Auto Mode — "93%" and the four category names | Exact quote matches, including the literal string `"bypass review or affect others"` |
| Measuring AI agent autonomy — "0.8%" and the friction quote | Both are accurate contiguous substrings of the real page |
| OpenAI governance paper | PDF wouldn't render as text for me (binary/compressed extraction failed) — could not independently confirm the quoted sentence. Not disproven, just unverified from my end |
| Bravo-Lillo SOUPS 2014 | Paper is real ("Harder to ignore? Revisiting pop-up fatigue…", SOUPS 2014) and on-topic, but the primary PDF 403'd for me, so I could not confirm the specific "50–70%", "two seconds", "two or three exposures" figures. The entry hedges this correctly by citing "the browser-warning literature it sits in" rather than pinning the numbers to Bravo-Lillo alone — reasonable, but flag as **unverified, not confirmed** |
| Capability Myths Demolished | Real paper (Miller, Yee, Shapiro); confused-deputy / "ambient authority is exercised but not selected" is a fair non-verbatim paraphrase of the paper's actual argument |
| NIST SP 800-53 AC-3(3) | Confirmed real control (Mandatory Access Control) and the record's framing — "a subject that can waive its own restriction is under discretionary control, not mandatory" — matches the control's own text ("constrained from... changing the rules governing access control," with self-exemption only via an org-level exception, never a self-grant) |
| AWS DL.LD.5 | **The fetched source text does not contain anything resembling the `--no-verify` / "the gate is whatever runs where the actor is not" framing.** The actual page just recommends pre-commit hooks plus CI/IDE integration in general terms; it says nothing about local hooks being bypassable or about "the gate" being elsewhere. This may be a JS-rendering gap on my end rather than a wrong citation, but as currently checkable, the "claim taken" reads as the record author's own inference dressed as sourced from DL.LD.5. See WARNING below |
| MCP tool annotations blog | Exact quote match: `"An untrusted server can claim readOnlyHint: true and delete your files anyway."` |
| Orseau & Armstrong, *Safely Interruptible Agents* (UAI 2016) | Real, correctly attributed venue/year; the record's claim ("irreversibility... a decade before coding agents") is a fair, non-quoted characterization |

Every row does carry a refutation condition, and every one I checked is actually checkable against something in this repo (an artifact — `events.jsonl`, `git reflog`, `.claude/settings.json`, `layer-model`, `wfctl-runs-the-verification` — or, for the two "second-vendor convergence" rows, explicitly and correctly scoped to "evidence from outside wfctl" rather than faking an in-repo check). That satisfies the file's own bar ("an entry with no refutation condition is not research").

Minor cosmetic nit: the new table's header reads "What would refute the claim here" vs. the existing section's "What would refute it here" — harmless but inconsistent within the same file.

### Pass 5 — Performance

N/A.

### Pass 6 — Over-engineering

Nothing to cut. This is prose carrying an argument, not code; the added "### The strongest argument against" subsection in the research index has no counterpart in the file's other entry, but it earns its place (it's where the record's own "not chosen" reasoning for the PreToolUse alternative gets its supporting citations) rather than padding.

net: 0 lines removable.

### Findings

WARNING `docs/references/README.md:~64` — the DL.LD.5 row's "claim taken" (pre-commit hooks are bypassable via `--no-verify` and "the gate is whatever runs where the actor is not") is not present in the linked AWS Well-Architected DevOps Guidance page as I was able to fetch it; the page only makes a generic pre-commit + CI recommendation → re-verify this specific framing against the live page (possibly JS-rendered content a fetch tool can't see) before treating it as sourced, or rephrase the row so the "gate location" argument is presented as the record's own inference rather than something DL.LD.5 states.

NIT `docs/references/README.md` — Bravo-Lillo SOUPS 2014's specific figures (50–70% click-through, ~2-second dismissal, habituation after 2-3 exposures) couldn't be confirmed (primary PDF returned 403 to my fetch); worth the author double-checking against the actual paper text once, since the entry currently reads as a direct empirical claim.

NIT `docs/architecture/wfctl-classes-the-action-not-the-command.md` (`## Owns truth`) — "wfctl owns the whole question" followed by "row three is owned by the human… not delegated to wfctl at all" reads as tension on a first pass; a half-sentence distinguishing "wfctl owns the classification" from "the human owns the grant" would remove the need for a second reading.

NIT `docs/references/README.md` — new table's refutation-column header ("What would refute the claim here") doesn't match the existing section's header wording ("What would refute it here").

net: −0 lines possible (this is a docs change; nothing here is over-engineered or removable).

**Verdict: Approve.** No blockers. Every load-bearing factual claim I could check against this codebase — the two quoted files, the issue history (#37, #40, #100, #101, #127, #131, #240, #277), `install-modes`, `test_install_hook_merge.py`, `layer-model`, `approval-mode-is-stored-intent`, and the `decision_text()` projection itself — checked out exactly as stated. The `Owns truth` section clears the bar the skill calls hardest (both halves, argued against two candidate owners). Scope is respected: no consumer file is touched. The one thing worth fixing before/soon after merge is the DL.LD.5 citation, which is a WARNING against the research index's own stated standard, not against the architecture record.

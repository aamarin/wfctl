## Review: origin/main...HEAD (17fd129 — docs(arch): give the three authority classes a home, and a criterion)

Reviewer: r2. Target: two doc files (`docs/architecture/wfctl-classes-the-action-not-the-command.md`,
`docs/references/README.md`), per the `code-review` skill's six passes, plus a direct check of
issue #277's acceptance criteria.

### Passes run

- **Correctness** — read cold as a naive reader; verified every checkable factual claim
  against the live repo, tests, source, `gh`, and the two primary web sources cited.
- **Security / Performance** — N/A, doc-only diff. No secrets, no code paths.
- **Architecture** — checked placement (`docs/architecture/` vs `docs/references/`) against
  `knowledge-placement` and the research index's own opening rules; checked the record against
  `architecture-decisions/SKILL.md` + `record-template.md`.
- **Readability / soundness** — read the record without prior knowledge of #131 (AC1); traced
  the "frequency decides delivery" argument back to its cited sources.
- **Over-engineering** — doc-only; checked for a droppable row/section.

### Findings

BLOCKER  `docs/architecture/wfctl-classes-the-action-not-the-command.md:100` — "Consequences"
asserts "#240 is unblocked" by this record. That is false as of the moment this commit landed,
and demonstrably so: #240's own comment thread (last comment 2026-09-07T16:20:10Z) states "The
gap named in my last comment now has a number: #280. That is what this issue waits on — not
#127, and not #100" (and, by the same logic, not this record). Issue #280
("Nothing grants per-feature authority for outward-facing actions...") was filed
2026-09-07T16:20:00Z, **before** this commit (2026-09-07 12:57:58 -0500 = 17:57:58Z) — the author
had ~1h38m to see it and did not reference it. #280's own text explains why classification alone
doesn't unblock #240: *"#131 settled the classification and it survives in its body:
local/reversible allowed, outward-facing refused by default, irreversible never. What has no home
is the switch that relaxes the middle row per feature."* This record supplies the durable home for
the classification (matching #280's premise) but not the per-feature grant switch — which this
record's own "Considered" section explicitly defers as "successor work" (the PreToolUse hook). So
by the record's own scope, it cannot be what unblocks #240, and #240's thread says so in as many
words. This directly falsifies #277's fourth acceptance criterion: *"#240 is unblocked by it, and
its comment thread says so."* The thread says the opposite. → Fix: correct the Consequences claim
(#240 remains blocked on the grant mechanism, tracked at #280, not resolved by this record) and
cross-reference #280. Do not ship "#240 is unblocked" as-is.

BLOCKER  `docs/references/README.md:71` — the quotation attributed to "Practices for Governing
Agentic AI Systems" — *"When people are asked to approve too many actions without much bandwidth
for doing so, the human may start rubber stamping the AI system's actions"* — does not appear in
that paper. I downloaded the actual PDF
(`cdn.openai.com/papers/practices-for-governing-agentic-ai-systems.pdf`) and searched it: zero
occurrences of "bandwidth" anywhere in the document; the only "rubber stamp" text is a different
sentence — "...minimum review times) to avoid their turning into a 'rubber stamp' for agentic AI
systems" — posed as an open question about review cadences, not a declarative finding about
approval volume. This is a fabricated quotation presented as verbatim in a table whose entire
premise is that "an entry with no refutation condition is not research; it is a belief with a
citation" — and, independent of the fabrication, this same row's own refutation cell reads
*"Nothing in this codebase... refuting it needs evidence from outside wfctl,"* which is itself
exactly the violation that house rule forbids. Two separate defects compound in one row. → Fix:
remove the invented quote entirely; either cite what the paper actually says with a
codebase-checkable refutation condition, or drop the row — it's a second citation for a point the
Auto Mode row already makes (habituation from over-approval), so cutting it costs no argument.

WARNING  `docs/references/README.md:75` — the specific figures attributed to "Bravo-Lillo et al.,
SOUPS 2014" ("50–70% click-through on security warnings, about half dismissed inside two seconds;
habituation is measurable after two or three exposures") do not appear in that paper. I downloaded
it (`usenix.org/system/files/soups14-paper-bravo-lillo.pdf`) and searched for "50%", "60%", "70%",
"75%", and "two second(s)" — no hits. That paper's actual finding is a *three-fold decrease* in the
proportion of participants who noticed a changed dialog under habituation — a different measure
entirely from a click-through percentage or a dismissal-time figure, which belong to other studies
(e.g., Akhawe & Felt's SSL-warning-adherence work) that aren't the one named here. → Fix: attribute
the specific numbers to their actual source, or restate only what this paper supports (the
three-fold attention-decrease finding).

NIT  `docs/architecture/wfctl-classes-the-action-not-the-command.md:64` — "no expression in any of
the **five** agent configurations surveyed" doesn't match either set of tools the record actually
names: line 26-27 names four ("Claude Code's ... Gemini's, Cursor's and Copilot's"), and line 82
(Considered) names a different four ("Claude Code, Codex, Gemini and Cursor" — swapping Copilot for
Codex). No five-tool set appears anywhere in the document, and the two four-tool sets it does give
don't even agree with each other. → Fix: enumerate the fifth tool explicitly and reconcile the
Copilot/Codex discrepancy, or say "four."

NIT  `docs/references/README.md:77` — Orseau & Armstrong (UAI 2016) is cited as having formalized
"irreversibility... as a formal property of agent safety," but the paper's named formal
contribution is *safe interruptibility* — a definition and proof that certain RL agents can be made
safe to interrupt — not irreversibility of actions; irreversible consequences appear only as
motivation, not as the formalized property. The citation slightly overstates its antecedent. → Fix:
narrow the claim to "the interruptibility literature treats an agent's own trajectory as the unit of
harm, not third-party notification" (close to what the row's own refutation condition already says)
rather than crediting the paper with formalizing irreversibility itself.

WARNING  AC2 ("a skill can determine an action's class without restating the list — or the change
says why prose is the right delivery here, per `a-rule-is-expressed-as-a-check`") is not actually
discharged, on two independent grounds:
  1. The record ships `status: proposed`. Verified directly: `wfctl arch context` on this tree
     prints "9 records not shown (9 proposed)" — this record among them — and `in_force()` filters
     to `accepted` only. `decision_text()` extracts the full three-row table correctly when run
     against the record directly (I checked this — the commit's claim that the table projects
     whole is true), but that is moot: the one shipped mechanism for a skill to consult the
     architectural contract excludes this record entirely until a human accepts it (a step this
     record correctly notes is separate, per #122). Nothing today can "determine an action's
     class" from it without opening the file directly and reading prose — which is the AC's
     "restating the list" case, not its alternative.
  2. The record *does* invoke `a-rule-is-expressed-as-a-check`, but for a different question than
     AC2 poses. Its Consequences section uses that record to argue the **push-without-asking
     rule** stays prose (a push looks identical whether asked or not — a violation-visibility
     argument). AC2 asks about **class lookup** — can a skill ask "what class is this action"
     without restating the list — which is a lookup-mechanism question, not a
     violation-visibility one. The record answers the record it cites for a claim it doesn't
     make. → Fix: either add the missing argument (why classification lookup, specifically, is
     fine as unconsulted prose for now) or say plainly in Consequences that AC2's lookup half is
     unmet pending acceptance + a consumer, rather than letting the a-rule-is-expressed-as-a-check
     citation stand in for an argument it doesn't cover.

WARNING  `docs/references/README.md:81-89` ("What the set argues, taken together") — the claim
"that makes **frequency**, not danger, the variable that decides delivery" overstates what the
cited sources show. I verified the two most load-bearing citations verbatim against the source
pages: Auto Mode's four blocked categories (*destroy or exfiltrate, degrade security posture,
cross trust boundaries, bypass review or affect others*) and the autonomy paper's "0.8% of actions
appear to be irreversible" — both are organized by **what an action does** (severity/reversibility
of harm), not by how often it fires. The table's own row for the four categories concedes "a class
in #277's split fits none of the four" for row 2, i.e., the source the section leans on for
"frequency" is actually a severity taxonomy that doesn't cleanly map. Frequency and irreversibility
are confounded in #277's own three rows (the frequent thing is also the reversible one; the rare
thing is also the catastrophic one), and the section never identifies or rules out a case where
the two diverge — so "frequency, not danger" reads as asserted rather than isolated by the
evidence given. The record's actual practice (row 3 is "always the human," which is argued by
irreversibility/finality, not rarity) is consistent with "danger decides it" just as well. → Fix:
soften to "frequency and severity move together in this split, and frequency is the one with a
falsifiable local test" (which the record does have, via #100/#101's reflog point) rather than
asserting frequency is *the* variable.

NIT  `docs/architecture/wfctl-classes-the-action-not-the-command.md:46-50` — the Decision table's
"Delivery" column answers *how the rule is communicated* (prose vs. check), not *who is asked /
what's allowed without asking*, which is the question the record's own H1 promises ("the class
decides who is asked") and the one #277 is actually about. The allow-without-asking verdict for
row 1 and the ask-before-doing verdict for row 2 are only recoverable by inference from
Consequences ("`end-session`... gates row one and omits row two" implies row 1 shouldn't be gated
and row 2 should be) — never stated as a direct rule in the Decision itself. A reader (or a future
skill) scanning the table for "may I push without asking?" gets "prose, plus a check wherever one
is cheap," not a yes/no. → Fix: add an explicit allowed/asks-first value per row alongside
Delivery, so the table answers the question its own title poses.

### Verified as correct (no finding)

- Both primary citations (Anthropic "Claude Code Auto Mode" 93%-approval figure and four blocked
  categories; "Measuring AI agent autonomy in practice" 0.8%-irreversible figure and the
  human-approval-friction quote) are verbatim-accurate against the live pages.
- `_pipeline.py:122` ("outward-facing and waits for a human") and
  `start-session/SKILL.md:258-259` ("local and reversible — edit files, commit, write the
  summary") are accurately quoted/paraphrased in the record's Context section.
- The commit message's technical claim — that the Decision projects whole through
  `wfctl arch context`'s `decision_text()`, checked rather than assumed — is true: I ran
  `_arch.decision_text()` directly against the parsed record and it returns the full paragraph +
  table, correctly following the colon-then-table branch added for #226.
- Record structure matches `record-template.md`: all required sections present (`Context`,
  `Direct baseline`, `Decision`, `Owns truth`, `Considered`, `Log`; `Consequences` present and
  used correctly as optional). `Owns truth` carries both required halves (who owns "what class is
  this action, and who has to be asked?", and why the harness/agent can't compute it). `Considered`
  lists five real alternatives, each with a substantive rejection reason rather than a manufactured
  weakness. `Log` has a dated `proposed` line.
- Placement is correct: the record sits under `docs/architecture/` (the arch root), and the
  research entries sit in `docs/references/README.md`, outside the arch root, matching that file's
  own stated reason for the split (a level-2 gate counts subdirectories under `docs/architecture/`
  as records; research must not count).
- Every row in the new "Agent action gating" research table except the one flagged above carries a
  genuine, codebase-checkable refutation condition, matching the file's own house rule.
- `git diff` scope matches #277's stated exclusions: no changes under `wfctl/`, no touch to
  `decompose`, `_STEPS`, or `end-session`'s prompt text. `grep` confirms zero hits for those in the
  diff.
- Ran `tests/test_arch_records.py`, `tests/test_architecture_view.py`, `tests/test_arch_root.py`
  (63 tests) — all pass; the new record doesn't break any corpus/structural validation.
- AC1 ("readable without knowing #131 existed") substantially holds: the Context section explains
  inline what #131 was and what happened to it, rather than assuming the reader already knows —
  I read it cold and could follow it without external lookup. (#37/#40 are cited more thinly — bare
  issue numbers with no inline gloss of what they document — but the argument they support
  ("not committing loses work") is stated in prose either way, so this doesn't block AC1.)

### Over-engineering

Doc-only diff; no code to shrink. One droppable row identified above (the OpenAI-paper citation) —
cutting it is also the fix for a correctness finding, not a separate cleanup.

net: −1 row possible (the fabricated/ungrounded citation)

### Verdict

**Request changes** (2 blockers: the false "#240 is unblocked" claim, contradicted by #240's own
comment thread and by issue #280 which predates this commit; and a fabricated quotation attributed
to the OpenAI governance paper, confirmed absent from the source PDF). A citation-table document
whose job is to be checked cannot ship with an invented quote in it, independent of how the #240
claim resolves.

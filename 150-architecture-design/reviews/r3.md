# Review: 150-architecture-design (r3)

Target: `git diff origin/main...HEAD` — 2 files, 294 insertions.
`wfctl/agents/skills/architecture-design/SKILL.md` (new),
`tests/test_architecture_design_skill.py` (new).

## Findings

WARNING  wfctl/agents/skills/architecture-design/SKILL.md:19-22 — the skill states
it "runs inside `design-levels` level 2", and as shipped nothing can reach it. It
is absent from `_MIRRORED_SKILLS` (`wfctl/cli.py:1227`), has no wrapper under
`wfctl/agents/commands/`, and no shipped file contains
`.agents/skills/architecture-design` (grepped the whole tree: the only hits are
the skill itself, its test, and `.workmux/PROMPT-*`). `design-levels`'s level-2
gate still points straight at `architecture-decisions`, so a level-2 pass today
never runs the driver loop. `tests/test_review_fanout_skill.py:test_the_panel_skill_is_model_invocable`
exists for exactly this condition — "without it the command wrapper is the only
way in, which is the failure being designed out" — and here there is not even a
wrapper. The deferral is deliberate and tracked in #151, which keeps it out of
BLOCKER, but the diff ships prose that asserts a control flow that does not exist,
and the AGENTS.md requirement for a `wfctl/agents/` change ("run `wfctl
install-skills` and exercise the thing you changed") cannot be met through the
skill's own entry path. → Either land the level-2 reference and the frozenset
entry with #151 before merge, or add one sentence to the Overview saying the
level-2 hand-in is not wired yet and naming #151, so a human reading the
installed tree is not told the loop is live.

WARNING  SKILL.md:210-213 — restates a rule it says it does not own, and gets it
wrong. The sentence "Where they land is `design-levels`' 'Where the levels land',
not this skill's to decide" is immediately followed by "levels 1 and 3 land in
`specs/<branch>/design.md`, level 2 lands in a record under `wfctl arch-root`".
`design-levels/SKILL.md:204-220` says level 3 lands in **both** — `design.md` for
the claims, plus one record under `<arch-root>/design/` when the choice weighed
credible alternatives. This is the #50 drift shape the repo already tests against
(`test_the_change_description_skill_does_not_restate_the_template`), and the copy
that falls behind will not announce itself. → Cut the restatement and keep the
pointer.

WARNING  SKILL.md:186-213 (step 6) — the iteration's distinctive output has no
durable destination, which is the thing #150 was filed about. Step 6 presents
ranked drivers, a `checked evidence | assumptions | open questions` split and a
boundary sketch, then hands off to `architecture-decisions`, whose
`record-template.md` carries only Context / Decision / Owns truth / Considered /
Consequences / Log — no driver field, no checked/assumed split. The skill forbids
a separate drivers artifact (correctly) but names no section that takes them:
"Carry the ranked drivers there in context, and pass only the decision-specific
rationale to the record" leaves both the file and the heading unnamed. The
worktree handoff's Correction 1 was explicit — "carried into the record's own
Context" — and that half did not survive into the text. Net effect: the
quality-attribute scenario stays in the transcript, which is design-levels' own
documented failure ("eleven designs carried an instruction to write the answer
into a section and none of them did"). → Name the destinations: ranked drivers
into `design.md`, and the ones the decision rests on into the record's `##
Context`. Worth noting while fixing:
`wfctl/agents/skills/software-design-decisions/design-record-template.md` already
ships `Verified` / `Assumed` sections that match step 5's output exactly, and the
skill never mentions it — if step 5's split is meant to land anywhere, that is the
template that has the fields.

WARNING  SKILL.md:57 and 202-203 — deference to a policy that does not ship. "This
skill consumes the gate and inconclusive-evidence rules owned elsewhere" and "pause
or escalate under the existing gate policy" have no referent: grepping
`wfctl/agents/skills/*/SKILL.md` and `wfctl/agents/commands/*.md` for gate policy
and escalation turns up only `agent-brief`'s headless protocol (write
`specs/<branch>/escalation.md`), which the skill neither names nor plausibly means
for an interactive design pass, and #100 has not landed. This fires on the
missing-evidence branch — the decisive one — so the agent is sent to look up a rule
it cannot find and will improvise. → Either name a concrete action (stop and ask
the human, which the Authority section already establishes) or say plainly that no
gate vocabulary is in force yet and cite #100. Same applies to "Escalation is
always available; waiving required review is not" (SKILL.md:59): no "required
review" concept exists in this skill or the bundle, so the line asserts nothing.

WARNING  tests/test_architecture_design_skill.py:18-22 — the comment justifying
case-sensitivity describes text that is not in the file under test, and the
case-sensitivity it justifies defeats the guard for the one wfctl-owned token it
names. The comment says "the skill's prose says a driver must be specific enough
to fail, and lowercase 'fail' is that sentence" — the skill says "specific enough
to be **disproved**" (SKILL.md:97), and `\bfail\b` matches nowhere in the skill in
any case. Meanwhile `wfctl verify` records lowercase `inconclusive`
(`wfctl/_verify.py:219`, `_pipeline.py:103`) and the skill already contains that
lowercase word at line 57 — so `INCONCLUSIVE` in the pattern is dead weight and a
verdict later written in the token's real casing passes the test. `BLOCKED` matches
nothing in the bundle either; `end-session` uses lowercase "blocked". Of the four
tokens only `PASS`/`FAIL` (speckit-implement, verification-before-completion) exist
in the form the regex looks for. Per CLAUDE.md, comments carry rationale for the
next reader; this one will send them looking for a sentence that is not there. →
Drop the stale justification, keep `PASS|FAIL` case-sensitive, and match
`inconclusive`/`blocked` case-insensitively — or scope the test to the uppercase
report tokens and cut the two that have no uppercase form in force.

NIT  SKILL.md:236-237 vs 124-128 — the checklist item "At least two credible
approaches were compared, including the direct no-new-structure baseline" carries
no escape for the case step 3 explicitly allows ("If literally doing nothing cannot
satisfy the required behavior, say so; an impossible option is not a credible
alternative"). In that case the box cannot be honestly ticked, in a skill that
elsewhere forbids inventing an option to complete the method. → "…, or the
baseline was named and shown not to satisfy the required behavior."

NIT  tests/test_architecture_design_skill.py:1-8 — the module docstring claims the
file guards "a handoff naming a skill that moved". Neither test here does that;
`test_every_referenced_skill_ships` (tests/test_skill_cross_references.py:33) does.
This file asserts the name is present, nothing about the target existing. → Trim
the claim or name the sibling that carries it.

NIT  test placement — `test_the_design_skill_hands_its_proposal_to_the_record_skill`
is a near-clone of `test_the_level_2_gate_names_the_record_skill`
(tests/test_skill_cross_references.py:78) and re-derives `_AGENTS` and
`_REFERENCE`. Per-skill files have precedent (`test_review_fanout_skill.py`), so
this is preference, not defect. Author's call.

## Passes run

1. **Correctness** — read the skill end to end as an agent would execute it, and
   checked every external claim it makes against the tree: `wfctl arch context`
   and `wfctl arch-root` both exist (`wfctl/cli.py:749`, `:897`);
   `architecture-decisions` does own the `proposed` status lifecycle and does
   point at `wfctl arch-root`; design-levels' "Where the levels land" heading and
   its ASCII-not-mermaid rendering rule are real. Two claims failed: the
   "Where the levels land" restatement (finding 2) and the "existing gate policy"
   (finding 4). The step-3 / checklist tension is finding 6.
2. **Security** — no code paths, no input handling. The test reads one packaged
   file through `importlib.resources`. Nothing to report.
3. **Architecture** — reachability (finding 1) and output destination (finding 3).
   Also checked for a competing account of an existing skill's territory: the new
   skill does not restate `architecture-decisions`' record rules, does not mint a
   verdict vocabulary (verified by grep, not just by the test), and does not
   duplicate `code-review` or `design-levels` content beyond finding 2.
   Frontmatter is spec-conforming (`name`, `description` only; description 468
   chars) and covered automatically by `test_skill_frontmatter`. Packaging is
   `graft wfctl/agents` in MANIFEST.in, so the new directory ships without a
   manifest edit.
4. **Readability** — prose is dense and matches house voice; 247 lines is in
   family with design-levels (287) and speckit-implement (242). Test docstrings
   name the failure they catch rather than restating the assertion, per CLAUDE.md.
   The one comment-style violation is finding 5.
5. **Performance** — n/a. Two `read_text()` calls on a 9KB packaged file.
6. **Over-engineering** — nothing substantive to delete. The 11-item checklist is
   house style (design-levels ships a 9-item one), so I am not flagging it. The
   only content-free line is Authority's "Escalation is always available; waiving
   required review is not."

## Verification I ran (read-only)

`uv run pytest -q` → 811 passed. `uv run ruff check wfctl/ tests/` → clean.
`uv run mypy wfctl/` → clean. Per the review brief I did not run `wfctl verify`,
`wfctl doctor`, or `wfctl install-skills`, and I could not read issues #96, #98,
#100, #151 (the github MCP server is failing auth), so the issue numbers cited in
the test docstrings are unverified.

net: −4 lines possible (the "Where the levels land" restatement, the
required-review line)
Verdict: Request changes (0 blockers, 5 warnings — findings 2, 4 and 5 are
one-sentence fixes; 1 and 3 are judgment calls the author may already have argued)

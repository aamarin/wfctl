# Review: 150-architecture-design (`git diff origin/main...HEAD`) — reviewer r2

Target: two new files, 294 insertions —
`wfctl/agents/skills/architecture-design/SKILL.md` (247) and
`tests/test_architecture_design_skill.py` (47).

## Findings

```
WARNING  tests/test_architecture_design_skill.py:L20 — the comment justifying case-sensitive matching cites skill text that does not exist ("specific enough to fail"; the skill says "specific enough to be disproved", and bare "fail" appears nowhere in it) → name the tokens case-sensitivity actually protects: lowercase "pass" ×3 (SKILL.md:L89-90 "the drivers this pass must address" / "the decision this pass must make", L212) and lowercase "inconclusive" (SKILL.md:L57)
WARNING  wfctl/agents/skills/architecture-design/SKILL.md:L57,L203 — defers to a "gate and inconclusive-evidence rules owned elsewhere" and an "existing gate policy" that ship nowhere: "inconclusive" occurs in no other file under `wfctl/agents/`, and #100, the owner, is still open → name #100 as the owner-to-be and state the interim behaviour ("stop and ask the human"), or drop the deferral and say that plainly
WARNING  wfctl/agents/skills/architecture-design/SKILL.md:L208-212 — points at `design-levels`' "Where the levels land" and then restates it in the same sentence, and the restatement is already incomplete: it drops the level-3 record under `<arch-root>/design/` that `design-levels:L213-220` requires for a structural choice with credible alternatives — which is exactly the outcome this skill produces → cut the clause after "not this skill's to decide:" and let the pointer carry it
NIT      wfctl/agents/skills/architecture-design/SKILL.md:L45 vs L200 — two phrasings for the same declared absence ("no architecture decision" / "no record"), and `design-levels:L93` says "no boundary drawn, no record" → use one, matching `design-levels`
NIT      wfctl/agents/skills/architecture-design/SKILL.md:L24,L58,L244 — shrink: the "no verdict, no acceptance" authority boundary is stated three times → keep Authority (L58), drop the Overview and checklist echoes (≈4 lines)
NIT      tests/test_architecture_design_skill.py:L28,L40 — test names say "the design skill"; the bundle ships `design-levels`, `software-design-decisions` and `architecture-design` → `test_the_architecture_design_skill_…`
NIT      tests/test_architecture_design_skill.py:L23 — `_VERDICTS` omits `BLOCKER`, the severity token the bundle's own `code-review` skill already uses and the likeliest accidental mint; `BLOCKED` is not in force anywhere → add `BLOCKER`
NIT      wfctl/agents/skills/architecture-design/SKILL.md:L67 — "Locate it; do not assume a path" hands the agent an unbounded search for a view that does not exist yet (#149) → say there is none today; the fallback paragraph at L74-77 already carries the behaviour
```

**Noted, not a request for change** — the skill is unreachable as shipped: absent
from `_MIRRORED_SKILLS`, unreferenced by `design-levels`, no command wrapper,
and `grep -rn architecture-design wfctl/ tests/` finds only its own frontmatter
and its own test. Its "When to use" opens "Use this skill when level 2 is
active", and `design-levels`' level 2 routes straight to
`architecture-decisions`. Both halves are deferred with written justification in
**#151** (open, with a definition of done that names both), which is what
`code-review` Step 6 asks of a deferred WARNING. I checked #151, #100, #149,
#96, #98, #145 — every issue number cited in the diff resolves and says what the
diff claims it says.

**Out of scope, pre-existing (from #123, present on `origin/main`):**
`wfctl/agents/skills/software-design-decisions/` ships
`design-record-template.md` and no `SKILL.md`, and nothing references it. It is
the home for the level-3 record the third WARNING is about, so the two touch,
but nothing in this diff caused or worsened it.

```
net: −6 lines possible
Verdict: Approve (0 blockers; 3 warnings, all local edits to prose/comments)
```

## Passes run, and what was checked in each

**Context first.** Read `CLAUDE.md` (AGENTS.md) in full, `.agents/skills/code-review/SKILL.md`,
the branch prompt `.workmux/PROMPT-150-architecture-design.md`, and the two
sibling skills the change layers over (`design-levels`, `architecture-decisions`)
plus the sibling tests (`test_skill_cross_references.py`,
`test_review_fanout_skill.py`, `test_skill_frontmatter.py`). Read the tests
before the skill.

**1. Correctness.** Traced every external claim the two files make rather than
taking them on faith:
- `wfctl arch context` exists (`cli.py:749`) and lists accepted records only —
  the skill's "do not treat proposed, rejected, retired, or superseded records
  as binding" is accurate.
- `wfctl arch-root` exists (`cli.py:897`); `architecture-decisions` does own the
  template, the write and the status lifecycle, as L205-206 claims.
- The handoff target `.agents/skills/architecture-decisions` ships, so
  `test_every_referenced_skill_ships` covers the rename half and the new test
  covers the absence half — not duplicative.
- The test comment's claim that `speckit-implement` and
  `verification-before-completion` report PASS/FAIL and `wfctl verify` records
  `inconclusive` is true (`speckit-implement/SKILL.md:L72-79`,
  `verification-before-completion/SKILL.md:L86`, `_verify.py:L211-231`). Only
  the "fail" attribution is wrong — first WARNING.
- Ran the two new tests' inverse by hand: `grep -io` over the skill for
  `pass|fail|blocked|inconclusive` returns 4 hits, all lowercase, so the
  case-sensitive regex is load-bearing and the test is not vacuous — it just
  documents the wrong near-miss.
- Frontmatter: two keys (`name`, `description`), so
  `test_every_shipped_skill_declares_only_spec_keys` passes; `name` matches the
  directory; description is 466 chars, inside the 1024 spec cap; the
  single-quoted scalar contains no apostrophe that would break the YAML.
- Packaging: `MANIFEST.in` grafts `wfctl/agents` whole, so a new skill
  directory needs no packaging edit (unlike `architecture-decisions`, whose
  extra template the sibling test guards).

**2. Security.** Nothing executable in the diff — no shell fence (unlike
`fanning-out-code-review`, whose fence needed a runner test), no file writes, no
input parsing beyond `read_text()` of a packaged file. No finding.

**3. Architecture.** The three-skill split (`design-levels` gates →
`architecture-design` proposes → `architecture-decisions` records) is coherent
and the authority boundary is stated consistently. Two seams:
- the restatement of "Where the levels land" (third WARNING) — the repo already
  guards this exact shape one skill over
  (`test_the_change_description_skill_does_not_restate_the_template`, #50);
- the deferral to a gate policy that has no owner in the bundle (second
  WARNING).
Checked for scope collision with `software-design-decisions` and with
`code-review`; neither overlaps.

**4. Readability & simplification.** 247 lines is in-family (design-levels 287,
speckit-clarify 260). The brief's open question — trim the 11-item Verification
checklist to match house style — rests on a false premise: `design-levels` and
`architecture-decisions` *both* carry a `## Verification` checklist, so keeping
it is the house-consistent choice and I would not trim it. The only shrink worth
taking is the triply-stated authority boundary. Test file duplicates `_AGENTS`
and `_REFERENCE` from two siblings, matching `test_review_fanout_skill.py`
verbatim — house style, not a finding. Docstrings say why the test exists and
name the failure (eleven designs that wrote nothing; #100's third vocabulary),
per the testing convention, and `dcf99d7` already removed the one cardinality
claim the first test did not assert.

**5. Performance.** Not applicable: two `read_text()` calls on packaged files at
test time.

**6. Over-engineering.** No dead flexibility, no speculative abstraction, no
hand-rolled stdlib. The one unreachable artifact is the skill itself, and that
is tracked (see Noted). `delete:`/`stdlib:`/`native:`/`yagni:` — none. `shrink:`
— the ≈4 duplicated authority-boundary lines and the ≈2 restated landing lines.

## Definition of done — verified, not claimed

```
uv run pytest -q            811 passed in 63.58s
uv run ruff check wfctl/ tests/   All checks passed!
uv run mypy wfctl/          Success: no issues found in 15 source files
```

`wfctl install-skills` had already been run in this worktree —
`.agents/skills/architecture-design/` is present on disk — and I read the
installed skill end to end as an agent would, which is where the second and
eighth findings come from. I did **not** run `wfctl doctor` or `wfctl verify`:
the panel brief forbids writing anything, and `verify` records outside the tree.
Nothing in this worktree was modified; `git status --porcelain` was empty before
and after.

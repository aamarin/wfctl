# Review: `git diff origin/main...HEAD` on `121-level3-records-in-pr` (reviewer r1)

Target resolved as the branch diff against `origin/main` (819134b). **Three**
commits, not four — a333ada, d93095a, bd1a9e7. 5 files, +262/−2.

Context read: `CLAUDE.md`, `uv run wfctl arch context` (9 accepted), the
`software-design-decisions` and `design-levels` skills, `speckit.brainstorm.md`
in full, `wfctl/_paths.py`, `wfctl/_pipeline.py`, `wfctl/cli.py` around the
design gate and `arch none`. Tests run: `NO_COLOR=1 uv run pytest
tests/test_skill_cross_references.py -q` → 18 passed. `ruff` and `mypy` clean on
the changed test file. `.agents/` mirror verified in sync with `wfctl/agents/`.

---

## Findings

WARNING  `wfctl/agents/skills/software-design-decisions/SKILL.md:99` — the
"no git at all" exemption is indistinguishable, by the signal the skill tells
the agent to read, from the failure it must not excuse. `git ls-files
--error-unmatch` exits **128** both for "not a git repository" and for "`<path>`
is outside repository at `<root>`" — and the second is the *exact* motivating
case (a record written into a second checkout, SKILL.md:93-97, and the record's
own Verified bullet). An agent that hits 128 for the bad reason has a
sanctioned-looking line to write ("this project has no branch to carry it") and
continue. Verified locally: `git ls-files --error-unmatch
~/Development/wfctl-specs/...` from this worktree → exit 128, `fatal: ... is
outside repository`.
→ Gate the exemption on a separate question, not on this one's exit code:
`git rev-parse --is-inside-work-tree` failing is "no git"; anything else is the
record in the wrong place.

WARNING  `wfctl/agents/skills/software-design-decisions/SKILL.md:74,80,146` —
`git ls-files --error-unmatch` reads the **index**, so `git add` alone satisfies
it. If the commit at step 2 fails (a `pre-commit` hook, no `user.email`, a
signing failure, a `commit-msg` rejection — the second `Assumed` bullet of the
design record anticipates exactly this repo shape), the record is staged, the
check exits 0, and the skill reports the property held. Demonstrated in a
throwaway repo: staged-only new file → `ls-files --error-unmatch` exit 0, `git
log -- <path>` empty. The checklist item at :146 pairs "is committed" with a
check that does not test it, so the two halves can disagree silently.
→ Ask the question the prose actually makes: `git cat-file -e HEAD:<repo-relative
path>` (or `git log -1 --format=%H -- <path>` non-empty). Both still fail for the
wrong-repo case. If the index is deliberately enough, say so at :80 instead of
claiming committed-ness at :146.

WARNING  `docs/architecture/design/121-visibility-is-asked-of-git.md:106` —
the `Considered` bullet that rejects the branch-diff question is factually wrong
about the implementation this repo already ships, and that implementation is the
strongest form of the check. `touched_on_this_branch` (`wfctl/_paths.py:284`)
answers "does the change under review add or modify `<path>`?", returns `None`
for a path outside the repository, and is already used at `wfctl/cli.py:992` for
the *same* question about a declaration ("No reviewer will see this claim"). The
bullet's three reasons do not hold against it: it runs `git status --porcelain`
*first*, explicitly so a record not yet committed still counts (:294-297), so it
does **not** "answer empty for a branch with no commits of its own"; and
`_trunk_branch` (`wfctl/_paths.py:84`) falls back to local `main`/`master`/`dev`,
so it does not need a remote. The result is two different answers to one question
in one repo, one in code and one in prose, with the weaker one shipped.
→ Either correct the bullet with the true reason it lost (it is Python, not a
command an agent can run — which is an argument for exposing it, e.g. `wfctl arch
record --check <path>`, and is what `a-rule-is-expressed-as-a-check` points at),
or reuse the helper.

WARNING  `wfctl/cli.py:301-303` (on `main`, not in this diff) vs. the check this
diff ships — the design gate excludes `design/` from what it counts, justified by
the comment *"#121 item 3 guarantees every such record lands in the branch
diff."* `git ls-files --error-unmatch` does not guarantee branch-diff membership:
it exits 0 for any file tracked in this tree, including one inherited from the
base branch. The exclusion at `cli.py:307` rests on a guarantee stronger than the
one delivered. This diff is the moment that comment becomes checkable, and it was
left unamended.
→ Either strengthen the check (see the finding above) or amend the comment to
what `ls-files` proves.

WARNING  `wfctl/agents/commands/speckit.brainstorm.md:62-72` — the ordering puts
a **commit** before Step 7's User Review Gate. `brainstorming`'s gate and
`idea-refine`'s "Only save if they confirm" are both downstream of where records
are now written and committed, so a direction the user rejects at that gate
leaves committed `proposed` records on the branch for a design that never
happened. The design record's `Consequences` names the docs-before-code ordering
but not this one, and no shipped prose says what to do about it.
→ One line: a record whose direction was rejected at the review gate is amended
or reverted before `design.md` is written. The skill's "an approved record is not
edited" rule is scoped to `approved`, so this is permitted — but nothing says it.

WARNING  `wfctl/agents/commands/speckit.brainstorm.md:74-92` — the second half of
the change (the `## Software design decisions` section in the one-pager) ships as
prose with no check and nothing that can observe its absence. `wfctl` reads
`design.md` for existence only (`_pipeline.py:289,464`), and `design.md` is
gitignored, so a missing section reaches neither a check nor a reviewer. The
diff's own thesis is that prose alone guarantees nothing; the design record and
the declaration are both silent on why this half is exempt.
→ Name the deferral in `Consequences` (and file it), or state the criterion under
which `a-rule-is-expressed-as-a-check` reads "no" here.

NIT  `tests/test_skill_cross_references.py:158-159` — ``command.index("invoke the
`idea-refine` skill")`` raises `ValueError` rather than failing an assertion if
that sentence is reworded, so a rename surfaces as a traceback instead of the
docstring that explains what broke. The test already guards its other two
strings with `assert ... in`.
→ `assert "invoke the \`idea-refine\` skill" in command` before taking the index.

NIT  `wfctl/agents/skills/software-design-decisions/SKILL.md:84-97` — `shrink:`
~12 lines. These two paragraphs restate the design record's `Direct baseline` and
`Considered` verbatim in substance. `knowledge-placement` puts the argument in
`docs/architecture/` and the instruction in the skill; the agent reading the
skill needs the command, the exemption, and one sentence on why not
`check-ignore`.

NIT  `wfctl/agents/commands/speckit.brainstorm.md:9` (security) — `Bash(git
add*)` and `Bash(git commit*)` are a ceiling on the whole turn, so every step of
a brainstorm can now stage and commit arbitrary working-tree content, not only
the record. `allowed-tools` has no pathspec form, so there is no narrower way to
write it — noting it rather than asking for a fix.

---

## Passes, and what each checked

**1. Correctness** — traced `git ls-files --error-unmatch` semantics against the
property the change claims (index vs. commit vs. branch diff), reproduced the
staged-only case in a scratch repo, reproduced the 128-outside-repository case
against `~/Development/wfctl-specs`, and confirmed the gitignored-root case is
caught (`git add` refuses an ignored path). Verified the template's section list
matches the new record exactly, that `_arch.load_records` uses `root.glob("*.md")`
and so does **not** recurse into `design/` — the skill's claim that level-3
records are invisible to `arch context` holds. Findings 1, 2, 4.

**2. Security** — no secrets, no untrusted input. The only surface is the widened
`allowed-tools` ceiling (NIT above). `git ls-files --error-unmatch <path>` is run
on a path the agent authored, so no injection surface beyond what `Bash(...)`
already grants.

**3. Architecture** — checked against all 9 accepted records.
`a-rule-is-expressed-as-a-check` is the one in force and the declaration engages
it honestly; `layer-model` is respected (source under `wfctl/agents/`, `.agents/`
only as a generated mirror, and the mirror is in sync); `vendor-upstream-skills`
is correctly cited for stating the one-pager rule in the command rather than in
`idea-refine`; `knowledge-placement` is where NIT 2 comes from. The declaration's
"two candidates were weighed" omits the candidate that already exists in this
repo's code — finding 3.

**4. Readability & simplification** — prose reads well and matches the file's
existing density; wrapping matches the surrounding files (the >80 lines in the
new content are two or three chars over, consistent with what is already there).
Comments and docstrings inform rather than narrate, per `CLAUDE.md`. Test
docstrings say why each test exists and name the failure — the
`test_brainstorm_allows_the_commands_its_records_need` docstring ("a step added
to the prose without its command is a step that reads correctly and cannot run")
is the strongest of the three. `NO_COLOR` is not needed here: these tests assert
on file contents, not console output. Finding: NIT 1.

**5. Performance** — not applicable. Three `git` invocations per record, once per
design session.

**6. Over-engineering** — the three new tests each pin a distinct failure and
none is redundant; `test_brainstorm_allows_the_commands_its_records_need` is the
one that would have caught a real silent break. The design record's 134 lines are
the template's mandated shape, not padding. Only shrinkable material is NIT 2.

net: −12 lines possible

**Verdict: Request changes.** No BLOCKER — the change is a real improvement and
its shape is right. But the first two WARNINGs sit on the single property the
change exists to add: as written, a record that was staged but never committed
passes, and a record written into the wrong checkout hands the agent a
128 it is told elsewhere to treat as benign. Fixing those two is small (one
command swap, one extra question) and turns an advertised guarantee into a real
one. Finding 3 should at minimum correct the record's `Considered` bullet, since
a design record that misstates the alternative is the one artifact here that
outlives the branch.

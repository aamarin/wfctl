## Review: bd1a9e7..b1976df (wfctl arch check)

### Passes run

- **Correctness (adversarial):** Traced `arch_check_cmd`'s three failure
  branches plus the success/exemption paths against real git behavior — not
  just the unit tests. Built scratch repos and ran the actual `uv run wfctl
  arch check` binary against: a committed record, a staged-only record, a
  record in a second checkout, a directory with no git at all, a repo with
  zero commits (unborn HEAD), a genuinely-diverged detached HEAD, a
  same-commit-as-trunk detached HEAD (to rule out a false detached-HEAD
  finding — see below), a submodule-nested path, and a bogus
  `WFCTL_REPO_ROOT` override pointing at a non-git directory. All produced
  the state the design record and skill claim. Traced `touched_on_this_branch`
  and `is_in_tree` in `wfctl/_paths.py` (not just the changed file) since the
  new command wraps them.
- **`except SystemExit` scope:** the `try` block wraps exactly one statement
  (`repo_root = get_repo_root()`), so it cannot swallow a `SystemExit`/`typer.Exit`
  raised by anything else in the function. `typer.Exit` is `click.exceptions.Exit`,
  a `RuntimeError` subclass, not `SystemExit`, so it isn't caught here either.
  Checked what `get_repo_root()` can raise: bare repo (no worktree), corrupted
  `HEAD` symlink, and a submodule `.git` file pointing nowhere were all tested
  directly against git — every one of them produces the same `CalledProcessError`
  → `SystemExit("wfctl: not a git repository")` that the "no repo" exemption is
  built for, so the catch isn't quietly absorbing a different, real failure in
  practice. (Pre-existing gap, not introduced here: if `git` itself isn't on
  `PATH`, `get_repo_root()` raises `FileNotFoundError`, which this `except
  SystemExit` does *not* catch, so it would crash out unhandled rather than
  print either message. Same latent behavior as every other caller of
  `get_repo_root()` in this file — not new, not blocking.)
- **`record.relative_to(repo_root.resolve())` (cli.py:1069):** cannot raise.
  `record` is resolved once at entry (`record = record.resolve()`) and never
  reassigned; the `relative_to` call is only reached after `is_in_tree(record,
  repo_root)` returned `True`, which computes exactly
  `record.resolve().is_relative_to(repo_root.resolve())` on the same resolved
  value — `is_relative_to` and `relative_to` agree by construction, so the gate
  guarantees the call it protects.
- **`tests/test_arch_check.py`:** read all four tests, then mutation-checked
  each by tracing which branch actually produces its assertion (not just that
  the assertion holds):
  - `test_a_committed_record_passes` — only reachable via the final
    `console.print("... is committed on this branch")`. Confirmed via the live
    detached-HEAD-with-a-real-commit run above.
  - `test_a_staged_record_is_not_a_committed_one` — `touched_on_this_branch`
    returns `True` here (staged shows in `git status --porcelain`), so the
    exit 1 can only come from the `git cat-file -e HEAD:<path>` branch, as the
    docstring claims — not from the touched-check. Confirmed by reasoning
    through the code path and reproducing the underlying git behavior
    (`git ls-files --error-unmatch` exits 0 on a staged-only file; `git cat-file
    -e HEAD:<path>` exits 128) directly in a scratch repo.
  - `test_a_record_in_another_checkout_fails` — the `is_in_tree` check runs
    *before* `touched_on_this_branch` in `arch_check_cmd`, so this fails on the
    "outside this working tree" branch specifically, not by falling through to
    the touched-check returning `None`/`False`. Order in the source confirms
    this test can't pass for the wrong reason.
  - `test_no_repository_is_not_a_failure` — only the `except SystemExit` branch
    prints "No git repository here"; nothing else in the function can produce
    that string.
  - None of the four can pass for a reason other than the one its docstring
    names.
- **Design record factual claims** (`docs/architecture/design/121-visibility-is-asked-of-git.md`):
  checked every cited line number and git-behavior claim against the actual
  code and actual git, rather than trusting the prose:
  - `git ls-files --error-unmatch` on a staged-not-committed file exits 0 —
    verified.
  - Same command exits 128 both for a path outside the repository and for no
    repository at all — verified (`fatal: ... is outside repository` vs.
    `fatal: not a git repository`, both 128).
  - `wfctl/_paths.py:84` (`_trunk_branch`) — correct line.
  - `wfctl/cli.py:992` (`arch_none_cmd`'s `touched_on_this_branch(...) is not
    True` gate) — correct line.
  - `wfctl/_paths.py:283` (`touched_on_this_branch`) — actual definition is at
    line **284**, off by one.
  - `wfctl/cli.py:302` (the "#121 item 3 guarantees..." comment) — actual line
    is **303**, off by one.
  - `wfctl arch check` on this record's own path exits 0 and only after commit
    — reproduced live.
- **Skill / brainstorm command / cross-reference tests:** `allowed-tools` in
  `speckit.brainstorm.md` drops `git ls-files*` and adds `wfctl arch check*`,
  matching the skill's new prose (no more raw git command in agent-run steps).
  `tests/test_skill_cross_references.py`'s updated assertions
  (`"wfctl arch check" in skill`, and the `allowed-tools` needed-list) match
  what's actually in the files. Ran the full suite, ruff, and mypy per
  `CLAUDE.md`'s definition of done: `uv run pytest -q` → 1124 passed, `uv run
  ruff check wfctl/ tests/` → clean, `uv run mypy wfctl/` → clean.
- **Architectural fit:** read `uv run wfctl arch context` — 9 accepted
  records. `a-rule-is-expressed-as-a-check` is the one this change leans on
  ("ship the rule as a check, not prose") and the diff is a direct instance of
  it (replacing prose git command with a real subcommand). The design record
  itself stays `proposed` (level-3, not binding), no conflict with anything
  accepted.
- **Code style:** local `import subprocess` inside the function matches the
  existing pattern at `wfctl/cli.py:3487` (`_worktree_roots`), not a new
  convention. Docstring is rationale-dense ("why this shape"), consistent with
  neighboring docstrings (`is_in_tree`, `_arch_location`) rather than narrating
  the diff. `disallow_untyped_defs` satisfied (`-> None`, typed argument).

### Findings

NIT `docs/architecture/design/121-visibility-is-asked-of-git.md` — two cited
line numbers are off by one: `wfctl/_paths.py:283` should be `284`
(`touched_on_this_branch`'s actual `def` line), and `wfctl/cli.py:302` should
be `303` (the `#121 item 3 guarantees...` comment). Low-stakes since the
surrounding function/comment names are unambiguous and the reader lands within
a line either way, but this record's entire premise is a "Verified" section
whose claims are supposed to be checked against the code rather than taken on
trust — worth a one-line fix → bump both to `284` and `303`.

No BLOCKER or WARNING findings. The three failure branches in `arch_check_cmd`
are correctly ordered (out-of-tree before touched-check before commit-check),
none of the exit-code/message combinations collide with each other or with the
"no repo" exemption, and I could not construct a real git state (detached
HEAD, zero-commit repo, submodule, bogus `WFCTL_REPO_ROOT`, symlinked/relative
argument) that made the command report the wrong thing — the closest
candidate (detached HEAD) turned out to be a test-setup artifact on my end
(the file was committed in the same commit as trunk, so "not touched" was the
factually correct answer), not a bug in the code.

net: −0 lines possible. Lean already — the command is a thin, well-ordered
wrapper around functions that already existed; no dead code, no unused
flexibility, no speculative generality to cut.

**Verdict: Approve.**

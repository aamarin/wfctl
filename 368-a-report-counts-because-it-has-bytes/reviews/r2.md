## Review: b0a0a39 — `fix(skills): a report counts because it carries a verdict, not because it has bytes`

Six passes over `wfctl/agents/skills/fanning-out-code-review/SKILL.md` and
`tests/test_review_fanout_skill.py`, against #368, #173, #205, `CLAUDE.md`, and
the ten in-force records (`a-rule-is-expressed-as-a-check` is the one that binds
here). Measurements below are against the 241 review files in
`/Users/andremarin/Development/wfctl-specs/*/reviews/`, which is the whole
corpus, not a sample.

The anchor's empirical claim holds. `^[^A-Za-z]*Verdict` accepts **241 of 241**
reports in that corpus; `^Verdict:` alone accepts **114 of 241** (47%), so "about
half the honest reports" is accurate to the point it is measured. The
`RUNNING`/`MISSING` split from #173 is untouched and the #205 fence is
byte-identical; `test_the_two_fences_carry_the_same_roster` is still green. Full
suite 1586 passed, ruff clean, mypy clean. `.agents/` and `.claude/` mirrors are
in sync with source, so the skill was installed and not just committed.

Both warnings below are test-side: the anchor and the `-s` are the two decisions
this change argues hardest for in prose, and neither is pinned by a check.

---

WARNING  tests/test_review_fanout_skill.py:L207 — the negative case does not
discriminate the anchor from a bare substring search, so `^[^A-Za-z]*` is pinned
by nothing. The docstring claims "an anchor relaxed to a bare substring search
would count it" and the skill (SKILL.md:L177) names this test as "what holds the
four apart from prose that merely mentions the word". Both are false: `The
verdict stands…` has a lowercase `v`, so `grep -qs 'Verdict'` rejects it too.
Measured over every fixture in the file:

| fixture | shipped anchor | `grep -qs 'Verdict'` |
|---|---|---|
| `Verdict: Approve` | reported | reported |
| `**Verdict: Approve**` | reported | reported |
| `## Verdict` | reported | reported |
| `- Verdict: Approve` | reported | reported |
| `The verdict stands, and nothing here reverses it.` | MISSING | MISSING |
| `looks good` | MISSING | MISSING |
| `""` (empty) | MISSING | MISSING |

The mutant passes the whole file. → capitalize the fixture:
`report.write_text("The Verdict stands, and nothing here reverses it.\n")`.
Verified: anchor → `MISSING`, bare substring → `reported`. One character, and
the docstring and the skill sentence both become true. The four positive cases
are real and *are* pinned; it is only the anchoring half that is prose.

WARNING  wfctl/agents/skills/fanning-out-code-review/SKILL.md:L180 — the new
paragraph makes `grep -s` load-bearing and states the asymmetry against the
undispatched check, but only the loud side of that asymmetry has a check. The
sibling rule is pinned by `test_the_undispatched_check_fails_loud_without_a_dispatch_mark`,
which asserts on stderr; the roster fence is read through `_run`, which returns
`.stdout.split()` and nothing else. Drop `-s` and the suite stays green while
every healthy panel with a `RUNNING` reviewer prints
`grep: …/reviews/r3.md: No such file or directory` once per read — verified on
BSD grep that `-q` alone does not suppress it (exit 2, message on stderr), so
this is not theoretical on the shell these runs happen in. A stderr line on the
normal state is the noise that trains a reader to discount the check, which is
#173's failure one level down. → assert stderr in
`test_the_roster_check_tells_the_three_states_apart`, the shape the sibling test
already ships.

NIT  wfctl/agents/skills/fanning-out-code-review/SKILL.md:L169 — "a bare 'looks
good' is a missing report wearing a verdict" now sits directly under the fence,
where it reads as a description of what the fence does. The fence tests for a
verdict line's presence; `LGTM\n\nVerdict: Approve` passes it. The trailing
clause ("— nine bytes that a **size test** records as…") is what keeps the
sentence honest, and the honesty premise is stated at L166, so this is not a
false claim — but the residual case is named nowhere, and it is the one a reader
will hit. → one clause: the check answers whether a verdict was written, and
"which passes ran and what was checked in each" stays the reconciler's rule to
apply in Step 4.

NIT  wfctl/agents/skills/fanning-out-code-review/SKILL.md:L173 — the skill
restates the four spellings *and* names the test, while the test's docstring
(L189-190) says "which is why the skill names this test rather than restating the
four". One of the two is redundant and the docstring is wrong about the file
beside it. The list is also already an undercount of what it measured: the corpus
writes the heading form two ways (`## Verdict` ×26, `### Verdict` ×7, plus
`### Verdict: R` ×1) — all accepted, but "four ways" is a closed list that has
already drifted, which is the argument the paragraph itself makes. → drop the
list from the skill and let the test carry it (−3 lines), or correct the
docstring.

NIT  wfctl/agents/skills/fanning-out-code-review/SKILL.md:L155 — false accept
worth naming rather than fixing: `Verdict` as the first word-token of *any* line
counts, quotations included. Verified — a report whose only match is
`` > `Verdict:`, `**Verdict: Approve**`, `## Verdict`, `- Verdict:` `` reads as
`reported` while carrying no verdict, and that is the exact line a reviewer of
*this* diff would quote. It is narrower than it looks (`- Review of
`Verdict:`…` does **not** match, because a letter before the word kills the
anchor), and tightening it would cost the legitimate `- Verdict:` spelling the
corpus actually uses. → leave it; one clause in the anchor paragraph saying the
word must open the line stops the next reader from rediscovering this as a bug
and "fixing" it into the tightening the paragraph already warns against.

NIT  wfctl/agents/skills/fanning-out-code-review/SKILL.md:L164 — the evidence
test now assumes `code-review`'s report format, and the skill blesses filing a
free panel member's review in the same directory (L305-311: "a session that files
a bot's review under `reviews/` — the obvious thing to do"). A bot's review
carries no `Verdict:` line, so an id like `codex` added to the roster `for` list
reads `MISSING`, and `MISSING`'s prescribed repair — ask again in `code-review`'s
format — cannot be performed on a comment already posted. Hypothetical today: 0
of the 241 reports on disk are bot reviews (ids are `r1..r9`, `p1 p2`,
`rebase-r1`), and the case list's own comment keeps free members out of the
`for`. → one clause saying the loop is for dispatched reviewers, who were told to
follow `code-review`.

---

Over-engineering: the skill gains ~36 lines of prose for a one-token fence
change, which is this file's existing density and not a finding — every new
paragraph names a case its own silence would hide, which is the standard the rest
of the file is written to. The Step 6 change is consistent: finding 4 moves r1→r2
so r3 can own the `clean` row, the header's "4 findings" still matches the four
numbered rows, and the blank `#` does not collide with the "a disposition table
shorter than the number of findings collected" red flag. The one genuine trim is
the four-way list duplicated against the test that holds it.

net: −3 lines possible
Verdict: Approve (2 warnings, both one-line test fixes; no blockers)

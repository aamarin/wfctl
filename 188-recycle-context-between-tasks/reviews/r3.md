## Review: r3 — `git diff origin/main..HEAD` (#188, five commits, four doc files)

Target: three architecture records marked `rejected` plus one clarify scan. No
code changes, so the correctness pass was run as *does every claim the records
make about this repository hold* — the records exist to be believed by a later
reader with no way to re-derive them.

WARNING  docs/architecture/design/188-the-recycle-is-a-payload-field.md:L37 — `Verified` presents two occupancy readings as corroborating ("summed to 89,765 against a statusline reporting 14%") and they disagree by roughly 3×: 89,765 at 14% implies a ~641K window, which matches no model this harness runs; on a 200K window the same sum is ~45%, on 1M it is ~9%. `wfctl-owns-the-recycle-verdict.md:L20-30` draws the same two sources as two observation points of one quantity. The record's whole threshold argument rests on "the transcript sum *is* occupancy", and the single datapoint offered for it falsifies the correspondence rather than confirming it. → Either say what each number measures (statusline percentage is not documented here as a fraction of the same denominator), or move the correspondence into `Assumed` with "falsified by a sum whose ratio to the statusline reading is not the window size" — the section that already holds the other three bets.

WARNING  docs/architecture/wfctl-owns-the-recycle-verdict.md:L82-84 — the `Considered` bullet declines a configurable threshold "for the reason #121 already argues: vocabulary before a second consumer wants a different answer". #121 is *Epic: Level-3 design decisions as PR-visible records*; nothing in the issue body or in `docs/architecture/design/121-visibility-is-asked-of-git.md` makes that argument (grepped both; the phrase occurs nowhere else in `docs/`, `.agents/skills/` or `wfctl/agents/`). The argument does exist in this repo — `a-human-accepts-a-decision.md:L114-118`, "No second project has asked, and a configurable authority question means the first reader of a strange repo cannot tell who accepted a record". → Cite that record, or state the reason inline. A rejected record kept for its argument must not send its one reader to a document that does not hold it.

WARNING  docs/architecture/wfctl-owns-the-recycle-verdict.md:L93-95 — `Consequences` says the per-pass occupancy reading "is what `session-state-is-re-derived` reserves for a session file to hold". That accepted record reserves the session file for one thing, named explicitly: "the handoff prose a human or agent wrote deliberately". It reserves nothing for history. The precedent the same sentence cites — the evidence digest from `wfctl-counts-the-passes` — is not in a session file either: it lands in `events.jsonl` (`_pipeline.py:421` carries it on the report, `wfctl resume` writes the event; `_stall.py` reads it back). The level-3 record gets this right ("joining the evidence digest it already writes"); the level-2 one overstates an accepted record's carve-out. → Drop the session-file clause and say the event log, which is where the cited precedent actually put it.

WARNING  docs/architecture/design/188-the-recycle-is-a-payload-field.md:L23,L27,L30,L33 — four of the five `Verified` citations do not land on what they quote, and the section's stated contract is "Quote what the line says, not what you remember it saying":
  - `_pipeline.py:355` → `class PipelineReport` is at 357, the `stall:` field at 414, the quoted comment at 409-412.
  - `_stall.py:216` → `def find_stall(` is at 210 (signature 210-215).
  - `_pipeline.py:61-70` → `_STEPS` is 62-71, and "carries both values or it does not parse" is a module comment at line 33, not a docstring. The substantive claims (eight rows, three fields) do hold.
  - `cli.py:4732` → `payload.get("transcript_path")` is at 4733; the same off-by-one is repeated at `wfctl-owns-the-recycle-verdict.md:L29`.
  Nothing in this diff touched those files, so the citations were wrong when written rather than drifted. → Re-read and correct the four numbers; a `Verified` line whose number misses is indistinguishable from one nobody checked.

WARNING  docs/architecture/design/188-the-recycle-is-a-payload-field.md:L75-92 — the two graphs are not "the same components in each", and the difference is not the one the paragraph below claims. `transcript` appears only in the decision graph, yet the baseline also has to read occupancy from somewhere before `_predicates` can write "window full — recycle before continuing"; and `step` disappears from the decision graph although steps still carry reason and remedy afterwards. As drawn, the baseline looks like it acquires the fact from nowhere, which makes it cheaper than it is — and the argument the record rests on is "which existing component absorbs the new fact". → Put `transcript` in both graphs with the baseline's read arrow going to `_predicates`, and keep `step` in both.

WARNING  docs/architecture/wfctl-names-the-reset-it-cannot-perform.md:L158-161 — the rejection Log rests on `no-hardcoded-agent` ("the hook moves to the developer's own agent layer, which `no-hardcoded-agent` already required independently"). That accepted record is narrowly about committed config never naming an agent in an `--agent` flag; it does not say an agent-specific mechanism cannot ship in wfctl. The record that does say it is `install-modes`, also accepted: "the hook schema belongs to Claude Code, not to wfctl's base layer, so it is claude-only rather than a base-layer path every agent would have to interpret", with `layer-model`'s per-agent layer table behind it. → Cite `install-modes` (and `layer-model`); the rejection is right and is currently hung on the wrong record. The same substitution applies to commit c5d76ca's body.

WARNING  docs/architecture/wfctl-names-the-reset-it-cannot-perform.md:L62 — `Owns truth` opens "wfctl owns 'should this run recycle now, and what exactly performs the cycle?'", and the first half is the question `wfctl-owns-the-recycle-verdict.md:L55` already owns in different words. The skill's reason for phrasing the owned thing as a question is that "a question is what a later reader checks a proposed change against" — two records answering the same question is exactly the ambiguity that defeats. This record's own `Context` scopes it correctly ("It leaves open the half #188 calls the hard one: the reset itself"). → Narrow the quoted question to "what performs the cycle?" and let the sibling record own the timing.

NIT  docs/architecture/design/188-the-recycle-is-a-payload-field.md:L80-L86 — the diagram's stems do not meet their boxes. Measured by column: baseline stem at col 18 lands on a `┴` at 19; decision stems at 51 and 58 land on `┴` at 52 and 61; the `_predicates` box has a top border of 12 columns against sides and bottom of 13. Also, the divider's label `pipeline-state-is-one-payload` covers cols 5-37, which is where the baseline's two `reads` arrows cross it — so the baseline reads as not crossing the boundary at all, while the decision side gets three `╪`. → Re-align, and shift the label right of col 20 or onto its own line.

NIT  docs/architecture/scans/188-clarify.md:L7 vs L91 — the header says `Outstanding: 0 · Deferred: 0`, and the correction at the bottom hands an open choice to `/speckit.plan` ("the choice between a distinct recycle event and a fourth row in step 9's table"). A reader who stops at the counts is told nothing is open. Arguable either way — a plan-level choice is not a clarification question — but the file is the one artifact the reviewer reads. → Either say so in the counts line or add one sentence to the correction stating that no clarification category is left open by it.

NIT  docs/architecture/scans/188-clarify.md:L8 — `Detail:` is an absolute path under `/Users/andremarin/…`, a directory no other reader of this repository has. `speckit.clarify.md` prescribes `FEATURE_DIR/spec.md § Clarifications`, and the newer scans use `` `<spec-root>/<branch>/spec.md` `` (299, 307, 325). The absolute form has precedent (321, 332), so this is an inherited inconsistency rather than a new one. → `<spec-root>/188-recycle-context-between-tasks/spec.md § Clarifications`.

NIT  docs/architecture/wfctl-names-the-reset-it-cannot-perform.md:L151-161 — commit 49fd033 added a whole section to a `proposed` body and the `Log` records only `proposed` and `rejected`. The repo's convention for exactly this is a dated `amended` line — `the-scan-is-attested-where-the-reviewer-reads.md` carries three, one of them for the same situation (a review panel finding something the body missed). The reasoning is in the commit message, which is the half a reader of the file does not have. → Append `- 2026-09-11  amended  — a third refusal found while driving the acceptance test; the decision stands, the performer narrows.`

NIT  docs/architecture/wfctl-names-the-reset-it-cannot-perform.md:L15-19, L66-67, L120-127 — the harness refusing `/clear` to the agent is stated three times in one 161-line record: as a quoted error in `Context`, as prose in `Owns truth`, and again as row one of the stacked-refusals diagram. The diagram is the version that earns its place (it is the only one that shows all three refusals together). → Cut the `Owns truth` restatement to a pointer; ~8-10 lines.

NIT  docs/architecture/design/188-the-recycle-is-a-payload-field.md:L126 — 81 display columns, against ~80 everywhere else in the file. Trivial.

### Passes run, and what was checked in each

1. **Correctness** — every repository claim in the three records and the scan was re-derived against the tree: `_STEPS` (eight rows, three fields, `_pipeline.py:62-71`), `PipelineReport.stall` and its comment (414, 409-412), `find_stall`'s signature (210), the `Stop` hook's `transcript_path` read (4733), `_workmux.py`'s no-`subprocess` docstring (lines 3-11, quoted accurately), `speckit-orchestrate` step 5's four-way branch and its "Both orderings are load-bearing" sentence (SKILL.md:117-145), `start-session` step 9's three-row table and the quoted sentence "a branch that has ended a session once is in row two from then on, even unattended", and `wfctl end` appending an `end` event (`_session.py:485`). All substantive claims hold; the failures are the citations above. #188 is CLOSED by the maintainer (aamarin, 2026-09-11), so the `rejected` status has a human act behind it — which is what `a-human-accepts-a-decision` requires for the opposite transition and what the design template means by "only a human moves it past proposed".
2. **Security** — nothing executable; no secrets. One privacy-adjacent item: the scan's absolute home path, filed as a NIT.
3. **Architecture** — checked against the ten accepted records `wfctl arch context` projects. No contradiction with `pipeline-state-is-one-payload` (the proposed field is on the one payload), `knowledge-placement` (constraints in `docs/architecture/`), `layer-model` or `install-modes`. Two mis-citations of accepted records are filed above. `wfctl arch context` still reports `2 rejected` at the top level and does not see the level-3 record, which is correct: `load_records` globs one level, so `design/` and `scans/` stay out of the projection.
4. **Readability** — record format checked line by line against `architecture-decisions`' verification list (filename is a slug, no sequence number; `Owns truth` has both halves in all three; `Direct baseline` is concrete in all three; `Considered` carries real alternatives with true reasons, including several that lose on fit rather than fault, which is the standard the skill asks for; `Log` carries a dated line for the status each file now holds). The level-3 record additionally has `Verified`, `Assumed` (three bets, each with a falsifier), `Diagram` and `Verification`, and is named for #188 rather than its epic #147. The extra `## The performer…` heading is not in the fixed section list, but two accepted records (`pipeline-state-is-one-payload`, `session-state-is-re-derived`) add `## Boundary`, so an added section is not itself a deviation.
5. **Performance** — `uv run wfctl arch check` exits 0 on all four paths (read-only; it shells `git rev-parse`/`ls-files`/`diff` and writes nothing). `arch context` is unaffected in cost: three more files, none of them projected.
6. **Over-engineering** — 541 lines for a closed issue is in band for this repo: the three records sit at 114/161/171 against a top-level median near 130 and existing records at 197, 243 and 259; the scan is 95 against 90-112 for its siblings. Three records rather than one is justified in the text — two questions with different owners, plus a shape — and the commit body states the cost of deleting them. The only real shrink is the thrice-stated `/clear` refusal above.

Not run: `wfctl verify` (excluded by the panel brief), and the test suite — no code is touched, and the suite asserts that skills ship and cross-reference, not that records read well.

net: −10 lines possible
Verdict: Approve with changes — no blockers. The seven WARNINGs are all the same class of defect (a claim a later reader cannot re-derive, in documents whose sole purpose is to be believed by a later reader), and all seven are one- or two-line fixes.

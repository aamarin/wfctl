# Review: `git diff origin/main..HEAD` — #188 records (r2)

Target: 5 commits, 4 files, +541/-0, all documentation. Rubric: `.agents/skills/code-review/SKILL.md`,
checked against `.agents/skills/architecture-decisions/SKILL.md`,
`.agents/skills/software-design-decisions/SKILL.md`,
`.agents/skills/writing-a-scan-file/SKILL.md`, `wfctl/agents/commands/speckit.clarify.md`, and `CLAUDE.md`.

No blockers. The three WARNINGs matter more than their severity suggests because both format
skills freeze a record's body once it leaves `proposed`, and these are at a terminal status — this
is the last edit window before the wrong pointers become permanent. Editing them now is legal:
the immutability clause covers `accepted` (level 2) and `approved` (level 3), not `rejected`.

---

WARNING  `docs/architecture/design/188-the-recycle-is-a-payload-field.md`:L22-33 — every `file:line`
in `Verified` is off, and that section exists so a later reader can jump to the line. Measured
against HEAD (this branch changes no code, so they were wrong when written):

| Cited | Actually |
|---|---|
| `wfctl/_stall.py:216` — `find_stall(...)` | `def find_stall` is L210; L216 is the docstring's first line |
| `wfctl/_pipeline.py:61-70` — `_STEPS` eight rows | `_STEPS: dict[str, Step] = {` is L62, rows L63-70 |
| `wfctl/_pipeline.py:355` — `PipelineReport` … `stall:` field … the comment | `class PipelineReport` is L357; the quoted comment is L409-410; `stall: "_stall.Stall \| None" = None` is L414 |
| `wfctl/cli.py:4732` — reads `payload.get("transcript_path")` | L4732 is `reply = payload.get("last_assistant_message")`; `transcript_path` is L4733 |

The same wrong `cli.py:4732` is repeated in `wfctl-owns-the-recycle-verdict.md`:L28. The *substance*
of all four checks out — `_STEPS` really is eight rows, the comment text is verbatim, #332 is a real
issue, and L4733 really is inside a `Stop` hook (`hook_response_shape_cmd`, from L4681).
→ Fix: correct the five numbers. The skill's wording is the standard being missed —
"Quote what the line says, not what you remember it saying."

WARNING  `docs/architecture/design/188-the-recycle-is-a-payload-field.md`:L74-93 — the diagram's
cross-boundary arrows point the wrong way, in both graphs. `wfctl/_pipeline.py:23` is
`from wfctl import _predicates, _stall`, and `wfctl/_stall.py:44-53` imports nothing from `wfctl`
at runtime (only `Evidence`, under `TYPE_CHECKING`). So `_pipeline`/`PipelineReport` depends on
`_stall` and `_predicates`, never the reverse. The template's grammar is explicit — an arrow
"points at what it depends on", and its own example points the volatile boxes *up* at the stable
one to mean exactly that. As drawn, the diagram asserts "`_stall` reads `PipelineReport`", which no
reading makes true: `_stall` reads `events.jsonl`. The two downward arrows (`_predicates` → `step`,
`_recycle` → `transcript`) use the convention correctly, which is what makes the upward ones read as
an error rather than a house style.
→ Fix: reverse the three (baseline: two) `reads` arrows to point down from `PipelineReport`, or keep
the heads and relabel them `built from`. This is the section the skill calls "the argument", so it is
worth being right about even in a rejected record.

WARNING  `docs/architecture/wfctl-names-the-reset-it-cannot-perform.md`:L99-134 — the appended
section `## The performer cannot be summoned by the agent either` refutes the record's own
`Decision` (L54-58) and `Owns truth` (L62-72) sixty lines earlier, and nothing at either of those
sections says so. `Decision` still reads "a workmux-managed pane can be sent `/clear` and then
`/start-session`"; L129-134 then says the performer "is not the agent under any arrangement". A
reader who stops at `Decision` — which is what a skim and `wfctl arch context`'s projection do —
takes away the refuted claim.

The extra `##` heading itself is fine: `pipeline-state-is-one-payload` and `session-state-is-re-derived`
both carry a `## Boundary` that is not in the template, so the repo already permits one. Two smaller
things inside it are not: its opening sentence narrates the change to a reviewer ("by a layer neither
the issue nor **the rest of this record** anticipated"), which CLAUDE.md rules out — a record is
written for the next reader of the file, not for the diff — and the heading is a sentence where the
precedent is a noun.
→ Fix: while the record is still editable, add one clause to `Decision` pointing forward ("— and see
below: the performer turns out not to exist"), or move the section above `Considered` so it is read
before the conclusions it overturns. Retitle to a noun phrase (`## The performer`).

NIT  `docs/architecture/wfctl-owns-the-recycle-verdict.md`:L82-84 — "#188 scopes it out for the
reason **#121** already argues: vocabulary before a second consumer wants a different answer."
#121 is "Epic: Level-3 design decisions as PR-visible records"; its body carries no such argument
(its only "threshold" is the record-writing threshold, and its one use of "vocabulary" is about the
two source books). The record is faithfully relaying #188's own sentence — "is the
vocabulary-before-use failure #121 already argues against" — so the misattribution is inherited, not
invented. It will now sit permanently in a record rather than in an issue thread.
→ Fix: cite #188, which is where the argument is actually written.

NIT  `docs/architecture/design/188-the-recycle-is-a-payload-field.md`:L35-38 — "summed to 89,765
against a statusline reporting 14%" does not reconcile: 89,765 / 0.14 ≈ 641K, which is no published
window size, and the record does not say what the 14% is a percentage of. It does not damage the
conclusion — the clarify scan's second finding concludes a percentage is *not* computable from the
transcript, which the mismatch supports — but as a `Verified` line the next reader cannot reuse it.
→ Fix: state the token reading alone, or name the statusline's denominator.

NIT  `wfctl-names-the-reset-it-cannot-perform.md`:L37 and
`design/188-the-recycle-is-a-payload-field.md`:L45-47 — "probe 7731" is cited as evidence three times
with no pointer to where its output can be read. The design skill asks for repository facts "each
with what was read"; a probe id from a session that is gone is neither reproducible nor locatable.
→ Fix: one line saying what the probe did and that it is not reproducible from the repo, or drop the id.

NIT  `wfctl-names-the-reset-it-cannot-perform.md`:L39-42 — "The `PreToolUse` guard wfctl itself
installs already allows `workmux`" overstates. It allows a subcommand allowlist (`_WORKMUX_OK`,
`wfctl/_guard.py:92-95`), and `workmux run` is explicitly refused with a comment saying why
("allowing it because of the tool it arrives under would make the allowlist self-defeating"). The
record's `Consequences` depends on `workmux wait`, which *is* in the set, so the reasoning holds —
only the sentence is loose, and it would mislead a reader into thinking any verb passes.
→ Fix: "allows `workmux send` and `workmux wait` by subcommand allowlist (`_guard.py:92`)".

NIT  `docs/architecture/design/188-the-recycle-is-a-payload-field.md`:L84 — the diagram's box
connectors do not line up with their arrow shafts. `┴` sits at columns 12, 19, 45, 52, 61; the `│`
shafts above are at 12, 18, 45, 51, 58 — three of five boxes render visibly detached. Separately,
the divider's label runs to column 33, so the baseline graph's two crossings (columns 12 and 18) are
swallowed by the text and only the decision side shows `╪`. The baseline then reads as if its modules
do not cross the boundary, which is the opposite of what the paragraph below argues.

NIT  `docs/architecture/scans/188-clarify.md`:L8 — `Detail:` is an absolute machine-local path
(`/Users/andremarin/Development/wfctl-specs/…`). The wrapper's shape
(`wfctl/agents/commands/speckit.clarify.md`:L104) writes `<FEATURE_DIR>/spec.md § Clarifications`,
and the existing scans are split: 121, 299, 307, 325 use the `<spec-root>/…` placeholder; 321 and 332
are absolute. No second developer can resolve the absolute form, and it commits a home directory
layout into the repo.
→ Fix: `<spec-root>/188-recycle-context-between-tasks/spec.md § Clarifications`.

NIT  `docs/architecture/scans/188-clarify.md`:L7, L79-95 — the `### Correction, same session` block
hands an open choice to `/speckit.plan` ("a distinct recycle event or a fourth row in step 9's
table"), while the counts line reads `Deferred: 0` and the coverage table has no Deferred row, so a
reader of the header cannot see the open item. Defensible the other way — mechanism selection is
plan's job, not a clarify question left unasked — so this is the author's call. The heading is also
outside the wrapper's declared shape (`### Coverage`, `### Findings`, `### Deferred`), though it
serves the skill's own rule that "a scan that corrected itself and one that got it right first time
should not read the same" better than silently amending the finding would have.

NIT  all three records' `Log` rejection lines — each says "#188 closed" without naming who closed it.
Both format skills reserve a status move past `proposed` to a human, and the level-3 template's Log
shape names the mover (`approved — who approved it`). It checks out: `aamarin` closed #188 at
2026-09-11T17:57:39Z, ahead of the records' commits. But nothing in the files says so, so a later
reader cannot tell a human's rejection from an agent's self-rejection — which is the argument
`a-human-accepts-a-decision` makes for the other direction.
→ Fix: append the citation, e.g. "rejected — #188 closed by @aamarin 2026-09-11".

---

## What each pass covered

**1. Correctness** — verified every external claim the four files make, not just the prose.
Checked: the four `file:line` citations in `Verified` plus the one reused in
`wfctl-owns-the-recycle-verdict` (all off — WARNING 1); the verbatim comment at `_pipeline.py:409-410`
(exact); `#332` (real, used correctly across `_stall.py`, `cli.py`, `wfctl-counts-the-passes`);
module import direction for the diagram (WARNING 2); the `_workmux.py` docstring quote (exact);
the `PreToolUse` guard's workmux handling (NIT); `speckit-orchestrate/SKILL.md:140` — "Both orderings
are load-bearing" is verbatim and the four-way branch at step 5 is as described; the two quotes
lifted from #188's body (both exact); `#188 / #147 / #121 / #101` state and titles via `gh` (#121
misattributed — NIT); the `session-state-is-re-derived` quote in the scan (exact, matches
`arch context` output); `#147`'s "acceptance test" (plausible, not checkable from the repo).

**2. Security** — no secrets, credentials, or tokens; the quoted harness refusal messages carry
none. One real item, filed as a NIT: the scan's absolute `Detail:` path commits a local home
directory layout, and the repo has both conventions already.

**3. Architecture** — ran the architecture-decisions checklist against both level-2 records:
slug-named filenames with no sequence number ✓; `Owns truth` carries both halves, phrased as a
quoted question, in both ✓ (the reset record's second half is unusually strong — it names wfctl's
own refusal as a chosen constraint, not a limitation); `Direct baseline` concrete in both ✓;
`Considered` carries real alternatives with true reasons, several losing on fit rather than on a
manufactured weakness, which is the standard the skill actually asks for ✓; `Log` has a dated line
for the current status in all three ✓. Section list: `wfctl-owns-the-recycle-verdict` matches the
template exactly; `wfctl-names-the-reset-it-cannot-perform` adds one (WARNING 3, on content not on
the heading — `## Boundary` precedent exists in two accepted records).

Level-3 checklist: under `design/`, named for the issue being implemented rather than its epic ✓;
`Verified` / `Assumed` / `Diagram` / `Verification` all present, and `Assumed` genuinely names
falsifiers rather than restating confidence ✓; "no divider is new" holds and the record says so ✓,
so the level-2/level-3 split is right — the two boundary decisions are filed at level 2 and the
shape decision at level 3, which is what keeps `arch context` honest. `uv run wfctl arch check`
exits 0 on both new paths ✓. One deviation: the template asks for "the same components in each"
graph, and the baseline carries `step` where the decision carries `transcript` + `_recycle` — the
decision genuinely introduces a module, and the paragraph names the difference, so I do not count
this against it.

Contradiction check: `uv run wfctl arch context` projects 10 accepted records and reports
"20 records not shown (18 proposed, **2 rejected**)" — the two new level-2 records land outside the
binding set, and `load_records` globbing one level keeps `design/` and `scans/` out. Nothing in this
diff can contradict an accepted record. The records it leans on are consistent with their sources:
`pipeline-state-is-one-payload` (accepted) as the boundary, `session-state-is-re-derived` (accepted)
quoted correctly, `wfctl-runs-the-verification` (accepted) as the unfalsifiability precedent, and
`wfctl-counts-the-passes` — which is `proposed`, so it is cited as an argument rather than as a
constraint, which is how the record uses it ✓. `rejected` is the right status value over `retired`:
these never reached `accepted`, so they never governed work.

**4. Readability** — heading shapes, the CLAUDE.md narration rule (one hit, in WARNING 3), and the
ASCII diagrams checked column by column (NIT). The scan's coverage table carries all ten taxonomy
rows in the wrapper's order, with statuses drawn only from the Clear/Resolved/Deferred/Outstanding
vocabulary ✓, and each of the three findings carries the two `Decided against` lines its lettered
options require, with reasons that are real losses rather than invented weaknesses — the second
finding's "falsified by reading the transcript — the field is not there. This was the option that
looked best before it was checked" is the shape the skill asks for.

**5. Performance** — not applicable; no code changes. `git diff --stat` is +541/-0 across four
markdown files, and the test suite, ruff and mypy are untouched by the diff.

**6. Over-engineering** — checked the three records for restated argument. The design record
cross-references rather than duplicates (L125-129 defers to `wfctl-owns-the-recycle-verdict` for why
option B loses), which is the right shape. The one restatement is the reset record's three-refusals
block (L120-127) re-stating the prose immediately above it at L101-115 — about 8 lines, and the
markers carry information the prose does not, so it earns its place. Keeping 541 lines of argument
for a closed issue is the point of the change, not bloat: `rejected` over deletion is exactly what
the status table is for.

net: −8 lines possible (not the axis that matters here)
Verdict: **Approve with changes** — 0 blockers, 3 warnings. The warnings are cheap to fix and the
window closes when these records stop being editable.

# Review: `git diff origin/main..HEAD` — r1

Target: 5 commits, 4 files, 541 insertions, documentation only. No code, no tests,
no packaging touched (`git diff --stat` confirms).

## Findings

```
WARNING  wfctl-names-the-reset-it-cannot-perform.md:L62 — `Owns truth` re-claims a
         question the sibling record already owns → narrow to the performer half
WARNING  design/188-the-recycle-is-a-payload-field.md:L35-38 — the one occupancy
         measurement in `Verified` does not reconcile with the statusline reading
         it is offered as corroboration for → state the denominator, or move it
         to `Assumed`
WARNING  wfctl-names-the-reset-it-cannot-perform.md:L159-161 — the rejection's
         load-bearing citation is a record about something else → cite
         `install-modes` L60, which already decides base-layer vs agent-layer
WARNING  all three records, `Log` rejected lines — the sole citation is "#188
         closed", and the tracker records #188 as COMPLETED → close as *not
         planned*, or name the disposition comment
NIT      design/188-…:L23-33 and wfctl-owns-the-recycle-verdict.md:L29 — four of
         six line anchors in `Verified` are off by one or two
NIT      design/188-…:L84 — `_predicates` box top border one dash short
NIT      design/188-…:L74-93 — the two graphs do not carry the same components
NIT      design/188-…:L82 — the divider is crossed only on the decision side
NIT      wfctl-names-the-reset-it-cannot-perform.md:L99 — a non-template section
         appended under a `Decision` it reads against
```

---

### WARNING — `docs/architecture/wfctl-names-the-reset-it-cannot-perform.md:L62`

`Owns truth` reads:

> wfctl owns "should this run recycle now, and what exactly performs the cycle?".

The first clause is the question `wfctl-owns-the-recycle-verdict.md:L55` already
owns ("has this run's window filled past the point where the next task should
start fresh?"), and this record's own `Context` (L9) says so: "`wfctl-owns-the-
recycle-verdict` settles when a run should discard its window. It leaves open the
half #188 calls the hard one: the reset itself."

`knowledge-placement` is **accepted** and its L36 rules on exactly this: "A fact
with two homes has no owner." Two records claiming one question is the shape that
record forbids, and it is the field the `architecture-decisions` verification
checklist calls the reason the format exists.

Nothing binds today — both are `rejected` — but the argument is what the commit
message says is being preserved, and a reader reconstructing it finds two owners
for one question.

**Fix:** `wfctl owns "what exactly performs the cycle?"`. The `Context`
cross-reference already carries the other half.

### WARNING — `docs/architecture/design/188-the-recycle-is-a-payload-field.md:L35-38`

> The transcript's last assistant message carries
> `input_tokens + cache_read_input_tokens + cache_creation_input_tokens`;
> read live in this session it summed to 89,765 against a statusline reporting 14%.

The two halves do not reconcile. 89,765 at 14% implies a ~641K window; against a
200K window the sum is ~45%, against 1M it is ~9%. #188's own disposition comment
reports 207,497 for the same pass. The sentence presents the statusline as
corroboration where what it actually records is a discrepancy between the two
readings.

This matters more than a stray number, because it is the *only* measurement
behind the claim the rejection commit says is the expensive part worth keeping —
"establishing that the threshold is observable" — and the record's own `Assumed`
section then bets on that reading being "stable enough to threshold on". A reader
who buys the argument from this record and rebuilds against it inherits an
unverified correspondence.

**Fix:** name the denominator the percentage is of, or drop the statusline clause
and move it to `Assumed` as a second reading that did not reconcile — which is
the truthful version and costs one line.

### WARNING — `docs/architecture/wfctl-names-the-reset-it-cannot-perform.md:L159-161`

> The hook moves to the developer's own agent layer, which `no-hardcoded-agent`
> already required independently.

`no-hardcoded-agent` (accepted) decides which agent a *committed config flag* may
name — "The developer's environment owns which agent runs" — not whether an
agent-specific mechanism may live in wfctl's base layer. It is adjacent, not the
same ruling.

The record that does settle it is `install-modes`, also accepted, at L60:

> Merge mode is scoped to the agent layer: the hook schema belongs to Claude
> Code, not to wfctl's base layer, so it is claude-only rather than a base-layer
> path every agent would have to interpret.

That is verbatim the argument the rejection makes, already in force, and it goes
uncited in all three records.

**Fix:** cite `install-modes` as the primary authority for the placement and keep
`no-hardcoded-agent` as the supporting one.

### WARNING — `Log` lines on all three records

Each `rejected` line cites "#188 closed" and nothing else. Checked against the
tracker:

```
closed by aamarin at 2026-09-11T17:57:40Z
stateReason: COMPLETED
```

A later reader following the citation finds an issue marked *completed*, which
reads as the opposite of "considered and not adopted". The argument is in a
comment on that issue (aamarin, 17:42Z, "Recommendation: scope the performing half
out of wfctl entirely") and the Log lines do not point at it.

Two separate rules sit behind this. The design-record template — which
`software-design-decisions` explicitly delegates the status values to — says
"Only a human moves it past `proposed`", and "#188 closed" does not name where the
human agreed. `a-human-accepts-a-decision` (proposed, so not binding) requires a
citation for the opposite transition and would refuse a placeholder.

The human's account did close the issue and authored the commit, so the substance
is sound; what is missing is the citation quality the repo applies to the mirror
transition.

**Fix:** either close #188 as *not planned*, or extend each Log line to name the
disposition comment.

### NIT — line anchors in `Verified`

Every claim checks out; four of the six anchors do not point at it.

| Cited | Actually at |
|---|---|
| `_pipeline.py:355` — `PipelineReport` | `class PipelineReport` is L357; the `stall` field is L414, its quoted comment L409 |
| `_pipeline.py:61-70` — `_STEPS` eight rows | `_STEPS` is L62, rows L63-70 |
| `cli.py:4732` — `payload.get("transcript_path")` | L4733; L4732 is `last_assistant_message` |
| `_stall.py:216` — `find_stall(...)` | `def find_stall(` is L210 |

Same off-by-one for `cli.py:4732` in `wfctl-owns-the-recycle-verdict.md:L29`. None
of these files is touched by this branch, so the anchors were off when written.
The design template's instruction is "Quote what the line says, not what you
remember it saying" — the quoting is right here and only the pointer is wrong.

### NIT — `design/188-…:L84`, box border one character short

Measured by column index:

```
L84 top     ┌ at 18 … ┐ at 29     (12 wide)
L85 sides   │ at 18 … │ at 30     (13 wide)
L86 bottom  └ at 18 … ┘ at 30     (13 wide)
```

`_predicates`' top edge is a dash short. Every other box in both graphs aligns
exactly, and the lower half (arrows at 23 and 65, centred on the `step` and
`transcript` boxes) is correct.

### NIT — `design/188-…:L74-93`, the graphs do not share components

The template requires "Two graphs — baseline and decision — with the same
components in each." The baseline draws `PipelineReport`, `_stall`,
`_predicates`, `step`; the decision draws `PipelineReport`, `_stall`, `_pred.`,
`_recycle`, `transcript`. `step` appears only left, `transcript` only right.

Substantively: the baseline has to read occupancy too — `_predicates` cannot write
"window full" as a reason without reading the transcript — and the drawing omits
that arrow, so the baseline reads cheaper than it is. That understates the option
the `Considered` section then rejects.

### NIT — `design/188-…:L82`, the divider is crossed on one side only

The `╪` marks land at columns 45, 51 and 58 — all on the decision graph. The
baseline's two `reads` arrows sit at columns 12 and 18, where L82 carries the
label text `pipeline-state-is-one-payload`, so they visually terminate at the
boundary and resume below it. The divider is the ownership claim the drawing
exists to make; the baseline graph does not show it being crossed.

### NIT — `wfctl-names-the-reset-it-cannot-perform.md:L99`

`## The performer cannot be summoned by the agent either` is a seventh section the
`architecture-decisions` format does not list ("The sections are fixed"). Custom
sections have precedent — two records carry `## Boundary` — and the body was
`proposed` when commit 49fd033 added it, so nothing was violated.

What is slightly off is the shape. The section says the `Decision` above it names
a performer that does not exist, leaving a `Decision` and a rebuttal stacked in one
file. `design-md-indexes-the-records` set the other precedent for the same
situation: "Revised in place rather than superseded: nobody had ratified the first
version." The section does state "What survives is the decision as written… what
changes is who the named performer can be", so it is self-aware, which is why this
is a NIT and not a WARNING.

---

## Passes run

**1. Correctness (does it do what it claims).** The change's stated job is to mark
three records `rejected` without losing their argument, and to commit the clarify
scan. Checked every repository fact the three records assert, not just the changed
lines:

| Claim | Verdict |
|---|---|
| `PipelineReport` frozen, `stall: "_stall.Stall \| None" = None`, quoted comment | true (L357, L414, L409) |
| `_STEPS` is eight rows of `Step(command, continuation, predicate)` | true |
| step-table docstring "carries both values or it does not parse" | true (`_pipeline.py:33`) |
| `Stop` hook reads `payload.get("transcript_path")` | true, in `hook_response_shape_cmd` (L4681, read at L4733) |
| `_workmux.py` "imports nothing from `wfctl.*` and never calls `subprocess`" | true, verbatim |
| `PreToolUse` guard "already allows `workmux`" | **true** — `_guard._WORKMUX_OK` is an explicit per-subcommand allowlist and both `send` and `wait` are in it (`_guard.py:92-95`) |
| orchestrate step 5 orders story-complete → stall → auto, "load-bearing" | true (`speckit-orchestrate/SKILL.md:140`) |
| `start-session` "a branch that has ended a session once is in row two from then on, even unattended" | true (`start-session/SKILL.md:289`) |
| `wfctl end` appends an `end` event | true (`_session.py`, `append_event(agent_dir, "end", …)`) |
| the scan's `Detail:` path and `spec.md` FR-005a | both resolve |
| occupancy sum vs 14% statusline | **does not reconcile** — WARNING above |

Line anchors: four wrong, all by one or two — NIT above.

**2. Security.** Nothing executable added; no hook, script, config or `.agents/`
path is touched. No secrets. The scan file's `Detail:` line carries an absolute
`/Users/andremarin/…` path — checked before flagging: all 13 existing scan files
do the same, so it is the established convention and out of this diff's scope.

**3. Architecture — against the in-force set.** Ran `uv run wfctl arch context`
(10 accepted records) and read each candidate for conflict. No accepted record is
contradicted. Two interactions found: `knowledge-placement` L36 vs the duplicated
owned question (WARNING 1), and `install-modes` L60 being the on-point record the
rejection does not cite (WARNING 3). Confirmed the projection stays clean —
`arch context` reports the three as "20 records not shown (18 proposed, 2
rejected)", so nothing rejected leaks into the contract.

Visibility: `wfctl arch check` exits 0 on all four files ("is committed, and this
change adds it"). The level-3 record is under `design/`, named for the issue being
implemented, and is listed in `design.md § Software design decisions` per
`design-md-indexes-the-records`. The scan matches `writing-a-scan-file`: one
`## Session` heading, all ten coverage rows present in wrapper order, reporting
vocabulary (`Clear`/`Resolved`) not scanning vocabulary, verdict from
`_predicates.Verdict`'s three values, counts consistent with the three findings,
each finding naming the alternatives it was decided against. The same-day
correction extends the existing session section rather than opening a new one,
which is the rule the skill states explicitly.

**4. Readability.** Prose matches house density — every paragraph carries a reason
rather than a restatement. The three ASCII drawings were measured rather than
eyeballed; two NITs above. Commit subjects are conventional-ish statements of what
changed and why (`docs(arch):`, `docs(scans):`), bodies carry the reasoning not
visible in the diff, and the first scan commit matches the subject
`writing-a-scan-file` prescribes verbatim. Attribution trailers present on all
five.

**5. Performance.** Not applicable to documentation. The one adjacent question —
whether a new arch-root subdirectory or record class costs any reader — is
answered: `load_records` globs one level so `design/` and `scans/` stay out of the
projection, and `records_on_this_branch` and `_observe` already exclude
`_paths.SCANS_DIR`, which is `the-scan-is-attested-where-the-reviewer-reads`'
existing carve-out and not something this change had to extend.

**6. Over-engineering.** The live question is whether 541 lines should survive a
closed issue at all. It should: the commit message makes the case ("a later reader
who deletes these has to buy that again"), `rejected` is the status the format
defines for exactly this, and deleting would leave the three-layer refusal finding
recoverable only from a closed issue's comments. One `shrink:` candidate — the
refusal table at L120-127 restates three bullets already argued in `Considered` —
but it earns its lines by stacking them into one reading and marking which two were
known when the record was opened. Nothing else is removable.

net: Lean already.

**Verdict: Approve** (0 blockers, 4 warnings, 5 nits). The change does what it
claims and improves the repository's record: the argument is preserved in the
format built for it, every file is reachable by a reviewer, and nothing in force is
contradicted. WARNING 1 and WARNING 3 are the two worth fixing before merge —
both are one-line edits to a record nobody has ratified, and both are about the
records' accuracy as an argument, which is the entire reason they were kept.
